# libslic3r-rs ↔ BambuStudio G-code Parity — Status

Goal: the Rust slicer (`crates/libslic3r-rs`, the `slicer` crate) produces G-code
**byte-identical** to C++ BambuStudio for the same job. Branch:
`alex/libslic3r-parity-engine`.

## How to measure

The two-engine compare harness runs the *same* job through C++ BambuStudio
(subprocess) and the Rust engine (in-process) and diffs the G-code:

```
COMPARE_KEEP_DIR=/tmp/cmp devbox run -- \
  target/debug/slicer-cli compare --config tests/configs/stl-inline-config.jsonnet
```

- Dumps `native.gcode` / `rust.gcode` to `$COMPARE_KEEP_DIR`.
- A clean Benchy slice takes **~17 s**. If it takes minutes, suspect orphaned
  slicer processes saturating CPU (`pkill -f slicer-cli`).
- **Per-feature MATERIAL** must be measured with `/tmp/feat_e2.py` (counts E only
  on moves with real XY motion). The naive sum (`feat_e.py`) is **contaminated by
  deretraction-priming** moves and inflates native — do not trust it.
- Track the **header filament length** and **per-feature material dE** (rust−native),
  not feature *counts* (counts are a feature-run-segmentation artifact).

## Current parity (ROUND 107 — see memory `project_byteparity_admesh.md` + `project_nondeterminism_roots.md` for the full round log; this branch's copy jumps R81→R96 because `alex/frame-unify` forked at R81. R82–R95 are on `alex/libslic3r-parity-engine` @e35e290 + memory.)

> **Branch note:** the gated slice-byte-match stack (R83 simplify, R84 F1-union, R85–R87 frame-unify/Eigen shim, R88 two-pass union, R91 simplify_p, R96–R98, R100, R101 below) lives on `alex/frame-unify` @7d3f93c, all behind env gates (`F1_UNION`/`FRAME_UNIFY`/`SLICE_SIMPLIFY`/`SLICE_CENTER`), default parity unchanged. R99 (determinism) is on the DEFAULT path. Branch PUSHED to origin (2026-07-02; standing policy: always push alex/* as backup, never main).

- **R323 — SMOOTH DIVERGENCE PINNED; HAND-FMA RULED OUT → SHIM IT (docs only, 68481).** Per-element bit-hash probes (reverted): post-threshold-conversion input to smooth is **byte-identical** (14254942ce34f177 both) — so divergence is entirely inside smooth_compensation_banded. But native output hash 5e8a1ccd54eb74b7 matches NONE of the four rust laplacian variants tried (no-FMA e579…, Option-A-mul_add 7d24…, Option-B, f64-accumulate 817e…). CONCLUSION: the divergence is not the laplacian FMA but the BANDED-NEIGHBOR branch (`l2 < dist_min2`, entered where resample_polygon places points closer than the 0.4mm band near corners) — its `sqrt`/Eigen-`.norm()`/`lerp` each carry their own clang FMA/precision, and hand-matching clang's per-expression contraction across all of them is fragile and version-brittle. DECISION: shim it (the proven ma-shim/varoff pattern — same clang, bit-exact by construction). NEXT (R324): add `cz_smooth_compensation_banded` to clipper_z_shim.cpp, verbatim from ElephantFootCompensation.cpp:465-532 (+ prev/next_idx_modulo + lerp), signature (contour i32 xy, comp f32[], n, band, strength, iters); rust binding; route smooth in EFC through it (L0_EFC-only path, default lock unaffected). Then detect_bottom → 1528, vshell_reg closes, parked layer lands. Keep the R321 mul_add as harmless improvement. GATES: default 147987/7adae05c ✓.

- **R322 — ★★ SHIM PROVEN BYTE-EXACT; GAP = 100% SMOOTH FMA DELTAS (12g 68481).** Dumped native's exact varoff input (VODUMP: resampled 266-contour + 8 holes + post-smooth deltas as raw f32 bits) and replayed through the rust cz shim (new gated clipper-z-sys test `replay_varoff`): output **1452 points = byte-identical to native's 1452**. OVERTURNS R321's guess — the 1498-vs-1528 gap is NOT a shim bug; it is entirely the sub-ulp smooth_compensation_banded delta divergence flipping mitered-offset corner classifications (sin_a·delta threshold decisions). So smooth must be FMA-**byte-exact**, not close. R321's `mul_add` (Option A) got 0.002 off; residual = clang's exact per-instruction contraction. NEXT (R323): try computing the laplacian accumulation in f64 then rounding to f32 (tests whether native's FMA ≈ higher-precision); if that matches, use it; else match clang's contract-fast instruction sequence exactly (verify strength const-fold 0.7f/0.15f, and whether clang contracts iteration-carried terms). Oracle: dist/smooth bit-hash exact (add per-element smooth bit-hash probe) → then detect_bottom → 1528 → vshell_reg closes → parked layer lands. GATES: default 147987/7adae05c ✓ clipper-z-sys 8/8 ✓.

- **R321 — EFC DELTA SOURCE BISECTED: SMOOTH FMA (fixed) + VAROFF-STRUCTURAL COUNT GAP (12g 68481).** EFCDBG stage bisect (probes reverted): resample_polygon + contour_distance2 **BYTE-EXACT** (dist bit-hash 4275b4cc2734f5ec both engines); divergence isolated to smooth_compensation_banded — sub-ulp f32 FMA-contraction difference (clang ARM fuses comp[i]*(1-s)+… ; Rust doesn't). Fixed with `mul_add` (0.007→0.002 off, sub-ulp residual = clang per-point operand nondeterminism). CRITICAL: detect_bottom stays **1498** (native 1528) after the fix → the 30-vertex gap is NOT the smooth delta (sub-ulp can't create/destroy 30 vertices) — it is STRUCTURAL in the cz varoff shim port. NEXT (R322): isolate the shim — dump native's exact deltas array (bit-hash + values) at the varoff call, replay through the rust shim with those identical deltas, compare output count; likely bug in mittered_offset_path_scaled port (the +0.5-sign rounding, shortest-edge-merge index handling, or the sharp-corner perp arithmetic) or the fix_after enclosing-rect erase. Oracle: docs/bisect0b.err 1528. GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R320 — ★ cz VAROFF SHIM LANDED: CLIPPER1 ENGINE IN PLACE (12g 68481).** cz_variable_offset_inner_ex added to clipper_z_shim.cpp (verbatim mittered_offset_path_scaled + fix_after_inner/outer + ctDifference PolyTree, ClipperUtils.cpp:1077-1440); rust binding variable_offset_inner_ex_clib + grouped reader; EFC call site (elephant_foot_compensation.rs:817) swapped off Clipper2. Oracle: detect_bottom L0 1512→**1498** (native 1528) — the vertex-engine mismatch (R318 root) is fixed; residual moved UPSTREAM to the delta SOURCES feeding varoff: contour_distance2 (EdgeGrid ray-fan visitor f32), resample_polygon (0.5mm resample interval rounding), smooth_compensation_banded (band f32 iters). NEXT (R321): audit these three vs native (ElephantFootCompensation.cpp:34-231/305-534) — the deltas array must match before varoff output can hit 1528; oracle docs/bisect0b.err per iteration on release harness. GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R319 — VAROFF SHIM PLAN STAGED (docs only, 68481).** Native recipe fully captured: variable_offset_inner_ex = mittered_offset_path_scaled (hand-rolled per-vertex mitered offset, CU.cpp:1120-1388) → fix_after_inner_offset (single-path ClipperLib union pftNegative + enclosing-rect trick + ReverseSolution(true), :1095-1118; outer variant :1077) → holes via fix_after_outer_offset → ctDifference PolyTree assembly (:1390-1440). Corpus extracted to scratchpad efc_corpus/ (fix_after.cpp 42, mittered.cpp 269, varoff.cpp 46 lines; re-extract: CU.cpp ranges above). cz shim already maps pftNegative (fill_type 3). PLAN (R320, fresh context): add `cz_variable_offset_inner_ex` to clipper_z_shim.cpp — VERBATIM adaptation (the ma-shim pattern; bit-exact by construction), reuse the shim's PolyTree reconstruction; rust binding; swap elephant_foot_compensation.rs:817 to the cz version; iterate vs docs/bisect0b.err oracle (expect detect_bottom → 1528/-1062555612 exact). GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R318 — EFC RESIDUAL ROOT: CLIPPER2-VS-CLIPPER1 IN variable_offset_inner_ex (docs only, 68481).** rust's variable_offset_inner_ex lives in **clipper2_utils** (Clipper2 booleans for fix_after_inner_offset/cleanup) while native runs the hand-rolled mittered_offset_path_scaled + **ClipperLib(1)** union with NEGATIVE fill (ClipperUtils.cpp:1390-1425) — different intersection/rounding engines → the 1512-vs-1528 vertex gap. FIX PLAN (R319, fresh session): route the EFC variable-offset cleanup through the vendored ClipperLib shim (cz union; needs pftNegative fill support in cz_* if absent — check clipper_z_shim fill-type mapping), keeping the mittered offset math (verify the rust port of mittered_offset_path_scaled arithmetic is verbatim: f64 domains, per-vertex delta scaling ClipperUtils.cpp:1290-1380). Oracle per iteration: docs/bisect0b.err detect_bottom (native 1528/-1062555612/53346513). ENDGAME CHAIN unchanged: EFC exact → L0 rsl exact → vshell_reg closes → parked quant layer (stage-verified) lands → bottom-ISI + downstream. GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R317 — EFC DRIVER DOMAINS HARDENED; RESIDUAL IN INTERNALS (12g 68481).** scaled_compensation/min_contour_width/resample_interval now native fractional doubles (scale_ DIVIDES, no rounding); d-threshold compares promote f32→double like native. Oracle UNCHANGED (detect_bottom 1512 vs native 1528) → residual in EFC internals: **variable_offset_inner_ex** vertex emission (16-pt gap = offset discretization suspect) and/or **contour_distance2** EdgeGrid visitor values. NEXT (R318): audit rust variable_offset_inner_ex vs native (VariableOffset/ClipperUtils variable offset path — per-vertex delta ClipperOffset construction) with the archived SDN oracle (docs/bisect0b.err) per iteration on the release harness. GATES: default 147987/7adae05c ✓.

- **R316 — L0_EFC WIRED: DIRECTION CONFIRMED, EFC PORT = NEXT CONVERGENCE TARGET (12g 68481).** L0_SLICES retired; corrected semantic implemented under **L0_EFC**: L0 surfaces = union_ex(elephant_foot_compensation_with_flow(raw, ext_flow, 0.15)) via the existing faithful port, lslices keep raw. Stage oracle: detect_bottom L0 1353 → **1512** (native 1528, sx within 0.05%) — right place, but the rust EFC ALGORITHM (elephant_foot_compensation.rs vs ElephantFootCompensation.cpp) isn't byte-exact yet. Saved SDN checksum oracles copied to docs/bisect{0b,1,2}.err. CHAIN TO CLOSE: EFC byte-parity → L0 rsl == 1528 exact → vshell step-5/piece-1/vshell_reg close → parked quant layer (FILLBOUND/RASTER already stage-exact) lands → bottom-ISI 3.2k + downstream. NEXT (R317): EFC port audit — diff rust elephant_foot_compensation.rs against native ElephantFootCompensation.cpp (the smooth/dist variant math, f32 domains, clipper calls) with the L0 checksum as oracle per iteration (release harness, seconds per test). GATES: default 147987/7adae05c ✓.

- **R315 — ★★★ ONE-SHOT BISECT DELIVERS: TWO STAGE-EXACT WINS + THE TRUE L0 ROOT (docs only, 68481).** Native STAGEDUMP sites applied per patch doc (REVERTED after; SDN/SD prefixes make attribution unambiguous — the R289-class ordering trap is now structurally impossible). RESULTS: (1) **key=1 with parked gates ON: tail_infill AND tail_nooverlap go EXACT** — FILLBOUND_QUANT + RASTER_QUANT verified at stage level; (2) key=1 sole survivor = vshell_reg (np 2513-vs-2457; vshell_solid nearly closes, sx off 36); (3) **key=0 authoritative: native L0 rslices = RAW 1528-pt contours; RUST collapses L0 region slices to the union'd lslices (1368)** — R307's attribution was FLIPPED by output ordering; L0_SLICES chased the wrong direction (retire it; keep gated-off as a tombstone). CORRECTED WORK ITEM (R316): find where rust's L0 region slices become union'd lslices (make_slices write-back?) and keep the raw pre-union contours like native; expected to close vshell_reg at L1+ (the L0 bottoms feed the shell projections) → land the parked quant layer. GATES: default 147987/7adae05c ✓ (native reverted, release-verified).

- **R314 — STAGEDUMP FULL SITE SET WIRED (12g 68481).** detect_top/detect_bottom + vshell_reg/vshell_solid + tail_infill/tail_nooverlap added; one-run L1 picture: lsl==rsl 145, detect_bottom 1512 (inherits L0 boundary via the diff), vshell_reg 2462 / solid 1273, tail 56/56. Locks ✓ (inert). NEXT (R315): apply docs/native_stagedump.patch + matching native sites (detect PO.cpp:1569-75, vshell :2018/2060, tail PG.cpp:1406/1424), native rebuild, ONE run each engine at the bottom-ISI layer (key=1 and key=3), diff the whole chain in one shot — first stage whose checksum diverges = the remaining web leg. GATES: default 147987/7adae05c ✓.

- **R313 — STAGEDUMP SKELETON LANDED (12g 68481).** Permanent env-gated staged-pipeline dumper: `stage_dump.rs` (`STAGEDUMP=<layer_idx>` → `SD key= stage= n= np= sx= sy=` checksum lines), first call sites (lslices/rslices at make_perimeters entry; smoke: L56 lsl==rsl 1913 pts), native counterpart documented in **docs/native_stagedump.patch** (apply per probe session, revert after; layer-index keys per R289 protocol). Runs at release-harness speed. NEXT (R314+): extend call sites (detect_top/detect_bottom, vshell_reg/vshell_solid, tail_infill/tail_nooverlap — names fixed in the patch doc), apply native patch, and run the full-pipeline bisect at the L1 bottom-ISI layer in single runs per engine. GATES: default 147987/7adae05c ✓ (dumper inert).

- **R312 — ★ RELEASE-BUILD HARNESS VALIDATED: 100× ON SLOW GATED RUNS (docs only, 68481).** `cargo build --release` binary verified BYTE-IDENTICAL to debug on every lock: default 147987/7adae05c ✓, 12-gate 68481 ✓, 13-gate/L0_SLICES 68566 ✓ (the C++ shims under -O2 preserve FP exactly — no contraction drift). Timing: the L0_SLICES gated run 10+ min (debug) → **5.7 s** (release). NEW MEASUREMENT PROTOCOL: use `target/release/slicer-cli` for ALL compare/probe runs (`devbox run -- cargo build --release`; re-verify default lock after each rebuild); debug binary only when a debugger/sample readability is needed. This unblocks the staged one-run pipeline-dump session (R310 plan) at interactive speed. GATES: default 147987/7adae05c ✓ (release-verified).

- **R311 — WALLS-F CLASSIFIED: INHERITED SPEED-CONTEXT, NOT INDEPENDENT (docs only, 68481).** Wall F-only mismatches decompose as: (a) cooling-ladder integer Fs with shifted VALUES (rust 6276/6143 vs native 6791/6656/6517 — layer-time dependent); (b) native 568 fractional Fs vs rust 76 (cooling×volcap on walls); (c) wipe-F distribution shifted (~50 blocks flipped F12000↔F15000, F18423.913 volcap-wipe native 59 vs rust 23) — wipe F = current-speed context, inherits path sequencing; (d) WIPE block count 1683-vs-1743 (60 gap, retract-gap class R232). VERDICT: every remaining major class couples through the geometry/time web — no independent quick wins left at this altitude. NEXT (R312): infrastructure — add a RELEASE-profile compare harness path (cargo build --release + justfile/devbox task) so probe rounds and the L0_SLICES-style slow gated runs drop from ~10min to seconds; then the staged one-run pipeline dump session. GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R310 — L0_SLICES MEASURED: +85, BOTTOM-ISI UNCHANGED — CHAIN HAS ANOTHER LEG (docs only, 68481).** The background 13-gate run completed (debug-slow but functional): 68566 (+85 scatter) and the bottom-ISI concentration UNCHANGED (3162 vs 3156) — aligning L0 PG-input surfaces did NOT collapse the class. IMPLICATION: the R306 causal chain (L0 surfaces → cache[0].bottom → step-5 → piece-1 → bottom-ISI) has an additional leg — either the vshell cache's L0 bottoms come from a different snapshot than PG input (detect re-typing order), or piece-1/regularize decisions diverge for reasons beyond the L0 shape. L0_SLICES stays PARKED-OFF. STATE: five parked faithful gates (VSHELL_REG_QUANT, FILLBOUND_QUANT, RASTER_QUANT, VSHELL_RAW, L0_SLICES), each value/content-correct, each individually net-negative — the razor-tie compensation web needs a wider joint convergence than single-op fixes deliver. RECOMMENDED NEXT (fresh session): (a) instrument-once end-to-end L1-ISI pipeline dump (surfaces → shell → regularize → fill boundary → raster ends) both engines in ONE run each, diff every stage in sequence — kills the round-trip-per-probe cost that limited this campaign; (b) or pivot to the walls F-only class (3.9k, likely cooling/speed values — independent of the geometry web). GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R309 — "HANG" = DEBUG-BUILD SLOWNESS IN traverse_loops (docs only, 68481).** Real-PID sample: 100% CPU in generate_classic_one → traverse_loops → offset_expolygons_clib (cz_offset_expolygon, unoptimized debug Eigen visible) — the merged L0 surface (2 expolys, ~1500 pts, 8 holes) makes traverse_loops' per-loop clib offsets grind in the DEBUG build; not a deadlock. Background full-gated run launched to completion-test + measure the cascade. SIDE-FINDING: native's L0 elephant compensation is effectively a NO-OP for this job (native rsl == its explicitly-uncompensated lslices backup) — so feeding uncompensated lslices as L0 surfaces is native-faithful. NEXT (R310): collect the background result — if it completes, measure 13-gate (12g+L0_SLICES) + re-test the parked layer; also consider a release-build compare harness for slow gated runs. GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R308 — L0 FIX IMPLEMENTED BUT HANGS — PARKED BEHIND L0_SLICES (12g 68481).** The R307 fix (L0 region slices := lslices, superseding the plain-shrink elephant step) HANGS the gated pipeline (>60s in pthread_cond_wait; default path unaffected). Ring counts confirm the content is right (native rsl rings=10 == lslices rings=10) — the hang is a downstream consumer choking on surface SHAPE: 2 expolygons-with-8-holes instead of the raw 10 pieces, or the thickness=-1.0 sentinel (fill height derivation?). NEXT (R309): debug the hang — run L0_SLICES + F1_UNION alone, `sample` the actual slicer-cli PID (not the devbox wrapper) to get the spinning frame; likely fixes: set thickness = layer height instead of -1, or split surfaces per-expolygon differently. Then re-measure the cascade (bottom-ISI + parked layer re-test). GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R307 — ★★★ L0 ROOT DIAGNOSED: REGION SLICES HOLD UNCOMPENSATED CONTOURS (docs only, 68481).** LSLDBG L0 (reverted): **lslices BYTE-IDENTICAL** (1368 pts both engines — rust's elephant-foot path on lslices is faithful); native region slices == lslices verbatim (1368); **rust region slices = 1528 pts (the PRE-compensation contours)** — rust builds L0 region slices from a snapshot taken BEFORE first-layer compensation (`elefant_foot_compensation: 0.15` in profile; native applies apply_first_layer_compensation inside slice() updating BOTH). The redundant make_perimeters-time `elephant_foot_compensation_step` (print_object.rs:680, plain-shrink approximation) either no-ops or diverges. FIX (R308): make L0 region slices copy the POST-compensation lslices (mirror native's build order in slice()); retire/gate the make_perimeters step. EXPECTED CASCADE: L0 surfaces exact → cache[0].bottom → shell step-5 → vshell piece-1 → bottom-ISI 3.2k → re-test parked quant layer (~20k exposure). GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R306 — ★★ ROOT OF THE CHAIN: L0 FIRST-LAYER SURFACES DIFFER (docs only, 68481).** BOTDBG at i=0/1 (reverted): **L0 bottom PRE-grow differs structurally** — native 1353 pts vs rust 1528 (+13%), same 10 pieces, exp identical (2285.35 both); L1 byte-identical (1512=1512). L0 bottoms = L0 slices (no lower layer → all surfaces stBottom) → the FIRST-LAYER slice/surface post-processing is the origin: elephant-foot / first-layer XY compensation (or L0-specific slicing path) produces different contours. FULL CAUSAL CHAIN NOW MAPPED: L0 surfaces → cache[0].bottom → shell step-5 (L1..L3 bottom projections) → vshell piece-1 → bottom-ISI 3.2k → parked quant layer (~20k). Combine sites verified 1:1 (PO.cpp:1936-1982 ↔ print_object.rs:3460-3490). NEXT (R307): probe L0 lslices + region slices (LSLDBG pattern, index 0) — if lslices differ → audit first-layer compensation op (elephant foot / xy_contour_compensation / first-layer offset chain) vs native; this ONE fix cascades the farthest of anything left. GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R305 — BOTTOMS EXONERATED: CACHES BYTE-IDENTICAL; STEP-5 DIVERGENCE IS IN ITS INPUT CONSTRUCTION (docs only, 68481).** BOTDBG (reverted): bottom sources (pre-grow) AND post-grow caches at L2/L3/L4 are **BYTE-IDENTICAL** in both engines (every n/np/sx/sy pair matches). So detect's bottom chain is exact too — the R304 step-5 input divergence (np 1355-vs-1506 on n=10) is created BETWEEN the exact caches and the combine call: the per-neighbor projection expression itself (native: cache polygons possibly re-expanded/clipped inline; rust: intersection(grown, lslices) — OR the two engines' step NUMBERING masks different call sequences). NEXT (R306): dump step-5 input FULL GEOMETRY (not checksums) + tag WHICH source expression built it in both engines (annotate each combine call site with a site-id in the probe) — the site-id ladder disambiguates sequence-vs-op; then fix the op. NOTE the causal chain stands: this one op → piece-1 → parked quant layer (~20k). GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R304 — LADDER: STEPS 1-4 BYTE-IDENTICAL; STEP-5 INPUT = BOTTOM-SURFACE SOURCE DRIFT (docs only, 68481).** Per-step LADDBG in the L1 shell assembly (reverted): combine_shells steps 1-4 (top projections + anchors) BYTE-IDENTICAL (n/np/sx/sy match); step 5's INPUT diverges structurally — n=10 both but rust 1355 pts vs native 1506 (10% fewer). VSHELL_RAW does NOT change it → the divergence is NOT the local grow() but the SOURCE: **bottom_surfaces (stBottom/stBottomBridge) at layers 2-4** — the hull overhang bottoms. Detect exactness was verified for TOP only (R283); the BOTTOM chain (diff vs lower lslices + opening) at overhang layers is the last unverified detect output. NEXT (R305): dump cache[i].bottom pre-grow for i=2..4 both engines (VSHELLDBG pattern, key-asserted); if bottoms drift → detect bottom-chain op audit (the opening_ex/diff_ex bottom path at PO.cpp:1534-1556, incl. the surface_type_bottom_other split). This is the LAST link: bottoms → step-5 → piece-1 → unlocks the parked quant layer (~20k). GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R303 — PROVENANCE VERIFIED: QUANT VALUES CORRECT; BLOCKER = VSHELL PIECE-1 (docs only, 68481).** AODBG probe (reverted): rust fill inputs sp=0.37707960605621338 (bit-exact), ov=0; legacy a1=−1885 (i64 round) vs native −1885.398f — REAL 0.4-unit clip-boundary error every solid fill. Monotonic (Top/ISI) native recipe documented (FillRectilinear.cpp:3338-3364): uses `params.flow.spacing()` (f32) with overlap FIXED 0, `line_spacing = scaled_spacing_int / density_f32` — with overlap=0 + spacing bit-exact, the RASTER_QUANT values EQUAL the monotonic natives. CONCLUSION: all four parked quants are value-correct; their joint regression can only come from the fill SURFACES (vshell drift) flipping razor clips → the ENTIRE parked layer (VSHELL_REG_QUANT + FILLBOUND_QUANT + RASTER_QUANT + VSHELL_RAW, ~potential −20k exposure) unlocks on ONE remaining item: **vshell piece-1 internal op**. NEXT (R304): per-step probe ladder inside L1 shell assembly — dump `shell` after EACH combine_shells/holes/anchor step, both engines, key-asserted; bisect to the op. GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R302 — JOINT PARKED-GATE TEST: +93, PARTIAL CANCELLATION ONLY (docs only, 68481).** All four parked gates together = 68574 (+93) vs naive sum +316 — razor ties partially cancel jointly but no unlock. NUMERIC NOTE: the legacy raster aoffset truncation is a REAL ~0.4-unit clip-boundary shift on every solid fill (rust round-to-i64 1885 vs native −1885.3982f); that exact values still REGRESS implies the assumed `overlap` provenance (or spacing entering fills via SurfaceFillParams) doesn't match native's runtime values. NEXT (R303): runtime AOFFSET probe — printf native's actual aoffset1/aoffset2/overlap/spacing at FillRectilinear.cpp:2843-2848 for one Top fill + rust counterpart (key-asserted); reconcile provenance; then re-test RASTER_QUANT. GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R301 — RASTER DOMAIN FIXES PARKED (+73, RASTER_QUANT; 12g 68481).** Implemented all three R300 hits (line_spacing div-domain; aoffset f32-float deltas via new ExPolygonWithOffset::new_scaled_f — legacy wrapper byte-identical). MEASURED +73 → parked behind **RASTER_QUANT**, the FOURTH instance of the decoherence pattern (FILLBOUND +106, VSHELL +137, RASTER +73 — all value-correct, all net-negative while upstream values drift). STRATEGIC READ: the parked quartet (VSHELL_REG_QUANT, FILLBOUND_QUANT, VSHELL_RAW, RASTER_QUANT) forms a COHERENT faithful layer that should be re-tested as a SET (razor ties may cancel jointly), and the remaining upstream drift is now narrow: `overlap`/`spacing` provenance into fills (SurfaceFillParams chain) + the vshell piece-1 op. NEXT (R302): (a) test ALL FOUR parked gates together (one run — joint razor re-alignment); (b) if still negative, audit SurfaceFillParams overlap/spacing provenance (group_fills → params.overlap value vs native). GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R300 — RASTER-MATH AUDIT: THREE DOMAIN HITS (docs only, 68481).** fill_surface_by_lines setup (fill_rectilinear.rs:2149-2160 vs FillRectilinear.cpp:2841-2848): (1) **line_spacing** rust `(spacing * 1e5 / density) as Coord` vs native `coord_t(scale_(spacing) / density)` = (spacing **/1e-5**) /density trunc — the R277 mult-vs-div class; (2) **aoffset1/aoffset2** rust `scale()` (ROUND → i64) vs native `float(scale_(overlap − (0.5−0.45f)*spacing))` — f32-of-div-scaled FLOAT deltas (fraction kept!) feeding ExPolygonWithOffset's contour offsets = the raster clip boundary → THE endpoint last-digit class; (3) coefficient `0.5 − 0.45f` uses the INEXACT f32 0.45 in native. IMPLEMENTATION NOTE: rust ExPolygonWithOffset::new takes Coord i64 offsets — mirroring needs an f64-delta path (check its ClipperOffset call precision) so the f32 fraction (e.g. .9f) survives; plus rotate() arithmetic check (native MultiPoint round(double + c*v + s*v)). NEXT (R301, fresh context): implement (1)-(3) under TOPFILL_FAITHFUL with an f64-capable ExPolygonWithOffset delta path; measure 12-gate + Top travel <5µm bucket count as oracle. GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R299 — TOP TRAVEL CLASSIFIED: 79% = RASTER-ENDPOINT LAST-DIGIT (docs only, 68481).** Near-miss pairing of the 3.8k Top-surface travel mismatches (same-z closest native travel): **2960/3770 within 5µm** (last printed digit, 0.001mm), 667 <0.5mm, only 140 ≥0.5mm (true ordering/seam). CONSEQUENCE: Top travel (7.9k) + Top XY+E (4.1k) ≈ 80% raster-endpoint VALUE drift — one class with the ISI raster tail: the fill raster computation (rotate → sort → adjust_solid_spacing → clip against no-overlap) carries sub-µm drift that crosses print-rounding boundaries, even where boundaries are byte-exact. NEW CAMPAIGN (R300+): fill-raster arithmetic parity — audit rust fill_rectilinear/monotonic scaled-domain math vs native FillRectilinear.cpp (rotation trig f64 domains, bbox extent ints, line construction lrint-vs-trunc, adjust writeback chain) with the trunc/f32 recipe; oracle = one Top raster line end at an exact-boundary layer (dump both engines' raster line coords pre-gcode). Covers Top 12k + ISI exact-layer tail + Sparse selection (~20k+ total exposure). GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R298 — VSHELL MICRO-CANDIDATES EXHAUSTED (12g 68481).** Raw cache-holes (native appends raw, PO.cpp:1859-64; rust unioned) gated under VSHELL_RAW — NEUTRAL. combine_shells granularity verified matching (per-append union both); all vshell local shadows clib-routed. Piece-1 pre-regularize jitter survives every tested candidate → DEPRIORITIZED (downstream classes ≈50 lines/layer at exact layers; the L1/L3 bottom-ISI concentration presumably still funnels through it but further op-bisection needs a per-op probe ladder — bank for a dedicated session). NEXT (R299): pivot to census yield — **Top-surface travel 7.9k** (single biggest kind-bucket): sample travel mismatches, classify ordering-vs-endpoint (travel endpoints derive from raster ends → may collapse with fill values; ordering = seam/shortest-path — separate fix family). GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R297 — VSHELL RAW GROW: NEUTRAL, PARKED (12g 68481).** Gated the native RAW-offset semantics (expand() = per-piece ClipperOffset paths, no union; offset_expolygons_clib_raw_scaled w/ f32 delta) behind VSHELL_RAW — measured EXACTLY neutral both with/without REG_QUANT → union-reconstruction was not the piece-1 jitter source; kept as inert hardening. PIECE-1 RESIDUAL still open (mixed ±1..20 miter jitter pre-regularize): remaining candidates = combine_shells PIECE-BY-PIECE union_ granularity (native unions after EACH neighbor append; rust may batch — different intermediate snap points), the holes diff path, neighbor accumulation ORDER. NEXT (R298): check rust's combine_shells granularity vs native (PO.cpp:1913-1922: union_ per append) — if rust batches, mirror the per-append union under gate; then re-probe piece 1. GATES: default 147987/7adae05c ✓ 12g 68481 ✓.

- **R296 — ★ +10 SIGNATURE KILLED: SHELL∩INTERNAL SAFETY OFFSET (12g 68481).** Native `intersection(shell, polygonsInternal, ApplySafetyOffset::Yes)` (PO.cpp:1993) — rust's plain intersection was the R295 +10-unit class; routed through intersection_clib_safety (R238 shim) under TOPFILL_FAITHFUL. Re-probe (vs saved native dump): uniform +10-Y GONE; piece-1 residual = mixed miter-join jitter (±1..20, no axis) — NEXT SUSPECT: rust vshell `grow()` = offset_expolygons_clib (UNION-reconstructing) where native `expand()` = RAW Polygons offset, no union (Class-5 pattern; use offset_expolygons_clib_raw_scaled). Also `combine_shells` union_() granularity (native unions piece-by-piece per neighbor; rust may batch). 12g 68483→68481 (−2; jitter still dominates piece 1). NEXT (R297): swap vshell grow()/anchor-expand to RAW offsets under gate, re-probe piece 1; then combine_shells union granularity if needed. GATES: default 147987/7adae05c ✓.

- **R295 — VSHELL PIECE-1 SIGNATURE: CONSISTENT +10-UNIT SHIFT PRE-REGULARIZE (docs only, 68483).** SHDBG probe at L1 under quant (reverted): pre-regularize `union_ex(shell)` piece 1 = 56-vs-59 pts, ALL drifted with a CONSISTENT +10-unit Y component (X near-exact on many verts); piece 2 EXACT (74 pts). +10 units = ClipperSafetyOffset AND float(SCALED_EPSILON) — one engine applies a safety/eps offset on piece 1's assembly path that the other doesn't. Piece 1 has 8 HOLES (piece 2 none) → the HOLES chain is implicated: native `polygons_append(holes, cache.holes)` + `combine_holes` intersection + the shell-minus-holes step; rust holes handling in discover_vertical_shells (~print_object.rs:3560-3595?) may miss a safety-offset or apply an extra eps closing. NEXT (R296): read native PO.cpp shell+holes assembly (1907-2010, esp. how holes subtract from shell and any union_safety_offset) vs rust; find the op with ±10; fix under a gate; re-run SHDBG expectation: piece 1 exact → L1 ISI 3.2k class unlocks + likely flips VSHELL_REG_QUANT net-positive. GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R294 — VSHELL HALF-CONVERGED UNDER QUANT: PIECE-LEVEL SPLIT FOUND (docs only, 68483).** VSODBG probe at L1 (reverted): with VSHELL_REG_QUANT **ON**, vshell piece 2 goes **BYTE-EXACT** (regularized 147 pts + output 74 pts) while piece 1 still drifts structurally (349-vs-336 pts) — the quantized deltas are value-correct AND partially converging; piece 1 carries a REAL internal op difference (structural vertex-count delta from the shell-propagation chain: neighbor-cache union/grow joins, holes intersection, eps closings, lslices/object-volume clips PO.cpp:1930-2010). Global quant re-test still +137 (razor scatter on drift layers, unchanged story). NEXT (R295): probe `shell` (pre-regularize) for piece 1 at L1 under quant — split the propagation chain (per-neighbor-layer accumulation) to find the op where piece 1 diverges; suspects: the eps_mm closing (native EPSILON closing value/join), holes combine_holes intersection chain, the expand+lslices anchor clip (:1940-47, only quantized for ext spacing). GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R293 — L1 BOUNDARY EXACT; BOTTOM-ISI CLASS = VSHELL-INTERNALS OUTPUT (docs only, 68483).** NFDBG at L1 (z=0.4, key-asserted, reverted): `not_filled_exp` **BYTE-EXACT** (56 pts) — the PG boundary is exact even at the worst ISI layer. The z=0.4 LINE_WIDTH clusters (rust ~0.455 vs native ~0.514) are both post-`adjust_solid_spacing` values → the FILL SURFACES themselves differ at L1 (different raster stretch factors → different leftover gaps/widths). L1 ISI fill surfaces = BOTTOM SHELLS = discover_vertical_shells output (horizontal-shells body is skipped both engines, R238) → the bottom-ISI 3.2k concentration funnels into the VSHELL park. VSHELL state: inputs (Top/Bottom sets) exact (R283), quantized deltas exact (R280), but +137 persists → the divergence is in vshell INTERNAL ops beyond those deltas (shell propagation grow/union chain, eps_mm closings, holes intersection, expand(...)/lslices clips at PO.cpp:1930-2010). NEXT (R294): dump vshell OUTPUT (internal-solid surfaces appended) at L1 both engines; then op-by-op forensic inside the shell assembly loop (the R276 leaf-first recipe, now with exact inputs guaranteed). GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R292 — ISI CLASS DECOMPOSED: BOTTOM VARIABLE-WIDTH CONCENTRIC IS THE CONCENTRATION (docs only, 68483).** ISI per-layer census: z=0.8 (1689) + z=0.4 (1467) = 28% of the 11.2k class in TWO bottom layers; 76 layers have ZERO ISI diffs; exact-boundary layers (z=11.4 etc.) carry only ~50/layer. The bottom-layer ISI = VARIABLE-WIDTH fill (100-vs-140 distinct LINE_WIDTHs at z=0.4, arcs both engines 122-vs-100, widths structurally different: rust 0.455 vs native 0.514 samples) — the variable-width concentric/floating family (same as Floating-vertical-shell 6.3k). CHAIN: bottom surface → variable-width concentric fill (fill_floating_concentric.rs) → arc fit; width VALUES differ structurally → path counts/geometry scatter. NEXT (R293): audit the bottom-fill width chain — which fill runs at z=0.4 ISI (pattern? FloatingConcentric?) and where its widths come from (thick-polyline inset accumulation vs native SurfaceFillParams flow), then the same trunc/f32 recipe on its inset math. ALSO: remaining ISI tail (~50/layer at exact layers) = raster-value class, separate. GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R291 — ★ FILLBOUND PARADOX RESOLVED: QUANT BRANCH BYTE-EXACT; +106 = RAZOR-TIE NOISE (docs only, 68483).** IEDBG probe (key-asserted, reverted): under FILLBOUND_QUANT the tail output `infill_exp` at z=2.2 is **BYTE-EXACT vs native** (63 pts). The quant branch is CORRECT end-to-end (input→deltas→output all native-exact at this layer). RESOLUTION of the +106: legacy's 0.18-unit-off deltas round to IDENTICAL integer output on most layers (ClipperOffset int rounding absorbs sub-unit delta differences); only razor-tie layers differ, and those flips scatter both ways — net +106 today by razor distribution, expected to flip as remaining input-drift layers converge. STANDING RULE unchanged: re-test FILLBOUND_QUANT + VSHELL_REG_QUANT each convergence round. STATE SUMMARY: perimeter chain (slices → insets → gaps → tail) is now byte-exact at every probed layer (L10/L11/L56); remaining 68.5k divergence lives in (1) fill-raster/surface-value classes downstream of exact boundaries (ISI/Top/Sparse rasters, bridges), (2) drift-carrying layers not yet probed, (3) E-accumulator digits, (4) M73/time. NEXT (R292): probe an ISI raster segment end at an exact-boundary layer — with boundaries exact, raster extents should match unless the fill algorithms themselves (adjust_solid_spacing/raster clip) carry a value bug; localize one segment. GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R290 — Z=2.2 CHAIN CONFIRMED EXACT; FILLBOUND PARADOX POSED (docs only, 68483).** Correct-paired NFDBG (key=print_z both sides, asserted; probes reverted): z=2.2 pre-tail `last` **BYTE-EXACT** (167 pts) — consistent with L56. PARADOX: inputs byte-exact + FILLBOUND_QUANT deltas numerically == native (R286) ⇒ tail outputs should be byte-exact ⇒ quant should strictly improve, yet it measured +106 GLOBAL (attribution-free). Candidate resolutions: (a) quant branch has a residual bug (e.g. rust tail's missing native `loop_number < 0 → inset=0` case, or loop_number value semantics differ vs native's post-loop mutation `loop_number = i-1`); (b) the tail output feeds consumers whose OTHER inputs make razor ties (union with legacy-computed pieces elsewhere); (c) offset_expolygons_clib_scaled prescaled path behaves differently from assumed. NEXT (R291): enable FILLBOUND_QUANT + dump `infill_exp` output at z=2.2 vs native (one probe round settles (a)/(c) — if output == native, it's (b) and quant lands with its consumers fixed). GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R289 — ⚠ RETRACTION: R287/R288 "STRUCTURAL DIVERGENCE" WAS A PROBE PAIRING ARTIFACT (docs only, 68483).** LSLDBG at matched indices: L10 (z=2.2) lslices AND region slices are **BYTE-EXACT** (167 pts both engines); L11 (z=2.4) also BYTE-EXACT (178 pts). The R287/R288 probes compared rust PG `layer_id=11` (= layer index 11, z=2.4, 178 pts) against native `slice_z=2.1` (= index 10, z=2.2, 167 pts) — DIFFERENT LAYERS. There is NO overhang-layer slice divergence and NO 0.14mm structural class; those entries are void. PROTOCOL LESSON (banked): every cross-engine probe must print an unambiguous layer key (print_z) on BOTH sides and the diff script must assert key equality before comparing. Standing facts intact: L56 chain byte-exact; FILLBOUND_QUANT deltas value-correct with bimodal ±layer flips (the +56 @z=2.2 attribution may itself be multiset z-attribution noise — travel lines shifting across layer boundaries between variants). NEXT (R290): re-probe z=2.2 tail inputs with CORRECT pairing (NFDBG=2.1 / NFDBG_ID=10); if exact (expected), the FILLBOUND bimodality needs a different explanation — check whether per-z attribution of the ±deltas is stable (attribute by preceding-Z line at diff time in both variants). GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R288 — ★ L11 CLASS ROOT = SLICE-INPUT DIVERGENCE (docs only, 68483).** ITDBG/ENTDBG probes (reverted, native rebuilt): the perimeter-loop divergence at L11 exists at **i=0** (166-vs-176 pts) and traces to the PG INPUT ITSELF — native `surface.expolygon` contour = **167 pts** vs rust `slice` = **178 pts** BEFORE any PG processing. The R248 "lslices byte-identical" held at L71 only; curved-hull overhang layers (L11/z2.2) diverge at the SLICE level (11 extra rust points → every inset shifts ~0.14mm → Floating/ISI/bridge classes on those layers). REVISED FUNNEL: slice pipeline (mesh slice → make_expolygons → region slices) is NOT uniformly exact — the flat-region layers converged (L56/L71) but curvature-dense layers didn't. NEXT (R289): dump L11 lslices both engines pre-PG; localize slice divergence (facet crossing / make_expolygons union / decimate / SLICE_SIMPLIFY gates at this z); the ±11-point difference smells like DP-simplify tolerance or slice segment chaining. GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R287 — Z=2.2 STRUCTURAL DIVERGENCE FOUND: OVERHANG-LAYER CLASS (docs only, 68483).** NFDBG probe at the FILLBOUND_QUANT worst layer (z=2.2, layer 11 — hull-bottom overhang curve; probes REVERTED): pre-tail `last` differs STRUCTURALLY — 167-vs-176 pts, systematic ~0.14mm (−14000-unit) contour shift, EVERY vertex drifted. NOT the ulp class: the two engines run a different geometry path on this surface. L11 also shows only ONE rust tail invocation (last=1) vs L56's clean two-surface pattern. SUSPECTS: overhang/ext_perimeter_smaller_width branch (PG.cpp:977-1010 — the precise-outer-wall/narrow-loop path R278 quantized but couldn't test), overhang splitting (detect_overhang_wall lower_slices clipping), or loop-count divergence upshifting the final inset. This class plausibly = the Floating-vertical-shell 6.3k + part of ISI on overhang layers. NEXT (R288): localize which loop iteration diverges at L11 — probe `offsets` per iteration i (counts + first verts) both engines; then audit the branch that differs (smaller-width vs overhang clip). GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R286 — TAIL INPUTS BYTE-EXACT AT L56; FILLBOUND_QUANT VALIDATED, BIMODAL (docs only, 68483).** NFDBG probe (PG.cpp:1399 + rust tail, REVERTED both trees): at layer 56 (z=11.4) the pre-tail `last` (231/51/3 pts) AND `not_filled_exp` (124/28 pts) are **BYTE-EXACT** vs native — R277/278 propagated through gap-trim + simplify_p + union. Numeric delta check: FILLBOUND_QUANT deltas == native EXACTLY (−25785.0/11312.0; legacy mm off by 0.18/0.39 units). Per-layer attribution of the +106: BIMODAL — 62 layers worse, 42 better, 137 same (worst z=2.2 +56; best z=0.6 −17) — value-correct deltas flip razor-edge geometry per-layer depending on residual input drift. UNLOCK RULE: re-test FILLBOUND_QUANT (and VSHELL_REG_QUANT) after each input-convergence round; expect flip when most layers reach L56-grade exactness. NEXT (R287): why do OTHER layers' tail inputs drift when L56 is exact? Probe a WORSE layer (z=2.2, +56) same way — likely first-layer/bottom-specific chain (different flows/ext handling at L11? z2.2=layer 11) or a class the R277 quant missed (smaller_width path, ext branch). GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R285 — FILL-BOUNDARY TAIL QUANT PARKED (+106) + MISMATCH SPLIT (12g 68483).** Tail deltas quantized to native coord_t domains (PG.cpp:1383-1430: INT-div inset, unscale/scale overlap trunc, DOUBLE-div offset2 deltas vs INT-div no-overlap branch, double(int) top offsets via _scaled prescaled) — MEASURED +106 → parked behind **FILLBOUND_QUANT**. Pattern now CONFIRMED 3×(vshell, tail): exact deltas DECOHERE while inputs drift — the compensation stack must converge INPUT-FIRST (order: inner-wall `last` chain → tail → vshell). MISMATCH SPLIT @68483 (rust-side): XY-drift dominates (ISI 4.8k, Floating 3.4k, Bridge 2.7k, Sparse 2.0k, Top 1.7k, walls 1.8k) vs E-only ~2.3k (inherits accumulator phase from XY-drift — deprioritized). NEXT (R286): converge the inner-wall `last` chain — the offsets feeding it are already F1-quantized (R277) but `last` also passes through union/simplify steps; probe ONE inner-wall inset expolygon (mid layer) rust-vs-native to count residual drift (native probe needed — new dump), then fix the surviving op. GATES: default 147987/7adae05c ✓ 12g 68483 ✓.

- **R284 — REGION-EXPANSION PARAMS QUANTIZED (12g 68483, −2 flat).** shell_width/expansion_min/expansion_top/closing_radius + SCALED_EPSILON fallback mirrored as native f32-on-coord_t chains (LayerRegion.cpp:529-550) under REGION_EXPANSION_FAITHFUL — near-coincident at this profile (hardening). CENSUS @68485 unchanged in shape: ISI 11.2k, Top travel 7.9k + XY 4.1k, Floating 6.3k, Sparse 5.4k, Bridge 4.0k — byte-exact Top CLASSIFICATION didn't shrink Top-extents: the toolpath extents depend on FILL-BOUNDARY values downstream (fill_expolygons → surface → raster extents chain). INSIGHT: the drift funnel continues PAST detect into the fill-boundary tail (perimeter-generator not_filled/fill_clip/top_fills chain — R249-253 territory) and expand/shrink in discover_horizontal/vertical shells. NEXT (R285): probe ONE Top-surface raster segment end (both engines) to identify which boundary value sets its extent — the R276-style leaf-first forensic, now that detect + gaps are exact. GATES: default 147987/7adae05c ✓.

- **R283 — ★ DETECT OFFSET TRUNCATION: TOP CLASSIFICATION BYTE-EXACT (12g 68485, −102).** Native detect offset = `float(scaled_width())/10.f` (PO.cpp:1508) — coord_t TRUNC first (41999→4199.8999f); rust passed width()/10 (4199.9999) — 0.1-unit error on every detect opening_ex. Fixed under TOPFILL_FAITHFUL. VERIFIED rust-only vs saved R282 native dump: pre-expansion Top polys idx57 now 4/4 BYTE-EXACT (was 30% verts ±1..10). VSHELL_REG_QUANT retest +137 → park stands (decoherence deeper in vshell internals). NEXT (R284): re-census — the byte-exact Top classification should shrink Top-extents/ISI/upper-ISI classes; then continue the truncation-recipe sweep (detect is one of MANY `scaled_width()/k`-style deltas — grep native for `scaled_width() /` and `scaled_spacing() /` patterns and audit each rust counterpart). GATES: default 147987/7adae05c ✓.

- **R282 — VSHELL INPUT LOCALIZED: TOP-CLASSIFICATION DRIFT ±1..±10 (probe round, docs only, 68587).** VSHELLDBG probe (PrintObject.cpp:1850 + rust cache build @idx57, REVERTED both trees): pre-expansion Top sets have IDENTICAL structure (4 polys, same vert counts) but ~30% of vertices drift ±1..±10 units — the drift enters BEFORE discover_vertical_shells, in the `layerm.slices` Top surfaces from **detect_surfaces_type** (confirms R248: divergence born inside detect). The ±10 magnitude = ClipperSafetyOffset — pointing at ApplySafetyOffset::Yes semantics + offset deltas in the detect top/bottom chain (R237's old note). Since lslices are byte-exact (R248), detect_surfaces_type internals are now THE bottleneck for ISI 11.2k + Top 12k + vshell park. NEXT (R283): audit rust detect_surfaces_type vs native PrintObject.cpp:1450-1650 — every diff/offset op: delta exprs (f32-of-scaled recipe), ApplySafetyOffset semantics (cz safety shims exist: difference_clib_safety, cz_intersection_closed_safety), join types; then re-probe pre_top idx57 (saved native dump in vshelldbg.err reusable — rust-only iteration). GATES: default 147987/7adae05c ✓.

- **R281 — ★ FLOW SPACING BIT-EXACT (12g 68587).** FLOWDBG probe (PrintObject cache loop idx 57, reverted both trees): widths + trunc ints matched but rust spacing was 1 f32-ULP off (.377079636 vs .377079606). ROOT: native rounded_rectangle_extrusion_spacing (Flow.cpp:191-199) = ALL-f32 chain (coef float(1.-0.25π) rounded FIRST, f32 mul/sub); rust did f64 + one final rounding. INVERSE width_from_spacing (Flow.cpp:201-204) differs AGAIN: double inner arith, float() ONCE. Both ported under FLOW_F32; probe confirms spacing now bit-exact. 12g 68588→68587 (line-flat but foundational: all trunc recoveries + R257 with_spacing writeback now on bit-exact ground). VSHELL_REG_QUANT re-test +88 → park STANDS: deltas provably native-exact now, so the vshell drift is in its INPUT surfaces (slices top/bottom sets, holes chain) — next probe = dump cache.top_surfaces counts/verts at idx 57 both engines (native FLOWDBG pattern, PrintObject.cpp:1850). GATES: default 147987/7adae05c ✓.

- **R280 — VSHELL DELTA QUANT PARKED (+91) — FLOW-SPACING MISMATCH SUSPECTED (12g stays 68588).** Quantized all discover_vertical_shells deltas to native f32-on-coord_t exprs (top_bottom_expansion f32(S)*0.05f PO.cpp:1793; ext expand f32(S_e) :1945; min_pis f32(S)*1.05f :1905; regularize radii :2012-17; area thr f32*scaled(1.5|8) :2047-48). MEASURED +91 (68679), isolating to the spacing/exp sites; radii/thresholds neutral. DIAGNOSIS: rust Flow::spacing() likely ≠ native f32 m_spacing → trunc(sp/1e-5) recovers the WRONG coord_t → quantization decoheres. Parked behind **VSHELL_REG_QUANT** (inert). UNLOCK: probe native scaled_spacing/scaled_width per role vs rust ints (one printf round), fix the Flow spacing chain, re-test — this would also re-open the R277-style recipe for ALL shell/surface deltas (the trunc recovery presumes flow-value parity!). NOTE the R277/R278 wins used perimeter_flow/new_flow spacings — those DID match (gaps went byte-exact), so the mismatch is likely role-specific (SolidInfill/ExternalPerimeter flows via lr.flow(...)). NEXT (R281): flow-value probe — dump scaled_width/scaled_spacing for all roles at one layer, both engines; fix drift; re-test VSHELL_REG_QUANT. GATES: default 147987/7adae05c ✓ 12g 68588 ✓.

- **R279 — LINEWIDTH_PERPATH UNLOCKED: PRIMARY = 12-GATE 68588 (docs only).** Width convergence (R277/R278) flipped the parked LINEWIDTH_PERPATH gate net-POSITIVE: 69345 → 68588 (−757; was +4.4k at R275) — exactly the roadmap's predicted unlock chain (medial-axis widths → LINE_WIDTH). **PRIMARY = 12-GATE (11g + LINEWIDTH_PERPATH) = 68588** (session 99449→68588, −31.0%). CENSUS @69345 (pre-LW): ISI XY+E 11.2k; Top travel+XY 12.0k; Floating 6.3k; Sparse 5.4k; Bridge 4.0k; Gap cluster 10.6k (3.7k was LINE_WIDTH structural → LW gate); walls XY+F 8.8k. NEXT (R280+): delta-quantization sweep over the shell/surface chain — discover_vertical_shells is already clib-routed (print_object.rs:3198-3240 local grow/shrink/offset2 wrappers) but its DELTAS (`exp`, ext_spacing_mm, min_pis, narrow_sparse_r, tiny_overlap_r) are raw mm — cross-reference each against PrintObject.cpp:1739-2110 native coord_t exprs (same recipe: trunc(v/1e-5) ints, double arith, f32 boundary); targets ISI 11.2k + Top 12k + upper-ISI/sparse island selection. GATES: default 147987/7adae05c ✓.

- **R278 — ★★ GAP-BAND DELTAS: −1210, 11-GATE 69345; GAPS INPUT BYTE-EXACT.** Re-probe vs the SAVED R276 native dump (no native rebuild): perimeter-chain `gaps` input now matches native 11/11 polys BYTE-EXACT at the F14761 layer — R277 fully converged the collection. Remaining drift was the BAND deltas: both gap-band sites (layer.rs perimeter+fill gap) now build min/max as native doubles from trunc(v/1e-5) coord_t under F1_UNION. Also quantized (INERT here, detect_thin_wall=0): i==0 thin-wall chain + smaller-width offset2. RESULTS: 11-gate 70555→69345; oracle F14761 .238→**.268** vs native .269 — ONE final digit (mm3 last-ulp). Session 99449→69345 (−30.3%). TECHNIQUE NOTE: saved-probe reuse (native side from R276 gapdbg2.err) made this a rust-only round. NEXT (R279+): the quantization pattern generalizes — sweep the SAME native-double-from-coord_t recipe over remaining mm-domain deltas feeding surfaces (shell/Top-extents chain: discover_vertical_shells offsets, fill boundary tail offset2, expand/shrink in detect_surfaces_type) — each is a candidate for the same ±0.5-unit class; re-census 11-gate classes first to re-rank. GATES: default 147987/7adae05c ✓ (probe reverted).

- **R277 — ★★ DELTA QUANTIZATION: −1304, PRIMARY NOW 11-GATE 70555.** The R276 ±1-unit class root-caused to TWO delta bugs in the F1_UNION perimeter loop: (1) inner offset2 `half_min = min_spacing_c / 2` INTEGER division vs native DOUBLE `min_spacing/2.` (PG.cpp:1026-1028) — dropped .5 shifts every inner-wall join on odd min_spacing; (2) gap-detection deltas halved the raw MM distance vs native `float(0.5*distance)` on the COORD_T int (PG.cpp:1030-1035); plus coord_t recovery now `v/0.00001` (native scale_ DIVIDES by inexact 1e-5; `v*1e5` trunc can differ by 1). RESULTS: 9-gate 72268→70964 (−1304); gap oracle F14761 .445→.238 (native .269, Δ 5.7× smaller); MEDIALAXIS_NATIVE and VOLCAP_FAITHFUL both flip NET-POSITIVE → **PRIMARY = 11-GATE (9g + MEDIALAXIS_NATIVE + VOLCAP_FAITHFUL) = 70555** (session 99449→70555, −29.1%). NEXT (R278+): same quantization sweep for the REMAINING perimeter-loop offsets (i==0 ext branch offset2/thin-wall opening at PG.cpp:963-975 — rust perimeter_generator.rs:632 still geo `offset2`; the smaller-width branch; last-inset shrink) then re-GAPDBG to count surviving ±1 verts; oracle residual .238-vs-.269 suggests one more sub-class. GATES: default 147987/7adae05c ✓.

- **R276 — BOUNDARY WALL EDGE FOUND: ±1-UNIT DRIFT IN PERIMETER-CHAIN `gaps` INPUT (probe round, docs only, 72236).** GAPDBG probe (PG.cpp:1335 + layer.rs gap block, REVERTED both trees, native rebuilt) at the F14761 layer (rust print_z=11.4 ↔ native slice_z=11.3): gap-band `gaps_ex` contours have SAME structure (37 verts, counts 9=9, PL 15=15) but vertices interleave EXACT matches with ±1..±66-unit drift. Walking up: the `gaps` INPUT (11 polys both) ALREADY drifts ±1 unit (1e-5 mm) on ~half the vertices ((669504,668331)-vs-(669503,668331) etc.) — the perimeter INSET chain rounds differently; the band ±min/2,±max/2 miter offsets amplify ±1 → ±66 at join vertices. CONCLUSION: boundary-value wall root = perimeter-loop offset rounding (rust inset offsets vs native offset_ex/offset2_ex float-delta vendored-clipper calls). Native gap collection: PG.cpp ~1130 `append(gaps, diff_ex(offset(last,-0.5f*d), offset(last,-d)))` per iteration. R277+: audit rust perimeter-inset offset route (which backend? delta float rounding? `last` drift from first inset?) — dump first-inset `last` L57 both engines, then route/quantize to native-exact (multi-round campaign like R250-253 fill chain). GATES: default 147987/7adae05c ✓ (probes reverted, native clean).

- **R275 — MA CLASS CLOSED: PORT-COMPLETE-GIVEN-INPUT (gated, 72236).** `medial_axis_scaled()` added; 3 callers build native-exact scaled params (gap: doubles from trunc(w|s/1e-5) coord_t, PG.cpp:864-5/1329-30; thin wall: trunc((nozzle_f32/3)f64/1e-5) + trunc-scaled_width + ALL-f32 spacing2 PG.cpp:868/972/979). Oracle F14761 UNCHANGED at .445 (native .269) and LOCALIZED: F-segment XY+E byte-match native; following arc J -25.699-vs-25.697 → gap-band INPUT expolygon sub-mm drift is the residual source — width residual (~6.6e-6mm, the R246 number) INHERITS from boundary values. Medial-axis port itself now exact given input (voronoi vertices bit-exact via shim, widths verbatim, postproc verbatim, params quantized). RETESTS under MA: +VOLCAP 72334 (+98 parked), +LINEWIDTH_PERPATH 76711 (parked) — unlock now waits on ROADMAP LEVER (2) shell/boundary edge values (gap band = covered-set/fill-boundary chain). NEXT: lever (2) — pick one gap-band boundary (the L~? F14761 layer) and forensic the covered→difference→tail chain values vs native, or start the broader shell/Top-extents boundary campaign. GATES: default 147987/7adae05c ✓.

- **R274 — MEDIALAXIS_NATIVE WIRED, SHIM ROUTE LIVE (gated, 72237 w/ gate).** ExPolygon::medial_axis (geometry/expolygon.rs:512) routes through `clipper_z_sys::medial_axis_native` under MEDIALAXIS_NATIVE; ThickPolyline gains gated `edge_widths` (C++ 2*(n-1) edge-pair mm array) consumed verbatim by thicklines() (a=w[2i], b=w[2i+1]) instead of the per-vertex collapse. RESULTS: 9g+MA = 72237 (vs 72268); VOLCAP gap oracle F14761.269: rust .459 → .445 — CONVERGING, NOT CONVERGED. Next suspect: min/max width param quantization — native callers do `coord_t(scale_(x))` TRUNCATION (PerimeterGenerator.cpp:955 nozzle/3, :1332 gap min/max) then pass double(coord_t); rust passes untruncated mm which the wrapper ×1e5 — feeds validate_edge acceptance + postproc max_width extension. ALSO EXPOSED (rust-port divergences the shim sidesteps): rust SCALED_EPSILON=0.1 vs native 10; rust applies the overflow guard native compiles out (CLIPPERLIB_INT32 defined, clipper.hpp:83). R275: quantize gate-path min/max to native coord_t values at all 3 callers (perimeter_generator.rs:656 thin walls, layer.rs:672 + :2778 gap), re-oracle; then LINE_WIDTH digits + re-test LINEWIDTH_PERPATH/VOLCAP under MA. GATES: default 147987/7adae05c ✓ czsys tests 7/7 ✓.

- **R273 — MA-SHIM: EXPOLYGON POSTPROC + ma_build FFI LANDED (smoke-tested, inert, 72268).** Verbatim ExPolygon.cpp:263-371: `fmaxf` max_w quirk KEPT (float truncation), endpoint extension = Eigen-faithful `(p2-p1).normalized()*max_width` (v/sqrt(squaredNorm), zero-vec passthrough) with TRUNCATING double→coord_t casts (Eigen cast<>), contour-only Polygon::intersection (closing edge (front,back) checked FIRST, first-hit-wins — Polygon.cpp:188), point_projection foot = floor(v+0.5) (Polygon.cpp:310), short-polyline removal (< max_w*2) + greedy reconnection (reverse/splice, j=i restart). line_alg::intersection mirrored (cross2 denom, EPSILON=1e-4 guard, t∈[0,1], trunc cast). FFI `ma_build(scaled i64 contour+holes, min/max) -> MaBuildResult` flat (i64 coords / f64 widths / sizes / endpoint flags) + `ma_free` + rust `medial_axis_native` safe wrapper; thin-rect smoke test passes (spine centered y=20000, widths 40000; 7/7 crate tests). NEXT (R274): wire MEDIALAXIS_NATIVE gate into libslic3r-rs medial-axis callers (geometry/medial_axis.rs / thin-wall + gap-fill paths), verify oracle gap F14761.269 under VOLCAP + LINE_WIDTH digits. GATES: default 147987/7adae05c ✓.

- **R272 — MA-SHIM: MEDIALAXIS WALK LANDED (compiles, inert, 72268).** medial_axis_shim.cpp now has the full walk verbatim-adapted from Geometry/MedialAxis.cpp:70-304: EdgeData half-edge store + `edge_data()` index-pair accessor (edge_id/2, edge_id&1), validate_edge (w0/w1 = segment-clamped distance×2 or endpoint-dist×2; PI/8 facing filter; SCALED_EPSILON=10; min/max width acceptance with twin swap), process_edge_neighbors (twin rot_next scan, single-neighbor chaining, reversed width push on twin parity, endpoint_second on dead end), build (seed loop, forward + reverse-splice growth, loop endpoint suppression). TWO FAITHFULNESS CATCHES: (a) native's CLIPPER_MAX_COORD_UNSCALED overflow guard is `#ifndef CLIPPERLIB_INT32` and BambuStudio clipper.hpp:83 defines CLIPPERLIB_INT32 unconditionally → guard COMPILED OUT in native, removed from shim; (b) interior-case distance = `(t*v - va).squaredNorm()` on integer-diff doubles (Line.hpp:42-68), NOT foot-point reconstruction (different rounding at 1e7-scale coords) — rewritten to mirror. NEXT (R273): ExPolygon::medial_axis post-processing (ExPolygon.cpp:263-375) + `ma_build()` FFI + rust MEDIALAXIS_NATIVE wiring; oracle = gap F14761.269 under VOLCAP. GATES: default 147987/7adae05c ✓ 9g 72268 ✓.

- **R271 — MA-SHIM: ANNOTATE MACHINERY LANDED (compiles, inert).** medial_axis_shim.cpp now carries the full inside/outside annotation: Vertex/Edge/CellCategory color-bit accessors (VoronoiOffset.hpp:30-97), contour_point, boost-ULP vertex_equal_to_point (the EXACT ULP comparison native uses, VoronoiOffset.cpp:204-226), on_site, reset_inside_outside_annotations, and annotate_inside_outside verbatim-adapted from VoronoiOffset.cpp:640-967 (contour marking → secondary-edge OnContour → infinite-edge outside + segment-side classification via double cross product → Point-Point expansion round → cell seed fill; asserts/debug stripped, arithmetic untouched). NEXT (R272): the MedialAxis walk (EdgeData, validate_edge with min/max width + angle filters, process_edge_neighbors chain growth, build) + ExPolygon::medial_axis post-processing + ma_build FFI. GATES: default 147987/7adae05c ✓ (shim inert).

- **R270 — MA-SHIM PORT CORPUS STAGED (docs only, 72268).** Native spans to adapt (re-extract with these ranges if scratchpad lost): Geometry/VoronoiOffset.hpp:30-120 (Vertex/Edge/CellCategory enums + color accessors), VoronoiOffset.cpp:190-260 (detail::vertex_equal_to_point/on_site/contour_point/vertex_point), 630-967 (reset_inside_outside_annotations @640 + annotate_inside_outside @650), Geometry/MedialAxis.cpp:380-690 (measure/validate_edge/process_edge_neighbors/build + EdgeData), ExPolygon.cpp:263-375 (medial_axis post-processing: boundary extension + min/max filtering). Adaptation rules: Point→MaPoint (lrint double ctor), Line→MaLine, Lines→std::vector<MaLine>, Vec2d→plain double pairs, strip asserts/debug, keep arithmetic VERBATIM. Remaining rounds: (a) annotate+helpers, (b) MedialAxis walk, (c) ExPolygon postproc + ma_build FFI + rust MEDIALAXIS_NATIVE wiring, (d) oracle verify (gap F14761.269 under VOLCAP). GATES: default ✓ 9g 72268 ✓.

- **R269 — MEDIAL-AXIS SHIM SCAFFOLD LANDED (72268, inert).** crates/clipper-z-sys/shim/medial_axis_shim.cpp compiles against the devbox nix boost **1.87.0** (SAME headers/version as the native binary per CMakeCache Boost_INCLUDE_DIR) — MaPoint/MaLine minimal types with Slic3r-faithful semantics (lrint Point(double,double), segment-clamped distance_to, [0,2π) orientation), boost::polygon point/segment concept registration, VD typedef, and an `ma_selftest()` smoke fn (voronoi over a square). build.rs wires the file + pkg-config boost includes. NEXT (R270+): verbatim-adapt annotate_inside_outside (VoronoiOffset.cpp:650+, color-bit vertex categories per Voronoi.hpp) + MedialAxis walk (validate_edge/process_edge_neighbors/build, MedialAxis.cpp:445-685) + `ma_build()` FFI + rust wiring behind MEDIALAXIS_NATIVE; oracle = gap F14761.269. GATES: default 147987/7adae05c ✓ 9g 72268 ✓.

- **R268 — MEDIAL-AXIS DRIFT ROOT + SHIM PLAN (docs only, 72268).** Audit: rust's width math (rounded Line points, clamped-projection distance ×2) is FAITHFUL to native Geometry/MedialAxis.cpp:598-645 — the drift is in the VORONOI VERTEX DOUBLES themselves (boost::polygon's builder vs rust's Voronoi port differ in last-ulp vertex arithmetic; everything downstream is exact). Fix = SHIM ROUTE (secol/clipper pattern): `boost/polygon/voronoi.hpp` COMPILES in the devbox env. R269 PLAN: shim `ma_build(contour+holes as scaled i32 pairs, min_width, max_width) -> ThickPolylines (i64 points + f64 widths)` — reimplement MedialAxis.cpp's ~200-line walk (validate_edge width math, angle filter, chain growth) against boost's voronoi_builder with plain structs (aabb-shim pattern); wire behind MEDIALAXIS_NATIVE gate into ExPolygon::medial_axis; oracle = gap-fill F14761.269 + LINE_WIDTH digits. Unlocks lever (1) of the roadmap. GATES: default ✓ 9g 72268 ✓.

- **R267 — VALUE-CONVERGENCE ROADMAP (docs only, 72268).** Sparse grid check: L40 fully aligned, L27 5/13, L60 2/8 — island-level line selection differs (small sparse islands, boundary-driven). CAMPAIGN STATE: all logic-level classes are FIXED (timelapse, OBJECT_ID, M106 floor, M204 pending-accel, seam-split travel, wipe F/paths, fill wipe, floating speed, spacing writeback, gap-trim reorder, templates) or DIAGNOSED-as-value-bound (bridge tie-break, LW/VOLCAP, Top extents, M73). ORDERED VALUE-CONVERGENCE LEVERS (by expected yield): (1) **medial-axis scaled-domain port** — unlocks gap widths → covered-set → VOLCAP → LINEWIDTH → frac-F → time → M73 (~10k+ chain); (2) **shell/boundary edge values** (top/bottom propagation offsets at clib precision end-to-end) — Top extents + upper ISI + sparse island selection + bridge anchors (~15k); (3) E-accumulator ULP ordering (~small). Measurement protocol reminders: pinned canon native (nondet ±35), SURFDBG/FILLDBG/CANDDBG probe patterns in git history. Session R224-R267: 99449 → 72268 (−27.3%), default locked 147987/7adae05c throughout. GATES: default ✓ F1 ✓ 9g 72268 ✓.

- **R266 — BRIDGE CLASS FINAL DIAGNOSIS: NEAR-TIE ANGLE FLIP (docs only, 72268).** Real sparse on L27/28 is GRID = symmetric ±45° in BOTH engines — so the anchor histogram is a near-tie between 45° and 135° families; the winner is decided by which anchor segments survive `intersection_pl(infill_lines, shrink(expansion_area, spacing))`, i.e. by sub-mm value drift in the synthesized lower-sparse polylines and expansion_area. Native picked 225°(≡45°), rust 135° — with the flipped angle, rust's construct_anchored_polygon (port verified: rotate/ray/clip chain + all post-clips match native 2900-2960) legitimately grows 3× along the part's long axis into 3 surfaces. NOT a logic bug — a TIE-BREAK INSTABILITY amplifying residual drift. Bridge class PARKS behind sparse-raster/boundary value convergence (anchors must byte-match to break ties identically). Remaining wall taxonomy is now UNIFORM: every open class (bridge, floating, sparse, gap, top extents, upper ISI, F/M204 tails, M73) reduces to sub-mm boundary/value convergence. GATES: default ✓ 9g 72268 ✓.

- **R265 — EXPANSION NARROWED TO THE DIRECTIONAL-GROWTH STEP + ANGLE-SOURCE SUSPECT (docs only, 72268).** EXPDBG @L28: rust anchors=13 (non-empty — the R262 "anchors empty" comment is stale), area_to_be_bridge = 5.18e11 + 7.33e11 = 1.25e12 (SANE, pre-growth) — but final BridgeInfill = 3.83e12, so the DIRECTIONAL GROWTH step (expand along bridging_angle toward anchors, native PrintObject.cpp:2900-2960) over-grows ~3×. Consistent with growing along the WRONG axis: the π/2-off angle would sweep across the part instead of between anchors. ANGLE-SOURCE SUSPECT: rust's anchoring InfillConfig (layer.rs:2905-2924) may not mirror the REAL sparse fill's gated faithful direction (+90 unconditional + parity alternation) — if anchor polylines are rotated 90° from the real lower-layer sparse lines, the histogram flips π/2 and the growth follows. R266: verify anchor-line angle vs real sparse; audit the growth step. GATES: default ✓ 9g 72268 ✓.

- **★ R264 — BRIDGE DIVERGENCE PINNED TO THE EXPANSION PHASE (docs only, 72268).** CANDDBG2 two-sided probes at L28: (a) unsupported_area/lower_layer_solids inputs match to 0.008%; (b) the recorded CANDIDATES match too — both engines find the same 2 candidates with areas within 0.05% (1.729e11 + 5.894e11). Yet the final bridge fill at z=5.8 is native 2.10e12 (candidates expanded ~2.7×) vs rust 3.83e12 (~5×) split into 3 surfaces with a π/2-different angle. THE BUG IS IN THE EXPANSION PHASE (native PrintObject.cpp:2700-2900: candidate → area_to_be_bridge growth toward anchors, max bridging distance, clipping to lower sparse; rust print_object.rs ~1700-1900). R265: audit the expansion loop — growth deltas, anchor clipping, the merge that produces 1-vs-3 surfaces, and the angle input (anchors from the over-grown area explain π/2). GATES: default ✓ F1 ✓ 9g 72268 ✓.

- **R263 — CANDIDATE PASS AUDITED FAITHFUL (docs only, 72268).** Line-by-line vs native PrintObject.cpp:2196-2279: unsupported_area gather (whole lower fill), lower_layer_solids rule (non-Internal or 100% density), the opening/expand sequence (shrink 1×sp → expand 4×sp; unsupported shrink 3×sp → diff), partially_supported test, 3×3×spacing² threshold (correctly SCALED in rust), worth_bridging expansion + leftovers merge — all match. The 1.8×/π-2 divergence therefore enters through the INPUTS: lower-layer fill_expolygons + Internal/InternalSolid classification values (the vshell/detect chains), whose small drift flips the BINARY threshold decisions (hence L72/L193 rust-only candidates and 6.8× on L28). NEXT (R264): probe unsupported_area + lower_layer_solids totals at L28 both engines to quantify which input diverges. GATES: default ✓ F1 ✓ (docs only).

- **R262 — BRIDGE ANGLE/AREA SHARE ONE ROOT: THE CANDIDATE AREA (docs only, 72268).** determine_bridging_angle is faithfully ported (fixed-pattern branches match — only 3DHoneycomb/CrossHatch; +π/2 histogram + sliding window identical; call sites match native PrintObject.cpp:2888/2894). The π/2 angle difference therefore comes from the INPUTS: `anchors` (sparse-infill lines under the bridge) and `area_to_be_bridge` — which is also the 1.8× over-classification. ONE root: the internal-bridge CANDIDATE construction (rust print_object.rs ~1023-1250 vs native ~2700-2900, the bridge_over_infill pass: what counts as unsupported-over-sparse). NEXT (R263): audit candidate-surface construction — the support test (lower sparse coverage), expansion rules, and the L72/L193 rust-only bridges (native finds NO candidates there). GATES: default ✓ F1 ✓ 9g 72268 ✓ (docs only).

- **★★ R261 — INTERNAL-BRIDGE ROOTS FOUND (docs only, 72268): angle off by π/2 + 1.8× over-classification.** SURFDBG at z=5.8 (L28) with bridge fields: native = ONE BridgeInfill surface, bridge_angle=3.927 rad, area 2.10e12; rust = THREE BridgeInfill surfaces, angle=2.3562 rad (EXACTLY π/2 less — rasters PERPENDICULAR to native's, explaining R234's "totally different geometry"), total area 3.83e12 (1.8×) spanning a wider bbox. Native also keeps a separate Floating-concentric surface rust lacks (likely swallowed by the over-wide bridge). TWO WORK ITEMS (R262): (1) rust internal-bridge ANGLE detection (BridgeDetector optimal-angle vs rust's — the π/2 offset suggests a direction-vs-normal convention flip or a missing +90); (2) internal-bridge AREA rule (rust classifies sparse-supported area as bridge that native leaves to floating shell/sparse). Probes reverted, native rebuilt. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ 9g 72268 ✓.

- **R260 — BRIDGE OVER-CLASSIFICATION MAPPED (docs only, 72268).** Per-layer Bridge extrusion-line counts: rust systematically over-bridges — L28: 453-vs-67 (6.8×!), L38: 521-vs-254, L1: 355-vs-204, L69: 133-vs-61, L72: 83-vs-0, L193: 24-vs-0, L187-194 band ~1.5-2×. Also rust ARC-FITS bridge extrusions (G3 ...E inside Bridge) where native bridges are straight rasters (+ lift arcs only) — either rust misses native's bridge arc-fit exclusion or the over-classified regions are curved. Two work items: (1) bridge surface classification (detect bridges in process_external_surfaces — rust marks ~2× the area; probe bridge surface areas at classification output on L28 both engines); (2) bridge arc-fitting exclusion check. Bridge + knock-on Floating shell ≈ 6-7k of the flip. GATES: default ✓ F1 ✓ (docs only).

- **R259 — RESIDUAL TAXONOMY (docs only, 72268).** (a) ISI L40 E stragglers: 12 lines, ALL 1-ULP in the 5th decimal (.01674-vs-.01673) — E-accumulator rounding boundary, near-floor. (b) TOP surfaces: raster GRID IS ALIGNED (offsets match to 4 decimals: −16.6085/−16.2313/... both engines) but segment EXTENTS differ (L73: 3/28 matches; L74 rust has one extra grid line at −17.3623) — the monotonic line CLIP boundary (no_overlap band edge values) differs sub-mm, so line ends trim differently. (c) L100-style upper-layer ISI: fragmented small regions, offsets within 0.03mm, bboxes exact. ALL remaining fill classes now reduce to BOUNDARY VALUE convergence (fill/no_overlap band edges — the covered-set/vshell drift family) + E ULP accumulation. No structural bugs left in the fill pipeline. GATES: default ✓ F1 ✓ (docs only).

- **★ R258 — NEW BEST 72268 (−479): spacing writeback extended to the MONOTONIC path (top-surface fills recompute flow from adjusted spacing too).** XY+E census by feature @72268: ISI 6.0k/5.2k (down from 6.8k/5.0k pre-GAPTRIM — converging), Floating 2.8k/3.5k, Sparse 2.7k/2.8k, Gap 2.5k/2.4k, Top 2.1k/2.0k, Bridge 1.3k/2.7k (geometry, parked), walls 2.5k/2.5k. Remaining per-class work: ISI residuals (upper-layer XY + E stragglers), sparse/gap value drift, floating shell (bridge-family geometry). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2× f97f91ec.

- **★★ R257 — NEW BEST 72747 (−1055): ADJUSTED-SPACING WRITEBACK LANDED.** Thread-local ADJUSTED_SPACING_MM set in fill_surface_by_lines' adjust branch (native `this->spacing = unscale(_adjust_solid_spacing(...))`); the rectilinear emission site recomputes flow via with_spacing(adjusted) for solid non-bridge surfaces (native FillBase.cpp:138-149) — mm3 AND path width/height from the effective flow. Gated under GAPTRIM_PRETAIL (same family: it only fires on the reorder-converged rasters). L40 ISI matched-XY lines with IDENTICAL E: 2/256 → 244/256. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2× b836a637. PRIMARY 9-gate = 72747.

- **★★ R256 — SOLID-FILL E ROOT: ADJUSTED-SPACING WRITEBACK MISSING.** Under GAPTRIM the L40/L41 ISI rasters now match XY-exactly (255/277, 240/286 segments — phase FIXED by the reorder), but E differs on matched lines by a CONSTANT ×1.005 (0.24879-vs-0.24754 etc.). Root: native writes the solid-spacing adjustment back (`this->spacing = unscale(_adjust_solid_spacing(...))`, FillRectilinear.cpp:2862-2863) and fill_surface_extrusion recomputes flow/mm3 via `with_spacing(this->spacing)` (FillBase.cpp:138-149); rust computes the adjusted line_spacing LOCALLY in fill_surface_by_lines and NEVER writes it back — actual_spacing stays unadjusted → mm3 0.5% low on every adjusted solid fill. FIX (R257): plumb adjusted spacing out of fill_surface_by_lines (out-param) → filler.spacing → fill_surface_extrusion's actual_spacing, gated. Upper layers (L60/80/100) still XY-diverge (separate: surface geometry drift). GATES: default ✓ F1 ✓ (docs only).

- **R255 — POST-REORDER CENSUS (docs only, 73802).** Class shape ~unchanged: XY+E 42.9k, travel 12.1k, F 7.4k, LW 6.5k (parked), zlift 2.5k, M204 0.8k — the topology fix didn't move gcode mass because raster VALUES (phase from the ~1e-5 area drift) still differ per layer. VOLCAP retest: +104 (worse than pre-GAPTRIM +42 — cooling cascade reshuffled; stays parked). NEW LEAD: fill_expolygons piece ORDER differs (rust 10/69/10/12 vs native 10/10/12/69) — multiset-same but downstream group_fills/surface ordering may drive part of the travel class; check where native's piece order comes from (per-surface tail append order vs rust's surface processing order). Remaining wall = value-level: gap covered-set geometry (medial-axis sub-micron), raster phase. GATES: default ✓ F1 ✓ 9g 73802 ✓ (docs only).

- **★★ R254 — fill_expolygons TOPOLOGY CONVERGED (docs only, 73802): L71 under GAPTRIM = 4 pieces n=10/69/10/12, ZERO holes — exactly native's structure (R249: 4 pieces n=10/10/12/69, 0 holes).** Areas match to ~1e-5 relative (12733742382-vs-12733609286): the ragged 2-blob/6-hole/272-vert structure is GONE; the remaining drift is VALUE-level (sub-micron boundary differences from the covered-set/gap-entity geometry), which still flips adjust_solid_spacing/raster phase on many layers — hence the modest −115 gcode delta despite the structural fix. Remaining fill-wall work is now value-parity: (a) covered set = rust gap entities' polygons_covered_by_width vs native's (gap widths from the medial-axis chain — the R246 sub-micron class), (b) piece ORDER (multiset-same, chain reorders downstream). Next: re-census the 9-gate diff @73802 to re-rank classes post-reorder. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ 9g 73802 ✓.

- **★★ R253 — GAP-TRIM REORDER LANDED: PRIMARY now 9-GATE (+GAPTRIM_PRETAIL) = 73802 (−115).** Checklist executed: (1) classic fill-boundary tail extracted to `classic_fill_boundary_tail()` (pure refactor, 8-gate byte-same 7977681f); (2) per-surface ClassicTailInput{last, top_fills, fill_clip, loop_number} captured pre-tail under GAPTRIM_PRETAIL and merged through generate(); (3) layer.rs gap block now does covered → difference_clib(pre-tail last) → RE-RUN tail per surface (native PG.cpp:1373 order), replacing Internal fill_surfaces + fill_expolygons + fill_no_overlap_expolygons; legacy post-hoc trim kept for the non-gated path. Result −115 (smaller than hoped — the reorder fixes topology but the covered set / band details may still differ from native; L71 SURFDBG re-verify queued). PRIMARY = 9-gate (F1_UNION FRAME_UNIFY SLICE_SIMPLIFY SLICE_CENTER REGION_EXPANSION_FAITHFUL TOPFILL_FAITHFUL ZSMOOTH_FAITHFUL FLOW_F32 GAPTRIM_PRETAIL) = 73802 baseline, det 540f19d0 ×2. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓.

- **R252 — GAP-TRIM REORDER IMPLEMENTATION CHECKLIST (prep round; implement next).** Exact plan:
  1. perimeter_generator.rs: extract the classic fill-boundary tail (the `if top_fills_gate()` body, ~1508-1697: inset calc → infill_peri_overlap → simplify_p faithful → union_ex_clib → offset2_ex_clib → top_infill_exp/top_fills merge → no_overlap block) into `fn classic_fill_boundary_tail(&self, last: &ExPolygons, top_fills: &ExPolygons, fill_clip: &ExPolygons, loop_number: i32) -> (ExPolygons /*infill*/, ExPolygons /*no_overlap*/, ExPolygons /*top_band*/)`; generate() calls it (pure refactor, byte-same).
  2. PerimeterResult: add `tail_inputs: Vec<(ExPolygons /*pre-tail last*/, ExPolygons /*top_fills*/, ExPolygons /*fill_clip*/, i32 /*loop_number*/)>` — pushed per SURFACE in the classic loop under a new gate GAPTRIM_PRETAIL (collected right where the tail currently runs, BEFORE it).
  3. layer.rs gap block (after `covered` computed, ~line 700): under GAPTRIM_PRETAIL, for each tail_input: `last' = difference_clib(last, covered_ex)` then `(infill, no_overlap, top_band) = gen.classic_fill_boundary_tail(&last', ...)`; REPLACE fill_surfaces Internal + fill_expolygons + no_overlap_area + perimeter_top_band with the recomputed sets; SKIP the existing post-hoc trim (it double-counts). Note covered is merged across surfaces — spatially local, diff vs per-surface last is equivalent.
  4. Measure 8-gate+GAPTRIM_PRETAIL vs canon; verify fill_expolygons L71 via SURFDBG (expect 4 clean pieces); locks; fold into primary set if positive.
  Layer.rs needs a PerimeterGenerator handle (or make the tail an associated fn taking config values: ext_perimeter_spacing/perimeter_spacing/solid_infill_spacing/infill_wall_overlap/surface_simplify_resolution/join_type — pass via a small TailParams struct exported on the result). GATES: default ✓ F1 ✓ (docs only).

- **★★ R251 — STRUCTURAL ROOT OF RAGGED fill_expolygons: GAP-TRIM ORDER INVERSION (docs only, 73917).** The classic tail (simplify_p → union_ex → offset2_ex) is already clib-routed, and `last` arrives clib-clean from the wall insets — but native subtracts the gap-fill footprint from `last` BEFORE the fill-boundary tail (PG.cpp:1373 `last = diff_ex(last, gap_fill.polygons_covered_by_width(10))`), so the tail's simplify/offset2 produce naturally-separate clean pieces. Rust computes the fill tail FIRST (un-trimmed last → merged area) and post-hoc diffs the covered polygons in layer.rs — the late diff cuts corridors through the already-merged/simplified area, creating the 2 hole-bearing ragged blobs (n=272, 6 holes) instead of native's 4 clean pieces (n≤69). FIX PLAN (R252): expose per-surface pre-tail `last` from generate() + a pub fill-boundary-tail fn; the gated layer.rs gap block then does covered→difference_clib(last)→tail per surface, replacing the post-hoc trim. This should collapse the fill_expolygons divergence and, downstream, the ISI/gap/sliver classes (the 58% wall). GATES: default ✓ F1 ✓ (docs only).

- **R250 — ARACHNE FILL-AREA CHAIN CLIB-ROUTED (inert: profile is CLASSIC; 73917 byte-same).** Routed add_infill_contour_for_arachne through clib (DP-rings simplify_p + simplify_polygons_clib + union_ex_clib + offset2_ex_clib jtMiter) under TOPFILL_FAITHFUL — output byte-identical because this job runs **process_classic** (wall_generator_mode Classic), whose fill area is built in the classic chain (perimeter_generator.rs ~1500-1700: not_filled_exp → fill_clip → top_fills merge → infill_exp, sites 1633/1696) with several ops still geo-routed (intersection, offset_expolygons, union_ex in non-f1 branches). NEXT (R251): op-by-op audit of the CLASSIC fill-area chain vs PerimeterGenerator.cpp:1380-1430, route the remaining geo ops through clib under gate — this is the R249 root's actual code path. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ (byte-same).

- **★★ R249 — FRAGMENTATION ROOT = PERIMETER-GENERATOR FILL AREAS (fill_expolygons).** Chain walk completed: slices identical (R248) → detect's top/bottom sets structurally identical (5 bottoms n=24/24/5/5/68, 4 tops n=26-29, all hole-free, areas within 1e-5) → **fill_expolygons L71: native 4 HOLE-FREE pieces (n=10/10/12/69) vs rust 2 HOLE-BEARING RAGGED blobs (n=24+4holes, n=272+2holes)**. The 4× vertex count and merged topology are born in the perimeter generator's fill-area computation (last-inset offset + union that produces infill_contour). Everything downstream (detect's Internal raggedness, shells, ISI phase, gap geometry, sliver islands) inherits from here. NEXT: audit rust perimeter_generator's fill-area chain vs native PerimeterGenerator.cpp (Arachne inner offset → union → fill_expolygons; check native's simplify/opening on the result + hole handling). Probes reverted, native rebuilt. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ (docs only).

- **★★ R248 — DETECT INPUTS ARE BYTE-IDENTICAL — the F1 "wall" is NOT foundational slicing; it is born INSIDE detect_surfaces_type.** FRAGDBG at detect inputs, L71: lslices AND region slices match native EXACTLY (n=240/81, holes 0/1, areas equal to the last digit). The slicing pipeline under the F1 gates is exact at this layer. So the 296-vert/6-hole Internal raggedness (R247) enters between clean inputs and post-detect output. The Internal construction (`diff_ex(surfaces_prev, top++bottom)`) is already clib-routed under TOPFILL_FAITHFUL — divergence must come from the TOP/BOTTOM sets built by detect's earlier chains (bottom_diff / opening_ex offsets / interface_shells branch), since Internal = slices − top − bottom and rust's top matched (37v). NEXT: dump top[] and bottom[] at the append site both engines (counts/verts/holes/areas), diff the chains op-by-op. This re-scopes the fill wall from "foundational F1" to "detect_surfaces_type bottom-chain audit" — much more tractable. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ (docs only, probes reverted, native rebuilt).

- **R247 — F1 FRAGMENTATION QUANTIFIED AT POST-DETECT (docs only, 73917): rust Internal surfaces are topologically DIFFERENT — L71: rust 2 pieces / 6 holes / 296 verts vs native 4 pieces / 0 HOLES / 101 verts; rust also carries micro-specks (areas 2459-11437 scaled² ≈ 1e-7 mm², L72-74) native lacks entirely.** FRAGDBG two-sided dump after detect_surfaces_type: native's Internal decomposition is hole-free and 3× fewer vertices; rust's is ragged (F1-union vertex jitter) with hole-bearing merged pieces + specks. Tops are near-identical (37 verts both). This is the concrete signature of the F1 wall: rust's boolean pipeline (slices→fill_surfaces partition) produces ragged hole-y topology that cascades into shells/fills (R236's 136-vs-111 ISI contour, sliver islands, gap-fill geometry). NEXT PROBE: same dump at detect INPUTS (region slices + lslices) to see whether raggedness pre-exists slicing booleans or is born in detect's diff_ex chain. Probes reverted both trees, native rebuilt. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓.

- **R246 — MEDIAL-AXIS WIDTH DRIFT SIZED (docs only, 73917).** The VOLCAP-blocking width drift is ~6.6e-6 mm (back-solved from F14761.459-vs-.269 → mm3 rel drift 1.3e-5 → dw = dmm3/h): SUB-MICRON, below the 1e-5 scaled grid — native ThickPolyline widths are coordf_t (f64 scaled), so no quantization mismatch; the drift is accumulated Voronoi/medial-axis arithmetic (rust computes in mm-domain f64, native in scaled-domain f64 — ×1e-5 conversions are inexact and every sqrt/dot drifts ulps that compound). Fixing = porting the medial-axis chain to scaled-domain arithmetic (or the shim route) — MULTI-ROUND, F1-family forensics. VOLCAP stays parked (+42). Unlock chain updated: medial-axis scaled-domain port → VOLCAP → frac-F → time → M73. NEXT CAMPAIGN: F1 fragmentation (fill_expolygons shape divergence, R239) — the single biggest lever left (fill wall 58%). GATES: default ✓ F1 ✓ (docs only).

- **R245 — PER-PATH F EMISSION LANDED (inert, 73917 byte-same) + cooling reassessment.** CORRECTION to R244: rust smoothing has largely CONVERGED — 8412/8497 rust fractional Fs MATCH native (L114 outer wall: 148-vs-143 ramp lines, values within 0.3%); the "under-slowing" story was wrong. Native's extra ~1.1k fractional Fs = PER-PATH F on non-loop entities: native _extrude emits set_speed(F) for EVERY path (GCode.cpp:6663, cooling buffer dedups), with F carrying the volumetric cap → variable-width gap/fill paths get fractional Fs. Landed: gated per-path speed in the Path dispatch arm (role table + FloatingVerticalShell % + VOLCAP-gated cap; writer set_speed dedups → inert without VOLCAP, byte-same 8-gate). Under VOLCAP the gap Fs now EMIT but values drift (rust F14761.459 vs native F14761.269 — mm3_per_mm of medial-axis mm-width paths, the R231 width-parity family) → VOLCAP stays +42. VOLCAP UNLOCK = variable-width mm3 value parity (medial-axis chain). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ 8g byte-same.

- **R244 — M73/TIME-ESTIMATE ATTRIBUTION (docs only, 73917).** Native emits a 116-line M73 P/R ladder (TimeProcessor post-process insertion at percent steps); rust emits only header P0 + final P100 — the insertion pass is unported. BUT values can't match yet anyway: native estimate 43m0s vs rust 36m3s (417s gap). Estimator itself is NOT the cause — rust process_M400 dwell handling is faithful (dwell lines byte-identical both sides, 2×M400 S180 etc.); the gap is the COOLING/SLOWDOWN class: native's output carries 1215 fractional cooling-slowed F lines vs rust's 85 (native genuinely prints slower; rust under-slows), which also explains the R-value divergence. M73 CLASS = COOLING-TIME COUPLED — parked behind cooling convergence; the ladder insertion pass is a mechanical port to do once times match. Cheap-lever queue is now empty; next levers by size: (1) cooling/slowdown application parity (unlocks M73 ~120 + fractional F ~1.2k + VOLCAP +42→neg), (2) F1 fragmentation (fill wall 58%). GATES: default ✓ F1 ✓ (docs only).

- **R243 — VOLCAP TREND + F CENSUS (docs only, 73917).** VOLCAP retest: +42 net (R230: +535 → converging as classes close; land when ≤0). F-only census @73917: native fractional (cooling-scaled) 1215 vs rust 85 — rust cooling under-applies factors (layer-time inheritance class); rust-only integer buckets: F12000 ×335 (wipe F where native carries a different/slowed value), F21000 ×175 (sparse 350 uncapped — closes with VOLCAP), F1200 ×36. Native-only integers = long tail of ~25-28× cooling-slowed values (F11599, F8230, ...). CONCLUSION: F class now almost entirely rests on layer-time/cooling convergence, which itself tracks fill parity (F1/F3). Cheap-lever inventory exhausted for now; next either (a) M73 time-estimate audit (may close ~120 M73 + help cooling times), or (b) start F1 fragmentation work. GATES: default ✓ F1 ✓ (docs only).

- **★ R242 — NEW BEST 73917 (−19): TEMPLATE PARSER — multi-line {if} join, {if( spacing, BALANCED outer-paren strip.** (1) Preprocess (gated): normalize `{if(`→`{if (` and JOIN {if}/{elsif} lines whose braces don't balance with following lines (the H2D start template's 2-line PLA/PETG condition) — native PlaceholderParser is char-stream. (2) eval_condition's naive outer-paren strip mangled `((A)||(B)) && (C)` into `(A)||(B)) && (C` (mis-split at the inner ||, garbage operands compared 0==0 → wrong M620.10 branch F74.8347); faithful path strips only when parens wrap the whole expression. (3) computed first_*_filaments[1]. M620.10 (F498.898 = flush_volumetric_speeds[0]/2.4053*60*0.8), M620.17 T1, nozzle-0.8 E45 branch, M145.2/M1015.4/M981/M1003 conditions all byte-exact now. Template class ~closed except M73 P/R times (~120 lines, inherited time-estimate class). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2×.

- **★ R241 — NEW BEST 73936 (−24): TEMPLATE EVALUATOR — ternaries, index expressions, string-eq spacing, computed vars (all gated).** eval_expr now handles: balanced outer-paren stripping (`{(a ? b : c)}`), top-level ternary `?:` (depth-0 scan over ()/[]), and `name[<expr>]` with a computed index. eval_condition normalizes `== "PLA"` (space before quote — the legacy parser required adjacency). Computed vars added: first_non_support_filaments[0]/first_filaments[0]=0, overall_chamber_temperature (max chamber temp, 0 → M141 S0). M620.17 lines now byte-exact (S220 L0). Remaining template queue: M620.10 F formula (volumetric), M1015.3 TPU condition, M73 R times (inherited). LOCK INCIDENT: first land left the computed-var arms UNGATED → default flipped to 1787f6a9 (caught by the per-round lock check, amended with `if env` match guards). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2×.

- **★ R240 — NEW BEST 73960 (−8): MACHINE-END TEMPLATE — max_layer_z injection + expression comparisons.** Census surfaced the machine start/end template residue class: rust left `{max_layer_z + 4.0}` raw and took the wrong park branch (Z52/Z50 vs native Z124/Z122). Fixes (gated): (1) print.rs injects max_layer_z (max print_z over layers) into the end-gcode settings (native GCode m_max_layer_z placeholder); (2) eval_condition numeric comparisons now evaluate ARITHMETIC operand expressions (`{if (100.0 - max_layer_z/2) > 0}`) via resolve_all_vars+eval_math with unbalanced-paren stripping — the split leaves `(...` / `...)` on operands. REMAINING template residue (queued): array-index vars `filament_type[initial_no_support_extruder]`, computed M620.10 F (498.898-vs-74.83 — volumetric formula), M1015.3 TPU condition inverted, M141 S<empty> (chamber_temperature var), M73 R-values (time-estimate class, inherited). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2×.

- **R239 — VSHELLS REGULARIZE CHAIN EXONERATED (byte-same 6753cf7e; clib swap landed as hardening).** Routed discover_vertical_shells' regularized_shell chain (shrink_ex∘offset2_ex jtSquare, PrintObject.cpp:2018-2024) through offset2_ex_clib + offset_expolygons_clib_scaled under TOPFILL_FAITHFUL — output BYTE-IDENTICAL, so the geo-vs-clib delta here is nil and the ISI contour divergence enters via the INPUT `shell`. This matches the standing forensic note at print_object.rs:3480 (vshells-holes): rust's window fill_expolygons are shape-divergent/fragmented vs native (sub-mm gaps, n=9 fragmentation) — root = detect_surfaces_type / perimeter→infill partition / upstream union F1 fragmentation = the F1/F3 FOUNDATIONAL BLOCKERS (PARITY.md). CONCLUSION: the fill-geometry wall (XY+E 58%) rests on the known foundational blockers; incremental shell-op fidelity is exhausted. Remaining cheap classes: F residuals, M204 tail, travel values on matching blocks. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓.

- **R238 — SHELL-OP INFRA LANDED (inert on this profile, 73968 byte-same): cz_intersection_closed_safety shim + intersection_clib_safety + offset2_ex_clib_miter(limit) + gated rewiring of discover_horizontal_shells (asymmetric opening margin→margin+10u jtMiter/5, safety intersection).** Output byte-identical (6753cf7e) because this profile has ensure_vertical_shell_thickness ENABLED → native (and rust) SKIP the horizontal-shells inner body entirely. **ATTRIBUTION CORRECTION: the ISI contour shaping lives in discover_vertical_shells (PrintObject.cpp:1739-2110)** — its tail expands infill by tiny_overlap_radius 0.2mm via offset2 (PrintObject.cpp:2016-2024, jtSquare) plus its own intersections/diffs. Next round: audit rust discover_vertical_shells ops vs native (offset2 join types, safety offsets, the shell_ex hull sequence). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓.

- **R237 — SHELL-OP FIDELITY GAPS IDENTIFIED (docs only): discover_horizontal_shells is the ISI-contour source; rust's port differs in three clipper details.** (1) native `opening(new_internal_solid, margin, margin + ClipperSafetyOffset, jtMiter, 5)` is ASYMMETRIC (shrink margin, grow margin+safety, miterLimit 5) — rust uses a symmetric mm-space opening with its own join defaults; (2) native's `intersection(solid, internal, ApplySafetyOffset::Yes)` and `diff_ex(internal, polygons_internal, ApplySafetyOffset::Yes)` carry safety offsets — rust polygons_diff/intersection don't; (3) the final internal rebuild (`union_ex` → set/append) reorders/merges surfaces. These sub-mm deltas reshape the ISI contour (R236's 136-vs-111 vertices) and leave rust's sliver islands alive (native's asymmetric grow-back + safety offsets absorb them). Sliver rule itself (minimum_sparse_infill_area 15mm² in prepare_fill_surfaces) EXISTS in rust and matches — slivers are born AFTER it, in the shell subtraction. WORK ITEM (multi-round): route discover_horizontal_shells' opening/diff/intersection through the vendored-clipper faithful ops (cz_*) with asymmetric offsets + safety-offset semantics, gated. GATES: default ✓ F1 ✓ (docs only).

- **R236 — SURFACE-LEVEL LOCALIZATION @z=8.2 (docs only, 73968): three upstream deltas found via SURFDBG (group_fills output dump, both engines).** (a) **ISI rectilinear surface: axis-frame bbox MATCHES (±1 unit) but contour differs — 136 rust vs 111 native vertices** → shape differs at edges → rotated-frame bbox min shifts 0.27mm → raster phase/spacing flip (R235). (b) Sparse Grid surface bounds differ ~0.38mm (native 618144 vs rust 579928 min-x). (c) **rust keeps 4 tiny sliver InternalInfill islands (~0.008mm² each, far-left edge) that native drops/merges entirely** — extra rust fills/travels. Next: (c) is likely the cheapest — find native's small-region filter (group_fills union/opening or discover_horizontal_shells cleanup); (a)/(b) = internal surface classification chain (top/bottom shell propagation + expand/shrink params). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ 73968 vs canon ✓.

- **R235 — ISI RASTER DIVERGENCE LOCALIZED TO SURFACE GENERATION (docs only, 73968 vs canon).** Two-sided FILLDBG probe (fill_surface_by_lines vs native make_fill z=8.2, matched by z via a thread-local hint): angle EXACT (2.356194), base spacing EXACT (37707), rotated bbox MAX equal (±1 unit) — but **bbox MIN differs 0.27mm** (native −1280259,−1280367 vs rust −1253191,−1253176), so adjust_solid_spacing diverges (37898-vs-38003) and every raster line shifts (the 0.083mm phase seen in gcode). The fill algorithm is CORRECT; the input ISI surface expolygon differs at its min corner → divergence lives in surface/shell generation (top/bottom shell propagation, discover_vertical_shells, internal-solid classification) — next probe layer = surface expolygons at group_fills input for z=8.2. Probes reverted both trees, native rebuilt. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓.

- **R234 — MEASUREMENT DISCOVERY: NATIVE OUTPUT IS NONDETERMINISTIC (±35 noise) + fill-class attribution (flat 73968 vs pinned canon).** Two same-build native runs differ by ~71 lines (F values incl. F14921×14, wipe blocks — likely TBB-parallel layer-time/cooling variance). ALL small round deltas (±35) are noise; big deltas stand. NEW PROTOCOL: PRIMARY measured against a PINNED canonical native.gcode (scratchpad canon/, sha 3f228e65). Attribution of XY+E (58%): ISI 6.8k/5.0k mostly geometry-divergent (only 576 same-XY-diff-E), Floating 2.8k/4.2k, Sparse 2.8k/3.0k, Gap 2.6k/2.5k, Top 2.1k/2.0k, Bridge 1.3k/2.6k = rust bridge raster GEOMETRY entirely different (anchors/angle) + arcs inside rust bridges. Fill-geometry campaign = the remaining wall. Landed inert: per-path LAYER_HEIGHT register (GCode.cpp:6619, %g) — triggers once bridge path.height=0.4 lands (rust bridge heights wrong = same class). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓.

- **★★ R233 — NEW BEST 73968 (−1719): FILL WIPE PATHS — native extrude_path installs m_wipe.path = the path's SOURCE polyline REVERSED (GCode.cpp:5703-5717); rust only installed wipe paths at loop ends, so 207 native wipes had no rust counterpart (bare retracts, ±WIPE_START/END + wipe-segment E lines + F12000 all missing).** Gated Path-arm install in extrude_entity. WIPE_START counts 1742/1535 → 1743/1744. Census @75687 was: XY+E 57.8%, travel 16.1%, F-only 9.9%, LINE_WIDTH 8.6% (parked), zlift 3.8%, comments 2.1%, M204 0.9%. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2× 6753cf7e.

- **★★ R232 — NEW BEST 75687 (−435): FLOATING VERTICAL SHELL SPEED — native uses vertical_shell_speed% of internal_solid_infill_speed (GCode.cpp:6492-6500; 80% × 250 = 200 → F12000), rust fell through the role match to 300 (the r-only F18000 ×397 bucket).** vertical_shell_speed was in the vetted config-key list but apply_key_value had NO arm — silently dropped; added field (coPercent default 80) + parse + gated speed arm (skips first layer, native branch order). Also probed the retract-gap class: G17 totals now 1742-vs-1746 (old 78-gap closed by prior rounds); 137 layers differ ±1-2 but L0's lift targets differ GEOMETRICALLY (fill islands) — class is geometry-driven, parked. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2× 5f8301e1.

- **R231 — LINE_WIDTH width-source attribution (8-gate flat 76122; wall f32 arithmetic landed, inert): the drifting per-path widths are NOT the wall spacing→width formula.** Ported native's f32 sum (VariableWidth.cpp:66 `unscale<float>(w) + height*float(0.2146)`) to all 5 rust sites (variable_width.rs ×3, perimeter_generator _2 variant ×2) under FLOW_F32 — 8-gate unchanged, LW-enabled barely moved (80736→80732). ATTRIBUTION of the LW mismatch (5681 n / 5432 r): Gap infill 3.2k/3.2k (medial-axis mm-width chain), Internal solid 1.5k/0.7k + Floating shell 1.0k/1.7k (fill flow widths), walls 1.4k r-only (register echo of neighbor mismatches). LINE_WIDTH class needs medial-axis + fill-flow width parity — parked. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ 8g 76122 (byte-same acbba773... verify hash printed).

- **★★ R230 — NEW BEST 76122, PRIMARY now 8-GATE (+FLOW_F32): f32 Flow-chain store quantization landed.** Flow constructors (new/new_with_spacing/with_spacing) quantize width/height/spacing/nozzle to f32 on store; mm3_per_mm casts its double arithmetic to float (Flow.cpp:212-219 `float res`). Gated FLOW_F32, geometry-safe (default/F1 untouched; 7-gate+F32 = 76122, −61). RE-TESTED parked levers under F32: VOLCAP now byte-RIGHT (F18423.913 + F21000 + F18000 all match — the cap digit fixed!) but still net +535 from the cooling cascade (capped generation-F shifts rust layer times → factor drift on ~45 layers; converges as travel/fill classes close — RE-TEST VOLCAP each round). LINEWIDTH still +5k: Arachne per-path widths (0.43272-vs-0.43273) come from the perimeter-generator chain, NOT Flow — separate f32 port needed. PRIMARY metric redefined: 8-gate (F1_UNION FRAME_UNIFY SLICE_SIMPLIFY SLICE_CENTER REGION_EXPANSION_FAITHFUL TOPFILL_FAITHFUL ZSMOOTH_FAITHFUL FLOW_F32) = 76122 baseline. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ 8g det 2× acbba773.

- **★★ R229 — NEW BEST 76183 (−967): WIPE F LINE ALWAYS EMITS — native GCodeWriter::set_speed has NO dedup (GCodeWriter.cpp:387-399), so every Wipe::wipe carries `G1 F<cur_speed>;_WIPE` (role_base_wipe_speed default true).** R222's dedup dropped ~1 native F line/layer (the n-only G1 F12000 ×878 bucket → 172). Volumetric cap (GCode.cpp:6560-6567, min(speed, filament_max_volumetric_speed/mm3_per_mm)) implemented but PARKED behind VOLCAP_FAITHFUL: rust's capped F is one digit off native (18423.914-vs-18423.913 — f64 flow chain vs native float Flow, ~2e-8 mm3 drift invisible in E's 5 decimals but visible in F's 3 decimals) and the changed generation-F cascades through cooling factors (net +887). Unlocks with mm3/width parity — SAME BLOCKER as LINEWIDTH_PERPATH: the f32 Flow-chain port is now the gating lever for LINE_WIDTH tags (~2.9k) + F caps (~1k) + likely fill E tails. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2×.

- **★★ R228 — NEW BEST 77150 (−2310): TRAVEL TARGET = POST-SPLIT FIRST POINT, not the raw seam.** travel_target_for_entity returned active_place_seam's raw point; for INNER walls the placer returns a point OFF the loop polygon (nearest stored perimeter data — e.g. the outer wall's seam vertex), which split_at then PROJECTS onto the loop edge. Rust traveled to the raw point, then extrude_loop's projected start forced a 0.4mm corrective mini-travel + duplicate feature-F line per inner wall (rust-only G1 F18000 ×993 / F21000 ×141 bucket). Native seam-splits BEFORE the travel (GCode.cpp:5085-5090) so the travel goes to loop.first_point() after split. Fix: run the same split_at in travel_target and return its actual first vertex. Inner-wall blocks now native-shaped: one travel → unretract → markers → single F → M204 → moves. F-only class 5093/4200 → 5361/3540 (n-only rose slightly as blocks realigned; net −893). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2× a8688218.

- **★★ R227 — NEW BEST 79460 (−758): M204 PENDING-ACCEL MODEL — native set_acceleration only STORES; every extrude move PREPENDS set_extrude_acceleration() (GCodeWriter.cpp:168-171, extrude_to_xy:717).** First eager-emit attempt of the 3 wrapper resets (GCode.cpp:5591/5676/5720) exploded rust-only M204 to 9826 — the resets are LAZY and usually overwritten by the next path's feature accel before any move emits; they only surface via wipe segments (the S8000 between outer-wall S5000 and travel S10000 = the wipe's extrude_to_xy flushing the reset). Ported: writer.pending_accel + flush_pending_accel() at extrude_to/extrude_arc/wipe-segment; set_feature_acceleration_shared stores under gate; resets at extrude_loop both exits + extrude_entity Path arm. ALSO fixes M204-vs-F ordering (native F first, M204 at move). Plus first-layer travel accel: writer.is_first_layer (native m_is_first_layer) selects initial_layer_travel_acceleration 6000 (gated branch was stomping print.rs's 6000 with travel 10000). L24 M204 seq = exact native prefix (native has +8 from extra travel blocks = the retract-gap class). M204 net 1592→834 (rest rides travel-block structure: r-only S10000 458/S8000 232/S2000 70; n-only S500 24 first-layer + S6000 25 + S5000 16). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2× 1851b367.

- **★★ R226 — NEW BEST 80218 (−2489): M106 FAN-DUTY CLASS CLOSED — native's fan interpolation is a FLOOR, not a round.** GCodeEditor.cpp:430: `int(floor(t*min + (1-t)*max) + 0.5)` — floor() yields an integer so the +0.5 is DEAD on truncation; effective op = floor (79.9→79 = S201.45). Rust rounded (→80 = S204) → the 1272×/1249× duty flip. Gated fix: f32 t widened to f64, floor. M106 unmatched 2615 → 126 (tail: 60× native-only S255 overhang bursts + value stragglers on ~45 layers from inherited layer-time drift + S0/P2 startup singletons). Profile: min60/max80/cool100/slow4; ramp disabled (full_fan_speed_layer=0), first_x_layer_part_fan_speed default 0 → else-branch/ramp omissions in rust are inert for this job (noted for other profiles: rust misses the first_part_fan*(1-factor) ramp term entirely). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2× 1e4b1c6d.

- **★★ R225 — NEW BEST 82707 (−480): OBJECT_ID 13 + LINE_WIDTH lever measured/PARKED.** (1) OBJECT_ID: native fallback = get_labeled_id() = ModelInstance ObjectBase id (13 for the STL single-object pipeline); rust fell back to object_idx (0). Gated `+13` fallback in BOTH the emission AND the editor's object_label (first attempt changed only emission → outwall smooth-mark lookup missed → F drift +339; full ObjectBase counter emulation future). (2) LINE_WIDTH: per-path `%g path.width` emission implemented at extrude_path choke point (native GCode.cpp:6605 register, per PATH not per feature) — but rust f64 flow/Arachne widths drift from native f32 chains in the 6th sig digit (0.43272-vs-0.43273, 0.42-vs-0.41999) → enabling ADDS unmatched (83187→87805). PARKED behind LINEWIDTH_PERPATH env; unlocks when width values converge (width parity likely shares roots with the fill E-value classes). CLUE for F-cooling class: disabling smooth marks matched 417 native `G1 F12000` lines (rust z-smooth F adjustment over-applies somewhere). GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2× cddb5b0b.

- **★★ R224 — NEW BEST 83187 @aed9342 (−2791): TIMELAPSE TEMPLATE CLASS CLOSED — three stacked defects kept rust in the wrong {if} branch of the per-layer M9711 block.** (1) print.rs injects TimelapsePosPicker outputs under gate (GCode.cpp:4522-4534; constant X0 Y83 all 240 layers this job; full picker port future): has_timelapse_safe_pos=1/timelapse_type=0/pos_x=0/pos_y=83/extruder ids=0/spiral_mode=0/max_layer_z. (2) eval_condition had NO `!` negation — `!spiral_mode` resolved as variable name → falsy; added with correct precedence AFTER &&/|| splits (`!a && b` = `(!a) && b`; checking first gave `!(a && b)` → wrong branch, only −875). (3) naive && split not paren-aware — `!(has_timelapse_safe_pos && timelapse_type == 0)` split INSIDE the parens → garbage operands → true → ws leak (960× 12sp + 958× 8sp); added find_top_level depth-0 scan. Evaluator fixes GATED (ZSMOOTH_FAITHFUL); default keeps legacy find() branches verbatim incl. `)||(` 4-char skip (ungated attempt broke default → 149907/9b3d7177, restored). Timelapse block now byte-identical both variants (M622 J0 X/Y + M622 J1 U/V); rust-side ws residue 1918→0. GATES: default 147987/7adae05c ✓ F1 62f65c22 ✓ det 2× dd3669a7.

- **R223 — CENSUS @85978 (no code): travel/lift/wipe campaign CONVERGED (G17 1738-vs-1742; Outer 20k→10.6k).** Breakdown of the flip: XY+E extrusion 50.9% (fill rasters — ISI 17.3k/Top 13.3k/Gap 19.5k/Float 11.6k, the deep fill classes), travels 13.7k, F-only 10.8% (cooling factors, inherited layer times), other 19.2k. RANKED CHEAP QUEUE from "other": (a) rust emits ~900 EXTRA BLANK lines (1394-vs-485); (b) per-path LINE_WIDTH tags (native-only 0.43-0.46 ≈2.9k — the parked R165 lever, now unblocked); (c) OBJECT_ID 13-vs-0 (480); (d) M106 duty (1358-vs-1258); (e) M204 532-vs-1060 asymmetry. R224 = (a)+(b). GATES: default ✓.

- **★★ R222 — NEW BEST 85978 @6b1cc80 (−1016): verbatim Wipe::wipe cluster landed as ONE unit — source-polyline path + forward walk + per-segment dE (len·(seg/wipe_dist)·0.95) + current-speed dedup + exact remainder.** KEY value bug: wipe_dist = filament_wipe_distance (1.0 profile) — the writer's 2.0 default drove the R221/R222a regressions (native E-.31367 back-solves to 1.0). L24 wipe now BYTE-MATCHES native. Session: 99449→85978 (−13.5%). GATES: default ✓ F1 ✓ det 2×.

- **R221 — WIPE RE-LANDS REVERTED (+487/+1110): the wipe is a COUPLED CLUSTER (class 6) — forward walk + SOURCE-polyline path + per-segment dE (len·(seg/wipe_dist)·0.95) + role-based speed must land together as a verbatim Wipe::wipe port (queued round).** rust wipe_path currently holds EMITTED (arc-fitted) endpoints; native uses the loop's source polyline; native wipes multi-segment (E-.31/-.066) at role-based speed vs rust single E-.38. Baseline restored 86994. GATES: default ✓.

- **★★ R220 — NEW BEST 86994 @2c5c9ea (−3424): SEAM-GAP LOOP CLIPPING (GCode.cpp:5107-5117) + seam_gap default 0→15% (PrintConfig.cpp:4664).** R219's "direction" verdict was WRONG (compared wipe moves, not loops — L24 inners identical). extrude_loop clips scale(nozzle)·seam_gap% off the loop end under the gate. Next: re-test forward wipe + per-loop wipe_path against clipped loops (R219 regression should invert), then F 10646-vs-10581 + M204/F order. GATES: default ✓ F1 ✓ det 2×.

- **R217–R219 — TRAVEL-BLOCK VALUE CONVERGENCE ARC @5e7b726: L24 "wall order" was a census artifact (toolpath correct, seam splits verified); FEATURE/LINE_WIDTH markers moved AFTER travel+unretract (native block shape; ordered match 72906/139498); wipe-direction probe REVERTED (+508) — real root = INNER-LOOP EXTRUSION DIRECTION (native 2nd pt −Y, rust +X @L24), the reversed wipe walk compensates.** R220 = loop-direction audit (make_counter_clockwise/orientation-per-role/split direction), then re-land forward wipe + per-loop wipe_path together. Block deltas remaining: wipe direction (blocked), F 10646-vs-10581, M204/F order. Flip 90418. GATES: default ✓ F1 ✓.

- **R215 — WALL-ORDER ROOT PINNED (no code): native emission FOLLOWS the entity order exactly ([hull-inner, small-outer, hull-outer] — ORDDBG pre==post, == rust entities); rust EMISSION reorders the outers (hull before small).** L24 full-run compare: both emit hull-inner first (same start −26.556,−6.383); native's Outer run then walks entity order (small@−25.009 → hull, n=65); rust's Outer starts at the hull (−26.556, n=63). The reorder lives in rust's exporter wall-pass logic (skip_inner_walls two-pass / wall_sequence handling), NOT in the perimeter data. Also: rust Inner n=9 vs native 11 (2 moves — separate). R216 = locate + fix the rust outer-pass iteration to strict entity order. GATES: default ✓.

- **R213 — FAR-RUN ROOT: EMISSION ISLAND ORDER on multi-loop layers (no code).** L24: seam data has 2 perimeters (small 23-cand loop + 211-cand hull); native gcode emits the SMALL loop first (start ≈ peri0 fin), rust emits the HULL first — generation sequences are equal (R209 PERIENT), so the divergence is in emit_layer_by_island's bucketing/order (suspects: island assignment via point_inside failing to the catch-all bucket, or first-appearance order not applied across regions). Also noted: rust L24 hull start (−26.556,−6.383) sits 1.25mm from its fin (−27.798,−6.239) — split_at snap/insert offset worth checking. R214 = island-assignment probe @L24 (entity→island_idx, emit_order) + split_at snap audit. GATES: default ✓ (no code).

- **R212 — MEASUREMENT ROUND @HEAD: retract decisions near-parity (≥1mm calls 2552-vs-2596; blocks 1664-vs-1742 = distributed skip-branch deltas); gcode seam census exact 59→**186**/240, far 52.** The 52 far layers form CONSECUTIVE RUNS (24-27, 57-65, 95-103, 137-153…) = string-shaped — but SEAMFIN finals matched 99.1%, so these are EMISSION-side: suspected first-emitted-loop selection on multi-loop/multi-region layers (bucket region iteration vs native). Class census @90418: Gap 20.2k, ISI 17.4k, Outer 15.2k, Top 13.4k, Float 11.8k. R213 = L24 emission-order dig OR pivot to ISI/Top. GATES: default ✓.

- **★★★ R211 — NEW BEST 90418 @520b113 (−3626): TRAVELS TARGET THE SEAM.** travel_target_for_entity resolves Loop travel destinations via active_place_seam (native _extrude order: split-at-seam BEFORE travel, GCode.cpp:5085-5090) at all 5 travel sites. 94044→92527 (walker)→90418 (all). G17 blocks 2194→1664 (native 1742 — now 78 UNDER; audit next). The session arc R188→R211: 99449→90418 (−9.1%). GATES: default ✓ F1 ✓ det 2×.

- **★★ R210 — EXTRA-RETRACTION ROOT: rust travels to the PRE-SEAM-SPLIT loop start @f7dfb6d.** RETDBG dual @L6: native outer travel → the SEAM (0.51mm hop, no retract); rust → the natural first vertex (6.39mm, retract). Seam data identical (picks 98.9%, finals equal, place_seam Some everywhere). ROOT = ORDERING: native _extrude does place_seam+split_at THEN travel(loop.first_point()); rust travels to the pre-split start then splits. Every loop travel lands on the wrong vertex → hop inflation (+332/+88/+62 retracts) + rotated travel targets. R211 = resolve the seam point BEFORE the travel decision at the 5 sites (GCode.cpp:5085-5090 order). GATES: default ✓.

- **★ R209 — NEW BEST 94044 @73112f3: chain_expolygons surface reorder + first-appearance island emission — entity SEQUENCES 156→239/240 native-equal.** Native reorders surfaces at PERIMETER GENERATION (chain_expolygons = KD-greedy over bbox centers, PerimeterGenerator.cpp:915-921) and emits collections in entities order; rust now matches both. BUT extra retractions UNCHANGED (+332 Outer/+88 Inner/+62 Float) with identical order/geometry/seams ⟹ the needs_retraction DECISION diverges on identical hops. R210 = dual decision dump at L6 (travel_len, last_role, branch). GATES: default ✓ F1 ✓ det 2×.

- **★ R208 — NEW BEST 94139 @7a0818c (was 95373): layer-change eager STATIC spiral (writer.eager_spiral_lift, GCodeWriter.cpp:455-495 via GCode.cpp:3039 apply_instantly) + writer.nominal_z travel destinations (GCode::m_nominal_z, GCode.cpp:6905).** zdest=writer.z() had landed first-travel-after-layer-change descents on the OLD layer (+1210 before nominal_z threading). Remaining travel-class: 451 extra retractions (entity ORDER — R207) + M204 alternation + F-neighborhood effects. GATES: default 147987/7adae05c ✓, F1 62f65c22 ✓, deterministic 2×.

- **R207 — EXTRA-RETRACTION LOCALIZATION (no code): the 451 split into (a) ENTITY-ORDER-driven hops and (b) the layer-change lift shape.** Per-feature extra retracts (rust−native): Outer +332, Inner +88, Float +62, Gap +26, ISI −53, Top −17. L6 case study: rust's extra Outer retract = a 5.9mm outer→outer hop that native's wall EMISSION ORDER avoids (short hop, no retract) — the R198-noted per-layer perimeter ORDER difference is now the retraction driver (Class-5 chain/island sequencing). ALSO: native layer-change lift = eager STATIC spiral (M204 S10000 + G17 + `G3 Z I<radius> J0 P1  F60000`, no XY follow, no Z-restore; eager_lift GCodeWriter.cpp:455-495) while rust emits a directional G3 whose F gets STRIPPED by the cooling rewrite. R208: (1) layer-change eager static spiral + cooling F preservation; (2) entity-order audit (emit_layer_by_island vs native wall_sequence/chain ordering). GATES: default 147987/7adae05c ✓ (no code).

- **R206 — LIFT SYSTEM WIRED @51ad623: travel blocks native-shaped (G17 + directional G3 "P1  F" + combined XYZ + G1-Z restore at unretract); flip 95543 (+170, documented structure-land).** 5 exporter travel sites → travel_to_xyz(layer z); retract defers via lazy_lift(Spiral — 1742 native spirals / 0 slopes); unretract unlifts. G3 matched 5763. Residual +170 = rust's 451 EXTRA retraction blocks (2193-vs-1742 G17 — needs_retraction class) + M204 S6000 placement (native re-emits inside travel_to_xyz; rust short-accel register dedups) + post-unretract F3000. R207 = localize the 451 extra retractions + M204 alternation. GATES: default 147987/7adae05c ✓, F1 62f65c22 ✓.

- **R205 — FAITHFUL LIFT MACHINERY LANDED INERT @HEAD: lazy_lift_faithful / travel_to_xyz 3-branch merge (Spiral G17+G3-Z-IJ, Slope G1-top-XYZ <3°, Normal G1-Z + ONE combined XYZ at lifted Z) / will_move_z / unlift_faithful — the R177/R178 lift-system port, now on converged seams.** Verified inert (default + 7-gate 95373 unchanged). R206 = exporter wiring: lift type via z_hop Auto → is_through_overhang (loverhangs bbox+intersection in print_z±0.4), lazy_lift at retract sites, travel_to_xyz(layer z) at the 5 travel sites, unlift at unretract — then measure (travel/lift class sits inside Outer 17.8k).

- **★★★ R204 — SEAM SUBSYSTEM CLOSED: pre-align 98.9% / post-align 99.1% EXACT, far=2 (both L0 = base-slice floor).** The R179→R204 campaign (41.3%→99.1% exact) is complete: candidates bit-exact (R197 entities), visibility bit-exact (R201 occlusion chain), frames consistent (R198/R203), alignment faithful. Post-seam class census @95373: Gap 20.7k, Outer 17.8k (now = line-splitting/travel/parked levers, NOT seams), ISI 17.5k, Top 13.4k, Floating 11.8k, Inner 8.9k, Sparse 8.8k. NEXT: parked levers now unblocked — R205 = slope-lift re-application (R178 notes: SlopeLift = 3° climb combined-XYZ, SpiralLift = G2/G3-with-Z; was +1174 only because seams diverged), then per-path LINE_WIDTH (R165). GATES: default 147987/7adae05c ✓.

- **★★★ R203 — NEW BEST 95373 @8bc5106 (prior best 98616, −3.3%); census far 87→22 (9.2%), exact 59.** R202's anomaly solved: the R199-turn BISECT (`git checkout <commit> -- crates/` sweep) left the pre-R198 seam_frame_offset in the worktree and the R199 commit captured it — R198's double-shift removal was silently reverted; R200–R202 measured with the shift active. Re-applying always-None on top of R201's bit-exact occlusion chain unlocked the seam cascade. PROCESS LESSON: after bisects that check out historical trees, DIFF the worktree vs intended HEAD before committing. NEXT (R204): remaining 22 far picks + 159 near (alignment strings on exact inputs); then the parked levers (slope-lift R178, per-path LINE_WIDTH R165) — seam convergence precondition now largely met. GATES: default 147987/7adae05c ✓, F1 62f65c22 ✓, deterministic 2× 74395151.

- **R202 — OPEN ANOMALY @8156e00: rust seam CANDIDATES moved to native−0.8245 under the full 7-gate (were EQUAL at R199's dump); gcode emission frames still agree.** Census: pre-align 0%/40.6% far, post 0%/25.2% (frame shift, not pick logic). CANDDBG: native candidates byte-stable since R199; rust moved with NO gather-path code change (only R201's occlusion quantize landed in seam_placer since). R203 decision tree: re-run PERIENT under full gates TODAY — if entities still bit-match native → gather/dump-site issue; if NOT → R200's eigen-transform-sys flag change (.debug(false)) recompiled the FRAME_UNIFY slice-transform shim and moved slicing — revert flags for the transform TU and re-lock. GATES: default 147987/7adae05c ✓.

- **★★ R201 — OCCLUSION CHAIN BIT-EXACT UNDER THE FULL 7-GATE @afa9656: flip 99193→**98904**.** The R199/R200 "drift" was a GATE-SET BLIND SPOT: under FRAME_UNIFY the CLI skips quantize_f32_center_roundtrip (slice shim reproduces it in-transform) → po.mesh() raw → occlusion verts unquantized; all prior occlusion validations ran ZSMOOTH-only (bake active). Fix: compute_global_occlusion reproduces the f32 center-store round trip (+ −0.0→+0.0). Under the REAL config now: entry 4fe99cdf ✓ dots e57773ba ✓ rounds ✓ samples 6551cd1c ✓. LESSON: validate bit-parity under the PRIMARY gate set, not a subset — measurement configs are part of the claim. Census mixed (34/119/87) — R202 = pick/align census on the exact chain. GATES: default 147987/7adae05c ✓, F1 62f65c22 ✓, deterministic 2× bfa488bd.

- **R200 (WIP) — OCCLUSION DRIFT = INPUT X-ULPS @f7428cc: kernel exonerated; entry mesh differs.** TU split (secol_raycast.cpp own TU) + native-flag alignment (.debug(false), no frame pointers) did NOT restore R188 hashes. Dual COLDBG: entry mesh has (a) 80 z=−0.0 verts (native 0; normalized, gated) and (b) scattered X-coordinate 1-12-ulp diffs (y/z exact) — the quantize_f32_center_roundtrip cx shifted between sessions (suspect devbox-setup toolchain refresh at the 7/11→7/12 boundary). Same input ⟹ kernel bit-exact (R188 proof stands). R201 = reconstruct occlusion verts NATIVE-exactly (admesh f32 volume store + native center posture / R87 voff shim), target entry vh 4fe99cdf3cbe5841, then chain → census. GATES: default 147987/7adae05c ✓.

- **R199 — FAR-PICK ROOT = VISIBILITY (radius drift) = COLLAPSE CODEGEN DRIFT @487c7ed.** CANDDBG @L6: positions/z/angle/embedding EXACT; visibility differs on ALL 220 candidates (max 0.29). Radius forensics: native bit-reproduces f32 formula from area 9401.07; rust implies 9388.5 — decimation now yields 15982/7983 (was 15986/7985 bit-matched at R188; sampin 1acd09b5 vs 92066df3). Bisect: EVERY commit incl. R188 itself now drifts ⟹ same source, different binary — secol_shim.cpp TU gained the raycast code (R190) and the collapse kernel's FP codegen shifted (TU-context inlining/vectorization). NEW FIDELITY LESSON: native-code shims must pin ONE KERNEL PER TU. R200 = split secol_shim per kernel, re-verify chain (entry→rounds→postdec→samples→vis→census). GATES: default 147987/7adae05c ✓.

- **★★ R198 — R191 DOUBLE-SHIFT REMOVED @d91487f: flip 102005→**99193**; gcode seam census exact 10→52, far 87→57.** Post-R197 re-dump showed picks at native−0.8245: with entities bit-identical, candidates/lslices/exporter loops ALL live in the CENTERED frame under SLICE_CENTER — R191's translations (gather/overhang/place_seam) double-shifted every candidate (0% exact with, 68% without). seam_frame_offset → always None; only R188's occlusion-mesh centering survives as the real frame fix. Position-matched censuses on the bit-exact base: pre-align 68.0%/21.3% far, post-align 70.8%/21.0% — ALIGNMENT IMPROVES CONVERGENCE AGAIN (R192's "alignment diverges" verdict was an artifact of the double-shifted inputs). Remaining: (a) per-layer perimeter ORDER differs (zip census pollution; entity sets match) — audit gather order; (b) 21% far picks. GATES: default 147987/7adae05c ✓, F1 62f65c22 ✓, 7-gate deterministic 2× (50aa3648).

- **★★★ R197 — OUTER-WALL ENTITIES BIT-IDENTICAL 239/240 @5811d71: the geometry floor is DEAD.** Single-layer bisect (L1): polygon/bbox/clipped-lower/inside ALL matched → divergence inside detect_overhang_degree. ROOT = C++ overload resolution: `dir * t` on a non-const lvalue binds the MEMBER `Point operator*(double)` → `Point(double,double)` ctor = **lrint round** (Point.hpp:179/200); rust modeled the FREE truncating operator (Point.hpp:255) — every inserted overhang split point 1 unit short. point_mul_f64 now lrint under ZSMOOTH_FAITHFUL (OnceLock gate; default keeps truncate, byte-locked). Entity hashes 157→**239/240** (only L0 base-slice floor). R193's "43.5% vertex exactness" bound eliminated. Flip 102532→102005 (−527); seam census still 10/143/87 — candidates now identical, so residual far picks live in alignment/visibility-averaging: R198 re-runs the seam chain dumps (pre-align picks → strings → post-align) on the new bit-exact base. LESSON (new PORTING_PATTERNS candidate): C++ OVERLOAD RESOLUTION is a fidelity surface — member-vs-free operator selection flips rounding semantics. ALSO: check native build output for FAILED, not tail -1 (silent stale-binary trap). GATES: default 147987/7adae05c ✓, F1 62f65c22 ✓.

- **R196 — CLIPPER2 UNIFIED TO NATIVE 1.5.2 @409bb33 (clipper2-z-sys vendor swapped to the reference tree; namespace Clipper2ZSys; BBS Clipper2Lib_Z suffix collapsed) + cz2_pl_open (verbatim _clipper2_pl_open) gated into intersection_pl_2/diff_pl_2.** clipper2c-sys ships 1.5.4 vs native 1.5.2 — the Eigen-class version-skew lesson repeating. Both locks HOLD on 1.5.2 (default 7adae05c, F1 62f65c22 — wave-seeds version-stable). Entities still 157/240; the 82 diverging layers are COORDS-ONLY (path+point counts identical). With loop polygon + lower series + clip version + clip semantics all native-exact, remaining suspects: the rust clip_clipper_polygons_with_subject_bbox port (vertex-drop algorithm), per-loop input association, or pl_open marshalling. R197 = single-layer stage dump (lower_back_clipped hash → inside_polines hash) to bisect. Flip neutral 102532.

- **★ R195 — lower_polygons_series BIT-EXACT 239/239 @012882f (three native postures recovered, ZSMOOTH-gated): (1) offset SERIES in f32 + delta=double(float(scale_(f32))) via offset_expolygons_clib_scaled; (2) native offset(ExPolygons)=RAW per-expolygon paths, NO PolyTree reconstruction (Class-5); (3) grow w/ >1 islands = ONE flat Execute(ctUnion,pftNonZero) — new cz_union_flat.** Progression 0→113→239 of 239 matched. Entities UNCHANGED 157/240 ⟹ with loop polygon AND lower series both bit-exact, the residual lives in the SPLIT OPS: clip_clipper_polygons_with_subject_bbox pre-clip and/or intersection_pl_2/diff_pl_2 (native Clipper2 vs rust clipper2_utils — version/impl/order). R196 = dump split outputs (inside/remain polylines hash) on one diverging layer + audit rust pl_2 wrappers (vendored Clipper2 version, FillRule, scale posture, output order). Flip neutral 102532 until entities converge. GATES: default 147987/7adae05c ✓, F1 62f65c22 ✓.

- **★★ R194 — OUTER-WALL OFFSETS BIT-IDENTICAL 239/240 LAYERS @f86e85e (dual PERIDBG; native probe reverted): the 43.5% vertex floor enters INSIDE traverse_loops, NOT the slice/offset chain.** Per-layer FNV hash of the i==0 external offsets (rust shrink_clib vs native offset_ex): every layer except L0 (known base-slice floor) hash-identical, so the pre-offset simplify_p is converged too. ⟹ Divergence is confined to: overhang splitting (lower_polygons_series intersections — audit rust for Class-1 geo-clipper use), path assembly, and the candidate midpoint-oversampling cascade (w²·4 threshold on split-shortened segments). This RE-SCOPES the "geometry floor": the loop CONTOURS are exact; only their path-decomposition diverges — a bounded, portable surface. R195 = hash loop polygon entering traverse (expect exact) → concatenated entity points → localize overhang-split vs simplify. GATES: default 147987/7adae05c ✓.

- **R193 — SEAM PORT FIDELITY AUDIT CLOSED + GEOMETRY FLOOR QUANTIFIED (no code; analysis on R192 dumps).** All three suspected surfaces are verbatim-faithful: find_nearby_points acceptance/order (strict dist<r², identical visit order; the rust eps only loosens subtree descent — harmless), calculate_point_visibility weighted averaging, and the KD build (median-of-3 quickselect). ⟹ alignment residual is DATA-driven. THE FLOOR, MEASURED: on the 333 count-matched outer perimeters, only **43.5% of loop vertices are exact** (<1.5µm; 51/333 loops fully exact; count deltas ±137 both signs, median 0) — vertices differ TANGENTIALLY (resampling), not by offset. Perimeter offsets already run shrink_clib, so the divergence enters between lslices and the final loop points (offset output vs simplify chain). EVERYTHING seam/gap/candidate-related bottoms out here. R194 = single-perimeter stage-split (e.g. L25 n=33, mean dev 24µm): dump native offset output → simplified loop vs rust shrink_clib output → simplified, find the first diverging stage of LOOP VERTEX GENERATION. GATES: default 147987/7adae05c ✓ (no code changes).

- **R192 — ALIGNMENT STAGE-SPLIT @d8ab058 (dual SEAMFIN/SEAMSTR dumps; native probes reverted): strings 90% STRUCTURALLY IDENTICAL (sizes/spans/lengths ±0.0003 — bspline inputs near-exact); divergence = string TERMINATION.** Post-align census 29.2% exact / 54.6% far (down from 73% pre-align). 21-vs-18 strings: rust MERGES strings native splits (13-36+38-44→13-44; 171-185+186-191→171-191) — find_next_seam_in_layer port is verbatim, so the extension is DATA-driven: extra rust candidates (over-segmentation) inside max_distance satisfy is_first_not_much_worse where native terminates. R193: (a) audit find_nearby_points result ORDER (index[0] seeds best/nearest tie-breaks) + calculate_candidates_visibility averaging; (b) if clean, alignment convergence is bounded by the candidate-set geometry floor (fewer levers). GATES: default 147987/7adae05c ✓.

- **★★ R191 — CANDIDATE FRAME FIXED (int-exact centering) + EXTERNAL-ONLY FILTER @5231708: pre-align picks exact 0%→**73.0%**, far 35.7%→6.6%; ALIGNMENT IS NOW THE DIVERGING STAGE.** Pick-provenance dump found TWO roots: (1) rust flat perimeter storage sent every loop through extract's unconditional else-branch — INNER walls polluted the seam data (1429-vs-749 perimeters; filter now applied → 747-vs-749); (2) ★ rust candidates sat in the UNCENTERED frame (Δx=0.8245 — the mirror half of R188): candidate positions NEVER bit-matched native (exact=0.0%!). Fixed with integer-exact scaled translations (gather polygons, overhang/embedding lslices, place_seam in/out via frame_offset_xy). KNOWN REGRESSION landed deliberately: 7-gate flip 98616→102532, final census 19→10 exact — align_seam_points takes the near-native picks and moves them AWAY from native's final seams (R181's "alignment improves" verdict only held in the old self-consistent uncentered regime). R192 = post-align dual dump → stage-split align_seam_points (find_seam_string thresholds / gaussian smoothing / final_seam_position write-back). Remaining pre-align far (49): 27 extra-candidate wins = over-segmentation floor. GATES: default 147987/7adae05c ✓, 7-gate deterministic 2× (a30fa06b).

- **R190 — OCCLUSION CHAIN BIT-EXACT END-TO-END @4f6bfa8: raycast_visibility native shim (AABBTreeIndirect.hpp verbatim copy into secol_shim — BoundingBox/Utils/igl includes swapped for minimal local defs) — visibility hash vh=1ca28631150231ab MATCHES native; flip NEUTRAL 98616, census unchanged (7.9%/34.2% far).** The 112 flipped rays were below the pick comparator's noise floor. IMPLICATION: with positions/angles/visibility all exact on matched candidates, remaining far picks must come from (a) the +303 extra rust candidates (over-segmentation) winning ties, (b) calculate_candidates_visibility KD-radius averaging, (c) overhang/embedding inputs, or (d) align_seam_points anchor mapping. R191 = pre-align pick dump with winning-candidate PROVENANCE (native-vertex vs rust-extra-point) per far layer. GATES: default 147987/7adae05c ✓, F1 62f65c22 ✓.

- **R189 — VISIBILITY 99.985% CONVERGED (dual OCCDBG vis-hash; native probe reverted+rebuilt): 112 of 750,000 ray decisions differ (sum 16931.64-vs-16936.12, Δ=112×0.04 step; first5 EXACT).** With samples bit-exact, `raycast_visibility` is the last f32 surface in the occlusion chain — edge-grazing rays flip in the AABB traversal / triangle intersect (igl f64 ray, nalgebra-vs-Eigen). ALSO CLOSED: 747-vs-444 candidates (R182 secondary) is NOT a seam-placer bug — `extract_perimeter_polygons` + oversampling (incl. the native `flow_width²×4` vs unsquared-distance quirk) match native verbatim; the +303 = rust perimeter OVER-SEGMENTATION (extra polygon vertices become candidates) = the known geometry floor (R123/R127), so all 444 native candidates position-match and rust adds in-between points. FRESH CLASS CENSUS (7-gate 98616): Gap 20.8k, Outer 20.0k, ISI 17.4k, Top 13.7k, Floating 11.8k, Inner 9.0k, Sparse 8.9k, Bridge 4.3k. NEXT (R190): shim `raycast_visibility` (copy AABBTreeIndirect build+first-hit subset + Frame/sample_hemisphere into secol_shim, ~300 lines) → visibility bit-exact → re-census picks; remaining far = candidate-density (geometry floor). Rust-side OCCDBG vis probe kept (env-gated).

- **★★★ R188 — DECIMATION + SAMPLING BIT-EXACT; OCCLUSION-FRAME BUG FIXED @b3c8d71: flip 99449→**98616** (best).** THREE fixes (all ZSMOOTH_FAITHFUL): (1) **Eigen VERSION SKEW was the R186/R187 ulp root** — the shim compiled against vendored Eigen 3.3.7, the native binary against nix eigen3 **3.4.0** (build.ninja `-isystem .devbox/nix/.../eigen3`; flags otherwise identical -O3 -DNDEBUG). Full `its_short_edge_collpase` moved into `secol_shim.cpp` (own TU on 3.4.0; transform shims stay on 3.3.7 — their gates are byte-locked): rounds now 66547/30113/17507/16012/16000 = native EXACTLY, decimated mesh 15986/7985 hash-identical. (2) `sample_its_uniform_parallel` → `secol_sample_uniform` (real libc++ mt19937_64+uniform_real, native Eigen area/interp). (3) **REAL BUG: rust raycast mesh sat in the VOLUME frame** — native applies `po->trafo_centered()` AFTER decimation (SeamPlacer.cpp:604, its_transform = f64 translate → f32 cast, reproduced as `(f64 - cx) as f32`); every rust visibility query was skewed by cx=0.8245mm. Samples now BIT-EXACT (position/tri-idx/area hashes match). Census (gcode-measured): exact 4.2%→7.9%, far 34.2% — remaining seam chain: raycast_visibility (AABB tree/ray math) + candidate count (747 vs 444, R182). ALSO: probe crash lesson — `mesh.vertices[0]` on the EMPTY negative_volumes_set call (SeamPlacer.cpp:606) killed the native engine; guard probes with `.empty()`. GATES: default 147987/7adae05c ✓, F1 62f65c22 ✓, 7-gate deterministic 2× (2bc9409f) ✓.

- **R187 (WIP) — NATIVE-EIGEN KERNEL SHIM @c82eda2: flip 99922→**99449** (below pre-R184 baseline) but decimation NOT yet bit-exact (seam census 41%→33% exact — the mesh moved again).** `secol_min_vertex_dots` (eigen-transform-sys) copies the exact native float pipeline (double-normalized face normals + nelson-weighted vertex normals + min-dot loop) and replaces rust's scalar math under the gate. Remaining bit-mismatch suspect: COMPILE FLAGS — the native CMake build's optimization/fp-contract/vectorization vs the cc-rs shim (opt_level 3); Eigen f32 codegen is flag-sensitive. R188: (a) dump kernel outputs for the decision-#324 vertex pair from both binaries; (b) align shim flags with the native build (check the devbox native:build CMAKE_BUILD_TYPE + flags) until the dots equalize; (c) then re-verify round-1 counts → decimation → samples → visibility → seam census in sequence. GATES: default 147987/7adae05c ✓.

- **★★ R186 — FIRST DIVERGING COLLAPSE DECISION FOUND (dual COLDBG2 full round-1 trace; probes reverted, native rebuilt): SHUFFLE EXONERATED AT SCALE (first-20 indices of 225154 IDENTICAL) and decisions 0-323 byte-identical — decision #324 (face 57809, edge 0) SWAPS keep/remove (native keeps 28619, rust keeps 31597).** The swap condition is `min_vertex_dot_product[remove] < min_vertex_dot_product[keep]` — the f32 dot for vertices 28619/31597 differs by ulps: the NelsonMaxWeighted vertex-normal pipeline (create_triangle_normal → edge norms → weighted accumulation → /=count → dot with face normal) runs through Eigen's f32 kernels natively vs rust scalar math — ulp-level differences are structural, not a port bug. DECISION: chase no ulps — **R187 moves the whole `its_short_edge_collpase` into a C++ FFI shim** (eigen-transform-sys pattern: copy native ShortEdgeCollapse.cpp + NormalUtils::create_normals_nelson_weighted + its_face_normals, ~200 lines, gated ZSMOOTH_FAITHFUL) — bit-exact by construction; the decimated mesh, sampling, visibility, and seam picks then converge in sequence.

- **R185 — COLLAPSE DIVERGES IN ROUND 1 (dual COLDBG per-round; probes reverted, native rebuilt).** Round trace: native 225154→66547→30113→17507→16012→16000; rust →66551→30077→17603→16016→16000 — already ±4 faces in ROUND 1 with identical inputs and the (ground-truth-verified) libc++ shuffle. Remaining suspects inside the round-1 decisions: (a) rust shuffle on LARGE N (python verification was N=10 — re-verify the pairing/uniform draws at N=225154 via a first-20-indices dump), (b) min_vertex_dot_product f32 computation (normal accumulation), (c) triangles_neighbors construction order, (d) get_final_index path-compression timing. R186 = dump first-20 shuffled indices (exonerates/implicates the shuffle port at scale), then the first diverging face decision (face_idx, edge, keep/remove, dots). Note: each round's divergence compounds (66551-vs-66547 reshuffles differently next round), so ROUND 1 is the only clean comparison point.

- **★★ R184 — TOOLCHAIN GROUND TRUTH + libc++ SHUFFLE @3f1900a: native = clang 21 + libc++ (PROVEN via test binary, _LIBCPP_VERSION 200100).** R183's frame theory REFUTED by the pre-decimation probe (identical 225154-tri input, same bbox) — the divergence is INSIDE its_short_edge_collpase. Root #1 fixed: rust's std::shuffle port followed LIBSTDC++'s paired-swap batching; libc++'s is plain Fisher-Yates w/ masked-rejection uniform_int — new shuffle_libcxx/uniform_int_libcxx verified EXACT against toolchain ground truth (shuffle [1 6 4 5 8 3 9 7 2 0], uid [1 5 2 2 4] from mt19937_64{27644437}). Decimation still ±10 tris (15976-vs-15986) — residual inside the collapse logic (per-round removal-count probe next); flip temporarily 99481→99922 (permutation noise, marker-up precedent). ★ SYSTEMIC IMPLICATION: EVERY libstdc++-modeled RNG/distribution port in the crate must be re-audited against libc++ (uniform_real/generate_canonical in TriangleSetSampling next; the R99/R149 mt19937_64 core is engine-level and unaffected). GATES: default 147987/7adae05c ✓.

- **★ R183 — VISIBILITY CHAIN LOCALIZED TO THE DECIMATION FRAME (dual OCCDBG; probes reverted, native rebuilt).** Occlusion pipeline compare: decimated tris **15986-vs-15984** (±2), area 9401.07-vs-9411.17 (0.1%) — but sample positions COMPLETELY different (s0 (-13.4,-7.6,0.5)-vs-(5.0,-12.7,12.7)): the mt19937_64(27644437) sample stream maps through the area-prefix map, so a ±2-triangle/reordered decimation relocates all 30000 samples → the whole visibility field. ROOT: native decimates each volume in its RAW frame (volume matrix only; `obj_transform` applied AFTER its_short_edge_collpase, SeamPlacer.cpp:598-605) — rust decimates the already-CENTERED po.mesh(); the coordinate shift flips f32 tie-breaks in the collapse. R184 FIX: decimate in native's frame (un-center or keep the raw model mesh through the pipeline), merge, THEN apply trafo_centered; verify tris/area/first-samples byte-match, then visibility, then the seam census. NOTE: exact recovery needs the pre-centering mesh (un-translating f32 verts loses ulps — thread the raw mesh instead).

- **★★ R182 — SEAM ROOT = VISIBILITY, PROVEN (dual CANDDBG per-candidate @L44; probes reverted, native rebuilt).** All 444 native candidates position-match rust exactly, and on those: overhang diff = 0, embedded_distance diff = 0, local_ccw_angle ≤ 3e-5 — but **visibility differs on 432/444** (mean 0.043, max 0.37). The pick_seam_point comparator weighs visibility against angle, so the approximated occlusion flips ~half the picks on smooth loops. SECONDARY: rust has 747 candidates vs native 444 (+303 — likely inner-perimeter/oversampling inclusion; audit gather_seam_candidates filtering). THE FIX (R183+, multi-round): port native's `compute_global_occlusion` EXACTLY — SeamPlacer raycast visibility (its mesh sampling sequence, ray count, BVH traversal, and seeded RNG must all match to reproduce the visibility field byte-for-byte). With visibility exact, pre-align picks converge, alignment strings anchor identically, and the seam meta-class (≈half of Outer 23k + travels/lifts/wipes) unlocks — including the parked slope-lift and per-path LINE_WIDTH levers.

- **★ R181 — SEAM STAGE-SPLIT: ALIGNMENT EXONERATED — ROOT = PRE-ALIGN CANDIDATE SCORING (dual SEAMST staged probes; reverted, native rebuilt).** Per-perimeter seam picks: PRE-alignment 44% exact / **52% far**; POST-alignment 41%/48% — align_seam_points slightly IMPROVES convergence, so the fault is upstream in `pick_seam_point`'s comparator inputs. PRIME SUSPECT (admitted in rust's own init comment): `compute_global_occlusion` — rust uses an approximation ("samples the object mesh and raycasts") vs native's specific raycast sampling (SeamPlacer visibility: sample counts, ray distribution, seeded RNG) — visibility values break angle ties differently on smooth hull loops. R182 ENTRY POINT: dump per-candidate comparator inputs (position, visibility, local_ccw_angle, overhang, embedded_distance) for ONE diverging perimeter (e.g. L44) both engines → identify which INPUT diverges → port that computation exactly (occlusion sampling is the heavy candidate). Campaign continues next session.

- **R180 — SEAM DIVERGENCE CONFIRMED AT THE DECISION LEVEL (dual SEAMDBG in place_seam; probes reverted, native rebuilt): 749 native outer-wall placements vs rust — 41% exact / 11% near / **48% FAR**, matching the gcode census exactly → the divergence is INSIDE the SeamPlacer (candidate scoring or the aligned-string pass), NOT downstream loop splitting.** Far picks spread ~1.5/layer through the hull region (L43+; L0 worst at 7). Also noted: rust calls place_seam 1423× (probe unfiltered incl. inner walls) vs native 749 external. NEXT STAGE-SPLIT (R181): dump the PRE-ALIGNMENT per-perimeter seam_index choice vs the POST-ALIGNMENT final on both sides — if pre-align matches, the fault is the align_seam_points string pass (anchor + smoothing); if pre-align differs, it's candidate scoring (visibility/angle weights). The campaign continues next session.

- **R179 — SEAM META-CLASS QUANTIFIED (census; no code): outer-wall loop starts = **41.3% exact (<5µm), 10.4% near (<0.5mm), 48.3% FAR** (748 loops).** Half the loops seam at a DIFFERENT VERTEX — rotating the entire loop's point sequence (every XE line unmatched even with identical geometry) and displacing the travel/lift/wipe cluster. This is the single dominant remaining class. Notes: R127's "seam scale_ posture parity-neutral" verdict covered only the flagged truncation instance; the 48% comes from the seam DECISION logic (SeamPlacer scoring: visibility/angle/alignment) — a full-subsystem dual-dump campaign (place_seam chosen-vertex + score per loop at an oracle layer) is the entry point, multi-round. PARKED READY: slope-lift (R178) + per-path LINE_WIDTH (R165) re-apply after seam convergence. Session close: 136431→**99481** (−27.1%), default 147987/7adae05c held through 31 rounds (R149-R179).

- **R178 — SLOPE-LIFT PROBED + REVERTED (+1174).** Implemented native's lazy SlopeLift (pending m_to_lift consumed by the next travel: 3°-slope climb to a top point + combined XYZ at lifted Z, GCodeWriter.cpp:530-580) — structure emits (926 XYZ+F + 1229 XYZ rust lines) but coordinates barely match (native XYZ native-only 1502→1469, only −33): the slope-top and target points are functions of the TRAVEL ENDPOINTS, which differ per the seam/wipe-end positions (the R165 lesson again — structure levers pay only after value convergence). Reverted; re-apply when travel endpoints converge (seam class). The travel/lift/arc classes are hereby CONFIRMED seam-coupled — the remaining ~99.5k is dominated by ONE meta-class: perimeter traversal alignment (seam position + direction), which drives outer-wall XE lines, travel endpoints, lift geometry, wipe paths, and F-line placement. NEXT: the seam class itself (R107 noted ~90% self-converges; the residual is the real floor) or bank.

- **R177 — TRAVEL CLASS = THE LIFT-TYPE SYSTEM (census; no code).** OBJECT_ID 13 traced to `ModelInstance::id().id` (the ObjectBase sequential allocation counter — native's STL path constructs 12 ObjectBase objects before the instance; faithful replication = mirroring every allocation, DEFERRED as brittle for 480 lines). Travel breakdown: native-only includes **1502 combined `G1 X Y Z` moves = SlopeLift** (z_hop_types="Auto Lift": needs_retraction picks SpiralLift-through-overhang / SlopeLift-otherwise; lazy_lift merges the hop into the first travel segment, GCodeWriter slope_threshold) and SpiralLift = G2/G3-with-Z arcs (part of the 5.5k arc class!); rust always emits plain vertical `G1 Z` hops (684 rust-only Z-only) + separate XY travels (5968 rust-only XY). R178 = port the lift machinery (LiftType + eager/lazy lift + slope merge into travel_to_xyz + spiral arcs) — couples travels, Z-lines, F-only and arc classes.

- **★★ R176 — 100k BROKEN @a3edbe5 (ZSMOOTH_FAITHFUL): flip 100869→**99481** (−1388; session 136431→99481, −27.1%).** Two comment-class closures from the fresh shape census: (a) LINE_WIDTH tag dedup — native's m_last_width is PERSISTENT across features (same-width feature change emits nothing); rust re-emitted per feature (1177 rust-only "0.42" tags → **0**). (b) LAYER_HEIGHT float semantics — native subtracts FLOAT print_z values (20.4f32−20.2f32=0.200001, %g-printed); rust's f64-sub-then-cast printed exact 0.2 (both directions → **0**). Class-3/7 pattern-book examples both. GATES: default 147987/7adae05c ✓, deterministic ✓. Remaining census: G1-XE 21k/22.9k (geometry+E), arcs 5.5k/6.1k, G1-noE travels 7.3k/9.9k, F-only 4.1k/5.4k, M 2.5k/2.2k, OBJECT_ID 13-vs-0 (480), native-only variable-width LINE_WIDTH tags (~5k, value-coupled).

- **★ R175 — GAP PIPELINE CONVERGED WHERE INPUTS MATCH (dual GAP94 pointwise @L94; probes reverted, native rebuilt): the shared 82-point medial polyline agrees to ±1-2 SCALED UNITS end-to-end (len 1611453-vs-1611449, w0 41789-vs-41790, w1 exact) — collapse+DP+medial+width math is effectively CLOSED.** The residual = WHICH gap areas exist: native has gap bands (e.g. 0.75mm² at (-3.8,-6.3)mm, 8-pt contour → 1.9mm polyline) that rust lacks entirely, and vice versa — the per-loop gap COLLECTION (`diff_ex(offset(last,-d/2), offset(offsets,d/2+10))` during the perimeter offset chain) inherits island-specific perimeter-geometry diffs (the R121 24%-of-islands class). VERDICT: Gap class beyond L0-floor + collection-inheritance = closed; further Gap convergence rides on perimeter/slice geometry (deep). Remaining top classes: Outer wall 23.4k (seam), ISI 17.8k + Floating 12k (classification), Top 13.5k (E/geometry) — all deep-class; the bounded runway from the R150 census is now EXHAUSTED. Session 136431→100869 (−26%).

- **R174 — L1 GAP EXCESS = THE L0 BASE-SLICE FLOOR (dual SURFDBG @L0; probes reverted, native rebuilt).** Both engines feed PG TWO surfaces at L0 — but the big surface's AREA differs 1.4% (native 41.34mm² vs rust 40.78mm²): the KNOWN L0 slice-topology floor (R106/R109 class). Downstream of that geometry, rust's small surface yields 0 gap polygons where native's yields 2, and the whole L0 gap band (786-line diff, the largest single-layer gap chunk) inherits. NOT a gap-block bug — R173's per-surface restructure stands correct for all layers. REMAINING Gap class = mid-layer value diffs (L52-54/L94/L114-120 ~200-300 each: medial-axis variable-width values on near-identical bands — the fractional LINE_WIDTH class) + this L0 floor. NEXT: point-level dual dump of one mid-layer gap run (L94) — polyline pts + widths — to split DP/medial/width-computation causes.

- **R173 — PER-SURFACE GAP COLLAPSE LANDED @b631b55 (F1_UNION): faithful restructure, measured INERT (100869 unchanged) — the real root is UPSTREAM.** PerimeterResult carries gap_fills_per_surface; the layer gap block collapses per surface. Inert because rust's PG receives ONE surface at L0 where native processes TWO (the 67-gap + 2-gap calls) — the L0 surface SEGMENTATION into PG differs (Layer::make_perimeters slices grouping / new_slices union, or region split). R174 = dual per-layer probe of surface counts+areas entering PG (native process_classic surface loop vs rust generate() slices) to find where the second surface disappears; THEN the R172 collapse-locality payoff unlocks.

- **★ R172 — GAP-FILL ROOT LOCALIZED: rust collapses gaps MERGED-ACROSS-SURFACES; native runs PER SURFACE (dual GAPDBG @L0, instrumentation reverted, native rebuilt).** Identical inputs (67 gap polygons, min=0.06/max=0.9142, pw=0.5/ps=0.45708 all byte-consistent) but native's per-surface collapse+medial (two calls: 67-gap surface → 39 gaps_ex → 47 polylines len 29.9mm, + 2-gap surface → 1 poly 1.6mm) vs rust's ONE merged call → 27 gaps_ex → 23 polylines len 20.7mm (65%). The opening/offset2/diff ops interact across islands when merged (nearby surfaces' gaps fuse/cancel through the ±max/2 offsets). ALSO: rust's merged n_gaps=67 vs native's 67+2=69 — the second surface's 2 gap polygons may be dropped in the per-surface result merge (R141-class candidate) or attributed differently. GCODE census L1 (=layer_id 0): native 708 gap lines vs rust 362 — this class is the Gap 22.5k's biggest structural piece. R173 = restructure: PerimeterResult carries PER-SURFACE gap_fills (Vec<ExPolygons>); layer.rs runs collapse+DP+medial per surface in surface order (gated F1_UNION like the existing block); verify the 2-gap surface reaches the result.

- **R171 — VETTED CONFIG-KEY DELEGATION @e60c9c7: six dropped keys land (flip ~flat 100869; L1 Bottom F FIXED 6300=6300, was 3000).** set_deserialize delegates a whitelist to apply_key_value under the gate. A BLANKET fallback (all 284 keys) measured **+119** — mass-landing mixed keys violates the cluster discipline; per-key bisection showed the five companions neutral and initial_layer_infill_speed +6 net (its F fix trades against exposed downstream ordering). Values now correct for: initial_layer_infill_speed, filament_max_volumetric_speed, vertical_shell_speed, reduce_fan_stop_start_freq, retraction_minimum_travel (no longer accidental), no_slow_down_for_cooling_on_outwalls. GATES: default 147987/7adae05c ✓. The remaining ~100.9k is now dominated by GEOMETRY+E classes (Gap 22.5k, ISI 17.8k, Top 13.5k, Outer 23.4k incl. seam) + ordering — the emission-fidelity runway is largely TAPPED; next major lever = gap-fill/medial-axis geometry or seam.

- **★ R170 — CONFIG-KEY COVERAGE AUDIT @d7f1da5: **284 of 427** resolved-profile keys silently dropped by set_deserialize; flip 100957→**100863**.** Gcode-affecting dropped keys QUEUED (each maps to a known diff class): `initial_layer_infill_speed`=105 (L1 Bottom F6300-vs-3000), `vertical_shell_speed`=80% (Floating F class), `reduce_fan_stop_start_freq`=1 (fan base), `filament_max_volumetric_speed`=25/40 (speed caps — rust may cap differently model-wide!), proper `retraction_minimum_travel` arm (currently 1.0 only by ACCIDENT via the mis-wired retract_when_changing_layer arm), `no_slow_down_for_cooling_on_outwalls`, `machine_max_acceleration_*` (M204 clamps), `small_perimeter_threshold`, `only_one_wall_top`, `interface_shells`. LANDED: `travel_short_distance_acceleration` (250) + writer.set_travel_acceleration_for (GCode.cpp:6880-6887 — short accel for sub-1mm travels to outer walls) at the 5 gated travel sites: S250 native-only 617→541 (residual cadence-coupled). GATES: default 147987/7adae05c ✓. NEXT (R171): land the queued speed keys (initial_layer_infill_speed + vertical_shell_speed + volumetric cap first — direct F-value classes).

- **★ R169 — LAYER-CHANGE TEMPLATE ACTIVATED @5194123: flip 101665→**100957** (−708; session 136431→100957, −26%).** The R168 branch was inert because `PrintConfig::set_deserialize` (the CLI/library config ingestion the compare harness uses) lacked the machine-gcode arms — `layer_change_gcode` existed only in the separate `apply_key_value` fn. Arms added; the faithful block now emits: H2D header 240=240 ✓, `M73 L{n}` exact ✓, marker-driven per-layer fan (M106 S0 3-vs-2). LESSON: rust has DUPLICATE config-application fns (set_deserialize vs apply_key_value) with divergent key coverage — audit candidates for other silently-dropped profile keys. GATES: default 147987/7adae05c ✓, deterministic ✓.

- **R168 (WIP) — LAYER-CHANGE TEMPLATE @6c950f8: implemented but INERT (config plumbing gap).** The layer-change block should come from the `layer_change_gcode` TEMPLATE (`;======== H2D 20250710 layer_change ========` header 240×, `M73 L{n}` 240×, M991, trailing blank, then the `;_SET_FAN_SPEED_CHANGING_LAYER` marker — native emits NO hardcoded M106 S0 pair; the cooling post-processor injects per-layer fan lines at the marker, which also explains rust's 241 hardcoded S0/P2-S0 rust-only lines AND rust's missing per-layer duty emissions). Gated implementation landed, but LCDBG probe shows `self.config.layer_change_gcode` len=0 at runtime — the machine-gcode keys never reach print.rs's config despite preset whitelist (preset.rs:657) + parse arm (print_config.rs:4665; likely a different struct's apply). R169 = trace the preset→config application path for machine keys and wire it; the template branch then activates (~1000 line payoff: header 240 + M73 240 + S0 pair 476 + blank).

- **★★ R167 — FAITHFUL RETRACTION AT ALL BUCKET SITES @561abea (ZSMOOTH_FAITHFUL): flip 104946→**101665** (−3281; session 136431→101665, −25.5%).** R164's decision was wired into ONE call site; the five bucket-start helpers retracted unconditionally (travel-length census: rust wiped 211/245 sub-mm outer→gap travels vs native 91/235). All now route through needs_retraction_faithful. LIVE GOTCHA: unretract must remain UNCONDITIONAL (native _extrude always unretracts) — gating it stuck the writer's initial retracted=true and produced a file with ZERO retractions (caught by WIPE/`G1 E.4` counts before commit). WIPE 2322→2022 (native 1742; residual 280). Also flagged: parse arm `retract_when_changing_layer` mis-assigns retract_before_travel (benign here, both 1.0). GATES: default 147987/7adae05c ✓, deterministic ✓, material 3845.22 ✓.

- **R166 — WIPE RESIDUAL CENSUS (measurement).** Transition histogram of WIPE_START blocks: rust excess concentrates at layer-end ("Outer wall→?" +379) and Outer-wall→Gap (+255), Inner→Outer (+57). LAYER-END is an ORDERING artifact, not a count diff: native retracts+wipes at the NEW layer's start INSIDE change_layer() (retract_when_changing_layer=1 — WIPE_START appears AFTER `; CHANGE_LAYER`/Z_HEIGHT/LAYER_HEIGHT headers), while rust retracts at the OLD layer's end (before the marker) — my census mis-attributed native's to feature pairs. Native retract() wipe gate = wipe && has_path && wipe_distance>0, path CONSUMED on use (Wipe::wipe reset_path) — rust equivalent (clears after use). Genuine excess (~580 blocks) needs a decision-level dual trace (RETDBG: per-retraction inputs at Outer-wall→Gap sites — suspects: rust FORCE-branch firing where native's travel<1mm skips retraction entirely, and wipe-path availability differences). ALSO REORDER CANDIDATE (LCS + block-structure): move rust's layer-end retract into the layer-change emission after the headers (change_layer semantics).

- **R165 — PER-PATH LINE_WIDTH probe: REVERTED (+3568 regression — lever is VALUE-COUPLED to gap geometry).** Post-R164 census: G1-XE extrusion moves dominate both sides (21k/23.5k = the geometry+E classes: Gap 22.5k, ISI 17.8k, Top 13.5k); native-only LINE_WIDTH 6403 looked bounded, and the faithful per-path emitter (GCode.cpp:6605 m_last_width + %g) is structurally correct — BUT the variable gap-fill width VALUES come from rust's unconverged medial-axis geometry, so per-path tags mostly mismatch while the removed legacy per-feature "0.42" lines HAD been matching. Lesson: emission-structure fixes only pay when the underlying VALUES already converge — sequence formatter/structure levers behind value convergence. The per-path width emitter is correct for later (re-apply when gap geometry closes). NEXT: back to value-level classes — Gap infill 22.5k (medial-axis/variable-width geometry) or the WIPE residual 580.

- **★★★ R164 — FAITHFUL needs_retraction @89ce4b1 (ZSMOOTH_FAITHFUL): flip 113074→**104946** (−8128, 2nd-largest win of the session; session 136431→104946, −23%).** GCode.cpp:6964-7095 branch order ported: min-travel; FORCE retract when leaving outer/overhang wall (writer.last_extrusion_role); reduce_infill_retraction SKIP (profile mode=Auto + metal_stickiness=None → ACTIVE) when dest not perimeter && density>0 && travel inside an internal island w/o wall crossing — RetractWhenCrossingPerimeters ported as per-layer RetractCtx (internal-slice islands + boundary lines + internal-region perimeter polylines; containment + exact-integer segment intersection). The retract/unretract/wipe/travel-F cluster follows each decision — WIPE blocks 3050→2322 (native 1742; 580 excess = decision borderline diffs + remaining semantic gaps e.g. lift types). GATES: default 147987/7adae05c ✓, deterministic ✓.

- **★ R163 — OVERHANG-FAN MARKERS + FAITHFUL M106 @d237adb (ZSMOOTH_FAITHFUL): flip 113364→**113074** (−290 net; the fan subsystem is now structurally live — S255 toggles match to ±61).** Exporter emits `;_OVERHANG_FAN_START/_END` per qualifying path (GCode.cpp:6633-6641); CRITICAL semantic: native tests `int get_overhang_degree()` — the double degree is TRUNCATED before `> threshold−1` (ExtrusionEntity.hpp:354) — the f64 compare over-toggled (+2222 regression caught live, fixed with `as i32`). `format_set_fan` now = GCodeWriter::set_fan (S = 255·pct/100 fractional e.g. 201.45; zero → "M106 S0" not M107). New configs enable_overhang_bridge_fan + overhang_fan_threshold (enum strings "0%".."95%" → 0..5; profile 50%→3). FAN CALC was already correct (FANDBG: 77/79/80% per layer) — the gap was markers+format. RESIDUAL M106: S201.45-vs-S204 restore pairs (~1.2k) = 79-vs-80% layer duty (INHERITED from layer-time estimates); per-layer S0/P2-S0 cadence (238 each — rust emits every layer, native on change; probe next). GATES: default 147987/7adae05c ✓.

- **R162 — FAN + RETRACTION + RESIDUAL CENSUS (measurement; no code).** (1) WIPE excess (rust 3050 vs native 1742 blocks): native SKIPS retraction into top-fill etc. via `needs_retraction` (GCode.cpp:6964-7095) — rust's length-only check misses BOTH the reduce_infill_retraction branch (skip when travel stays inside internal regions without wall crossing, `travel_inside_internal_regions_no_wall_crossing`, gated rirEnabled/rirAuto+metal-stickiness) AND the force-retract-when-leaving-external-perimeter branch; porting needs the RetractWhenCrossingPerimeters containment test. (2) M106 fan CHASM: native per-layer part fan `M106 S201.45` (1347×, cooling-computed duty) + overhang fan `M106 S255` (1392×, overhang_fan_speed=100 toggled around overhang paths via ;_OVERHANG_FAN_START/_END markers) — rust emits only S0 per layer (241×): the exporter NEVER emits the overhang-fan markers (cooling.rs supports them, 5 refs) and the per-layer fan duty computes 0 (fan calc or config parse bug — probe fan_min/max_speed + fan_cooling_layer_time flow). (3) M204 residuals: rust-only S10000 966 = travel-accel set at helper sites with no actual travel between register flips; native-only S250 617 source still unknown (bridge/overhang accel candidate). NEXT (R163): per-layer fan duty root (probe calculate_fan_speed inputs), then overhang-fan markers, then needs_retraction port.

- **★★ R161 — UNIFIED M204 REGISTER @76f60b3 (ZSMOOTH_FAITHFUL): flip 116947→**113364** (−3583; session 136431→113364).** POST-R160 CENSUS first: Outer wall COLLAPSED 40647→24259 (the F-format win); shape analysis of native-only lines exposed M204 5605 + M106 2786 + M73 355 + WIPE markers 2616 rust-only + OBJECT_ID 13-vs-0 (480) + LINE_WIDTH precision (native prints actual f32 path width 0.41999/0.38292, rust prints config 0.42). M204 FIX: native routes travel+feature accel through ONE m_last_acceleration register (set_acceleration_impl) — alternation re-emits constantly; rust had separate sticky states + hardcoded 6000 travel + role-change-only feature emission (double-emission caught live and gated off). RESIDUAL M204: native-only S250 617 (unknown source — bridge/overhang accel probe next) + S8000 416; rust-only S10000 966 (rust calls set_travel_acceleration at helper sites where native has no travel). NEXT LEVERS (census-ranked): Gap 25638 (largest), M106 fan 2786+476, WIPE marker strip 2616, rust-extra S10000 966, S250 source, OBJECT_ID 13, LINE_WIDTH from path width. GATES: default 147987/7adae05c ✓, deterministic ✓.

- **★★★ R160 — FAITHFUL F FORMATTING @03bf103 (ZSMOOTH_FAITHFUL): flip 133367→**116947** (−16420 — the LARGEST single-round win of the campaign, 3× R146; session 136431→116947).** (a) Skip the collection-level feature-speed `G1 F` before overhang-modulated perimeter loops (native's first F = first path's overhang speed; −117). (b) ★★★ writer set_speed F format `{:.0}`→native emit_f semantics (3 decimals, trailing zeros/dot trimmed): native's FRACTIONAL F lines (smooth ramps 8828.637, overhang interp, volumetric caps) pass through the cooling rewrite UNTOUCHED (GCodeEditor re-formats only slowed lines, as int(floor(60·f+0.5))), so rust's integer rounding made every fractional-F line unmatchable even where the speed VALUE was already byte-correct (−16303). LESSON (add to PORTING_PATTERNS class 3-adjacent): output FORMATTERS are fidelity surfaces — a correct value behind a lossy format is still a 100% mismatch; audit every emit path's precision against the native formatter constants (XYZF_EXPORT_DIGITS=3). GATES: default 147987/7adae05c ✓ (both gated), deterministic ✓. Material header 3845.22 (pre-existing 6/7-gate offset). REMAINING L2-class: seam/start position (rust ascends from deg-1.9, native descends from deg-1.3 — traversal start differs; the known seam floor) + smooth-ramp VALUE diffs where traversal aligns.

- **★ R159 — OVERHANG CLASSIFICATION EXONERATED TOO (L2 dual OV2DBG: degree distributions IDENTICAL — same counts/lengths per 0.1-degree bucket, 46/54 first-points byte-equal, 0 mismatches); REAL ROOT = WITHIN-LAYER SPEED-SMOOTHING EMISSION (instrumentation reverted, native rebuilt).** Final-gcode L2 outwall F sequences: native starts at the overhang speed (F9300) and ramps through SPLIT sub-segments with interpolated fractional speeds (8828.637→7702.529→…→3000 — GCode.cpp `need_smooth_speed`/`split_and_mapping_speed`, sqrt-coordinate mapping get_speed_coor_x=√(v/m_smooth_coefficient), smooth_coefficient=4); rust emits a RAW FEATURE-SPEED `G1 F12000` first (extrude_collection's initial Outer-wall speed — native never emits it) then its own different ramp (3900/4115/4571… from exporter smooth_speed_discontinuity_area). So the R151 outer-wall F-class = the within-layer smooth-speed subsystem (exporter.rs smooth_speed path + smooth_speed.rs vs GCode.cpp:5975-6100), NOT classification and NOT the interp table. NEXT (R160): (a) kill the leading feature-speed F line on overhang-modulated walls (native sets speed per path only), (b) audit smooth_speed_discontinuity_area against split_and_mapping_speed (split geometry + speed values).

- **R158 — OVERHANG-SPEED INTERPOLATION EXONERATED; RESIDUAL = OVERHANG-DEGREE CLASSIFICATION (measurement; OVDBG probe reverted).** Rust's `overhang_degree_corr_speed` port is FAITHFUL and live: OVDBG shows deg=1.3→155.0 exactly (the R157 native value), deg=1.0→speed_at(1)=0→base fallback ✓, deg=0.7→200 ✓, apply=true on ext perimeters. THE ACTUAL DIVERGENCE: rust classifies **2011 ext-perimeter paths deg=0.000** where native rates e.g. L2's ENTIRE outwall loop ≥1.3 (node max 155 means NO native line at 200 there — the hull flares outward from the base so the whole L2 outline slightly overhangs L1; rust's detect_overhang_degree gives most of those lines 0). NEXT (R159): dual-dump per-path (first-point, overhang_degree) on the L2 outer wall native-vs-rust; suspects = OverhangDetector inputs (lower_polygons source/offset, lower_bound/upper_bound thresholds, the classic split params in perimeter_generator.rs:2244 detect_overhang_degree call).

- **★★ R157 — SMOOTHING ALGORITHM VALIDATED 340/340; RESIDUAL = OVERHANG-SPEED INTERPOLATION ON OUTER WALLS (measurement; SMDBG instrumentation reverted both trees, native rebuilt, default 147987/7adae05c re-verified).** Dual SMDBG (per-(layer,node) max/filter feedrate pre+post smoothing): node sets match 340-vs-341 (the R155 merge-group diff), but **241/340 nodes diverge on INPUT max_feedrate**. Python-replicated Smoothing.cpp gaussian run on NATIVE's pre inputs reproduces native's post outputs **exactly (340/340, worst 0.000)** — the rust smoothing port is ALGORITHM-CORRECT; all residual divergence is upstream. INPUT ROOT IDENTIFIED: native's exact 155.000 (L2) and 170.000 (L11-13) = **overhang-degree speed INTERPOLATION on outer walls** — 155 = 200−(200−50)·0.3, 170 = 200−(200−50)·0.2 (interpolating outer_wall_speed→overhang_2_4_speed=50 by fractional overhang degree); rust emits raw 200 (no interpolation / different overhang classification). Remaining input diffs: fractional cooling-slowdown feedrates (inherited layer-time diffs) + marked-line counts (native ~88/node vs rust ~73 on L2 — emission granularity). NEXT (R158): rust exporter overhang-speed interpolation for outer walls (GCode.cpp path.overhang_degree speed table lookup — bounded lever; native emits interpolated F pre-cooling, so this feeds BOTH the raw F lines and the smoothing inputs).

- **★ R156 — TWO-PHASE COOLING + SMOOTHCALCULATOR WIRED @3164dcd (ZSMOOTH_FAITHFUL): flip 134921→**133367** (−1554; session total 136431→133367).** Pieces 5-7 landed: parser marks (OBJECT_ID/COOLING_NODE comments → outwall_smooth_mark on ext-perimeter adjustable lines; native mark/record two-step collapsed to a direct set), two-phase print.rs path (parse-all → build_node_postproc → smooth_layer_speed → recalc+write, GCode.cpp:3396-3417), Smoothing.cpp bridge over live PostProcAdjustments (reuses smoothing.rs gaussian core incl. the std::map operator[] self-densify loop). RUST OUTER-WALL F NOW RAMPS (10550/10558/10801…) but ≠ native (9300…): the filter input = parse-time outwall feedrates which inherit upstream cooling-slowdown/overhang differences — native L12-13 outwall reads 2948/1244 (already cooling-slowed, fractional) where rust reads ~10900, so native's gaussian drags L2-L11 much lower. 219/240 outer first-F still differ but now by ramp-value, not missing-ramp. NEXT: dual SMDBG — per-(node,layer) max_feedrate/filter_feedrate before+after smoothing, find first diverging input (suspects: no_slow_down_for_cooling_on_outwalls config parse, overhang outwall F, slowdown-target overshoot from R151(2)). GATES: default 147987/7adae05c ✓, 7-gate deterministic ✓.

- **★ R155 — GATED `; COOLING_NODE:` EMISSION (new gate ZSMOOTH_FAITHFUL; PRIMARY is now the 7-GATE path): flip 136256→**134921** (−1335).** Native KEEPS the markers in final gcode (1406 lines) — GCode.cpp:5738-5747 emits before every perimeter entity with cooling_node != -1 (its compare var never updates from -1; per-entity, not per-change). Rust mirrors via bucket-carried node ids + per-entity injection in extrude_perimeters_entities. MARKER PARITY: 1407-vs-1406; ids exact for 5/7/8/9 (node 9: 121=121); residual = ONE merge-group diff (36 entities rust node 0 vs native node 3 — a single continuity-link difference flips one merge; next probe NODEDBG per-layer first-divergence). GATES: default 147987/7adae05c ✓, F1 62f65c22 ✓, 7-gate deterministic ✓. REMAINING pieces 5-7 (parse marks → two-phase cooling → SmoothCalculator apply): the F-value rewrite payoff (~25k class) still ahead.

- **R154 — SMOOTHING PIECES 2-3 LANDED @e95527e (default-inert, 147987/7adae05c verified): Layer::calculate_perimeter_continuity faithful port (Layer.cpp:353-431; NOTE native `dist_scale_threshold` is `static const int = 1.2` → truncates to 1, so the width limit is exactly the prev width — ported faithfully) + record_cooling_node (parallel `entity_cooling_nodes` vec on LayerRegion) + PrintObject driver (PrintObject.cpp:583-615).** Found+fixed an R141-class merge bug LIVE: `PerimeterGenerator::generate()` dropped per-surface `loop_nodes`/`loop_node_range` on merge (rebased node_id/loop_id now). NODEDBG: benchy graph = 739 nodes, 728 linked (98.5%), max_merged_id=10 (11 cooling nodes). NEXT (R155, pieces 4-7, gated ZSMOOTH_FAITHFUL): `; COOLING_NODE:` emission in the perimeter walk + parser marks (cooling.rs) + two-phase cooling restructure + SmoothCalculator wire.

- **R153 — SMOOTHING PIECE 1 LANDED @f9dfc54 (default-inert, 147987/7adae05c verified): outwall NodeContour collection (PG.cpp:1074-1100) + LoopNode push after wall-sequence reorder (PG.cpp:1281-1317, faithful Point::is_in_lines) + `z_direction_outwall_speed_continuous` config parse + collection.loop_node_range + Layer::loop_nodes rebase-plumbing.** Metadata unconsumed until pieces 2-3 (Layer::calculate_perimeter_continuity + driver + record_cooling_node) land next.

- **R152 — Z-CONTINUOUS OUTWALL SMOOTHING: FULL DEPENDENCY MAP (planning; no code).** The SmoothCalculator wire needs SEVEN pieces (native refs): (1) **NodeContour/LoopNode build in classic perimeter path** — PG.cpp:1074-1100 collects `outwall_paths` at depth i==0 under `z_direction_outwall_speed_continuous` (thin_walls ThickPolylines w/ per-pt widths + offsets_with_smaller_width + offsets as polylines w/ single width scale_(flow.width())); PG.cpp:1281-1317 post-reorder pushes LoopNodes (1 path → loop_id=0; else match non-erPerimeter entity first_point `is_in_lines` an outwall_path → loop_id=entity_idx) + sets `entities.loop_node_range`; (2) **Layer::calculate_perimeter_continuity** (Layer.cpp:355-431, rust STUB at layer.rs:1786) — links nodes across layers via distance_from_perimeter<width with Continuitious_length threshold; (3) **driver** PrintObject.cpp:583-615 (continuity per layer, merge_layer_node — rust port EXISTS print_object.rs:2865 — then recrod_cooling_node_for_each_extrusion Layer.cpp:435-460 mapping merged_id→entities via loop_node_range/loop_id, rust TODO at print_object.rs:676); (4) **cooling_node on ExtrusionEntity** + `; COOLING_NODE: <id>` emission on change in extrude_perimeters (GCode.cpp:5738-5747; rust never emits — GATE for default byte-lock); (5) **parser marks** GCodeEditor.cpp:134-235 (`; OBJECT_ID: `/`; COOLING_NODE: ` tokens + mark_node_pos/record_wall_lines set outwall_smooth_mark/cooling_node_id on TYPE_EXTERNAL_PERIMETER+ADJUSTABLE lines; rust cooling.rs fields EXIST but parser never sets from markers); (6) **two-phase cooling restructure** (split rust process_layer into parse+slowdown / write; buffer all layers; native GCode.cpp:3396-3417: pipeline-1 parse→cooling→build_node, append_data per flushed layer, smooth_layer_speed(), pipeline-2 recaculate_layer_time(layer>0)→write); (7) **SmoothCalculator over cooling.rs types** — smoothing.rs (503L faithful port incl. Gaussian window-11/r=2/stop-5/max-1000) consumes g_code_editor::PerExtruderAdjustments, needs adapter or ~80-line build_node/exclude_participate/recalc duplicates for PostProcAdjustments (Smoothing.cpp:5-80: node keyed (object_id, cooling_node_id); max_feedrate from TYPE_EXTERNAL_PERIMETER lines; recalc clamps line.feedrate→filter_feedrate, sets slowdown, CLEARS TYPE_ADJUSTABLE, time=length/feedrate). LANDING ORDER: pieces 1-3 are DEFAULT-INERT metadata (land ungated); 4-7 output-changing (gate, e.g. ZSMOOTH_FAITHFUL). Payoff: 219/240 outer-wall F ramps + part of the 25.1k F-class.

- **★★ R151 — F-CLASS SPLIT 3 WAYS (dual CBDBG per-layer, instrumentation reverted, native rebuilt): (1) ★ SMOOTHCALCULATOR OUTWALL RAMP MISSING IN RUST — 219/240 layers diverge on outer-wall F: native ramps +~58/layer (L2 9300→L11 9807, overhang dips L12-13 to 1244, climbs back 3000→10910) while rust emits raw config speeds; the across-layer smoothing port EXISTS (gcode/smoothing.rs, 503 lines, CoolingNode/OutwallCollection ported from Smoothing.hpp) but is NEVER WIRED — and rust's single-pass `process_layer` (parse→slowdown→write per layer, cooling.rs:1829) structurally CANNOT smooth across layers: native runs TWO passes (pipeline 1 parse→cooling→build_node collects ALL layers; pipeline 2 smooth_layer_speed→write_layer_gcode rewrites, GCode.cpp:3330-3380). (2) rust slowdown misses the target native hits exactly: slowed layers land native t_after=4.004 (slow_down_layer_time·1.001) vs rust 4.009-4.237 — algorithmic overshoot inside calculate_layer_slowdown_postproc. (3) t_before (per-layer time estimate) differs on EVERY layer (mean 0.57s, max 11.4s) with nlines/nadj parse-count differences — INHERITED from upstream gcode geometry diffs, not a cooling bug (cooling amplifies: one factor diff → every adjustable F on the layer unmatched). Also L0: rust force-disables slowdown via is_first_layer hack (nadj 0 vs native 164) — inert on benchy (L0 unslowed anyway) but unfaithful. R152 = restructure cooling to two-phase (buffer per-layer parses + gcode; run SmoothCalculator build_node/smooth_layer_speed between; then write), wire smoothing.rs, fix the target overshoot; expect a large share of the 25.1k F-class + part of Outer-wall's 40k to close.**

- **★★ R150 — COOLING/SPEED SUBSYSTEM = LARGEST BOUNDED LEVER (census + flow exoneration; instrumentation reverted both trees, native rebuilt): stripping F-words from both gcodes closes **25110** of the 136256 multiset (→111146, 18.4%); stripping fan lines (M106/M107) another **3262** (→107884). 818/1110 feature blocks have different first-F on ALL 240 layers — rust emits raw config speeds (12000/18000/21000) where native emits per-layer cooling-slowdown values (e.g. L5: native Inner/Floating/Sparse SHARE 10221 = the `slowdown_below_layer_time` per-layer rescale signature).** FLOW EXONERATED first (FLOWDBG dual dump @L196 z=39.2): native mm3_per_mm=0.075415924 (with_spacing roundtrip IDEMPOTENT — pf_mm3 identical) vs rust 0.075415927 (4e-8 relative) — the L196 E-residual is GEOMETRY (1µm travel-start offsets scaling segment length), NOT flow; layer.rs:2326's raw `params.flow.mm3_per_mm()` is byte-equivalent to native's roundtrip here (native FillRectilinear.cpp:3270 does `with_spacing(this->spacing)` but spacing is nominal under dont_adjust). RUST COOLING TOPOLOGY: exporter's `apply_layer_cooling` (exporter.rs:2046) is fan-only with a TODO, but the real implementation is the gcode post-processor port `gcode/cooling.rs` (~3k lines, `calculate_layer_slowdown_postproc` :2562, applied at :1870) — the F divergence lives inside it (per-line time estimation / adjustable-flag classification / target-time computation vs native CoolingBuffer.cpp). R151 = dual CBDBG: per-layer dump (elapsed-before, target slow_down_layer_time, factor, per-feature adjustable set) native-vs-rust, find first diverging layer.

- **★ R149 — MONOTONIC ACO n==1 FAITHFUL (gated TOPFILL_FAITHFUL): flip 136431→**136256** (−175); L196 top-fill SWEEP ORDER now MATCHES native (same start X3.435 Y8.854 + same zig-zag order — the R147/R148 target closed).** Two fixes: (a) deleted the n==1 shortcut under the gate — native runs the ant loop even for single regions, `flipped` chosen by the deterministic mt19937_64 draws + cost (the R148 'empty path' claim was WRONG: the rust ant loop DOES push the first link at fill_rectilinear.rs:3696; verified empirically — n==1 produces a 1-link path); (b) pheromone reinforcement now iterates the POST-SWAP `path` like native (FillRectilinear.cpp:2643 uses `path` — the last ant's path, or the previous best after the improvement swap — NOT `best_path`, despite its comment; rust `path` hoisted to fn scope so the swap is observable). rng fidelity audited: rust `uniform_int_below` = libc++ masked-rejection AND libc++ short-circuits size-1 ranges with ZERO draws — matches `std::uniform_int_distribution<>` on darwin; the `rng() > rng.max()/2` flip draw matches. GATES: default 147987/sha256 7adae05c ✓ (sha1-vs-sha256 false alarm — the lock sha was always sha256), F1 5-gate 62f65c22 ✓, flip determinism ✓ (2 runs byte-identical c56a8b89). MATERIAL note: 6-gate header reads 3845.22 vs native 3858.97 — PRE-EXISTING at the R147/R148 baseline (3845.30; the 3858.63 memory figure is the 5-gate/feat_e2 number), R149 delta −0.08mm. RESIDUAL at L196: pure E-rounding (E.0339 vs E.03394) + sub-µm coords — the width-source class, not order. Next: with order closed, re-census the 136k by feature class.

- **R148 — MONOTONIC SWEEP-ORDER ROOT LOCALIZED (measurement + fidelity-note; shortcut restored, instrumentation stripped, default 147987 verified): rust hardcodes `flipped=false` for single-region monotonic chains; native's ACO picks orientation by COST (dual CHDBG: native 137 single-region chains split 70F/67; rust chain fires only for n≥2) — L196's whole chain runs back-to-front (rust's last extrusion = native's first). Removing the shortcut exposed a SECOND rust ACO bug: the ant loop never initializes the first region's link, so n==1 yields an EMPTY path (silent line loss) — shortcut restored pending the faithful fix.** THE MAPPED FIX (next round): port native's ant-path initialization (FillRectilinear.cpp:2410-2460 — first-pick from the queue + flipped chosen by AntPath cost), which simultaneously (a) fixes the n==1 empty path and (b) gives every single-region chain its cost-chosen orientation; then delete the shortcut. Affects ~137 single-region monotonic fills (most top/bottom solids) — the largest remaining bounded lever after the R146/R147 connector work.

- **R147 — 4-POINT GAP-COMPENSATION CONNECTORS (config `monotonic_travel_into_wall`=45% + faithful overlap extension): connector geometry byte-matches native (±0.133mm extensions); flip 136431 (≈R146's 136368 — the residual is ORDER, see below).** The profile sets 45% (NOT the 0 default — R146's 2-point branch was the wrong branch for benchy); native extends each connector past both line ends by scaled(width·0.45) along each line's OWN direction (hpp:688-701, overlap_rate=1.0, truncating casts; the raw percent number ×0.01 per Fill.cpp:636 — first attempt used the raw 45.0 and shot connectors 13mm into the model, caught by the L196 oracle). NEW TARGET LOCALIZED: with connector geometry identical, L196 shows native sweeping the monotonic region in DECREASING Y where rust sweeps INCREASING Y — the monotonic traversal start/direction in fill_surface_by_lines_monotonic. Affects every monotonic solid layer (Top 12.3k + Bottom + ISI share): the next bounded lever.

- **★★★ R146 — extrusion_entities_append_paths_with_wipe (the MonotonicLine WIPE CONNECTORS): flip 141616→**136368** (−5248, the LARGEST single-round win of the campaign; 5-gate base 141654, default 147987/7adae05c + F1 62f65c22 + flip-determinism 96db0e62 all verified).** Root (UXDBG dual dump): native coll_nosort has **73** coverage polys from 37 lines — `extrusion_entities_append_paths_with_wipe` (ExtrusionEntity.hpp:671-724) chains consecutive monotonic lines whose hop ≤ 3·scale(width) with a NON-EXTRUDING 2-point connector path (5th ExtrusionPath ctor arg = no_extrusion; `monotonic_travel_into_wall` defaults 0% ⟹ the simple 2-point branch). The connectors (a) appear in native's gcode as **E-less G1 moves** (GCodeWriter.cpp:705/710: force_no_extrusion skips filament()->extrude AND omits the E word) and (b) count in polygons_covered_by_spacing, blanketing the inter-line margins — rust emitted separate paths with travels, leaving 10× the uncovered band (1.82 vs 0.18mm²). IMPLEMENTED (4 pieces, intrinsically default-inert — no_extrusion paths only created under TOPFILL_FAITHFUL): faithful `extrusion_entities_append_paths_with_wipe`; gated dispatch in the mono emit; `GCodeWriter::wipe_to` (E-less LinearMove, filament E untouched); exporter dispatch on `path.no_extrusion`. RESULT: L196 gap 79→**5** (native 6); coverage 73-vs-73; unex_total 1.82→0.97mm² (residual band diff dies in the opening/medial filters). The top-surface gcode structure now matches native's multipath form (extrude-wipe-extrude chains).

- **★ R145 — polygons_covered_by_* → faithful ClipperLib polyline offset (new `cz_offset_polyline` shim; TOPFILL_FAITHFUL): flip 141711→**141616** — the 6-gate path is now BELOW the 5-gate base (141654) = NET WIN, and the R144 promotion is vindicated.** Measured first: raster endpoints now agree to ≤1.64µm (median 0.74µm, 37/37 lines matched — R135's 25-47µm figure was STALE, closed by R137-R143). The last geo op in the coverage chain was `offset_polyline` inside `polygons_covered_by_spacing`/`_width` (its own FIDELITY-NOTE admitted it): geo grids coverage rects to 1µm, flipping the razor-thin 0.21µm adjacent-line coverage overlaps into gaps. New shim `cz_offset_polyline` = native `offset(Polyline, delta)` exactly (per-path jtSquare/etOpenButt ClipperOffset, MiterLimit=DefaultLineMiterLimit=0, ShortestEdgeLength=|δ|·0.005, then NonZero union, ClipperUtils.cpp:418/360/273); both covered fns gate-dispatch. GATES: default 147987/7adae05c, F1 5-gate 62f65c22, both re-verified; instrumentation stripped both trees. RESIDUAL: L196 mono-band still 75-vs-4 gaps_ex (native's 4 → 0 medial polylines — its band effectively vanishes; native's 6 gap lines there come from the PERIMETER gap fill, not this tail) — with endpoints ≤1.6µm and coverage faithful, the remaining suspect is the mono_no_overlap INPUT extent itself (rust band wider than native's) — next: NOOVDBG at z=39.4.

- **R144 — L196 STRIP ROOT CHARACTERIZED (measurement; instrumentation stripped, default 147987 re-verified): the mono-band gap fragments are ZIGZAG END-MARGINS at the raster line ENDS, not between-line gaps — the SAME R135 endpoint-clipping residual that keeps Top from closing. One residual, two symptoms.** Point-level geometry (covered rects vs unextruded polys, raw coordinates): adjacent coverage rectangles OVERLAP correctly along their long edges (c-offset −141 units — coverage 377.28µm ≥ pitch 377.07 tiles as designed), but the unextruded pieces connect consecutive rect END-CORNERS — the diagonal stair margins between the butt caps and the no_overlap boundary, where rust's raster lines stop short of native's (25-47µm, R135) so the margins survive `opening_ex(min/2)` and fragment into ~20 pieces where native keeps 4. CONSOLIDATION: the remaining flip cost (+57) and the Top residual now reduce to ONE target — the raster clip-contour/endpoint fidelity (the ±1pt fill-surface polygon residual feeding ExPolygonWithOffset). MEASUREMENT-CONFIG DECISION: the 6-gate configuration (F1_UNION+FRAME_UNIFY+SLICE_SIMPLIFY+SLICE_CENTER+REGION_EXPANSION_FAITHFUL+TOPFILL_FAITHFUL, content-multiset **141711**, sha 4d80b6ff) is now the PRIMARY measurement path — cost-neutral vs 5-gate (141654) and structurally far more faithful (Top/Sparse/ISI geometry + material −0.009%); fixes converge against the faithful chain from here.

- **★★ R143 — INSET_OVERLAP_TOLERANCE 0.45→0.4 (gated F1_UNION; default 147987/7adae05c re-verified): F1 base 142594→**141654** (−940, the biggest single-constant win since R118), flip 141711 (+57 over new base). MATERIAL: 3858.63 vs native 3858.97 (−0.009%, closest ever).** Found via the R142 medial-axis stage-split: native's dumped `min=4524.8` implied `(1−x)=0.6` ⟹ native `INSET_OVERLAP_TOLERANCE = 0.4` (libslic3r.h:72) while BOTH rust gap tails (layer.rs perimeter gap @573 + mono-WGapFill tail @2424) hardcoded 0.45 — a too-small `min` kept sub-threshold slivers alive through `opening_ex(gaps, min/2)`, fragmenting the gap band model-wide (the L42/L53/L93… gap churn). perimeter_generator.rs already had 0.4 (the bug was only in the layer.rs ports). Both sites gated (F1_UNION → 0.4; default keeps legacy 0.45 — an UNGATED first attempt broke the default lock to 147714/e9e6bc21, caught by the mandatory default check and immediately gated). **NEW F1 REFERENCE: sha 62f65c22, content-multiset 141654** (guardrail baselines updated). Flip cost now +57, concentrated in the mono-band medial fragmentation (L196 79-vs-6 — the band slivers are ~3.7µm inter-line strips that survive even min=0.4's 22.6µm opening; separate root, next probe: why rust's unextruded band has inter-line strips at all where native's coverage tiles them away).

- **R142 — GAP-TAIL MIXED-GRID GUARDS + `PORTING_PATTERNS.md` (TOPFILL_FAITHFUL; F1 08ec4445 + default 147987 verified): flip 142662→142639 (45 above base). Gap churn LOCALIZED to the MEDIAL-AXIS EMITTER, not the band.** Per-layer flip-vs-base gap analysis concentrated on top layers (L196: native 6 lines / base 5 / flip 71). Two mixed-grid guards landed (covered-footprint union → union_ex_clib; gapfill intersection → intersection_clib — the R74 class: full-res no_overlap diffed against gridded footprint slivers). DECISIVE MEASUREMENT: the unextruded band EXISTS in native too (L196 native extrudes 7.84mm of gap in 4 LONG medial runs; rust extrudes 4.69mm in ~12 SHORT fragments, median 0.176mm) — with dont_adjust the nominal-spacing raster leaves the band by design and native covers it with continuous medial-axis polylines. So the remaining flip cost is the **medial-axis emitter fidelity** (fragmentation + min-width filtering drops ~3mm), NOT band computation — the R135 "rust mono gap-fill doesn't cover the band the same way" observation, now precisely characterized. ALSO: adopted the Bun-rewrite (bun.com/blog/bun-in-rust) PORTING.md practice — new `PORTING_PATTERNS.md` documents all 6 fidelity classes (gridding, safety-offset, scale_/cast quartet, DP, op-structure, emitter clusters) as mechanical mappings + grep-able anti-patterns, so the remaining work can run as a class-based work queue and new code can't reintroduce solved classes. NEXT: medial-axis emitter stage-split (native ExPolygon::medial_axis + ThickPolyline joining vs rust) on L196.

- **R141 — TOP-BAND CONVERGED (TOPFILL_FAITHFUL; F1 08ec4445 + default 147987 + flip-determinism 295d5e27 all verified): flip 143213→142728→142662 — 68 above base.** Three fixes from the L23 stage-split: (1) `top_fills` union + the :1168 `fill_clip` offset in only_one_wall_top were geo → union_ex_clib/offset_expolygons_clib (fill_clip 177→168pts = native-exact count); (2) the layer-level GAP-FOOTPRINT trim (native PG.cpp:1373 diffs `last` BEFORE the fill-boundary block; rust diffs the FINAL fill region in layer.rs) was cutting 0.126mm² out of the top band that native never trims — the grown band is now threaded through `PerimeterResult::top_band` and RESTORED after the trim (union_ex_clib), plus the trim itself runs difference_clib; (3) BUG: the per-surface→outer result merge at perimeter_generator.rs:371 never propagated `top_band` (silent empty — the restore was a no-op until propagated). L23 ORACLE NOW: main fill_expoly 768.527900 vs native 768.527540 (Δ4e-4), band 7.607154 vs 7.607555 (Δ4e-4, was Δ0.126), top_infill_exp 6.543269 vs 6.543629. The fill-boundary chain is CONVERGED at the area level; residual = the ±1pt near-collinear class + the last 68 flip-points (remaining churn on other features). NEXT: flip is nearly at parity with base — hunt the remaining 68 (likely bottom/ISI angle churn) then evaluate flipping TOPFILL_FAITHFUL into the measurement gate set.

- **R140 — PERIMETER-TAIL FAITHFUL simplify_p (TOPFILL_FAITHFUL; F1 08ec4445 + default 147987 re-verified): flip 143655→143213 (−442; trend 144712→144328→143655→143213, 619 above base).** Param dump (PARMDBG dual, reverted) EXONERATED the fill-boundary parameters (inset/ipo/minpis/eps/ps/sis all within ~1 scaled unit — the R101/R109 truncation classes, NOT the 6µm driver). The real input delta: the perimeter-tail `not_filled_exp = union(simplify_p(last))` used the R122-class UNSCALED-tolerance rounded-projection DP (near no-op simplify — L23 fill_expoly 29pts vs native 24); gated faithful `simplify_p_dp_rings_faithful(tol/0.00001)` per native `ex.simplify_p(m_scaled_resolution, &pp)` (PerimeterGenerator.cpp:1397). L23 first fill_expoly 768.4048→768.4574 (native 768.5275 — closer); the top-band expoly still 29pts/7.4822 vs native 24/7.6076 (Δ0.125mm²) — the residual lives in the `top_fills`/`fill_clip` merge (`infill_exp = union(infill_exp, offset_ex(top_infill_exp, ipo))`), i.e. the only_one_wall_top block's top_fills product — the NEXT stage-split. Also confirmed native classic-path formula matches rust's (PerimeterGenerator.cpp:1464 is the ARACHNE variant — not benchy's path).

- **R139 — GROUP_FILLS POST-LOOP UN-GRID LANDED (TOPFILL_FAITHFUL; measured-paths inert, F1 08ec4445 + default 147987 re-verified): the R138 stage-walk pinned the hole's entry to Fill.cpp:361-373 (geo union_safety_offset_ex + diff cut a spurious hole into stTop); new `cz_union_ex_safety` shim (subject raw +10u safety offset then the two-pass NonZero union) + `union_safety_offset_ex_clib` wrapper + difference_clib_safety at the diff — flip trend: 144712→144328 (R137)→143655 (−673 this round).** STAGE-WALK RESULTS (dual TSDBG/TSDBG2, all reverted): detect_surfaces_type EXONERATED — in_slice/in_upper/post_diff/post_open ALL byte-identical at count/area (168/1, 169/1, 48/1, 16/0 1.3438mm²); the L23 top then goes native 10/0/0.6731 vs rust 10/0/0.6710 at the fill-expolygon CLIP (slices_to_fill_surfaces_clipped — Δarea 0.3% from fill_expolygons, i.e. the perimeter infill_area, still not byte-equal), stays topology-equal through vertical shells + process_external (native 42/0/6.5336 vs rust 43/0/6.5343), and the HOLE entered at group_fills' post-loop — now fixed. REMAINING (why the flip is still net-above base 142594): (a) fill_expolygons ~0.3% area delta at the clip stage (perimeter infill_area chain — not byte-equal even un-gridded, needs its own stage-split); (b) L23 raster still 12-vs-11/0-exact (endpoint contour + the extra line inherit (a)); (c) the +90 angle churn on solids whose endpoints can't close yet. The flip is CONVERGING round-over-round; each fixed stage moves it down. NEXT: stage-split the fill_expolygons delta (perimeter infill_area vs native fill_expolygons — dual dump at L23).

- **R138 — stTop SURFACE STAGE-SPLIT (measurement, dual TOPDBG, instrumentation reverted both trees): the top-fill oracle can't close because the stTop SURFACES themselves differ in TOPOLOGY — L23 native 49pts/0 holes vs rust 46pts/1 hole (same area 6.53mm²), and 0/146 stTop surfaces byte-agree globally (npts+nholes+area+first-point key). The spurious rust hole explains the 12-vs-11 raster segments.** The divergence is one stage up from everything R134-R137 fixed: in `detect_surfaces_type`'s stTop construction — `opening_ex(diff_ex(slices, upper_lslices, ::Yes), width/10)` — even with ::Yes honored and full-res clib ops (TOPFILL_FAITHFUL was ON for the dump). Candidates for the hole+shape diff: (a) the diff INPUTS (layerm.slices.surfaces / upper lslices — R121 proved slicesets byte-exact per island on the PERIMETER chain but the region-slices copy may differ), (b) the opening offset VALUE (rust `width()/10` mm-f64 vs native `scaled_width()/10.f` — the R101 f32-trunc class), (c) opening/diff op-order semantics. NEXT (R139): dump the diff_ex inputs + intermediate (post-diff, pre-opening) at L23 both engines — find which stage introduces the hole; then the R113 walk-up applies. NOTE the whole R134-R138 arc is preserved behind TOPFILL_FAITHFUL; once the surface topology closes, the entire top-fill chain (raster mechanics already proven to 1µm/byte-exact starts) converges with it.

- **R137 — CLASSIFICATION PIPELINE UN-GRID LANDED behind `TOPFILL_FAITHFUL` (inert on F1 [08ec4445] + default [147987/7adae05c], both re-verified): detect_surfaces_type + discover_vertical_shells + the shared surface-op wrappers now gate-dispatch to the vertex-exact vendored ClipperLib. Full-flip measured; the L23 top-fill oracle STILL doesn't close — the Top surfaces genuinely differ beyond gridding; NEXT = L23 stTop surface stage-split.** DIRECT team-lead work. IMPLEMENTED: (1) 7 shared wrappers in clipper_utils.rs (opening_ex → offset2_ex_clib(−d,+d); diff_ex / diff_ex_surfaces_expolygons / diff_ex_polygons_surfaces → difference_clib[_safety] — the geo path IGNORED ApplySafetyOffset, native ::Yes raw-offsets the clip +10u, R116 shim now honored; intersection_ex / intersection_surfaces / intersection_surfaces_expolygons → intersection_clib) — gate-dispatch inside the wrappers so every classification call site flips at once; (2) discover_vertical_shells: gated SHADOW fns (grow/shrink/closing/offset2/union_ex/intersection/difference) covering its ~20 direct primitive calls; (3) the last direct difference in detect_surfaces_type. FULL-FLIP RESULT (6 gates): content-multiset 144328 (vs R133 base 142594; pre-R137 flip was 144434) — Top −298 (16605→16307), ISI −132, but Gap +680 / Floating +394 / Bridge +938 churn as every classification boundary reshapes; L23 top raster moved to a different region (surfaces reshaped) and STILL 0 byte-exact segments, 12-vs-11. VERDICT: the remaining Top divergence is NOT the R100 gridding class — the stTop surface polygons differ from native's for a real geometric reason (candidate: opening_ex miter/collinear semantics, the expand/detect interplay, or upstream). NEXT (R138): dual-engine dump of the stTop SURFACE expolygons at group_fills time on L23 — byte-compare; surfaces match ⟹ residual is raster/order (close); surfaces differ ⟹ walk detect_surfaces_type stage-by-stage (the R113 stage-split method on the classification chain).

- **R136 — FAITHFUL TOP-FILL CLUSTER IMPLEMENTED + LANDED behind `TOPFILL_FAITHFUL` (separate gate; INERT on the F1 measurement path and the default — both verified: gated sha 08ec4445 = R133, default 147987/7adae05c): the complete native FillMonotonicLineWGapFill semantics are now in-tree; byte-convergence blocked ONLY by the still-gridded SURFACE-CLASSIFICATION pipeline.** DIRECT team-lead work. IMPLEMENTED (all faithful to native, one coherent cluster): (1) fill-angle parity alternation `(idx&1)·90°` + the unconditional `_infill_direction` +90 (native FillBase.hpp:207/cpp:239 — NOTE: the old `90·idx` form coincides only mod 180; the raster frame needs mod-360 equality since a 180-rotated frame point-reflects the bbox and shifts x0 on every adjust-branch solid); (2) `InfillConfig.dont_adjust` threaded through both rectilinear generators; MonotonicLine sets it (native FillRectilinear.cpp:3247); (3) the align_to_grid raster branch (refpt=(0,0), origin-centered object bbox); (4) MonotonicLine rasters over `intersection_clib(no_overlap, expoly)` (native :3253); (5) un-gridded the ENTIRE upstream chain: perimeter_generator infill-area block (not_filled_exp union → union_ex_clib; offset2→offset2_ex_clib; top_infill intersection/offsets→clib) + no_overlap block (offset2/offset/union→clib) + ExPolygonWithOffset raster clip offsets (outer offset_expolygons_clib; inner union_ex_clib+shrink_clib — flat-polygon wrap panics on holes, nested reconstruction required). ORACLE RESULT (L23 top): raster normal-positions exact-nominal-spacing, START endpoints byte-exact (−25.731,1.759 ≡ native), Top-surface E CONVERGED (163.51 vs native 163.61, was 169.66) — but far endpoints stay exactly 1µm short and 12-vs-11 segments: the binding clip contour inherits the 1µm grid of the FILL-SURFACE polygons themselves (detect_surfaces_type / process_external / discover_vertical_shells classification chain — the LAST remaining geo domain). Cluster measured on F1: net +1840-2100 churn (angle moves every solid fill; endpoints can't close) → NOT enabled on F1; landed complete behind TOPFILL_FAITHFUL for when the surface pipeline is full-res. **NEXT (R137+): un-grid the surface-classification pipeline (the true remaining F1 conversion — detect_surfaces_type/process_external/discover_vertical_shells geo ops → clib, gated), then flip TOPFILL_FAITHFUL into the measurement gate set; Top (16.6k) + ISI-angle share converge then.** Native NOOVDBG/ADJDBG instrumentation applied + REVERTED (rebuilt clean).

- **R135 — TOP-FILL RASTER MECHANISM PROVEN, FULL FIX BLOCKED ON FAITHFUL WGapFill (implemented, measured, REVERTED — tree back to R133/08ec4445, both trees clean).** DIRECT team-lead work, ground-truth via dual-engine ADJDBG (native rebuild + revert). ROOT CHAIN CONFIRMED (3 pieces beyond R134's angle): (1) native `FillMonotonicLineWGapFill::fill_surface_extrusion` (FillRectilinear.cpp:3245-3253) sets **`params2.dont_adjust = true`** ("BBS: always don't adjust the spacing of top surface infill") — native top MonotonicLine keeps NOMINAL flow spacing 37707 (native ran _adjust_solid_spacing only 49× vs rust 208×; rust adjusted top to 38260); (2) with dont_adjust the raster takes the **align_to_grid branch** (FillRectilinear.cpp:2865-2875) anchored at `Fill::bounding_box.center()` — and `PrintObject::bounding_box()` is ORIGIN-CENTERED (Point(−size/2)..(size/2), PrintObject.hpp:395) so **refpt == (0,0) EXACTLY** (no bbox threading needed — align bbox.min down to global spacing multiples); (3) native rasters over **`no_overlap_expolygons`** (FillRectilinear.cpp:3255-3258), NOT the surface expolygon — line ENDPOINTS clip at the no-overlap contour, and the WGapFill medial-axis pass covers the unextruded band. MEASURED with (1)+(2)+R134's +90 gated: raster normal-positions converge to **<1µm of native** (−19.2135 vs −19.2128 on L23; spacing exact at nominal) — mechanism PROVEN — but endpoints stay ~25-47µm long (clipped at the wrong contour) + 1 extra line, so no byte gain, and the angle churn on unmatched solids nets +822 → REVERTED. Adding (3) naively LOSES 13mm material (3846.00 vs 3858.97): rust's mono gap-fill does NOT cover the perimeter-overlap band that shrinking the raster area exposes — **the blocker is a faithful FillMonotonicLineWGapFill port: raster over no_overlap + medial-axis gap-fill of `unextruded = surface − covered(lines)` (FillRectilinear.cpp:3260-3320), landed TOGETHER with dont_adjust+align+90 as one gated cluster.** All instrumentation reverted both trees (native rebuilt clean). This is the mapped R136 — the entire top-fill (16.6k) + ISI-angle share converges through it or not at all.

- **R134 — TOP-FILL DIVERGENCE FULLY LOCALIZED (measurement round; fix attempted, measured, REVERTED — 3-layer root, angle alone regresses): the Top-surface 0.0%-XY-match mystery = fill-angle 90° flip + raster spacing delta + duplicate line.** DIRECT team-lead work. (1) ★ ANGLE: rust top/bottom/solid (Monotonic/Rectilinear) fill runs at native+90° on EVERY layer (native L0=45°/L23=135°; rust L0=135°/L23=45°; ISI deltas ~89° = same root) — the live angle formula (`config.angle + 90·layer_index`, fill_rectilinear.rs:2236/4350) is missing native `Fill::_infill_direction`'s UNCONDITIONAL `out_angle += M_PI/2` (FillBase.cpp:239). The crate HAS a faithful `infill_direction` (fill/mod.rs:1297) with the +90 — but it's DEAD CODE, never called. Grid/sparse is 90°-symmetric (two perpendicular sweeps) so the bug is invisible there (masked since R77). layer_id itself verified 0-based/correct (ANGDBG). (2) TESTED gated +90: angles match after (135°/135° L23, equal seg counts) but content-multiset REGRESSES 142594→142876 (+282) and material −1.79mm because the lines are still PHASE-shifted: (3) beneath the angle, on angle-matched L23 the raster-normal projection shows native adjusted line-spacing 0.3770 vs rust 0.3826 (~1.5% — `adjust_solid_spacing` sees a different rotated-bbox width or rounds differently) AND rust emits a DUPLICATE line (two at normal-pos −17.016, Δ=0.3µm). So top-fill convergence needs the coupled triple: +90 angle (proven) + adjust_solid_spacing fidelity + duplicate-line fix — an R113-R120-style coupled cluster in the rectilinear raster; angle alone flips every solid surface into a DIFFERENT wrong place. REVERTED to R133 (gated sha 08ec4445 verified). NEXT (R135, the mapped attack): dump native/rust `adjust_solid_spacing` inputs (rotated bbox width, nominal spacing) on L23 + find the duplicate-line source (n_vlines off-by-one? seam between two expolys?), then land angle+spacing+dup TOGETHER with a staged L23-oracle.

- **R133 — FAITHFUL RegionExpansion extended to `propagate_waves_ex` (bridge-anchor path) @HEAD: R132 only wired the main `propagate_waves`; the bridge-expansion path was still geo. Wired it to the same faithful seeds + cz_propagate_wave — content-multiset 142748→142594 (−154).** DIRECT team-lead work. Gated branch in `propagate_waves_ex` (REGION_EXPANSION_FAITHFUL → `wave_seeds_faithful` anchors + `propagate_waves_faithful` raw expansions; default keeps geo). rust lines 146662→146508 (toward native). GATES: default byte-IDENTICAL 147987 (sha 7adae05c); faithful deterministic 2× (sha 08ec4445); R65 post-slice-safe. Combined R131+R132+R133 faithful-RegionExpansion = content-multiset −324 (142918→142594). NOTE (probed + reverted this session): the ISI/Floating classification SPLIT itself is NOT a single-op gridding flip — routing the narrow-strip floating-vs-concentric intersection test (Fill.cpp:479, `intersection_ex_expolygons_polygons`) through clib was INERT (+2, counts unchanged). The main surface classification already runs the faithful wave (via expand_merge_surfaces→propagate_waves), so the residual ISI/Floating (~33k) is DOWNSTREAM in the fill-emitter/fragmentation (the deep R69-R77 subsystem), confirmed distributed not bounded. Remaining: fill-emitter subsystem + Outer-wall seam/arc geometry floor (40k, R123/R127-confirmed, not tractable) + Gap inherited-floor.

- **★★ R132 — FAITHFUL RegionExpansion WIRED (gated `REGION_EXPANSION_FAITHFUL`) @HEAD: byte-faithful Clipper2-Z `wave_seeds` + byte-faithful ClipperLib `cz_propagate_wave` (R131) together move the ISI/Floating classification toward native — content-multiset 142918→142748 (−170). The multi-increment build (R131 propagation + R132 seeds) landed; the "classification split needs unwritten foundational work" ceiling is BROKEN.** DIRECT team-lead work. Brought the `clipper2-z-sys` crate (vendored Clipper2 v1.5.4 USINGZ, `cz2_offset_z`/`cz2_intersect_open_z`) + the `clipper2_z.rs` safe wrapper (both from the wave-seeds branch, compile clean here), added as a libslic3r-rs dep. Ported `wave_seeds_faithful` + Z-path builders (`expolygons_to_zpaths64`[_expanded_opened], `merge_splits`, `polylines_merge_z`) VERBATIM from RegionExpansion.cpp:108-389 (the seed generation is byte-faithful — Z-callback intersection provenance + Z-preserving open offset). NEW: `propagate_waves_faithful` groups the faithful seeds by (boundary,src) and drives the R131 `cz_propagate_wave` ClipperLib shim — this is STRICTLY BETTER than the wave-seeds branch, whose own propagation was a geo approximation that over-expanded (+81); here BOTH seeds and propagation are byte-faithful. Gated in `propagate_waves` (REGION_EXPANSION_FAITHFUL → faithful; default keeps the geo polygon-approximation). RESULT (5-gate path = the 4 standard + REGION_EXPANSION_FAITHFUL): content-multiset **142918→142748 (−170)** (rust-unmatched −166, native −4, both converge); rust lines 146824→146662 (toward native 139498). GATES: **default byte-IDENTICAL 147987 (sha 7adae05c — new gate off by default)**; faithful path DETERMINISTIC 2× (sha d0719207); material 3859.02→3859.08 (+0.06mm, native 3858.97 = +0.003%, exposure — faithful geometry); R65 post-slice-safe. NOTE: this needed BOTH increments — a faithful propagation on approximate seeds (or vice-versa) shows no byte signal; only the full faithful path converges (as R72-R73 predicted the partial paths wouldn't). RESIDUAL: the classification is now faithful in the wave, but the ISI/Floating split still has ~33k unmatched — the remaining divergence is downstream (surface merge / fill-emitter on the now-faithful surfaces) + the seam/arc geometry floor. The `scale_`-posture + fill-emitter are the next probes.

- **R131 — WAVEFRONT-PROPAGATION SHIM `cz_propagate_wave` LANDED (unwired infrastructure) @HEAD: first increment of the byte-faithful RegionExpansion port (the blocker for the ISI/Floating classification split, ~33k unmatched — the biggest remaining chunk).** DIRECT team-lead work. Investigation resolved the structure of native `RegionExpansion.cpp`: `wave_seeds` (:278) uses **Clipper2-Z** (Clipper2Lib_Z, Z-callback intersection) but the wavefront **propagation** (`propagate_waves`/`propagate_wave_from_boundary`/`wavefront_initial|step|clip`, :391-490) is plain **ClipperLib** (Clipper1) `ClipperOffset` @1e5 — the SAME library `clipper-z-sys` already compiles. So the propagation can be made byte-faithful with existing infra; only the seeds need Clipper2-Z. FIX (clipper-z-sys shim, +~90 C++ / +FFI decl / +Rust extern): `cz_propagate_wave` ports `wavefront_initial` (jtRound, etOpenRound/etClosedLine open offset), `wavefront_step` (closed offset, orientation-aware sign + reverse), `wavefront_clip` (ctIntersection, pftPositive), and the initial→clip→(N× step→clip) loop VERBATIM — byte-identical to native by construction. The boundary bbox-trim is a result-neutral optimization (wavefront ⊆ inflated seed bbox ⟹ `wf∩(bnd∩bbox) ≡ wf∩bnd`) and is omitted. Builds clean (clipper-z-sys + full workspace link); UNWIRED (no Rust caller yet) so default+gated byte-UNCHANGED. **REMAINING for the faithful classification (next increments): (1) Clipper2-Z seeds — re-add clipper2-z-sys crate + port faithful `wave_seeds` (exists on wave-seeds branch, ~320 lines); (2) Rust wrapper for cz_propagate_wave; (3) wire `propagate_waves` gated (faithful seeds → cz_propagate_wave) replacing the geo `wave_seeds_polygon_based`/`propagate_wave_from_seeds` approximation; (4) A/B. Only when seeds+propagation are BOTH faithful does the byte signal appear (faithful propagation on approximate seeds ≠ byte-faithful) — this is a genuine multi-increment build, no partial byte-signal.**

- **R130 — zone-trim `difference` → difference_clib LANDED (gated F1_UNION) @HEAD: content-multiset 142927→142918 (−9, marginal but faithful + resolves the R74 caveat).** The expand_merge_surfaces zone-trim `diff_ex(zone, expanded)` (native ClipperLib @1e5) was geo-clipper @1µm; R74 had measured difference_clib WORSE, but that was with a GEO closing — retested now that R128 makes `expanded` the full-res clib closing output, and it's net-positive (the R74 "worse" was the mixed-grid artifact). Faithful port (native uses ClipperLib here). Default byte-IDENTICAL 147987 (sha 7adae05c); gated deterministic (sha 29a15d46). Combined R128+R129+R130 = content-multiset 143538→142918 (−620 this direct-work session). **The closing-class fill sub-levers are now tapped; the remaining fill-classification residual needs the wave-propagation un-grid = the clipper2-z-sys / faithful wave_seeds port (R72-R73 infra, NOT on this branch) = the genuine multi-session F1 piece.**

- **R129 — BRIDGE-EXPANSION CLOSING un-grid + Miter LANDED (gated F1_UNION) @HEAD: the 2nd instance of the R128 class — region_expansion.rs:1647 bridge closing (native LayerRegion.cpp:375 `closing_ex(acc, closing_radius)`, Miter@1e5) was geo-clipper @1µm + Round. content-multiset 143040→142927 (−113); combined R128+R129 = 143538→142927 (−611).** Same +7/−1 gated fix: `offset2_ex_clib(&unioned, +r, −r, Miter)` under F1_UNION, default keeps geo Round. rust lines 146950→146833 (toward native). GATES: default byte-IDENTICAL 147987 (sha 7adae05c); gated deterministic 2× (sha 73e57991); R65 post-slice-safe. (The geo `union_ex` before the closing @:1646 still grids — a further sub-lever; native `closing_ex(Polygons)` unions during the offset2 grow, so the faithful form would offset2 the raw acc directly. Left minimal to match R128.)

- **★★ R128 — FILL-CLASSIFICATION CLOSING un-grid + Miter LANDED (gated F1_UNION) @HEAD: the process_external_surfaces expand_merge closing was geo-clipper @1µm + ROUND; native = closing_ex = offset2_ex(+r,−r, jtMiter) via ClipperLib @1e5. First byte-parity lever in the "R79/F1 fill subsystem" the bank packet had parked — content-multiset 143538→143040 (−498 PRIMARY), reopening the subsystem as having bounded sub-levers.** Direct team-lead work (no subagent). LOCALIZED: region_expansion.rs `expand_merge_surfaces` (native LayerRegion.cpp:503 `expanded = closing_ex(expanded, closing_radius)`) called the geo-clipper `closing(...OffsetJoinType::Round)` — the R71-R74 fragmentation source (A/B on the old branch: Round+geo n_solid 1911 vs Miter+clib 707, native 605). R74 had measured this MATERIAL-neutral and banked it, but never tested BYTE-parity. FIX (+13/−1, F1_UNION): route the closing through `offset2_ex_clib(&expanded, +r, −r, OffsetJoinType::Miter)` (vertex-exact ClipperLib @1e5, Miter per native DefaultJoinType); default keeps geo Round (byte-locked). The zone-trim `difference` @:637 left as geo (R74: difference_clib made n_solid WORSE — not this lever). RESULT: PRIMARY content-multiset-unmatched **143538→143040 (−498)** (rust-unmatched −517, native +19 = strong net convergence); LCS differing-lines 147020→146430 (−590); rust total lines 147486→146950 (toward native 139498); filament 3859.30→3859.20 (toward native 3858.97). GATES: **default byte-IDENTICAL 147987 (sha 7adae05c, verified ungated compare)**; gated DETERMINISTIC 2× (sha 1f532c44); material toward native (filament −0.10mm); R65 untouched (post-slice fill classification). NOTE: feature-COUNTS (ISI 248→244, Floating 174→175) barely moved / slightly "worse" — but those are the segmentation artifact R107 flagged; content-multiset (the truth metric) converged −498. RESIDUAL: the wave-propagation path (propagate_waves/wave_seeds, RegionExpansion.cpp) is still geo-clipper @1µm (intrinsically tied to the Clipper2-Z open-path offset infra from R72-R73, NOT on this branch) → the `expanded` input to the closing is still gridded, so this is a partial un-grid; the full fill-classification convergence needs the wave-seeds/clipper2-z-sys port (the genuine multi-session F1 piece). But the closing was a real bounded sub-lever — the "fill subsystem = monolithic floor" framing is now WRONG (like arc R123 inside "R79").

- **★★★ SESSION WRAP / BANK PACKET (R108→R127, 2026-07-03…06) — GEOMETRY BYTE-PARITY ESSENTIALLY COMPLETE; bounded-lever runway EXHAUSTED; recommended BANK pending user direction on 3 big-decision levers.** Gated diff (content-multiset-unmatched, the PRIMARY metric adopted R119) 243105-era → **143538**; default path byte-LOCKED at 147987 throughout (every fix gated F1_UNION, verified stash-diff each round). **LANDED (11 fixes):** R108 gap-detection un-grid (−22%), R111 island-order faithful chain (−7%), R116 diff_ex_safety shim, R117 scale_faithful helper, R118 faithful bridge, R119 faithful ctIntersection, R120 top chain, R122 faithful simplify_p (DP byte-exact 578/578 islands), R124 arc-center lrint-round, R125 medial-axis lrint-round. **THE ONLY_ONE_WALL_TOP BLOCK is now fully faithful** (bridge+intersection+top-chain coupled port, R113→R120). **FLOORS REFUTED 8× — the R107 "30-40% deep geometry floor" was WRONG:** it was part measurement artifact (R121 island-mispairing) and part bounded bugs (offset op exonerated R112; arc was a ±1 rounding bug not a floor R123/R124). Geometry is byte-exact per island on 76%+ of remaining divergers. **BOUNDED-LEVER RUNWAY EXHAUSTED (4 clean re-probes):** arc→bounded/fixed (R123/R124), medial-axis→fixed (R125), reverse-cast class→inert (R126), seam→genuine FLOOR (R127, R110 strengthened to 100% on byte-exact loops). Material held at +0.268% (WATCH, never breached). Determinism (R99) held on both paths every round. **REMAINING = 3 BIG-DECISION LEVERS, all requiring USER direction (I did not commit autonomously):** (1) **crate-wide `scale_` posture** (rust `scale()` ROUNDS vs native `scale_()` TRUNCATES, point.rs:53-57) — HIGH blast-radius (touches byte-locked default everywhere) AND R127 measured its seam instance as **parity-NEUTRAL on benchy** (fires but converges nothing), so likely low byte-payoff; (2) **R79 toolpath subsystem** — seam now CONFIRMED floor, arc was bounded (fixed), order was R111-fixed → what remains is fill-generation classification/chaining (ISI/Floating/Sparse split, ~27% diff share), which is F1-geo-clipper foundational territory (R69-R77), a genuine multi-session effort; (3) **L0 base-slice topology** (1 layer, R106/R109-hard, negligible payoff). **RECOMMENDATION: BANK** — every bounded/clean lever is landed or refuted; the remainder is risky-systemic, multi-session-foundational, or 1-layer-negligible. Branch `alex/frame-unify` @d7311c2 pushed, default byte-locked, both trees clean, R65 intact. All state in this file + memory `project_byteparity_admesh.md`.

- **★★ R127 — SEAM RE-PROBE on the NOW-byte-exact loop set (the seam analog of R123's arc re-probe): VERDICT (a) FLOOR-CONFIRMED — byte-identical geometry ⟹ byte-identical seam; NO bounded seam bug (measurement-only, no code, tree unchanged @d0db4b2). Unlike R123 (which overturned the arc exoneration and found a ±1 center bug), the seam re-probe on the STRONGER dataset UPHOLDS R107/R110: seam is genuinely geometry-inherited/self-converged, a real floor component, NOT a lever. Bank-supporting.** Method: three triangulated gcode analyses on the committed gated dump (F1_UNION, the most byte-exact set), pairing outer loops by SEAM-INVARIANT geometry keys (centroid+area+perimeter; and point-set overlap ≥90%), then comparing the emitted seam (= travel-dest / first extrusion point; native carries it in pre-FEATURE-tag + WIPE, rust emits an explicit travel — parser handles both). RESULTS: (1) on **100%-byte-identical outer loops, seam MATCH = 12/12 = 100%** (vs R110's 90.3% on the weaker centroid-matched set) — byte-identical geometry always yields byte-identical seam; (2) **ZERO adjacent-vertex (2µm-2mm) divergences** — a seam-comparator tie-break bug (< vs <=) would flip seams to a NEARBY candidate on MANY loops; none exist, ruling out that class; (3) the ONLY seam divergences are **3 FAR (>2mm, opposite-side ~12mm) — all the SAME hull loop across 3 consecutive layers z=8.8/9.0/9.2**, on PRE-SEAM byte-identical geometry (24 shared vertices; the single non-shared point in each is the seam-SPLIT artifact, i.e. downstream of the seam choice, NOT a pre-seam geometry difference). That signature (opposite-side + exactly one feature + 3 contiguous layers + no adjacent-tie divergences anywhere) is a **cross-layer ALIGNMENT CASCADE** in spAligned `align_seam_points`/`find_seam_string` (the alignment string resolved to the opposite side in rust vs native for this one feature-string), NOT a per-loop rounding/tie-break — consistent with R110's inheritance (corr 0.98). Non-bounded (lives in the cross-layer alignment optimization) and negligible payoff (**3 of 772 outer loops = 0.4%**). **SEAM-POINT COORDINATE CONSTRUCTION probed per team-lead (seam_placer.rs:2884 `crate::scale` = ×1e5 + ROUND vs native `Point::new_scale`/`scale_` = ÷0.00001 + TRUNCATE, SeamPlacer.cpp:1496): applied the fully-faithful native `(x/0.00001) as i64` (gated F1_UNION) + built + measured → it DOES FIRE (gated sha b3757465→a1e84bd1, 82 lines / ~41 seam points shift ±0.001mm) but is **PARITY-NEUTRAL** (PRIMARY 143538 / SECONDARY 162460 EXACTLY unchanged) — the shifted seams are on loops that diverge from native for other reasons, so matching this one construction site moves nothing toward native. This is the crate-wide `scale_` posture manifesting (fires but doesn't converge); REVERTED, NOT landed (fails the acceptance bar + is the systemic posture held for the user, not a per-site seam bug).** GATES/HYGIENE: seam probe rust-only + gated (default 147987 never at risk), reverted; native trees READ-ONLY this round (BambuStudio2 + the actual compare-native `references/BambuStudio` both untouched); both trees clean, rebuilt to committed gated sha `b3757465`. **VERDICT: seam is a FLOOR, R110 STRENGTHENED on the byte-exact set — no bounded seam lever exists (the seam analog of R123 gives the OPPOSITE result: seam converges, arc had a bug). The bounded-lever runway (R124 arc + R125 medial-axis forward, R126 reverse, R127 seam) is now exhausted. Remaining are the big-decision items only: the crate-wide `scale_` posture (systemic, user-held), L0 base-slice topology (R106/R109, 1 layer), and the PARKED R79 toolpath subsystem. This is the clean continue-vs-bank point.**

- **★ R126 — REVERSE-CLASS SWEEP (rust ROUNDS/over-truncates where native TRUNCATES/keeps-fractional): BOTH flagged live-path candidates verified FIRE-then-WASH-OUT, NEITHER landed (measurement-only, no code — tree unchanged @bb3a271). The reverse class produces ZERO byte-relevant payoff on benchy; the R124/R125 forward levers were the whole realizable lrint story.** Mirror of R124/R125. Candidate #1 `print_object.rs:1278` (internal-bridge angle, `determine_bridging_angle` in `bridge_over_infill`): native `Point a = (start + v*(i*step_size)).cast<coord_t>()` (PrintObject.cpp:2515) = Eigen static_cast TRUNCATE; rust `.round()` — a reverse ±1 for every fractional sample. APPLIED (gated F1_UNION, truncate) + built + measured → gated **sha UNCHANGED** (`b3757465`), PRIMARY 143538 / SECONDARY 162460 FLAT. Fire-probe (BRIDGEDBG counter): the sample loop barely executes on benchy (few internal-bridge regions) and the sampled `a` point feeds a QUANTIZED nearest-anchor-line + histogram-vote for the bridge angle — robust to per-sample ±1, so the winning angle is unchanged → byte-identical. INERT. Candidate #2 `fill_base.rs:887` (grid `connect_infill`, `mark_boundary_segments_touching_infill`): native keeps `Vec2d vperp = perp(v)` FRACTIONAL and truncates only the endpoints a/b at the end via `.cast<coord_t>()` (FillBase.cpp:1132-1143); rust truncated `v`→`Point` BEFORE the perp, dropping vperp's fractional part (over-truncation). APPLIED (gated, fractional `Vec2d::new(-vn.y, vn.x)`) + built + measured → gated **sha UNCHANGED** (`b3757465`), PRIMARY/SECONDARY FLAT. Fire-probe (VPERPDBG counter): it FIRES **1116×** and the fractional-vs-truncated vperp changes the FINAL truncated integer endpoints on **every one** (diff=1116=exec) — genuinely different grid-cell traversal — yet `mark_boundary_segments_touching_infill` marks the SAME boundary segments as consumed (the thick-line ±1 endpoint shift doesn't cross a marking-decision threshold) → byte-identical output. INERT (strong: fires-with-effect-then-washes-out, R125-elephant_foot precedent). GATES (both, while gated candidate applied): default 147987 byte-lock never at risk (candidates gated), no material/R65 impact (both post-slice + gated). INSTRUMENTATION (BRIDGEDBG/VPERPDBG counters, rust-only; native READ-ONLY this round) reverted — both trees clean, rebuilt to committed state (gated sha `b3757465`). NOT TOUCHED: `seam_placer.rs:2883` (crate-wide `scale_` posture, held for the user per team-lead). **VERDICT: after R126 the bounded Point-from-double lever runway (forward R124/R125 + reverse R126) is SWEPT — the only realizable lrint levers were the arc center and medial-axis VD; every other round/truncate mismatch on a byte-relevant benchy-classic path either matches native already or fires-then-washes-out. Remaining: the crate-wide `scale_` posture decision (systemic, user-held), L0 base-slice topology (R106/R109), and the PARKED R79 seam/arc/order + fill-generation toolpath subsystem. This is the clean continue-vs-bank decision point.**

- **★★ R125 — F2 POINT-FROM-DOUBLE AUDIT (census→classify→land): MEDIAL-AXIS VD-VERTEX ROUND-FIX LANDED (gated, F1_UNION) @7d262d5. BOTH metrics improve — content-multiset 143618→143538 (−80, PRIMARY), LCS/global-diff 162542→162460 (−82, SECONDARY, own-script consistent). Second confirmed hit of the R124 lrint class.** The gap-fill medial axis builds its ThickPolyline points from Voronoi-diagram vertices via `Point(double,double)` / `Line{double,double}` / `emplace_back(double,double)` (MedialAxis.cpp:463-464, 492-493, 566, 606-607), ALL of which route through `Vec2crd(coord_t(lrint(x)), coord_t(lrint(y)))` (Point.hpp:179) — round-to-nearest (ties-even); rust `medial_axis.rs` TRUNCATED toward zero (`as Coord`) with comments that WRONGLY asserted native narrows/truncates (the same misread R123/R124 corrected for the arc center). The ±1 propagated to gap-fill toolpath XY (sites :303/:304/:580 output points) AND to the w0/w1 widths + edge-length validation (:449/:450 `edge_a`/`edge_b`, native `Line{d,d}` brace-init = `Point(double,double)` since double→int narrowing is ill-formed in a braced-init). GAP FILL IS LIVE on benchy classic (layer.rs:631 = PerimeterGenerator.cpp:1335-1364 → variable_width; ~17% diff share, R108). FIX (medial_axis.rs, +42/−10, F1_UNION-gated via cached `f1_union_vd_round()` + `vd_coord()` helper = `x.round_ties_even() as Coord` on the gated path, legacy truncate on default). The `.round()` extension/intersection sites (:664/:665/:696/:697/:960) already round (ties-away vs lrint ties-even differ only at exact-half → negligible, left untouched). GATES: default **147987 byte-IDENTICAL** (stash-diff, sha `7adae05c` both pre/post — fix is gated), gated **deterministic 2×** (sha `b3757465`), material filament **3859.30 unchanged** (WATCH +0.268% held — gap widths shift sub-unit toward native but deposited E flat), R65 untouched (medial-axis is post-slice gap-fill). Gated rust lines 147500→147486 (−14, toward native). All prior closures (R118-R124) hold. **CENSUS (F2 forward class = rust `as i64`/`as Coord` truncates where native ROUNDS via `Point(double,double)`/`Point(Vec2d)`/member `operator*`/brace-init/`emplace_back(d,d)`; vs TRUNCATE-correct via Eigen `.cast<coord_t>()`/`coord_t()`/`scale_`/free `operator*`/`operator*=`/direct-coord-assign):** swept circle.rs (only the R124 arc center was lrint; :127/:367 are `.cast`/direct-assign = correctly truncate), variable_width + perimeter_generator.rs:1540 (native `.cast<coord_t>()` = CORRECT truncate), clipper_utils/simplify/multi_point/ex_polygon (all `.cast`/`operator*=`/integer-copy = CORRECT; the F1 clip-sampling `.round()` sites are a separate class), line.rs (`.cast<coord_t>()` = CORRECT + dead), fill_rectilinear/fill_base/fill_concentric (grid+monotonic live, all `.cast`/`std::round`-matching or dead), seam_placer/edge_grid/overhang (CORRECT or dead). **ONE forward candidate verified INERT & NOT landed: `elephant_foot_compensation.rs:524`** — native `Point radius_vector(search_radius,search_radius)` (ElephantFootCompensation.cpp:386) IS `Point(double,double)`=lrint (rust truncates), but it only sizes an SDF cell-query bbox on L0 (already washed out by the slice-topology floor); applied+built+measured → gated sha UNCHANGED (`b3757465`), PRIMARY/SECONDARY flat → reverted (fails the acceptance bar; faithful-but-inert). **Reverse/other-shape flags (NOT this class, reported not landed):** print_object.rs:1278 (rust `.round()` where native `.cast<coord_t>()` TRUNCATES, internal-bridge angle — a reverse divergence for ALL fractionals); fill_base.rs:887 (extra `vperp` truncation native keeps fractional); seam_placer.rs:2883 (rust `crate::scale`=round where native `Point::new_scale`/`scale_` TRUNCATES — the DOCUMENTED crate-wide scale_ posture per point.rs:53-57, a separate F2 decision). **VERDICT: the F2 Point-from-double lrint class is now EXHAUSTED for byte-relevant benchy-classic forward sites — arc center (R124) + medial-axis VD (R125) were the two real levers; the rest of the crate's `as Coord` truncations faithfully mirror native `.cast<coord_t>()`. NEXT: the reverse `.round()`-vs-`.cast` items (print_object bridge, fill_base vperp) are a small separate sweep, or the R79 toolpath subsystem (PARKED, user bank decision).** 

- **★★ R124 — ARC-CENTER ROUND-FIX LANDED (gated, F1_UNION): the R123 bounded lever, landed. BOTH metrics improve — LCS 173973→172292 (−1681, SECONDARY), content-multiset 143877→143618 (−259, PRIMARY). The cleanest lever since R118.** Native `Point(double,double)` = `Vec2crd(coord_t(lrint(x)), coord_t(lrint(y)))` ROUNDS to nearest (Point.hpp:179, default FE_TONEAREST = ties-to-even); rust `Point::new(center_x as i64, …)` truncated toward zero (circle.rs:183) → the fitted arc center was off ±1 unit on ~75% of arcs vs native → G2/G3 I/J divergence on byte-identical loops (R123 oracle). FIX (circle.rs `try_create_circle_from_points`, +~12 lines, F1_UNION-gated via cached `f1_union_center_round()`): center = `center_x.round_ties_even()` (matches lrint's mode EXACTLY — NOT `.round()`, which is ties-away-from-zero and would diverge); default path keeps the legacy truncation. RESULT: arc-fit output match on byte-identical loops **67.7%→98.8%** (R123 measured); per-layer **80 improve / 5 regress** (net −279), L1-L20 band net −19 (improved), the 5 regressors ≤3 lines (seam/order exposure on now-faithful arc I/J). GATES: default 147987 byte-IDENTICAL (stash-diff — fix is gated), deterministic (2×), material +0.268% (WATCH held, unchanged — arc I/J don't change deposited E), R65 untouched (arc-fit is post-slice). All prior closures hold (R118-R122 geometry is upstream of arc-fit; L1-L20 improved). **NEXT R125: the F2 Point-from-double AUDIT — native `Point(double,double)`=lrint EVERYWHERE, rust `as i64` truncate at Point-from-double sites is a systemic same-class gap (arc center was one instance); census the sites, land the clean gated ones.** The 4% R123 structural residual (endpoint cascade, shared crate DP R122-class) remains a smaller secondary lever.

- **★★ R123 — ARC-FITTER PROBE (measurement-only, docs; NO code landed): R107's arc exoneration HOLDS for SEGMENTATION but a BOUNDED ±1-unit CENTER-ROUNDING divergence is exposed on the now-larger byte-identical dataset — VERDICT (b), a clean ~2-line gated fix that improves BOTH metrics (proven: LCS −1681, multiset −259). Arc is NOT a floor.** Dual-engine arc-fit oracle (ARCDBG in `ArcFitter::do_arc_fitting_and_simplify` both engines, dumping per-loop: seg types + indices + arc center/radius/start/end + tolerance, keyed by the original input-loop identity): **11227 byte-identical-input loops**; arc-fit output MATCH **67.7%**. Of the 3629 diffs, **96% (3485) are CENTER-ONLY** (segmentation + start/end points + radius BYTE-IDENTICAL — only the arc center off by ±1 unit), 4% (144) structural (a 1-unit endpoint cascade). ROOT: native `Point(double,double)` = `Vec2crd(coord_t(lrint(x)), coord_t(lrint(y)))` **ROUNDS** to nearest (Point.hpp:179); rust `Point::new(center_x as i64, …)` **TRUNCATES** toward zero (circle.rs:183, with a stale "// truncates" comment) → ±1-unit center on **74.9% of arcs** on byte-identical loops → the G2/G3 I/J params diverge (so the R121 "L80 outer +37 lines = arc over-seg" attribution was WRONG — the segmentation matches; it's the I/J center that diverges + seam). PROVEN FIX (verified by applying + measuring, then reverted per measurement-only): rust circle center = `round_ties_even()` (matches lrint's default FE_TONEAREST) → arc-fit match **67.7%→98.8%**, global **LCS 173973→172292 (−1681)** AND **multiset 143877→143618 (−259)** — BOTH improve (cleaner than R122). ~2 lines at circle.rs:183. CAVEAT: it's a DEFAULT-path change (arc-fitting runs on both paths; default 147987→147977) → must be **gated F1_UNION**. **BROADER (F2-class): native `Point(double,double)`=lrint EVERYWHERE; rust `as i64` truncate at Point-from-double sites is a SYSTEMIC same-class fidelity gap** (the arc center is one instance; audit candidate alongside the R122 scale_/Line::distance_to_squared items). The 4% structural residual = a 1-unit endpoint cascade (likely the arc-fit's shared crate DP [R122 rounded-distance class] or endpoint snapping) — a smaller secondary lever. VERDICT for the bank call: the arc-fitter is a CLEAN bounded lever (not a floor); with R122's geometry exhaustion, the next levers are this arc-center round (R124 candidate, both metrics improve) + the Point-from-double F2 audit + L0 base-slice, THEN the R79 seam/order toolpath subsystem.

- **★★ R122 — FAITHFUL simplify_p LANDED (gated, F1_UNION): the slice-simplify DP is now BYTE-EXACT on ALL 578 islands / 240 layers. Content-multiset 143947→143877 (−70, PRIMARY); LCS 173453→173973 (+520, SECONDARY exposure). The GEOMETRY SIDE IS NOW EXHAUSTED — the marginal gcode payoff confirms R121 (residual = toolpath, not geometry).** ROOT was TWO coupled bugs (R121 pinned it as "DP tie-break" — deeper: it's a tolerance-scaling bug the rounded distance masked): (1) **rust's `surface_simplify_resolution` is UNSCALED (0.2·resolution = 0.0024) but native's is SCALED (`0.2·m_scaled_resolution`, m_scaled_resolution=`scaled<double>(resolution)`=resolution/0.00001 ≈ **240** scaled units)** — a 1e5× too-small DP tolerance, so the rust DP's `dist_sq ≤ tolerance²` removal test never fires on real (scaled) coordinates; (2) the crate DP's `Line::distance_to_squared` computes the distance to the integer-ROUNDED `project_onto_segment`, which for near-collinear points on a long (anchor,floater) span ROUNDS the projection onto the point (distance 0) → removes them anyway, *accidentally masking* bug (1). Native's `line_alg::distance_to_squared` returns the pure-`double` perpendicular `(t·v−va)²` (the `nearest_point` rounding does NOT affect the return). LOCALIZATION (staged simplify oracle, per-island keyed by byte-exact input-slice first point + Python DP replay on L80's raw contour): the pure-double DP at rust's 0.0024 → 243 pts, at the scaled 240 → **235 = native** exactly. FIX (multi_point.rs + geometry/expolygon.rs + perimeter_generator.rs, F1_UNION-gated, +131 lines): `douglas_peucker_faithful` (native pure-double point-to-segment distance) + `simplify_p_dp_rings_faithful`, fed the SCALED tolerance `0.2·(resolution/0.00001)`; wired into the gated slice-simplify only. RESULT: simplify byte-exact **578/578 islands** (was ~5 layers diverging: L33/L34 dropped/L54 kept near-tolerance pts); L33/L34/L54 + sweep all close. GATES: default 147987 byte-IDENTICAL (stash-diff — fix is gated), deterministic (2×), material +0.268% (WATCH held, unchanged), R65 untouched. **SCOPE NOTE (F2-class, reported): the crate `Line::distance_to_squared` (rounded-projection) AND the unscaled `surface_simplify_resolution` are GLOBAL fidelity bugs shared by many default-path callers (arc_fitter, polyline/polygon simplify, the inner-wall simplify at perimeter_generator.rs:1311/:2531) — scoped to the gated slice DP here to protect the byte-locked default path; a global fix (+ the inner-wall simplify) is a future F2 candidate, not manifesting on the sampled offsets (R121 showed inner offsets byte-exact).** GEOMETRY EXHAUSTED — only L0 base-slice topology (1 layer, R106/R109) + the R79 seam/arc/order + fill-generation toolpath subsystem remain; the next byte-parity lever is the toolpath subsystem (or bank).

- **★★ R121 — a_last FLOOR LOCALIZED (measurement-only, docs; NO code change): the "30-40% deep ClipperOffset/slice-topology floor" R107 feared is REFUTED (8th floor refutation). On the sampled high layers the PERIMETER GEOMETRY IS BYTE-EXACT per island; the residual is the R79 TOOLPATH-EXPRESSION floor (seam/arc/order + fill-generation), NOT geometry.** ★ MEASUREMENT-ARTIFACT CORRECTION: R119/R120's "high layers = upstream-inherited, a_last_entry diverges" was a per-surface ISLAND-MISPAIRING artifact — the a_last/p0 dump overwrote per surface, capturing whichever island was processed LAST, and native (chain_expolygons order) vs rust (slices.iter order) process islands in different order → the dumps compared DIFFERENT islands (e.g. L80 "243 vs 37 pts" = two different islands with non-overlapping bboxes). Re-probed with per-island pairing (dump keyed by the byte-exact input-slice first point) + an order-independent full-island-set dump. CENSUS (21 diverging layers, `pS_sliceset` → per-island `p1_simplified` → `p2_offset_i0/i1`): **16/21 (76%: L53/L55/L75-130) have the FULL perimeter chain BYTE-EXACT** — sliceset + simplify_p + i=0 outer offset + i=1 inner offset all match on EVERY island. L80 feature-split of the (byte-exact-geometry) gcode residual: Top-surface fill 331 / Outer-wall 235 (rust +37 lines = arc over-segmentation on byte-identical loops, R79b/R107) / infill-classification (Floating/ISI/Sparse) — i.e. seam + arc + fill-generation, the R79 floor. The 5 GEOM-DIVERGE layers: **L33/L34/L54 = simplify_p chain** (sliceset byte-exact, but `simplify_p` diverges: L33/L34 rust DROPS a native near-collinear point 0.05–0.08µm; L54 rust KEEPS 3 points ~2µm off native's edge = a DP tolerance/tie-break mismatch — bounded, a faithful `simplify_p` port [DP + ClipperLib SimplifyPolygons vs BBS `ExPolygon::simplify_p`] closes it), cascading into i=0/i=1; **L0 = slice-set topology** (2 islands, the slice STAGE diverges — the known base-layer floor, R106/R109, upstream of perimeter gen). DUAL-BUILD gate not triggered: no ClipperOffset op diverges on byte-identical input (the offsets are byte-EXACT = strongest possible evidence, floor refuted by construction); the geom-divergers are simplify-algorithm-fidelity (L33/34/54) + slice-stage (L0), NOT a ClipperOffset floor. **VERDICT: the campaign's dominant remaining residual is the R79 seam/arc/order + fill-generation toolpath floor on FAITHFUL geometry — a KNOWN, previously-quantified class (R107: seam ~90% self-converges given matching geometry, arc inherited/EXONERATED, order = R111-class island/seam tie-breaks), NOT a new deep-geometry floor. Bounded geometry levers left: `simplify_p` fidelity (L33/L34/L54 + likely other single-island layers) and the L0 base-slice topology (1 layer, hard). This DECIDES continue-vs-bank: the geometry is largely done; further byte-parity gains require attacking the toolpath-expression subsystem (seam placement / arc fitter / fill generation on byte-exact regions) or the bounded simplify_p port.**

- **★★ R120 — FAITHFUL TOP CHAIN LANDED (gated, F1_UNION): upper_series un-grid + scale_faithful min_width/offset_top/inner_off deltas + top_polygons/inner_polygons ::Yes. Content-multiset 144741→143947 (−794, PRIMARY); LCS 173709→173453 (−256, SECONDARY — BOTH improve). The 4 confirmed top-chain targets L23/L32/L42/L60 close BYTE-EXACT at all stages.** Staged dual-engine oracle on the R119-confirmed a_last-OK targets (`b_upper_series→c_top_polygons→d_inner_polygons→e_lastOUT`): with the wire, ALL FOUR stages close byte-exact on ALL FOUR targets, no L1-L20 regression, lastOUT native-match 62→69 layers. **DELTA REPRODUCTION (bit-exact vs native dump on L23):** native builds the top-chain offsets in SCALED coord_t from `scaled_width()/scaled_spacing()` (= `scale_faithful`); reproduced eps=37707 pw=44999 epw=41999 psp=40707 → **min_width=44999, offset_top=87423** (`scale_(1.5·unscaled(epw+psp·(wl−1)))` minus the 0.9-reduction), **inner_off=113569** — the last needs `eps/2` as INTEGER division (`double(ext_perimeter_spacing/2)` = coord_t/int = 18853, NOT 18853.5); each scaled delta → mm via `offset_delta_mm_from_scaled_f32` (f32-cast /1e5) for `offset_expolygons_clib`. FIX (perimeter_generator.rs, F1_UNION, all 3 coupled per R115): (a) upper_series `offset(clip(upper,last_box), float(min_width))` → `union_ex_clib(clipped) + offset_expolygons_clib` (un-grids the R100/R115-gridded upper); (b) top_polygons diff → `difference_clib_safety` (::Yes, R116 shim); (c) inner_polygons diff → `difference_clib_safety` + `offset_expolygons_clib` with the scale_faithful inner_off. **EXPOSURE (why positional/Top-count drift while primary+secondary improve):** per-layer difflib 76 improve / 38 regress (net −426); material TOTAL +0.284%→**+0.268%** (toward native, filament 3859.30 vs 3858.97). Positional +614 and Top-surface count 142→144 are downstream TOP-FILL TOOLPATH churn (R79 seam/arc/order floor): the top region GEOMETRY is now byte-exact on the targets, but the top-surface FILL toolpath that consumes it is not yet faithful, so it churns on the wall-floor layers (Top-count is a segmentation artifact — Top material dE flat +5.33 vs +5.27). The remaining `e_lastOUT` divergers (L0/L33/L34/L53-55/L75-130) are all UPSTREAM-INHERITED (`a_last_entry` diverges = the deep inner-offset/wall floor, NOT top-chain — R119 split confirmed). GATES: default 147987 byte-IDENTICAL (stash-diff), gated deterministic (2×), material WATCH improved +0.284→+0.268%, R65 untouched. **NEXT: the deep wall/inner-offset floor (the a_last-diverging layers L53-130 — owns the remaining top-chain + bridge + inner residual); the top-chain is DONE for the a_last-OK set.**

- **★★ R119 — FAITHFUL INTERSECTION SHIM LANDED (gated, F1_UNION): the L2-L20 band residual was the `last=intersection(inner,last)` op, NOT the bridge and NOT the top chain. Content-multiset 145055→144741 (−314, the new PRIMARY metric); LCS marker 163161→173709 (secondary, +10548 exposure — see table). Top-surface count 143→142 = NATIVE EXACT.** RE-LOCALIZATION (dual-engine ISECTDBG stage-split oracle, both engines byte-keyed at `a_last_entry→b_upper_series→c_top_polygons→cg_temp_gap→d_inner_polygons→d2_last_isect→e_lastOUT`): on the band L2/L3/L4/L6/L8/L16, `a_last`=OK, `c_top_polygons`=EMPTY (top chain contributes NOTHING — the R118 "top chain closes these" premise was WRONG), `temp_gap`=empty, `d_inner_polygons`=BYTE-EXACT (**the bridge is NOT the driver**), yet `e_lastOUT`=+1 pt. The +1 enters ONLY at `last = intersection_ex(inner_polygons, last)`: byte-identical inputs, native single-op `intersection_ex`=147pt vs rust `A−(A−B)` double-`difference_clib`=148pt. The extra vertex is near-collinear (**0.001µm off the edge** — perp dist 0.0975 scaled units) that native's single ctIntersection drops but the second ctDifference re-processes and keeps. **★ DEAD-LIST CORRECTION (R79-style honest overturn): R118's "intersection op EXONERATED / NO cz intersection shim needed" was OVER-GENERALIZED from L7 (a closed layer where the geometry has no such vertex). 9-layer sweep: A−(A−B) closes 3 (L1/L5/L7), inserts +1 on 6 (L2/L3/L4/L6/L8/L16).** FIX (+122/−5, F1_UNION): new `cz_intersection_closed` (clipper_z_shim.cpp = `clipper_do<Paths>(ctIntersection, subject, clip, pftNonZero)`, ClipperUtils.cpp:802 `intersection_ex` ApplySafetyOffset::No) → `intersection_clib` (clipper_utils.rs, same F1_UNION `union_ex_clib` reconstruction as `difference_clib`, matching native `PolyTreeToExPolygons(clipper_do_polytree(ctIntersection,...))`); routes `last = intersection(inner_polygons, last)` (perimeter_generator.rs). CLOSES the whole L1-L20 band byte-exact AND all 7 R118-closed layers (L1/L5/L7/L9/L10/L18/L19) STAY byte-exact (oracle-confirmed). **EXPOSURE JUSTIFICATION (why LCS rose while content improved):** per-layer difflib 54 improve / 24 regress / 163 flat (net −286); positional diff 146543→146415 (−128). Every LCS regressor is stage-verified UPSTREAM-DIVERGING — the fix removes genuinely-spurious collinear points from rust's `last` on the ~69 layers whose `a_last_entry` ALREADY differs (wall-floor), so rust's line-count shifts and the global LCS cascade-re-anchors WITHOUT closing native-match. Top regressors: L92 +180 (a_last 1exp diverges grossly), L91 +51 (idem), L79 +17, L107 +10 — all a_last-DIFF layers. This is the R104/R106 exposure class (both LANDED with the marker UP). **HIGH-LAYER SPLIT (answers R118's open question):** (i) UPSTREAM-INHERITED — L53/L75/L80/L90/L91/L92/L100/L112/L113: `a_last_entry` already diverges grossly (e.g. 274 vs 77 pts) = the wall/inner-offset floor, NOT block-fixable; (ii) TOP-CHAIN TARGETS — L23/L32/L42/L60: `a_last`=OK + non-empty `top_polygons` that DIVERGES (b_upper_series 100%-on-grid rust vs 0% native = the R100/R115 gridding class) = the real top-chain lever (R120). GATES: default 147987 byte-IDENTICAL (stash-diff), gated deterministic (2×), material at native (TOTAL +0.284%, filament 3859.91 vs 3858.97 — WATCH held, was +0.285%), R65 untouched (block is post-slice). **METRIC POLICY (team-lead, R119+): report BOTH content-multiset-unmatched (PRIMARY — truer convergence, order-independent) AND the LCS marker (SECONDARY — order-sensitive, whipsaws on exposure).** NEXT R120: wire the top chain against the CONFIRMED targets L23/L32/L42/L60 (a_last-OK ⟹ full native-match closure expected, both metrics improve, no upstream exposure) — upper_series bbox-clip→union_ex_clib→offset_expolygons_clib w/ scale_faithful min_width delta + top/inner ::Yes via difference_clib_safety.

- **★★ R118 — FAITHFUL BRIDGE LANDED (gated, F1_UNION): the R113-R117 coupled-cluster fix converges. Gated diff 175185→163161 (−12024, −6.9%); 41 layers improve / 4 regress.** PHASE-1 RE-LOCALIZATION (ISECTDBG stage-split of `last` at intersection IN/OUT, L0-20, dual-engine) OVERTURNED the R114/R116 "bridge-not-L1's-root" negative: with the faithful bridge, L1 `lastOUT` closes **145→148 BYTE-MATCH** (innerP 147/147 byte-identical). R116 churned ONLY because its delta lacked the f32 cast (0.45/0.00001=45000→67500 vs native f32-cast 44999→67498.5); R117's `scale_faithful` fixes it. The intersection op (`A∩B = A−(A−B)`, double difference_clib) is EXONERATED — L7 byte-identical inputs → byte-identical lastOUT; **NO cz intersection shim needed (kills that queued idea).** FIX (perimeter_generator.rs:940, three coupled, all under F1_UNION): (a) bbox-clip lower to last_box (was unclipped); (b) ::Yes safety difference via `difference_clib_safety` (R116 shim); (c) scale_-faithful `bridge_offset` via `scale_faithful` (R117). CLOSES L1/L5/L7/L9/L10/L18/L19 byte-exact. GATES: default 147987 byte-IDENTICAL (stash-diff), gated deterministic, material at native (TOTAL +0.285%, filament 3859.93 vs 3858.97 — WATCH held). The 4 regressions (L6/L16/L112/L113, 0.4-0.6%) are TOP-NON-EMPTY layers where the faithful bridge shifts geometry but the top chain (upper_series un-grid + top_polygons/inner_polygons ::Yes + min_width/inner_off deltas) is still un-fixed — R106-class exposure, closes in R119. NEXT R119: the top chain (all pieces exist: R115 un-grid shape + R116 shim + R117 helper) → the L2/L3/L6/L8/L11-17 residual band.

- **R117 — scale_-faithful delta helper LANDED (unwired, pure addition).** `scale_faithful(mm) = trunc(f32(mm)/0.00001)` reproduces native `coord_t(scale_(m_spacing))` where the BBS macro divides by the 0.00001 f64 literal and the spacing is f32-stored: scale_(0.45)=44999, scale_(0.5)=49999, scale_(0.42)=41999 (unit-tested). `offset_delta_mm_from_scaled_f32(scaled) = f32(scaled)/1e5` converts a native scaled offset delta (post the `offset(paths, float delta)` f32-cast) into the mm value `offset_expolygons_clib` (×1e5 internally) needs — e.g. bridge_offset 1.5·44999 = 67498.5 round-trips exact. No caller yet (R118 wires it). Default 147987 / gated 175185 byte-unchanged. NEXT: R118 coordinated only_one_wall_top port (phase 1 re-localize L1 root → phase 2 wire).

- **★ R116 — diff_ex_safety CZ SHIM BUILT + ORACLE-VERIFIED + LANDED (unwired infrastructure). Shim reproduces native `diff_ex(subject, clip, ApplySafetyOffset::Yes)` BYTE-EXACT.** New `cz_difference_closed_safety` (clipper_z_shim.cpp) = `clipper_do(ctDifference, subject, safety_offset(clip), pftNonZero)`: safety_offset = raw_offset(clip, ClipperSafetyOffset=10u, jtMiter, ML3, orientation-aware per-path, NO union) — faithful to ClipperUtils.cpp:273-307/334. Rust `difference_clib_safety` wraps it (same F1_UNION union_ex_clib reconstruction as difference_clib). **SHIM ORACLE PASSED: `difference_clib_safety(last, clip(lower,last_box))` on the R113 L1 dump = native `diff_last_lower` 1329 pts BYTE-IDENTICAL.** WIRE TEST (bridge lower-diff, full faithful: clip + safety-shim + scale_-faithful bridge_offset): global 175185→174394 (−791) but **30 improve / 19 regress** (identical profile to R114's unfaithful bridge) AND **L1 does NOT close** (L1 gcode byte-unchanged) — [claimed] the bridge_checker's inner_polygons contribution is washed out by the downstream `last = intersection(inner_polygons, last)` on L1, so the bridge is NOT L1's residual root despite R113's localization. **[CORRECTED @R118: WRONG — the bridge IS L1's root. This R116 wire-test churned/failed-to-close ONLY because its `scale_-faithful bridge_offset` LACKED THE f32 CAST (0.45/0.00001=45000→67500 vs native's f32-cast 44999→67498.5, a 1.5-unit delta error). R117's `scale_faithful` (WITH f32 cast) supplies 67498.5; then the faithful bridge closes L1 145→148 BYTE-EXACT (R118 phase-1 ISECTDBG proof) and −6.9% global. The intersection op (A−(A−B)) is FAITHFUL/EXONERATED (L7 byte-identical in→out) — no cz intersection shim needed.]** Per acceptance (churns → unwire), bridge left UNWIRED for R118; the other two ::Yes sites (top_polygons@1144, upper diff) are coupled to R115 un-grid + R117 deltas → also R118. **LANDED: the shim only, UNWIRED — gated 175185 / default 147987 byte-UNCHANGED (pure addition, no existing path touched), deterministic, R65/material untouched.** R114-BAND ROOT now OPEN: L1's i=1-input divergence (145/148) is NOT closed by the faithful bridge; the driving path is elsewhere (top_polygons chain or the intersection itself) — re-localize in R118 with the shim available. NEXT: R117 delta helper → R118 coordinated only_one_wall_top port.

- **R115 — upper_series UN-GRID: WORKS BUT CHURNS ALONE, REVERTED (no commit; gated 175185 / default 147987 verified). ★ STRATEGIC PIVOT: the only_one_wall_top block is a COUPLED CLUSTER — three interlocking faithfulness gaps that must land TOGETHER.** The re-route (offset_polygons@865 → union_ex_clib+offset_expolygons_clib, F1_UNION) un-grids upper_series 100%→0% on-grid, matches native's 101-pt count, stale "clib no-op" NOTE confirmed WRONG. But: (1) R109-class DELTA — native offsets by float(min_width_top_surface) built from scale_-faithful spacings (scale_(0.45)=44999); rust passes f64 mm → upper_series full-res but **0.31µm off** (4/101 exact, max_dev 31 units); (2) downstream top_polygons diff still No-safety (R114 gap) → diverges anyway. Global 175185→175207 (+22), same failure shape as R114 → reverted per acceptance. **THE PATTERN (R113/R114/R115): only_one_wall_top has (i) geo gridding (upper_series, fill_clip), (ii) R109-class scale_/f32 deltas (min_width, bridge_offset), (iii) missing ApplySafetyOffset::Yes on ALL block differences (top_polygons@1144, upper diff, bridge lower-diff) — fixing any ONE churns because the others dominate. CONVERGES ONLY AS A COORDINATED FAITHFUL PORT, staged against native byte-identity (R113 STGDBG oracle harness).** PLAN: R116 = diff_ex_safety cz shim (raw-offset clip +10u, NO union, then ClipperLib difference; the gate for the whole block + R114 bridge); R117 = scale_-faithful delta helper (R109 shape) for min_width/bridge_offset; R118 = coordinated block landing (upper_series un-grid + 3 ::Yes diffs + faithful deltas), staged L1 byte-identity at upper_series → top_polygons → inner_polygons → last.

- **R114 — BRIDGE-PAIR FIX ATTEMPTED, FAILED ACCEPTANCE, REVERTED (no commit; tree restored, gated 175185 / default 147987 verified). **[CORRECTED @R118: the bridge fix was RIGHT in shape but failed for TWO now-understood reasons — (1) the ::Yes safety was reconstructed via union+offset (unfaithful; R116 shim fixed it), (2) the bridge_offset delta lacked the f32 cast (67500 vs 67498.5; R117 fixed it). With BOTH landed the faithful bridge closes L1 byte-exact and gives −6.9%. The "L1 washes out / not the block's driver" reading was an artifact of the two delta/reconstruction bugs, not the geometry.]** ★ SYSTEMIC DISCOVERY: rust lacks a faithful `diff_ex(…, ApplySafetyOffset::Yes)` — the safety-offset gap is EVERYWHERE difference_clib is used.** Built the 3-part gated chain: (a) clip lower to last_box, (b) safety offset (ClipperSafetyOffset 0.0001mm) via union_ex_clib→offset_expolygons_clib→difference_clib, (c) scale_-faithful bridge_offset 67498.5. FAILED: (1) L1 (the localized target) gcode BYTE-UNCHANGED (base==fix, 1775 lines); L1-15 band all +0.0 — on L1 the bridge contribution is partly clipped back out by the later last=intersection(inner_polygons,last), so the R112 snapshot divergence reaches emitted gcode less than expected; (2) 19 layers regressed >0.3% vs 30 improved, diff only −791 (churn wash) — acceptance #1 violated. WHY: native diff_ex(::Yes) = clipper_do(ctDifference, subject, **raw_offset(clip, +10u)**) — the safety offset is applied to RAW paths fed straight into the difference; rust's union+offset reconstruction detour produces different vertices → 118-layer churn without case closure. VERDICT: unlandable via approximation; needs a dedicated **cz diff_ex-with-safety shim** (raw-offset clip, no union, then difference) — which ALSO covers top_polygons@1144 + the upper diff (both ::Yes in native; rust difference_clib is No-safety everywhere) = possibly a broad residual driver. PLAN: R115 = upper_series Top-surface un-grid (bounded R100-class re-route); R116 = build diff_ex_safety shim with L1 byte-identity as the oracle (if L1 still doesn't close with the faithful shim, the band has a further root).

- **★★ R113 — L1-15 INNER ROOT LOCALIZED (stage-split, measurement-only, no commit): the R112 inner_polygons divergence = the BRIDGE_CHECKER chain — rust MISSING the lower-slice bbox clip + an R109-class delta. NOT slice-inherited, NOT the offset op — outcome (a) op-level.** Stage-split L1 (dual-engine, every intermediate): upper_slices BYTE-IDENTICAL (149/149 full-res); upper_series=offset_polygons @865 grids to 100% on-grid via geo-clipper (native full-res) BUT top_polygons EMPTY on L1 both ⟹ no cascade here (cascades on TOP-SURFACE layers — separate lever, below); inner_polygons @923 still 143/143 byte-identical ⟹ injection is LATER: the bridge_checker union (perimeter_generator.rs:940-965). BRIDGE CHAIN: `lower` (L0 slice) BYTE-IDENTICAL (1368/1368) — **FIRST DIVERGING INTERMEDIATE = diff_last_lower (rust 1254 vs native 1329 pts): rust diffs against UNCLIPPED lower; native clips first — lower_polygons_series_clipped = clip_clipper_polygons_with_subject_bbox(*lower_slices, last_box) (PerimeterGenerator.cpp:1172 → 1351 pts) — rust MISSING the bbox clip.** SECONDARY: bridge_offset delta rust 67500 vs native 67498.5 — the R109 scale_ class (native scale_(0.45)=44999 ×1.5; rust 0.45×1e5=45000 ×1.5). Cascade: diff_last_lower → bridge_checker (933 pts, ~0.1µm off) → union into inner_polygons (147/294, the 2-vs-1 topology split) → last=intersection → i=1 input (145/148) → all inner offsets inherit. FULLY EXPLAINS R112. FIX SHAPE (R114, coupled pair): (a) bbox-clip lower before the diff (~5 lines, mirror upper clip @857); (b) scale_-faithful bridge_offset (~2 lines). Payoff: bridge-condition layers (L1-15 band+), inside Inner 13% + Bridge 2% — low-single-digit est. R113b REFUTED: the geo fill_clip @981 is DEAD code in the block (reassigned before use, zero payoff — R112's flag corrected); the REAL live geo lever is upper_series @865 (its "upper pre-gridded upstream, clib no-op" NOTE is WRONG — upper arrives full-res and the geo offset grids it) feeding top_polygons→top_fills on top-surface layers = a separate Top-surface (7.9%) R100-class lever.

- **★ R112 — DUAL-BUILD TOPOLOGY PROBE: VERDICT (i) — the ClipperOffset OP IS BYTE-FAITHFUL cz-vs-native; the L1-15 divergence is LIVE-PATH-injected upstream. FLOOR NOT CONFIRMED (7th floor-claim refutation; measurement-only, no commit, both trees clean).** Case A (L1, corroborated L3/L4/L7/L15): dual-engine dump at live call sites (perimeter_generator.rs / PerimeterGenerator.cpp, keyed layer,i) — the i=0 outer offset runs on BYTE-IDENTICAL input with IDENTICAL delta f64 bits and produces BYTE-IDENTICAL output (L1 143→143 pts exact; same L3/L4/L7/L15) ⟹ cz ClipperOffset == native ClipperLib on identical input+params; cz_offset2_ex = same engine twice ⟹ inner op faithful by extension. (Standalone replay harness blocked — slicer lib test binary has 85 pre-existing compile errors — but live-path i=0 byte-identity is the replay evidence: same op, matched input, identical output.) MECHANISM: divergence enters in the only_one_wall_top block (PerimeterGenerator.cpp:1162-1201): `last` ENTERS byte-identical; temp_gap empty; but **`inner_polygons` DIVERGES** — full-res (0% on 1µm grid ⟹ NOT R100 gridding), ~3.3µm at verts 37-38 + TOPOLOGY split (native 2 ExPolygons / rust 1) → `last = intersection(inner_polygons, last)` → inner-offset input diverges (rust 145 / native 148 pts) → i=1+ INHERITS. Secondary find: fill_clip diverges via geo-clipper offset_expolygons @ perimeter_generator.rs:1038 (un-routed R100-class site — feeds top_fills, NOT last). NEXT (R113): stage-split the inner_polygons = diff_ex(last, upper-slices-derived clip) chain — localize the first diverging sub-op (prime suspect: the upper_polygons_series clip region), since `last` into the block is proven identical.

- **★★ R111 — ISLAND-ORDER TIE-BREAK ROOT CRACKED + FIX LANDED (gated, F1_UNION): the last bounded lever. Rust `make_slices` chained lslices with a simplified NN-from-index-0 instead of native's `chain_segments_greedy`; island EMISSION order (= lslices order) diverged on 67/126 multi-island layers. Fix routes through the faithful chain; total gated diff 188922→175185 (−13737, −7.3%).** VERDICT: pure (b) ORDER, ZERO geometry entanglement. Dual-engine dump (rust ISLDBG in print.rs::emit_layer_by_island + native NISLDBG at GCode.cpp:4362, both keyed by print_z, bbox-area sequence in lslices order): of 126 multi-island layers, **SET-DIFF = 0** (island sets byte-identical both engines) but **67 REORDERED** (34 exact reversals, 33 adjacent swaps of near-equal-area islands, e.g. z=19.0 swaps 1.8375e12 vs 9.94e11; odd island-counts 3/5 almost always reordered, even 2/4/6 almost always same). ROOT: both engines emit islands in lslices index order (rust print.rs:2002 / native GCode.cpp:4869), and lslices is ordered by `chain_points(ordering_points)` (Layer.cpp:69-70) = each slice contour's first_point chained. Native `chain_points`=`chain_segments_greedy` (ShortestPath.cpp:1073, KD-tree greedy); rust `make_slices` (layer.rs:1529) called a LOCAL simplified `chain_points` (layer.rs:2798, NN from index 0) — divergent on multi-island. FIX (+13/−1, F1_UNION): route make_slices through the faithful port `crate::shortest_path::chain_points` (wraps chain_segments_greedy, ShortestPath.cpp:1073-1081 exact). RESULT: island order match 59/126→**126/126** (0 reordered); gated diff 188922→175185 (−7.3%); per-feature content-multiset near-flat (−627, pure order — content was already matched, now positioned so LCS re-anchors); biggest drops Gap −337 / Inner −275 / Outer −27 (reordered layers span all features). MATERIAL unchanged (+0.285%, filament 3859.92). Default path byte-IDENTICAL (stash-diff verified, 147987); gated + default deterministic byte-identical (chain_segments_greedy deterministic — R99 held); R65 order-invariant. Instrumentation reverted both trees (native rebuilt clean). REMAINING: the deep ClipperOffset/slice topology floor (owns outer-wall 45.6% + L0 8-vs-10 + inner) and the deferred E/width byte work — all levers above it now retired (arc R107, gap-local R108, seam R110, L0-width shelved R109, island-order R111).

- **R110 — SEAM-ALGO LEVER CLOSED: (c) INPUT class, fully inherited from the wall-geometry floor (gcode-analysis only, no instrumentation, no commit).** On 195 centroid-matched (<20µm) outer loops: seam agreement <2µm = 166/195 = 85.1% (R107's 90.3% minus a ~1µm proxy floor — agreeing loops' own vertex noise median 1.41µm). The 29 divergent: ALL adjacent (<0.5mm, zero far-apart ⟹ NO term-class), ALL same candidate/vertex count, **Pearson corr(seam divergence, wall-vertex noise) = 0.980**, 26/29 noise-explained (≤3·vertnoise+3µm), the 3 largest driven by 13-122µm genuine loop-geometry deltas, min vertnoise among divergent 1.0µm (NO zero-noise divergence ⟹ no pure-FP tie-break residual). Comparator faithful both engines (strict `penalty_a < penalty_b`, SeamPlacer.cpp:711 / is_first_better:1500) — confirms R79c: same geometry ⟹ same seam; the residual is the R100-class sub-µm wall floor perturbing near-equal ADJACENT penalties past the strict `<`. Inner: centroid method inconclusive (concentric rings share centroids — mis-pairs); moot given outer verdict. **The ~1.5-2% seam payoff is INSIDE the outer-wall floor (45.6% share), not separately realizable. RETIRED alongside arc (R107) and gap-local (R108).** REMAINING LEVERS: island-order tie-breaks (~1-3%, last bounded lever) → then the deep ClipperOffset/slice topology floor (30-40%, owns L0 8-vs-10 + L1-15 inner + outer residual; dual-build probe on exact diverging input BEFORE any floor verdict) + deferred E/width byte work.

- **R109 — L0 WIDTH-SOURCE FIX REFUTED AS STANDALONE (investigation, NO COMMIT, tree clean; R107's "~1% CLEAN" estimate WRONG): the width delta is real and the 1-line fix reproduces native exactly, but L0 byte-divergence is owned by the SLICE/ClipperOffset topology floor — the fix gains ZERO native-matching lines and regresses the gated diff 188922→188953 (+31), while breaking L1 bridge neutrality.** TRUE ROOT (deeper than R107's pin, confirmed at source): BBS `scale_(v)` = `(v)/SCALING_FACTOR` with SCALING_FACTOR=**0.00001 (f64 literal, slightly >1e-5)** then coord_t-truncate → 0.5/0.00001 = 49999.9999 → **49999**; rust nsc multiplies by exact 1e5 → **50000**. Only w=0.5 diverges (0.42/0.45 truncate identically both ways — why only L0, the sole 0.5-width layer). SHELVED FIX (for the future topology-floor round): nsc = divide-by-0.00001-then-truncate (ODBG-verified: L0 delta −24999.5 = native, L1 unchanged). ACCEPTANCE FAILURES (both deterministic, 2x reproduced): (1) L1-239 NOT byte-neutral — per-layer diff exactly {L0,L1}; L1 change is BRIDGE: L0's outer-wall position defines the supported region L1 bridge detection reads — structurally unavoidable cascade. (2) L0 topology unmoved: rust L0 slice = 2 ExPolygons/8 holes at SLICE stage (upstream of width); outer blocks 5 vs native 6; L0 multiset match 0.1%→0.1%. F2-CLASS NOTE (new, cross-cutting): any rust scaling site using ×1e5 can diverge from native's ÷0.00001 wherever v/0.00001 ≠ v*1e5 in f64 — audit candidate for future scaled-width work. DECISION (team-lead): SHELVE (option a) — necessary-but-not-sufficient; bundle only with an L0 topology-floor attack. NEXT: R110 seam-algo probe.

- **R108-CODA (team-lead follow-up measurements on the R108 gated dump, /tmp/cmp_final; round CLOSED):** (1) INSTRUMENTATION: native tree (BambuStudio2) verified clean — git status empty, no GAPDBG in PerimeterGenerator.cpp; rust tree clean (comment-only reference). (2) MATERIAL PROOF: aggregate gap E/len rust/native = **1.02200** (len ratio 0.98418, nseg 5505 vs 4987) — NOT pure path-length exposure; but aggregate-over-different-geometry is the exact R103 confound and gap width varies per segment, so non-probative as a regression verdict; geometry faithfulness IS proven (on-grid 100→0%, XY multiset 20x) ⟹ exposure ACCEPTED with **WATCH flag: total dE +0.285%** (was +0.059% @R106) — the per-segment gap E/width class joins the deferred-LAST E-byte work; revisit if total drifts past ~0.5%. (3) RESIDUAL gap 53% CLASSIFIED (XY non-wipe, layer-aligned): multiset 59.5% vs positional 9.1% ⟹ **ORDER 50.4pts / GENERATION 40.5%**; inheritance control: layers with wall XY-match ≥95% → gap 84.0% (n=2, thin), walls <80% → gap 19.0% (n=41), corr 0.279 ⟹ the generation component tracks the wall floor — **NO further gap-local lever; gap converges when walls/order do.** NEW POSITIONAL SHARE TABLE (206936 layer-aligned move-line diff): Outer 45.6 / Inner 13.0 / Gap 12.2 / ISI 7.9 / Top 7.9 / Float 5.2 / Sparse 4.5 / Bridge 2.0 / other 1.7. NEXT: R109 = L0 width-source fix (~1% CLEAN, cause pinned in R107), then seam-algo outer (~2%), then island-order.

- **★★ R108 — GAP-INFILL LOCALIZED + FIX LANDED (gated, F1_UNION): the 17% gap share was CLASS (a) GRIDDING, gap-LOCAL (not inherited), 7th hit of the R100 class. Fix routes the gap-detection ops through clib; total gated diff 243105→188922 (−22.3%).** VERDICT: (a) gridding dominant, (b) ORDER refuted, (c) generation/inherited = residual. Gap detection at perimeter_generator.rs:663 ran through geo-clipper (`shrink`/`grow` @ GEO_CLIPPER_SCALE=1000, `difference` @1µm) with NO gate — while its inputs `last`/`offsets` are R104/R105-faithful. MEASURED (GAPDBG, internal on-grid of gap ExPolygons, rust engine): inputs **0.0% on the 1µm grid (full-res)** but detected_gaps output **100.0% on-grid** ⟹ the geo ops re-gridded faithful inputs, fragmenting thin gap regions into ~13x too many slivers (885 vs 67 gap-expolygons/probe-set). This is GAP-LOCAL: fixing only the ops (inputs untouched) converged gap ⟹ not inherited from the wall-floor. GCODE EFFECT (gated, gap-infill G1 XY-motion lines, layer-aligned): gap-geometry **XY multiset match 2.3%→46.9%** (20x), full-line multiset 0.1%→20.1%, output on-grid 100%→0%. Per-feature content-diff (order-independent multiset-unmatched): gap **20875→17365 (−16.8%)**, gap share 16.8%→14.4%; raw positional diff 243105→188922 (−22.3%, un-gridding also un-fragments → global LCS re-anchors). (b) REFUTED: XY multiset was only 2.3% pre-fix (pure order would be high). FIX (+33/−3, F1_UNION): `shrink`→`shrink_clib`, `grow`→`grow_clib`, `difference`→`difference_clib` (same mm deltas — geo delta is unscaled mm; clib shims take mm ×1e5; difference_clib/offset_expolygons_clib reconstruct via union_ex_clib @i32/1e5 under F1_UNION per R106). MATERIAL at native: gap dE +0.47%→+0.58% (+0.27mm, faithful-geometry-longer exposure, provably more faithful — XY match 20x), TOTAL +0.274%→+0.285%; header filament 3859.50→3859.92 (native 3858.97). Default path byte-UNCHANGED (147987 verified); gated deterministic byte-identical (R99 held). RESIDUAL gap (post-fix): ~53% XY-unmatched = INHERITED wall-floor (last/offsets full-res but only ~44% hash-match native) + E/width-per-segment (full-line 20.1% ≪ XY 46.9% ⟹ same E-per-length class as walls) + medial-axis/arc/order. Gap line count still +30% (8932 vs 6865) — other gap sources (only_one_wall_top temp_gap, thin-walls) and generation not yet swept. NEXT: L0 outer width-source (R107, ~1%) or the gap E/width + remaining gap-source gridding.

- **★ R107-MEASURE — SEAM/ARC/ORDER floor QUANTIFIED (gcode analysis on 1509c3c, no code change): the "floor" is ~80% a GEOMETRY floor — ARC-FITTER EXONERATED (do not attack), seam mostly SELF-CONVERGED.** DIFF SPLIT (243105): Outer wall 40.5%, Gap 17.4%, Inner wall 12.4%, ISI 8.8%, Top 7.3%, Floating 5.6%, Sparse 5.1%, Bridge 1.9%. ARC: G2/G3 14150 vs 11906 (+18.8%) BUT on geometry-matching loops (centroid<20µm) segment counts match **98.9% outer / 98.4% inner** ⟹ over-seg is fully INHERITED from geometry divergence — the R79b "arc fidelity rung" is DEAD as an independent lever. SEAM: on tight-matched loops **90.3% outer / 50.2% inner** within 2µm (global was 33.1% @R100) — R79c confirmed: seam_placer picks the same candidate given matching geometry; residual = small independent seam-algo divergence (~10% of matched outer loops). ORDER: churn = seam-rotation + island emission-order tie-breaks, both present (L120 dump). L0 CAUSE PINNED (ODBG delta dump, all 240 layers): ONLY L0 mismatches — rust sources outer width from `external_perimeter_extrusion_width` (nominal 0.5 → delta −25000.0) vs native `ext_perimeter_flow.scaled_width()` (49999 → −24999.5); L1-239 coincide at 0.42; nsc was a no-op here (f32(0.5) exact). Fix = source outer-offset width from ext_perimeter_flow (verify byte-neutral L1-239) — CLEAN, ~1%. FINAL ATTACK TABLE (payoff of 243105): arc ~0% NEVER (inherited 98.9%); seam-algo outer ~1.5-2% CLEAN; L0 width-source ~1% CLEAN; island-order ~1-3% bounded; **GAP INFILL up to ~17% UNSCOPED (best next probe — separate gap-fill geometry/order path, never localized)**; L1-15-inner + overhang-split residual + complex geometry ~30-40% DEEP ClipperOffset-topology floor (may hit cz-vs-native ClipperLib limit). Clean bounded wins ≈4-6% + gap unknown. **USER DECISION: BANKED @f8af6c4.**

- **★★ R106 — HELPER-LEVEL GRIDDING BUG: `difference_clib` reconstructed its output via geo-clipper `union_polygons_ex` @1µm — silently defeating the R104/R105 clib routing it served. Fixed @cc54229 (gated, PUSHED): inner region i=1 67.0%, i=2 24.8%→49.0% (DOUBLED), overall 74.4%; common lines 68746 (+11.4% vs baseline, 46.7% match).** Stage-split on R105: inner input i=1 56.4% on-grid but offsets(i=0) pre-block 1.4% and offset2_ex_clib output 1.0% → the only_one_wall_top BLOCK still gridded despite R104/R105, via its helper: difference_clib ran the ClipperLib difference full-res then RECONSTRUCTED through geo union (top_polygons 96.3%/temp_gap 100% on-grid). FIX (+13/−2): F1_UNION → union_ex_clib reconstruction; default keeps geo (difference_clib has a default-path caller in layer.rs gap-fill — global change flipped default 147987→147978, gating restored byte-identity, stash-diff verified). RESULT: inner input on-grid 56.4→1.9% (≈native 5%); material at native (+0.059%); walls held; R65 untouched. Diff-marker 197194→243105 UP = the newly byte-matching inner walls now carry the long-standing SEAM/ARC/ORDER divergence (R79 class) — content converged, order churns (exposure). RESIDUAL inner (i=1 33% off) is NOT gridding → seam/arc/order = the next frontier. AUDIT FLAG → R107: other *_clib helpers may share the geo-reconstruction bug (offset2_clib; grep union_polygons_ex in helpers) — exhaustive sweep funded.

- **★ R105 — REMAINING only_one_wall_top SUB-PATHS UN-GRIDDED @1a7c776 (gated, PUSHED): gated diff-marker drops BELOW baseline for the first time (243734→197194 vs baseline 212604); R104's ordering churn fully resolved.** PREMISE CORRECTION: `upper_slices` was NOT the residual source — it's already full-res (0.0% on-grid; benchy single-region, lslices=to_expolygons, no geo). The residual gridding came from THREE geo sub-paths R104 missed, all feeding `last`: fill_clip = offset_ex(last,−ext_perimeter_spacing); temp_gap = diff_ex(top_polygons, fill_clip) unioned into last; the bridge_checker offset_ex/diff_ex/union_ex chain re-gridding inner_polygons on bridge layers. All routed through clib (offset_expolygons_clib/difference_clib/union_ex_clib; bridge emptiness boolean stays geo). RESULT: common lines 65085→65132 (+3420 vs baseline); rust raw 147138; material TOTAL −0.017% (at native); walls held; default byte-unchanged; **but INNER REGION FLAT (i=1 60.9→61.2%, i=2 24.8%)** — the win is gap-fill/top-classification/bridge toolpaths, NOT inner walls. R103b also landed @c55279c (cz_offset2_ex stepB delta2-sign dispatch, byte-inert, correctness for delta2>0 callers). NEXT (R106): stage-I re-dump on R105 decides (a) hidden `last`-gridding source remains vs (b) genuine successive-offset divergence (then per-loop delta on first/top layers [R101 nsc covered only 0.42] before any reconstruction/FP theory). Compounding signature (i=1 61%→i=2 25%) suggests per-iteration re-divergence.

- **★★ R104 — INNER-WALL ROOT CRACKED + FIX LANDED @a9539c0 (gated, PUSHED): the `only_one_wall_top` last-chain re-gridded `last` at 1µm between i=0 and i=1 — 5th hit of the R100 gridding class. Inner region i=1 hash 0.3%→60.9%, common gcode lines +3373, material TOTAL −0.005% (at native).** Weaver's initial "offset2 build-level FP floor" verdict was WRONG (the escape-hatch class, 6th refutation) — the stage-split probe settled it: stepA control (slice-closing) 747/747=100% (shim+hash sound); the inner offset's INPUT matched native only 0.1% and was 99.8% ON the 1µm grid (native 5.2%) ⟹ outcome (a): the op inherits a gridded input; the offset2 op is FAITHFUL. ROOT: `last = intersection(inner_polygons, last)` + top_polygons/inner_polygons chain (perimeter_generator.rs, only_one_wall_top block, i==0; native PerimeterGenerator.cpp:1169+868/876/886) ran through geo-clipper @1µm before feeding ALL inner offsets and the infill-area computation (also explains why the Lever-A infill un-grid only half-worked). DELTAS integer-identical (DELTADBG: distance=39207 min_spacing=24424 d1=51418 d2=12211) — inner-delta port refuted for good. FIX (+72/−17, F1_UNION): last-chain via difference_clib/union_ex_clib (intersection as A−(A−B)); inner successive offset via faithful offset2_ex_clib + integer coord_t delta. RESULT: inner input on-grid 99.8%→53.3%, region i=1 0.3%→60.9% / i=2 0%→24.8% / overall 38.2%→66.0%; common lines 61712→65085 (42.1%→44.2%); filament 3855.52→3859.70 (native 3858.97), TOTAL dE −0.005%, inner −0.03%, outer held; default byte-unchanged. Diff-marker +31k = LCS ordering churn (seam/arc/order not yet converged) while CONTENT converged — exposure class. RESIDUAL: remaining 53.3% gridded inner inputs are TOP-SURFACE layers — `upper_slices` is pre-gridded at ITS source (R105: un-grid upper_slices where computed; routing the upper offset@868 alone was a measured no-op). Then seam/arc re-alignment on matching geometry. LATENT (approved, not yet landed): cz_offset2_ex delta2-sign dispatch (should use expand_paths for delta2>0; byte-identical for benchy).

- **R102d — FINAL INVESTIGATION (no commit): inner-delta port refuted 3rd time (inner hash 0/676 even on R102c-faithful outer regions, diff regresses → NOT the delta, distinct root); bridge_checker@854 un-grid SKIPPED (balloons: 4 geo-clipper ops, classification-only). GEO-CLIPPER SWEEP (report): NONE left on the wall-offset chain (walls fully clib); remaining geo-clipper in perimeter_generator.rs = thin-wall path (off for benchy: 535/548-554/574), gap-fill (649-652), top-surface/bridge_checker (819-896), INFILL-AREA computation (1098-1159) — the latter feed the pre-existing ISI/floating CLASSIFICATION divergence → NEW HYPOTHESIS: the ~15% classification gap may ALSO be the 1µm-gridding class (route 1098-1159 through clib in a future round). NEXT-SESSION WALL LEVER: ITERDBG i=2 (multiset-per-layer) — does the FIRST inner offset diverge on faithful input? (R102c's grow fix already un-grids inner overhang boundaries, so suspect the inner offset op/conversion itself.)**

- **★★ R102c — OVERHANG-GRID ROOT CONFIRMED + FIX LANDED @818efaa (gated, PUSHED): outer-wall contour hash 38.5%→82.8%.** The overhang-split suspect (below) was right, and the mechanism is the R100 gridding class AGAIN: `detect_overhang_wall` splits outer perimeters at overhang boundaries from `generate_lower_polygons_series` = grow(lower_slice, [off_front, +nozzle/2]) — rust grew via geo-clipper @1µm → gridded boundaries → gridded/shifted vertices INSERTED into the outer contour where walls cross overhang regions; holes rarely overhang (explains O/c 38.5% vs O/h 71.9%). FIX (perimeter_generator.rs:1598, +20/−2, F1_UNION): route both lower-slice grows through `offset_expolygons_clib`. RESULT: outer contour 38.5%→**82.8%**, outer hole 71.9%→**91.6%**, total wall hash 24.1%→**44.6%** (635/1425); diff lines 146207→145929; default byte-unchanged (147987 verified); outer-wall E toward native (+0.48%→+0.21%), total −0.20% (delta = pre-existing infill classification jitter, exposure precedent). REMAINING: inner walls 0% (re-run R102 port on now-faithful outer regions — R102d); outer top-surface `bridge_checker` (perimeter_generator.rs:854) still geo-clipper-gridded; per-loop delta on non-0.42-width layers.

- **R102/R102b/R102c/R103 — INVESTIGATION ROUNDS (no commits, all reverted; tree clean @fa442f8). Net: inner walls + material collapse to ONE open question — WHY do 54% of outer-wall offsets diverge on matching input? — now localized to OUTER CONTOURS with the OVERHANG-WALL SPLIT as top suspect.** R102: narrow inner-delta port (scaled coord_t spacings + single-pass offset2_ex_clib) REFUTED — inner hash stays 0/676 because the successive-offset region chain diverges at the outer step (ITERDBG i=0 96%/layer, i≥1 ~0%); no inner-delta fix can work. R103: the "E-per-length +0.5%" nit REFUTED — native f32 mm3_per_mm vs rust f64 differs 3.1e-8; rust E/len is actually 0.12% LOWER; the outer E delta is rust walls being 0.66% LONGER (the 54% diverging geometry) → R101's material −0.11% = geometry path-length exposure, resolves when walls match; true E-byte work (f32-trunc mm3_per_mm + E-string format) deferred LAST. R102b: fill-rule reconstruction difference (native single-ExPolygon offset_ex uses do_union=false→pftEvenOdd vs rust NonZero re-union) TESTED = zero byte change on benchy; also re-measured ITERDBG cleanly (the "region divergence" was just the AND of per-loop matches). R102c (WALLDBG split by role×contour/hole): outer CONTOUR hash 38.5% (n-count 78.5%) vs outer HOLE 71.9%, inner 0% — the 54% is CONTOUR-concentrated. SHARP CONTRADICTION: input `last` matches 96%/layer, ClipperOffset deterministic, params identical (jtMiter/ML3.0/SEF0.005) ⟹ the plain offset CANNOT drop to 38.5% — something mutates the outer contour AFTER the offset. TOP SUSPECT (explains contour≫hole): **detect_overhang_wall** — outer perimeters are SPLIT at overhang boundaries derived from m_lower_slices_polygons = offset(lower_slice, +nozzle/2), inserting vertices into the contour; holes rarely overhang. **NEXT SESSION (R102c-2 probe, teed up): pair each outer loop's INPUT-contour hash with its OUTPUT hash both engines (matching-in→non-matching-out ⟹ overhang-split/conversion, not offset); then byte-compare m_lower_slices_polygons (lower grown nozzle/2) + the overhang clip rust vs native.** Secondary candidates: per-loop delta variation on non-0.42 layers; PerimeterLoop→ExtrusionLoop conversion.

- **★ R101 — OUTER-WALL FLOW-WIDTH SCALING (F2 class): the R100 sub-micron miter residual was the FLOW WIDTH → coord_t conversion, not the offset delta. Gated fix LANDED @7d3f93c, PUSHED.** The R98-style offset-delta f32 truncation was a NO-OP for benchy (deltas already f32-exact — audited, zero byte change). DELTADBG (bit-exact, both engines): native `Flow::scaled_width()` = `coord_t(scale_(m_width))` with m_width stored FLOAT and scale_ TRUNCATING toward zero → 0.42 → f32(0.41999998) → 41999; rust carried f64 0.42 + round → 42000; outer offset delta 20999.5 vs 21000.0 → 0.5-unit inset diff amplified to tens of units at miter joins (flow.rs:238 FIDELITY-NOTE(F2) already flagged the class). `last` (perimeter input) byte-matches native **231/240** layers → R100 chain sound, divergence offset-side. FIX (+23/−1, F1_UNION-gated `nsc()` f32-trunc scaled width at the offset-delta site ONLY, extrusion width raw): outer-wall canonical-hash **0 → 45.9%** (344/749), overall walls 0 → 24.1%; default byte-unchanged. GUARDRAIL EXCEPTION (team-lead approved): material −0.02%→−0.11%, diff lines 146103→146207 — the bit-faithful (larger) geometry EXPOSES a pre-existing rust E-per-length gap (~+0.5% outer E at identical geometry: native 710.71 vs rust 714.56) that R100's less-faithful geometry masked; exposure not regression; E lever queued (R103). NEXT: R102 = scoped F2 perimeter coord-scaling port (spacings + inner-wall single-pass offset2_ex chain, local gated helpers, NOT crate-wide flow.rs) → inner walls 0→>0; R103 = the E-per-length nit (mandatory for byte-parity).

- **★★ R100 — WALL UN-GRIDDING: the ~54% wall gap's first divergence was NOT seam — rust's classic-wall perimeter INPUT was quantized to a 1-micron grid by geo-clipper. Gated fix LANDED @658b92e, PUSHED.** Localization (WALLDBG env-gated dump of every wall loop in extrude_loop, both engines, keyed by layer; loops 1425/1425, 240/240 layers): wall-vertex on-100-unit-grid rust **99.4% vs native 3.6%** (seam coords 68.7% vs 2.3%); rotation-invariant canonical-hash match **0/1425**. Entry points (GRIDDBG: input slices 5.3% on-grid = full-res, simplify_p output 100% on-grid): (1) `ExPolygon::simplify_p` (expolygon.rs:325) → `simplify_polygons_clipper` → `union_polygons_ex` (clipper_utils.rs:275) @ GEO_CLIPPER_SCALE=1000 (1/1000mm = 100x coarser than native ClipperLib coord_t 1e5/mm), gridding `last` at perimeter_generator.rs:417 BEFORE any wall offset; (2) `offset_expolygons_clib` (clipper_utils.rs:1073) reconstructed clib offset output via the same geo union — shrink_clib/offset2_clib were vertex-COUNT-exact (R91) but position-RE-gridded. Global `GEO_CLIPPER_SCALE` 1000→1e5 diagnostic = NON-VIABLE (21min/21GB RSS runaway; the narrow clib fix is the right shape). FIX (3 files +41/−3, all F1_UNION-gated, mirrors the R91 slice chain at triangle_mesh_slicer.rs:1770): classic `last` = DP full-res (`simplify_p_dp_rings` split) → `simplify_polygons_clib` + `union_ex_clib`; `offset_expolygons_clib` reconstructs via `union_ex_clib`. VERIFIED: default path byte-IDENTICAL with fix (inert w/o F1_UNION); gated: on-grid 99.4%→**3.6% (= native exactly)**, diff lines 146958→146103, G2/G3 14265→14089 (over-seg +19.6%→+18.3%), vertex-count match 59.6%→73.5%, seam-within-2µm 22.8%→33.1%; material −0.02% (walls ≤+0.76%); slice path untouched by construction (changed helpers reachable only from walls/gap-fill — verified callers; slice uses separate offset2_ex_clib). NOTE: benchy resolved config = CLASSIC wall generator (not Arachne) — R99 #4's geometric-key deviation is moot here; `generate_arachne` (perimeter_generator.rs:1969) still needs the same clib chain for Arachne configs (deferred). RESIDUAL wall divergence (now measurable on un-gridded geometry): SUB-MICRON vertex noise — on a matched loop ~half the verts byte-identical, rest differ <70 units (0.2–0.7µm) at miter joins, bbox off by exactly 1 unit → feeds seam mismatch (67% still differ) + residual arc over-seg. NEXT: (a) byte-compare rust `last` vs native per layer — pin the sub-unit input delta (R98-style round-vs-truncate / f32-boundary suspect); (b) arc-fitter tolerance/f32 audit (may be independent); (c) Arachne-path clib chain.

- **★★ R99 — RUST GCODE NOW DETERMINISTIC (default path @f3c3036). BANK POINT for this arc.** Rust emitted different gcode every run (a hard gate on byte-parity); root class = the port mirrored C++ `std::unordered_map`/`unordered_set` (stable-per-run) with Rust `HashMap`/`HashSet` (per-run-random `RandomState`), + one ASLR-leaking pointer-set. 4 sites fixed (one dominant Arachne cascade): #1 `fill_rectilinear` ACO `thread_rng`→faithful `mt19937_64` seed 5489 (ISO-verified, native-matching); #2 `region_expansion` bridge-dir HashMap→BTreeMap; #3 `fill_floating_concentric` `.iter().next()`→`.min()`; #4 `skeletal_trapezoidation` wall-start ptr-set→min-by-geometric-key; +`tool_ordering` sweep. Result: rust 3× byte-identical, material-neutral (8/10 gcode features byte-identical; 2 fill-chaining features move within pre-fix jitter), slices 238/239 + R65 intact. Determinism holds on the gated path too. **MEASURED gated byte-parity gap (F1_UNION+FRAME_UNIFY+SLICE_SIMPLIFY+SLICE_CENTER, inline config): native 139498 vs rust 146958 lines (+5.3%).** Concentration BY LINE-SHARE of the diff (the byte-parity-relevant metric; ~75% of lines have no exact content twin, only 24.9% match): **WALLS DOMINATE (~54%)** — Outer wall 41.2%, Inner wall 13.2%; Gap infill 14.7%; all infill ~28% (ISI 9.0% + Top 7.7% + Floating 6.0% + Sparse 5.3%); Bridge 2.1%. So the ISI/floating fill-CLASSIFICATION split (R69–R77) is only ~15% combined — the block-count view (ISI 389 vs 242) OVER-weighted it; by actual gcode lines the gap is **WALL TOOLPATH EXPRESSION**. Mechanism: **SEAM start-point divergence + ARC over-segmentation** (G2/G3 11923 vs 14265, +19.6%; G1 +19%) — every wall loop starts at a different seam vertex and arc-fits differently (R79b/R79c floor). First divergence = trivial machine-start template (`G29.1` vs `M629`). Material at native (−0.1%) → it's toolpath-expression (seam/arc/order), NOT geometry/volume. **DECISION: BANKED here** — 75% of lines differ, dominated by wall seam/arc fidelity = a multi-session floor. **Biggest lever (corrected): WALL seam-start + arc-fitting fidelity (~54%), NOT the classification.** IMPORTANT: the R79c–R79g attribution of seam divergence to the "+93-vert admesh mesh" is STALE — that mesh was solved (R80 admesh repair, R87 frame-unify → gated verts bit-match, slices now 238/239 match), so the seam/arc divergence on MATCHING geometry needs FRESH investigation (seam placer faithfulness on the full model + arc-fit fidelity), not the mesh. Then the ISI/floating classification (~15%, R71–R77 @1e5 process_external — retest now that slices match) + per-feature.

- **★★ R96–R98 — SLICE BYTE-MATCH ESSENTIALLY COMPLETE: per-layer slice ExPolygons match native 0/240 → 238/239 (structure nex/nholes 240/240). Gated @e99f0cc (R96 7b8b011 + R97 c054370 + R98 e99f0cc), UNPUSHED, default byte-unchanged, R65 intact, material at native.** The R88–R94 union hunt was a 7-round detour — the real slice root was NOT the union/clipper (all proven byte-faithful) but a **skipped post-union morphological close**: rust never threaded `slice_closing_radius` (0.049). **R96** — thread closing_radius into `MeshSlicingParamsEx` + a faithful `offset2_ex` via a new `cz_offset2_ex` shim (ClipperLib @1e5, not geo-clipper @1e3); L0 contour npts → exact C++ (1092/205). **R97** — the 11-layer hole-nesting residual (L0+L43..L52) was a rust EXTRACTION bug: `polytree_to_grouped` interleaved nested contours between a parent's sibling holes → hole mis-attached; 9-line fix (emit all holes before recursing) → nex/nholes 240/240. **R98** — the 43-layer 1–2-vertex residual was a double-vs-float DELTA truncation: native `offset2_ex(const float delta)` f32-truncates `scale_(0.049)`=4899.99987 → 4900.0; cz passed the un-truncated f64 → shifted negative-offset points ~1 unit; 2-line f32-truncation fix → 238/239. Each residual was mis-called "compile-env / IntPoint / F-class FP floor" and cracked by dumping/localizing instead — byte-identical clipper source+input ⟹ identical output, so always localize (pass1→pass2→extraction; grow→shrink→delta) before blaming the build. **Remaining slice nits (negligible):** L0 (8-hole cabin floor) 2 verts; a z-list off-by-one (rust dumps 239 layers vs native 240, `rust L_i ≡ native L_{i+1}`). **NEXT BLOCKER (was): rust non-determinism — FIXED in R99 (below). Remaining for gcode byte-parity:** the ISI/floating fill-classification split + arc over-segmentation + the pervasive downstream F1-walk (route fill/perimeter clipper ops through the faithful @1e5 shims). See R99 for the measured gap.

- **★ R81 — FRAME SUB-LEVER CLOSED: there is NO frame offset (the X-frame was a MIRAGE).** Tested (b)
  export-origin-alone → measurement decided it: the frame is ALREADY aligned, nothing to fix. ALGEBRA (full
  C++ chain): shift = instance_translation_xy (PrintApply.cpp:149) `+= m_center_offset` (PrintObject.cpp:108);
  m_origin = unscale(shift) (GCode.cpp:5244); point_to_gcode = unscale(p) + m_origin (GCode.cpp:7591); slice
  frame = raw − center_offset. NET gcode = (raw − center_offset) + (instance + center_offset) = **raw +
  instance**; the center_offset CANCELS. slicer_cli STL → add_instance at offset 0 (no arrange/center;
  multi-plate-3MF branch skipped) → instance = 0 → **C++ gcode = raw**. rust slices raw + export-origin 0 →
  also raw → frames MATCH with no rust change. (The R79e/f "net = raw − 2·center" was wrong; R79f's
  "export-shift toward native" was confounded by the then-unfixed mesh.) EMPIRICAL: with mesh fixed (R80,
  default), benchy object bbox native vs rust agree within micron rounding (Xc 0.8200 vs 0.8195, Δ~0.001-0.002
  — NO 0.8245/2·center shift). The R79b "first-div X.936/X3.146" = DIFFERENT START POINTS on the same loop
  (dx 2.21 ≠ dy −1.726, non-uniform → not a translate), i.e. seam/path, not frame. So R79c-g's "0.8245 offset"
  was the slice-INTERNAL frame (cancels in gcode) — invisible in the output; the real mesh root (+93) was the
  separable issue, now fixed (R80). **No source change (measurement-only); frame-pair stays gated+dormant
  (export-origin=0 is already the default).** REMAINING byte-parity gap = PATH GENERATION on the already-correct
  frame: rust emits ~18k more moves (114698 vs native 96495; only 11995 identical as multisets) — driven by
  per-feature fill CLASSIFICATION (Floating 122→173, ISI 389→237, Sparse 193→180 — the long-standing R69/R71-74
  near-cancel split, possibly F1-geo-clipper-tied), seam-start-on-different-loops, and arc-fitting (+25.7%).
  NEXT lever (if byte-parity pursued): re-localize on the now-correct mesh+frame — do perimeter loops byte-match
  C++ now? → splits "seam+arcs (bounded)" vs "fill-classification (deep R69/F1)".
- **★ R80 — ADMESH REPAIR LANDED (the +93-vert mesh root SOLVED; material-neutral, faithful).** R79h's
  "2284-line bail" was OVER-scoped: PHASE-0 ground truth showed benchy is MANIFOLD after the exact check
  (conn3 = number_of_facets) → C++ SKIPS the bit-sensitive nearby-merge entirely (the guard
  `connected_facets_3_edge < number_of_facets` is false). So the +93 decomposed into just (1) 552 degenerate
  facets removed + (2) topology-based shared-vertex generation — ~270 DETERMINISTIC lines, no
  tolerance/order ambiguity. Ported `stl_repair.rs` (faithful admesh: degenerate-facet removal + exact-edge
  neighbor graph [HashEdge byte-key, −0/+0 normalized] + `stl_generate_shared_vertices` fan traversal),
  now the DEFAULT binary-STL path, replacing the exact-f32-bit HashMap dedup that wrongly kept 552
  degenerate facets + skipped shared-vertex gen (correct-beyond-parity — rust was genuinely wrong vs C++
  `from_stl(repair=true)`). RESULT (default, re-verified): **vert count 112569 EXACT** (was 112662 — the
  +93 GONE), facets 225154, manifold — all EXACT vs C++; **material-neutral** (per-feature stable: ISI
  −30.5, sparse −12.48, floating +31.55, outer +2.12; total −0.42 ≈ baseline noise); R65 floor intact
  (li=1 loops==1, repair is pre-quantize); exact-identical moves 11964→11971; build green. Landed parity
  @0aad342. The mesh is now FAITHFUL. REMAINING byte-parity blocker = the X-FRAME centering (separable last
  sub-lever): REPAIR alone leaves the ~0.8245mm slice-frame X-offset (first-div native X.936 / rust X3.146);
  REPAIR+FRAME_PAIR regresses material +9.92 because frame-pair's PRE-SLICE per-vertex mesh-translate
  perturbs the f32 slice geometry (+ R65-quantize interaction). The faithful X-frame needs either (a)
  centering INSIDE the slice-time transform (C++ make_trafo_for_slicing fused op), or (b) the export-origin
  ALONE without shifting slice geometry. frame-pair foundation is on parity but GATED (FRAME_PAIR, default-off).
- **★ R79h — (resolved by R80) suspected the admesh REPAIR subsystem (2284 lines) as a likely bail.** Scoped the
  +93-vert root. Per-stage bisect: benchy binary STL = 225706 facets → 677118 raw verts. rust dedups by
  EXACT f32 bit-key (`[x.to_bits(),y.to_bits(),z.to_bits()]` HashMap, stl.rs:158-180) — NO tolerance, NO
  repair → 112662. C++ `ReadSTLFile(repair=true)` → `trianglemesh_repair_on_import` (admesh) →
  `stl_generate_shared_vertices` → 112569. The +93 enters at the REPAIR stage rust SKIPS ENTIRELY (stl.rs:82-85
  comment admits repair + shared-vertex generation are not reproduced). C++ repair (TriangleMesh.cpp:79-160):
  stl_check_facets_exact → **stl_check_facets_nearby(tolerance)** → stl_remove_degenerate →
  stl_fix_normal_directions, then stl_generate_shared_vertices builds the index from the repaired TOPOLOGY
  (neighbor graph, not a hash). The 93 = near verts merged by stl_check_facets_nearby (tolerance =
  stl.stats.shortest_edge, ITERATED 2× with increment — DATA-DEPENDENT, not a fixed epsilon) + degenerate
  facets removed, that rust's exact-bit dedup keeps. SIZED: NOT a bounded one-tolerance tweak — the faithful
  fix needs the admesh subsystem bit-for-bit (~2284 lines: connect.cpp 743 facet-connectivity graph, shared.cpp
  263 topology traversal, stlinit 389, util 399, normals 239, stl_io 251), and matching the EXACT 93 merges
  needs admesh's exact edge-matching + union-find ORDER + iterative tolerance — hits 2 of 3 bail triggers
  (whole subsystem + order/tolerance-sensitive). Does NOT touch R65 (repair is pre-quantize). **VERDICT:
  BAIL/BANK. Material parity (1.000×) is the achieved + landed goal; byte-identical gcode is gated SOLELY on a
  full faithful admesh-repair port — a large (multi-session), bit-sensitive lever with uncertain byte-payoff
  (porting it may still not reproduce exactly 93). Every other layer end-to-end is faithful.** frame-pair
  @9892a5d banks the faithful f64 centering (gated, NOT merged). branch stl-load (scope-only, no edits).
- **R79g — SLICE ARITHMETIC EXONERATED; true root = STL-LOAD MESH (+93 verts), a discrete root
  (corrects R79f).** Funded the slice-intersection lever — it OVERTURNED R79f's "slice-intersection is the
  wall". C++ slice_facet interpolation (TriangleMeshSlicer.cpp:261-280): `t=(double(slice_z)−double(b.z))/
  (double(a.z)−double(b.z))`, `x=coord_t(floor(double(b.x)+(double(a.x)−double(b.x))·t+0.5))`, on-vertex →
  `a.x` passthrough. rust slice_facet (triangle_mesh_slicer.rs:331-358) is ALREADY BIT-IDENTICAL (same f64
  t, same floor(+0.5), same passthrough) → **slice-intersection arithmetic is FAITHFUL.** The loop-point
  bit-check is MOOT because the INPUT mesh differs: a full-mesh FNV bit-hash over ALL transformed verts
  shows **C++ n=112569 vs rust n=112662 — +93 VERTICES** (FRAME_PAIR-independent — same count with centering
  off). So rust's STL-load / mesh-repair (vertex merge/dedup tolerance or degenerate-facet/edge collapse)
  keeps 93 verts C++ merges → different triangulation → all downstream byte-divergence inherits from this.
  **STRATEGIC: every downstream layer is now EXONERATED** — slice arithmetic, centering/frame (verts that
  exist bit-match), seam placer, perimeter-gen, entity order, coords-format all proven faithful. The mesh is
  the SOLE remaining upstream root. So the +93-vert STL-load fix is a BOUNDED discrete lever (NOT the f32
  rabbit hole) and is potentially the LAST keystone — if the mesh is made identical, byte-parity could
  cascade out. NEXT LEVER (if byte-parity pursued): scope why rust keeps 93 verts C++ merges — STL-load
  vertex-merge tolerance / degenerate handling (TriangleMesh repair/its_merge_vertices). Material parity
  (1.000×) remains the achieved + landed goal. frame-pair @9892a5d banks the faithful f64 centering (gated,
  NOT merged); default unchanged (−0.44), R65 li=1 loops==1 intact, build green.
- **★ R79f — FLOOR REFINED (corrects R79e): centering is FAITHFUL (verts bit-match C++); the wall is the
  slice-INTERSECTION f32, one layer deeper (F-class; BAILED CLEAN).** Funded the fused-matmul port — it
  OVERTURNED R79e's "matmul wall" framing. C++ instrumentation (`transform_mesh_vertices_for_slicing`,
  TriangleMeshSlicer.cpp:1840): the slice tf is essentially PURE ×1e5 scale (tf[0][3]≈0.008 negligible) — the
  centering is NOT in the slice matrix, it's baked into the MESH VERTICES (C++ vert.x 5.7975 vs rust raw
  6.622, Δ=0.8245 exact; scale step byte-identical). Build = -O3 arm64 (ffp-contract=on). FIX = clean
  f64-subtract of the exact truncated-grid center_offset (NO FMA matmul needed — R79e's regression was just
  double-rounding + a spurious export-origin). RESULT: **rust verts now BIT-MATCH C++ EXACTLY** (in_bits
  40b9851f == C++, scaled out 579750 == C++), R65 floor intact (li=1 loops==1) → the centering/frame
  arithmetic is FULLY FAITHFUL. **But the SLICES STILL DIVERGE on bit-identical verts** — material +10.22
  (broad: ISI −21/sparse −6/bridge +5/top +3), seam 0%, first-div native X.936 vs rust X2.32. Same input →
  different loops ⇒ the wall is the slice-INTERSECTION f32 edge-interpolation (`slice_facet_at_zs` line-plane
  crossing), non-faithful vs C++ and entangled with the R65 quantize hack. BAILED CLEAN: byte-identical
  perimeter coords need bit-exact f32 edge-interpolation across the whole slicer — the open-ended precision
  rabbit hole (each f32 layer reveals the next). **Material parity (1.000×) is the achieved + landed goal.**
  frame-pair branch now holds the FAITHFUL gated centering (verts bit-match) + export-origin, banked for any
  future slice-f32 work (NOT merged — regresses default w/o the deeper f32 match). (Caveat: 10 verts
  bit-checked; the broad shift points to intersection arithmetic, not incomplete centering.)
- **R79e — (SUPERSEDED by R79f) suspected the fused f32 slice-transform matrix as the wall.** Built
  `slice_center_xy` per-vertex XY −center (Z untouched, R65-safe), (2) `GCodeWriter.gcode_origin` +
  `set_gcode_origin` subtracting m_origin from absolute coords at the writer chokepoint (I/J left relative,
  correct), threaded app_slice→export. Net = raw − 2·center_offset (derived: slicer_cli `set_instances` does
  shift += center_offset so m_origin = center_offset, doubling). RESULT: the FRAME IS CORRECT — slices now
  match C++ to 0.001mm (Δx 0.8245→0), export shift lands (first-div 3.146→1.495 = −2·center) — BUT material
  REGRESSES +9.76→+9.98, seam-match stays 0%. Applied BOTH exactness fixes (exact center_offset =
  unscale(trunc(center/SCALING_FACTOR)·SCALING_FACTOR), C++ truncation not rust's round; single f64-subtract
  → one f32 cast, not double-f32): bit-check UNCHANGED → the residual is NOT the center value or rounding
  sequence. ROOT (definitive): C++ `make_trafo_for_slicing` (TriangleMeshSlicer.cpp:1827-1862) FUSES the
  −center translation AND the ×1e5 scale into ONE f32 matrix-multiply per vertex (`tf=t.cast<float>(); v=tf*v`);
  rust does TWO separate f32 ops (center-subtract in mm, then ×1e5 scale) → sub-ULP drift per vertex → facet
  on-plane classification re-quantizes (the +9.98, R65-family) → loops/seam don't byte-align. The bbox
  matches to 0.001mm; the gap is SUB-ULP. **BAILED CLEAN** per the pre-set criterion: matching it requires
  reproducing C++'s fused `make_trafo_for_slicing` Eigen-f32 matmul (FMA rounding) bit-for-bit across the
  slicer — the F-class precision port scoped as bail-worthy. **VERDICT: material parity (1.000×) is the
  achieved + landed goal; BYTE-identical gcode is gated on the fused-f32 slice-matrix port (deferred).** The
  frame+export-origin mechanism is correct/gated/reusable (banked on branch frame-pair, NOT merged — it
  regresses default without the f32 port).
- **★ R79d — ROOT PINNED: constant ~0.8245mm X PLACEMENT offset (R65-XY family, BOUNDED; diagnosis-only).**
  Scoped the perimeter-geometry rung — 3 probes. DECISIVE bisect: the RAW SLICES fed into PerimeterGenerator
  ALREADY differ — at L100 every input-slice bbox is X-shifted by a dead-constant +0.8235..0.8254mm (both
  min AND max corners), dy ±0.0009 ≈ 0; n_slices match (5=5). A pure RIGID X TRANSLATION → NOT F1 geo-clipper
  (which would give variable per-loop deltas + Y noise), NOT a PerimeterGenerator bug. ROOT = `app_slice.rs`
  mesh placement: it does `mesh.translate(0,0,dz)` with the comment *"no XY centering, matching C++
  slicer_cli"* — but C++ slices land ~0.8245mm offset in X that rust never applies. This is the **R65
  family**: R65's `quantize_f32_center_roundtrip` handles only center-**Z** (Benchy center_z=24); the C++
  X/Y placement/centering round-trip (volume mesh stored bbox-centered + instance trafo re-place, in X+Y) is
  NOT replicated. Same shape as the gap-interleave empty-collection bug: a comment-asserted no-op that isn't
  faithful. (Probe-3 note: rust raw slice CONTOURS carry ~4× points [C++ 96 / rust 389] — a separate
  densification; but matched output perimeter loops are 32=32 pts, so perimeter-gen normalizes it — the
  densification is a candidate FOLLOW-ON if arcs don't converge after the placement fix.) VERDICT: BOUNDED —
  fund a faithful XY placement fix in app_slice.rs (match C++'s 0.8245mm). Expected cascade: slices →
  perimeters → seam (proven faithful) → likely shrinks the arc gap. NOT the F1 rabbit hole.
- **R79c — SEAM EXONERATED; root descends to PERIMETER GEOMETRY (diagnosis-only).** Scoped + debugged the
  seam rung: it is NOT a missing subsystem — `gcode/seam_placer.rs` is a full 3165-line SeamPlacer (aligned
  mode, wired, running). Both-engine unit-case on one matched 32-pt external loop at L100 (both finalized):
  the seam placer is FAITHFUL — both pick the SAME candidate index (k=6), SAME local_ccw_angle (−1.51531 vs
  −1.51518), SAME visibility rank (0.166/0.169), SAME comparator decision, SAME npts (32). The map's
  "12-14mm" was index-misalignment; the true order-independent per-seam delta is ~0.825mm — and it's a
  UNIFORM shift of the WHOLE candidate set (every point x ≈ rust_x − 0.825), so the seam lands on the
  identical vertex of a SHIFTED loop. NEITHER engine has the other's exact X → not a comparator tie-break.
  ROOT = the PERIMETER GEOMETRY fed into the placer: (1) a uniform ~0.825mm X offset of the loop, and (2)
  loop-SPLITTING — rust feeds 10 external perimeters at L100 vs C++ 5 (rust splits some walls into 2: C++
  npts=93 ↔ rust 65+92; C++ 32 ↔ rust 22+32). Both are PerimeterGenerator wall-placement divergences,
  BEFORE the seam placer. NOTE: material is at-native (R77) and a pure ~0.8mm translation preserves loop
  length → consistent. **The seam placer would byte-match if fed identical perimeters.** REVISED CHAIN:
  PERIMETER-GEOMETRY (offset ~0.825mm + loop-split; possibly F1 geo-clipper precision) → seam [faithful,
  waits on geometry] → arcs (partly follows wall geometry) → coords [done].
- **R79b — STRUCTURAL MAP (diagnosis-only; SEAM is the gating rung, coords already exact).** With order
  converged (R78/R79), mapped the remaining structural divergences. **FIRST body divergence = move 13**, the
  first perimeter loop's START point (native `G1 X.936 Y2.228` vs rust `G1 X3.146 Y.502`) — a SEAM
  divergence; prelude (first 12 moves) is byte-identical. **COORDINATE FORMAT IS ALREADY BYTE-EXACT** where
  geometry aligns (same decimals/precision; 11964 moves exact-match any-order) → **no F1 coordinate rung
  needed.** SEAM: loop counts match (outer 770/772, inner 667 EXACT) but only **9.2%** of native outer
  seams have a rust loop starting within 50µm (6.6% within 0.6mm) — ~84% start at a different seam point,
  median delta ~12-14mm → a real Seam.cpp PLACEMENT-algorithm divergence (nearest/aligned/rear), gating
  because it cascades into loop direction + arc segmentation. ARCS: both engines arc-fit
  (enable_arc_fitting=1); native 11906 G2/G3 / rust 14960 (+25.7%, ~3000 extra) — an arc-fit FIDELITY
  divergence (rust over-segments), SECOND to seam (partly follows from seam's loop start/direction). Body
  lines native 139498 / rust 148631 (+6.5%). **Recommended rung order: SEAM → ARCS → (coords done).**
- **★ R79 — GAP-FILL INTERLEAVE LANDED (order rung 2; material byte-unchanged).** Next divergence after
  island grouping = gap-fill EMISSION position. Native interleaves gap-fill per-island (C++ Fill.cpp:757-762
  wraps each thin_fill in its own EEC and PUSHES it into `layerm->fills`); rust's port had the loop but
  pushed an EMPTY collection — `collection.entities.push(thin_fill)` was MISSING (latent no-op), so gap
  stayed batched in thin_fills (584 blocks vs native 816). FIX (two faithful parts): (1) actually move each
  thin_fill into `fills` so gap rides the normal infill island-assignment + chaining + per-island emission;
  (2) persistent FEATURE-role dedup — the `;FEATURE` marker used a per-CALL local role; C++
  `m_last_extrusion_role` (GCode.hpp:538) is a PERSISTENT member, so consecutive same-role entities across
  separate extrude calls don't re-emit the marker — added `GCodeWriter::last_extrusion_role` (always-on,
  independently material-neutral). RESULT: **gap blocks 584→830** (native 816, ~97%; inner-wall 667 EXACT,
  outer 770/772); **MATERIAL BYTE-UNCHANGED** (feat_e2 XY-gated: total −0.43, gap +1.28, ISI −30.53,
  sparse −12.48 — all stable vs R77/R78). Localization verdict: divergence was the GENERATOR entity-tree
  placement (make_fills), NOT the emission iteration. Un-gated → default, landed parity @15bd4f7, build
  green. RESIDUAL = the ISI-grouping rung (Internal-solid blocks ~237 vs native 389 under, Floating ~210 vs
  122 over — the near-cancel ISI/floating split, now an emission/grouping difference distinct from gap).
  Dependency chain: island-order [R78] → gap-interleave [R79] → ISI-grouping → seam → arcs (G2/G3) → F1.
- **★ R78 — ISLAND-GROUPED LAYER EMISSION LANDED (byte-parity phase opens; material byte-unchanged).**
  Material is at native (R77); the remaining gap to byte-identical is STRUCTURAL (entity emission order →
  seam → arcs → coordinates). First divergence localized = entity emission ORDER. Ported C++
  `GCode::process_layer` island grouping (GCode.cpp:4340-4392): layer extrusions grouped by island
  (`lslices` by bbox area), per-island perimeters→infill, replacing the flat per-region loop (print.rs).
  Added `extrude_{perimeters,infill}_entities` (subset emit) in exporter.rs; fixed a latent bug —
  `layer.lslices_bboxes` was never populated by the rust port (the island grouping was a silent no-op
  without it). RESULT: order converges at the ISLAND level — **outer-wall blocks 746→770 (native 772,
  near-exact); gap-fill 279→584 (native 816, ~60%)**; **MATERIAL BYTE-UNCHANGED** (feat_e2 XY-gated: total
  −0.37, sparse −12.48, ISI −30.50 — stable vs R77; the raw move-set differs ~15% but that's
  deretraction-prime re-ordering + loop re-seeding from re-chaining the re-grouped entities, NOT material).
  Time 43m28s, build green. Un-gated → default (landed @2113bbc). RESIDUAL = the INTRA-region per-perimeter
  gap-fill interleave (native emits gap ~1:1 after each perimeter LOOP, 816≈772; rust still batches gap
  per-island, 584) — a perimeter-generator/emission ordering inside each island, the NEXT sub-lever toward
  full order convergence → then SEAM becomes cleanly measurable. Dependency chain: island-order [LANDED] →
  per-loop gap interleave → seam → arcs (G2/G3) → F1 coordinate byte-exactness.
- **★ R77 — MATERIAL AGGREGATE AT NATIVE (1.0000×). The emitter-pair landed.** Ported faithful FillGrid
  `fill_surface_by_multilines` (combined two-direction sweep over a SHARED copy-rotated offset base +
  `make_fill_lines_raw` + grid-align) and wired the already-ported (never-called, proven-faithful)
  `fill_base::connect_infill` once on the combined set. The blocker was NOT connect_infill (a both-engine
  UNIT-CASE replay proved rust's connect byte-exact vs C++); it was the RAW-LINE input — rust's
  rotate-then-offset put endpoints off the shared `polygons_outer` so the connect couldn't snap them. Fix =
  copy-rotate the offset base (FillRectilinear.cpp:501) + faithful `make_fill_lines` + `align_to_grid`.
  RESULT: **sparse +55→−12.48** (519.2/531.6, 78% closed); **TOTAL +67→−0.09 (3858.88 / native 3858.97 =
  0.99998×)**; time 44m22s→43m53s. Blast radius PERFECTLY contained — ISI/bridge/top/bottom/gap/walls all
  BYTE-UNCHANGED (single-direction rectilinear path untouched). Build green. The biggest single converging
  fix of the run; the connect_infill stub is retired for grid. REMAINING (all per-feature, aggregate is
  done): the **ISI −30 / floating +31.5 split** (near-cancel material; classification/attribution — rust
  narrow-floats regions native keeps internal-solid; entangled w/ the gated fragmentation work), and small
  bridge +3 / top +2 / gap +1. The remaining path to BYTE-identical is structural (seam/toolpath ordering,
  G2/G3, coordinate byte-exactness), not material.
- **R75 — infill raster `overlap=0` LANDED (faithful, biggest single material move yet).** rust passed a
  spurious `overlap = spacing*0.15` into the raster offset (layer.rs) over-extending EVERY infill line; C++
  `Fill::overlap=0` for the main filler (FillBase.hpp:183, Fill.cpp:995/1007 — `infill_overlap` flows only
  into `no_extrusion_overlap`, NOT the raster geometry). Fix = `overlap=0`. **Total +114.83→+67.33 (−47.5;
  3926.30 / native 3858.97 = 1.017×, was 1.030×)**; sparse +68→**+55**, bridge +10→**+3**, top +15→**+2** (all
  toward native); walls/gap unchanged; time 44m23s; build green. Honestly **unmasked the ISI deficit**
  (−23→**−30**) — the spurious overlap was a compensating bug inflating solid lines (completeness-over-aggregate
  per the playbook). REMAINING material levers (post-overlap): **sparse +55** (still the biggest over — overlap
  was only −13 of it; ~+10.5% line excess remains, beyond the raster offset → re-localize the grid emitter),
  **floating +31.56 / ISI −30.28** (near-cancel net; the concentric/floating emitter mis-split, R69 lever).

- **R74 — group_fills post-loop + Ord fix LANDED (faithful, −6.87 material, no regression).** Ported the
  missing C++ Fill.cpp:361-373 post-loop (`union_safety_offset_ex` + `diff_ex` vs accumulated groups) + fixed
  a real `SurfaceFillParams::Ord` defect (rust omitted C++'s first sort key — decreasing bridge_angle,
  "bridges first"). Effect: the union merges near-touching **bridge** fragments → bridge +16.82→**+10.07**;
  total 3973.85→**3967.08**. DECISIVE NEGATIVE: union is **MOOT for sparse** (599.85 unchanged) — the grid
  emitter already unions internally, so **sparse +68 is NOT a group_fills problem**; it's in the grid
  emitter's line generation on the already-unioned area. Walls/gap/top/bottom/ISI/floating all unchanged.
  NEXT material targets (now precisely localized to the EMITTERS): **sparse +68** = grid emitter line-gen
  (spacing/boundary/connect on the unioned area); **ISI −23 / floating +32** = concentric/floating emitters.

- **Material: per-feature is the metric, NOT the aggregate.** Three big subsystem fixes landed (R65 slicer +
  R67/R68 Arachne + R69 floating). Current rust 3973.85 / native 3858.97 (aggregate +115, time 45m0s vs 43m).
  The aggregate is temporarily OVER because the Arachne port CORRECTED the under-features (the Arachne pipeline
  now produces beads where it produced 0), which UN-MASKED the pre-existing **sparse +68 / bridge +17**
  over-production. Per the playbook "completeness > coincidental aggregate closeness".
- **THE ISI/FLOATING SPLIT IS ONE FRAGMENTATION ISSUE, NOT TWO DEFICITS (R69, both-engine proven).** ISI −23
  (470.9/494.1) and floating +32 (202.7/170.8) look like independent under/over, but **COMBINED ISI+floating =
  rust 673.6 / native 664.9 = +8.75, near parity**. R69 ported the faithful `FillFloatingConcentric` (Z-clipper
  `detect_floating_line` — was thought blocked, actually fine via `clipper-z-sys`/`cz_clip_extrusion`) and
  measured: native's floating filler does NOT prune bead material (both engines emit the SAME WallToolPaths
  beads); `detect_floating_line`/`resplit_order_loops` only re-tag/re-seed. The split is a DOWNSTREAM
  consequence of rust **over-fragmenting the narrow-solid fill regions ~3× (FLOATCLASS_DBG: rust 4237 fragments
  vs native 1369)** — the same surface-classification/slicing fragmentation lineage — which `detect_narrow`
  then classifies differently against `lower_internal_areas`. The faithful floating port is a real fidelity win
  (genuine floating detection + seam, deretraction-prime 186→138) and material-neutral / no-regression, landed.
  **The real lever for floating→170 AND ISI→494 is reducing narrow-solid fragmentation (group_fills surface
  merge / the slicer fragment lineage), upstream of the fill stage.** See `docs/parity/R69_floating.md`.
  REMAINING per-feature levers: **sparse +68** (biggest single material gap, pre-existing), **bridge +17**
  (pre-existing). Walls + gap-fill at parity (untouched by the infill changes).
- **R70–R73 — THE FRAGMENTATION THEORY WAS DISPROVEN FOR MATERIAL (a multi-round investigation, banked).**
  R70/R71 traced the ISI/floating/sparse material gap to rust over-fragmenting `fill_surfaces` ~2–3× (R71:
  the explosion is inside `process_external_surfaces` — shells enter clean at 404, exit at 1929 vs native 605).
  We funded the faithful fix to de-fragment: built a reusable **Clipper2-Z engine shim** (`crates/clipper2-z-sys`,
  vendored Clipper2 + USINGZ, ODR-namespaced + symbol/full-link verified), ported the faithful `wave_seeds`
  (R72), and the faithful Miter/ClipperLib **closing** (R73, which collapses the fragmentation 1911→707).
  **BUT both-engine A/B proved the surface FRAGMENT COUNT does NOT drive the material**: the closing fix moves
  the gcode by ~0 (ISI/floating/sparse unchanged) because the fill is computed on the **unioned area** (already
  identical between engines, 404 entering, area matches). So R69–R73's "fragmentation is the lever" framing is
  a RED HERRING for material. (Three roots refuted by cheap assess-first measurement before any expensive fix
  shipped: wave_seeds-approx→units-bug-artifact, F1-difference→clib made it worse, fragment-count→gcode~0.)
  **CORRECTED next-session target:** the real ISI −23 / sparse +68 / floating +32 material gap is DOWNSTREAM in
  **FILL-PATH GENERATION on the (already-correct, unioned) surfaces** — `group_fills` + the grid/concentric/
  floating emitters — NOT in process_external surface classification. Fresh both-engine localization needed
  (no current hypothesis). The Clipper2-Z shim + faithful wave_seeds + faithful closing are PRESERVED gated on
  branch `wave-seeds` (pushed, no-regression, env-gated REGION_EXPANSION_FAITHFUL/CLOSE_CLIB) — correct fidelity
  foundations to revive when the real material lever is found. Docs: `docs/parity/R70_sparse.md`,
  `R71_defrag.md`, `R72_wave_seeds.md`, `R73`.
- **THE ARACHNE PIPELINE IS NOW LIVE (R67/R68).** The keystone `SkeletalTrapezoidation` VD→half-edge graph
  builder (`construct_from_polygons` + make_node/transfer_edge/discretize/compute_point_cell_range, ~415
  lines, SkeletalTrapezoidation.cpp:92-504) was ported against the `bv::Diagram` index API and wired into
  `WallToolPaths::generate` (replacing the stub). This unblocks the ENTIRE Arachne pipeline (concentric
  infill here + the Arachne perimeter path elsewhere). Two latent bugs fixed (surfaced now the graph is
  non-empty): `collapse_small_edges` use-after-free (LinkedList rebuild moved payloads → dangled the
  raw-pointer graph; fix = `LinkedList<Box<STHalfEdge>>` for stable payload addresses) + `generate_junctions`
  size_t underflow. See `docs/parity/R67_arachne.md`.
- **Time estimate: CONVERGED** — native 43m0s / rust 43m21s (the old "1h29m vs 43m"
  line-3 divergence is **RESOLVED**; the overhang/speed trio is landed).
- **Byte-identical: NO**, but the structural subsystems now match or are faithful:
  outer-wall vertex density matches native (offset rerouted to clipper-z-sys),
  gap-fill 81% closed, seam ~89% byte-exact at established layers, arc-fitter /
  simplification / medial-axis (boostvoronoi) / chaining / retraction / overhang-trio
  all proven faithful. `lslices` and `detect_surfaces_type` are faithful **given inputs**; the mesh slicer
  was NOT bit-faithful at the Benchy hull bottom (R62's "slicer faithful" claim was overturned by R63's
  both-engine A/B) — **R65 ROOT-CAUSED + FIXED it** (the f32 center round-trip below; floor now slices
  exactly as C++).
- **The remaining residual is ONE lever — ROUND 63 (both-engine A/B) RELOCATED it from the fill
  stage back to the MESH SLICER (F2), overturning R62's "fill reclassification / slicer ruled out"
  framing.** R62 inferred slicer-faithfulness from code reading and never measured native's slice. R63
  instrumented BOTH engines through the cascade: at **layer 1 (pz=0.4) rust's slice carries 8 spurious
  ~10mm² holes (~86mm² total) that C++ does not** → li=2 over-classifies ~290mm² as BottomBridge → steals
  from InternalSolid → the ISI leftover fragments into narrow slivers → `FillConcentric` starves them.
  internal-solid −60..−80, floating −64, bridge +15 ALL fall out of this one slicer divergence;
  `detect_narrow`/`FillConcentric`/`lslices` are faithful given inputs. **R63.5 correction:** the
  `make_expolygons:1312-1313 scale()` suspect is a runtime NO-OP (closing_radius=0 → pure union) and the
  `closing_radius=0.049` lever is refuted by magnitude (10mm² holes can't be sealed by a 0.049mm close).
  Real root = **F2 mesh-slicer on-plane facet classification** at the near-horizontal cabin floor
  (exact-f32 z==slice_z) — VINDICATES R61. NO faithful fill-stage fix converges it. **R64 took the decisive
  raw-loops-before-union measurement (both engines, branch `f2-rawloops`): at z=0.3/li=1 C++ `make_loops`
  emits 1 clean loop, rust emits 10 (outer 545.95mm² byte-identical + 8 phantom holes ~87mm²). The UNION is
  EXONERATED (the split is in the raw loops); the bug is F2 `slice_facet`/`make_loops` on-plane cap-facet
  classification — rust's cavity closes one slice late.** **R65 ROOT-CAUSED AND FIXED (LANDED, this branch).**
  Both-engine F2TRAFO dump showed the divergence is a COORDINATE-FRAME / f32-precision gap, not the facet
  logic: C++ stores the ModelVolume mesh **f32-centered** on its bbox and re-places it via the instance
  trafo (`trafo_centered() * volume.get_matrix()`, PrintObjectSlice.cpp:60 — identity + Z translate of
  exactly +24 for the Benchy). That f32 round trip QUANTIZES geometry sitting exactly on a layer-midpoint
  slice plane OFF the plane (f32 is ~21 ULPs coarser near center_z=24): the cabin floor f32(0.3)=0.300000012
  → `f32(f32(0.3−24)+24)` = 0.299999237 (7.75e-7 below slice plane zs[1]) → clean floor. Rust stores
  vertices f64 and only casts to f32 at slice time, so it kept exact f32(0.3) == slice_z → bit-coincident →
  degenerate. **FIX = `TriangleMesh::quantize_f32_center_roundtrip()`** (bakes the f32 round trip into the
  f64 vertices before slicing; app_slice.rs). RESULT (clean A/B): li=1 raw loops **10→1** (matches C++);
  layer-3 cabin-floor ISI **13.07→38.19** (native 38.08, parity); outer-wall G1 **+212→+158** and inner
  **+578→+554** (toward native); wall+ISI material all toward native; time 42m49s; gap-fill parity; build
  green; NO guardrail regression (outer-wall G1 IMPROVED — opposite of the rejected R59 slicer-fix at +371).
  The cabin-floor cascade is resolved; the REMAINING ISI deficit (−76) is the SEPARATE distributed
  **FillConcentric no-boundary-loop starvation** (body+top narrow vertical-shell strips) — that is the next
  lever. See `docs/parity/R65_floor_z.md`, R63/R64 docs, and the R63-R65 round-log.

## What's done (verified, on the branch)

- Surface classification: removed a spurious mesh-slicer `detect_surfaces_type`
  that fragmented surfaces (3/layer → 37-44) — **Top surface → parity**.
- Gap-fill: fixed a `variable_width` mm/scaled units bug + a missing
  `douglas_peucker` pre-simplify (gap-fill is at parity; the apparent gap was a
  priming-measurement artifact).
- Arc-fitting: wired `ArcFitter` into G-code export — **0 → ~12k G2/G3 moves**
  (native ~12k); also fixed a missing arc filament-length accumulation.
- Time estimator: ran the (faithful) `GCodeProcessor` and wired its accel-aware
  time into the header (correct format `; estimated printing time (normal mode) =`).
- Per-segment **speed modulation** (overhang speed + smooth-speed): toolpath
  density now matches native (outer-wall ~40k moves, ~6.7k distinct feedrates).
- **`crates/clipper-z-sys`**: vendored BambuStudio `ClipperLib_Z` (clipper.cpp +
  `CLIPPERLIB_USE_XYZ`) via a C-ABI shim; `clip_extrusion` validated. Portable
  binary (static C++; only libc++/libstdc++ residual). Wraps in
  `crates/libslic3r-rs/src/clipper_z.rs`.

## Remaining levers (all foundational/large — tackle as separate scoped efforts)

1. **Overhang trio + time-estimate — RESOLVED (ROUND 48).** `overhang_degree` is now
   `f64`, `merge_same_speed_paths` and `detect_bridge_wall` are ported + called, the
   speed interpolation is faithful, and the **time estimate converged (43m0s vs 43m21s)**.
   Overhang-wall feature matches (90 vs 91). This whole lever is done — do not re-attempt.
2. **THE FILL-SURFACE RECLASSIFICATION (the one remaining cascade root — coupled, needs a holistic fix).**
   Material (feat_e2): internal-solid 494→414 (−80, UNDER), sparse 531→598 (+66, OVER),
   floating-vertical-shell 171→107 (−64). Cascades into the systematic extrusion-arc over (+~1250).
   **ROUND 58-62 definitively RULED OUT everything upstream** (do NOT re-chase these):
   the mesh slicer (`slice_facet`/`make_loops` bit-faithful; slice grid bit-identical;
   cabin-floor facets at z≈0.3001 → cavity open at li=1 / closed at li=2 in BOTH engines —
   rust closes it correctly), `lslices` (byte-identical to `slices`), `detect_surfaces_type`
   (rust creates the CORRECT bottom-bridge at li=2, 91.7mm²), `discover_horizontal_shells`
   (no-op), `has_voids`/`surfaces_covered` (Benchy fill_density=0.15 → C++ also nullptr),
   the geo-clipper offsets in the narrow gate (A/B clib reroute byte-identical), and
   `clip_fill_surfaces` (dead code: `infill_only_where_needed` static-false).
   **ROOT (ROUND 62, definitive):** the correctly-born li=2 bottom-bridge/internal-solid is
   **reclassified DOWNSTREAM in the fill stage** — `detect_narrow_internal_solid_infill`
   (Fill.cpp:453-546, `fill/mod.rs`) narrow-detects it → routes to Concentric/floating →
   the **`FillConcentric` no-boundary-loop bug** (`fill_concentric.rs`; C++ seeds
   `loops=to_polygons(expolygon)` at FillConcentric.cpp:30) emits ~0 for sub-spacing strips.
   Native keeps/fills it (bridge or rectilinear); rust narrow-floats + starves it.
   **WHY IT'S NOT YET FIXED — it's COUPLED (3 pieces must land together or it regresses):**
   (a) **R53 gap-fill subtraction reorder** (branch `L74-fill`) — faithful, but alone un-masks
       the deficit (material 3850→3828); (b) **`FillConcentric` boundary-loop seed** (in
       `/tmp/vshell_findings.md`) — alone OVERSHOOTS +106mm (fills mis-sized regions); (c) the
       **reclassification correction** — why rust narrow-floats what native keeps as
       bridge/rectilinear (the unresolved knot). HOLISTIC NEXT STEP: land (a)+(b)+(c) on one
       branch off `L74-fill`, verifying **per-feature** convergence (internal-solid→494,
       floating→170, bridge→237, total→3859) NOT the coincidental aggregate, and guarding the
       byte-matched outer wall (G1 ~22087/native 22053 — the rejected `slicer-fix` regressed it to +371).
3. **Clipper coordinate byte-exactness (F1).** The live clipper backend is
   `geo-clipper` at scale 1000 (1 µm grid) fed via an mm float round-trip, vs C++
   ClipperLib at scale 100000. For byte-exact coordinates, feed the C++ clipper the
   same i64 inputs (raw FFI / vendor BambuStudio's exact `clipper.cpp` as a `-sys`
   crate). NOTE: F1 is *not* the cause of the toolpath-density gap (verified —
   bumping the scale changed nothing).
4. **Seam / toolpath ordering** — perimeter/seam emission order differs; needed
   before the two G-code streams are 1:1 alignable past the headers.
5. **Byte-exact bridges/overhang** would use **Clipper2Lib_Z** (`SetZCallback` +
   Z-preserving Clipper2 offset), a later `clipper-z-sys` extension.

## Notes

- `cargo test` for the `slicer` lib is **pre-existing-broken** (unrelated
  arachne/surface/fill errors) — only `cargo build` gates are authoritative on this
  branch. `clipper-z-sys` tests pass.
- Many compiler warnings are intentional: `///` C++-reference doc comments and
  faithfully-ported-but-gated/unwired code.

## WIP branches (preserved on GitHub — building blocks for the holistic fill-reclassification fix)

All are pushed; none merged to `alex/libslic3r-parity-engine` (each is a real fix or diagnosis held back to avoid regressing parity). The holistic fix (lever #2) should branch off `L74-fill` and combine the faithful pieces, verifying per-feature.

| branch | holds | status |
|--------|-------|--------|
| `L74-fill` | R53: gap-fill subtraction reorder (C++ order: subtract before infill opening) — **faithful** | use as the BASE; alone it un-masks the deficit (3850→3828) |
| `vshell-fix` | diagnosis: the `FillConcentric` no-boundary-loop bug + analysis (`/tmp/vshell_findings.md`) | the FillConcentric fix; alone OVERSHOOTS +106mm |
| `f1-fill` / `void-clamp` / `bottom-surface` / `slicer-fix` / `lslices-phase2` / `slice-facet` | diagnosis trail that RULED OUT slicer / lslices / clamp / region-partitioning (with the data) | reference only — do NOT re-chase these dead ends |

Measurement (unchanged): `COMPARE_KEEP_DIR=/tmp/cmp devbox run -- target/debug/slicer-cli compare --config tests/configs/stl-inline-config.jsonnet`; per-feature material via `/tmp/feat_e2.py`. The merged units-fix (`make_expolygons` mm-not-scaled, `dd93fc4`) is a no-op today but required the moment any nonzero `closing_radius` is plumbed.
