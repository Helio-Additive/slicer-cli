# libslic3r-rs ↔ BambuStudio G-code Parity — Status

Goal: the Rust slicer (`crates/libslic3r-rs`, the `slicer` crate) produces G-code
**byte-identical** to C++ BambuStudio for the same job. Branch:
`alex/libslic3r-parity-engine`.

## How to measure

The two-engine compare harness runs the *same* job through C++ BambuStudio
(subprocess) and the Rust engine (in-process) and diffs the G-code:

```
COMPARE_KEEP_DIR=/tmp/cmp devbox run -- \
  target/debug/slicer-cli compare --config tests/configs/stl-inline-config.jsonnet
```

- Dumps `native.gcode` / `rust.gcode` to `$COMPARE_KEEP_DIR`.
- A clean Benchy slice takes **~17 s**. If it takes minutes, suspect orphaned
  slicer processes saturating CPU (`pkill -f slicer-cli`).
- **Per-feature MATERIAL** must be measured with `/tmp/feat_e2.py` (counts E only
  on moves with real XY motion). The naive sum (`feat_e.py`) is **contaminated by
  deretraction-priming** moves and inflates native — do not trust it.
- Track the **header filament length** and **per-feature material dE** (rust−native),
  not feature *counts* (counts are a feature-run-segmentation artifact).

## Current parity (ROUND 107 — see memory `project_byteparity_admesh.md` + `project_nondeterminism_roots.md` for the full round log; this branch's copy jumps R81→R96 because `alex/frame-unify` forked at R81. R82–R95 are on `alex/libslic3r-parity-engine` @e35e290 + memory.)

> **Branch note:** the gated slice-byte-match stack (R83 simplify, R84 F1-union, R85–R87 frame-unify/Eigen shim, R88 two-pass union, R91 simplify_p, R96–R98, R100, R101 below) lives on `alex/frame-unify` @7d3f93c, all behind env gates (`F1_UNION`/`FRAME_UNIFY`/`SLICE_SIMPLIFY`/`SLICE_CENTER`), default parity unchanged. R99 (determinism) is on the DEFAULT path. Branch PUSHED to origin (2026-07-02; standing policy: always push alex/* as backup, never main).

- **R135 — TOP-FILL RASTER MECHANISM PROVEN, FULL FIX BLOCKED ON FAITHFUL WGapFill (implemented, measured, REVERTED — tree back to R133/08ec4445, both trees clean).** DIRECT team-lead work, ground-truth via dual-engine ADJDBG (native rebuild + revert). ROOT CHAIN CONFIRMED (3 pieces beyond R134's angle): (1) native `FillMonotonicLineWGapFill::fill_surface_extrusion` (FillRectilinear.cpp:3245-3253) sets **`params2.dont_adjust = true`** ("BBS: always don't adjust the spacing of top surface infill") — native top MonotonicLine keeps NOMINAL flow spacing 37707 (native ran _adjust_solid_spacing only 49× vs rust 208×; rust adjusted top to 38260); (2) with dont_adjust the raster takes the **align_to_grid branch** (FillRectilinear.cpp:2865-2875) anchored at `Fill::bounding_box.center()` — and `PrintObject::bounding_box()` is ORIGIN-CENTERED (Point(−size/2)..(size/2), PrintObject.hpp:395) so **refpt == (0,0) EXACTLY** (no bbox threading needed — align bbox.min down to global spacing multiples); (3) native rasters over **`no_overlap_expolygons`** (FillRectilinear.cpp:3255-3258), NOT the surface expolygon — line ENDPOINTS clip at the no-overlap contour, and the WGapFill medial-axis pass covers the unextruded band. MEASURED with (1)+(2)+R134's +90 gated: raster normal-positions converge to **<1µm of native** (−19.2135 vs −19.2128 on L23; spacing exact at nominal) — mechanism PROVEN — but endpoints stay ~25-47µm long (clipped at the wrong contour) + 1 extra line, so no byte gain, and the angle churn on unmatched solids nets +822 → REVERTED. Adding (3) naively LOSES 13mm material (3846.00 vs 3858.97): rust's mono gap-fill does NOT cover the perimeter-overlap band that shrinking the raster area exposes — **the blocker is a faithful FillMonotonicLineWGapFill port: raster over no_overlap + medial-axis gap-fill of `unextruded = surface − covered(lines)` (FillRectilinear.cpp:3260-3320), landed TOGETHER with dont_adjust+align+90 as one gated cluster.** All instrumentation reverted both trees (native rebuilt clean). This is the mapped R136 — the entire top-fill (16.6k) + ISI-angle share converges through it or not at all.

- **R134 — TOP-FILL DIVERGENCE FULLY LOCALIZED (measurement round; fix attempted, measured, REVERTED — 3-layer root, angle alone regresses): the Top-surface 0.0%-XY-match mystery = fill-angle 90° flip + raster spacing delta + duplicate line.** DIRECT team-lead work. (1) ★ ANGLE: rust top/bottom/solid (Monotonic/Rectilinear) fill runs at native+90° on EVERY layer (native L0=45°/L23=135°; rust L0=135°/L23=45°; ISI deltas ~89° = same root) — the live angle formula (`config.angle + 90·layer_index`, fill_rectilinear.rs:2236/4350) is missing native `Fill::_infill_direction`'s UNCONDITIONAL `out_angle += M_PI/2` (FillBase.cpp:239). The crate HAS a faithful `infill_direction` (fill/mod.rs:1297) with the +90 — but it's DEAD CODE, never called. Grid/sparse is 90°-symmetric (two perpendicular sweeps) so the bug is invisible there (masked since R77). layer_id itself verified 0-based/correct (ANGDBG). (2) TESTED gated +90: angles match after (135°/135° L23, equal seg counts) but content-multiset REGRESSES 142594→142876 (+282) and material −1.79mm because the lines are still PHASE-shifted: (3) beneath the angle, on angle-matched L23 the raster-normal projection shows native adjusted line-spacing 0.3770 vs rust 0.3826 (~1.5% — `adjust_solid_spacing` sees a different rotated-bbox width or rounds differently) AND rust emits a DUPLICATE line (two at normal-pos −17.016, Δ=0.3µm). So top-fill convergence needs the coupled triple: +90 angle (proven) + adjust_solid_spacing fidelity + duplicate-line fix — an R113-R120-style coupled cluster in the rectilinear raster; angle alone flips every solid surface into a DIFFERENT wrong place. REVERTED to R133 (gated sha 08ec4445 verified). NEXT (R135, the mapped attack): dump native/rust `adjust_solid_spacing` inputs (rotated bbox width, nominal spacing) on L23 + find the duplicate-line source (n_vlines off-by-one? seam between two expolys?), then land angle+spacing+dup TOGETHER with a staged L23-oracle.

- **R133 — FAITHFUL RegionExpansion extended to `propagate_waves_ex` (bridge-anchor path) @HEAD: R132 only wired the main `propagate_waves`; the bridge-expansion path was still geo. Wired it to the same faithful seeds + cz_propagate_wave — content-multiset 142748→142594 (−154).** DIRECT team-lead work. Gated branch in `propagate_waves_ex` (REGION_EXPANSION_FAITHFUL → `wave_seeds_faithful` anchors + `propagate_waves_faithful` raw expansions; default keeps geo). rust lines 146662→146508 (toward native). GATES: default byte-IDENTICAL 147987 (sha 7adae05c); faithful deterministic 2× (sha 08ec4445); R65 post-slice-safe. Combined R131+R132+R133 faithful-RegionExpansion = content-multiset −324 (142918→142594). NOTE (probed + reverted this session): the ISI/Floating classification SPLIT itself is NOT a single-op gridding flip — routing the narrow-strip floating-vs-concentric intersection test (Fill.cpp:479, `intersection_ex_expolygons_polygons`) through clib was INERT (+2, counts unchanged). The main surface classification already runs the faithful wave (via expand_merge_surfaces→propagate_waves), so the residual ISI/Floating (~33k) is DOWNSTREAM in the fill-emitter/fragmentation (the deep R69-R77 subsystem), confirmed distributed not bounded. Remaining: fill-emitter subsystem + Outer-wall seam/arc geometry floor (40k, R123/R127-confirmed, not tractable) + Gap inherited-floor.

- **★★ R132 — FAITHFUL RegionExpansion WIRED (gated `REGION_EXPANSION_FAITHFUL`) @HEAD: byte-faithful Clipper2-Z `wave_seeds` + byte-faithful ClipperLib `cz_propagate_wave` (R131) together move the ISI/Floating classification toward native — content-multiset 142918→142748 (−170). The multi-increment build (R131 propagation + R132 seeds) landed; the "classification split needs unwritten foundational work" ceiling is BROKEN.** DIRECT team-lead work. Brought the `clipper2-z-sys` crate (vendored Clipper2 v1.5.4 USINGZ, `cz2_offset_z`/`cz2_intersect_open_z`) + the `clipper2_z.rs` safe wrapper (both from the wave-seeds branch, compile clean here), added as a libslic3r-rs dep. Ported `wave_seeds_faithful` + Z-path builders (`expolygons_to_zpaths64`[_expanded_opened], `merge_splits`, `polylines_merge_z`) VERBATIM from RegionExpansion.cpp:108-389 (the seed generation is byte-faithful — Z-callback intersection provenance + Z-preserving open offset). NEW: `propagate_waves_faithful` groups the faithful seeds by (boundary,src) and drives the R131 `cz_propagate_wave` ClipperLib shim — this is STRICTLY BETTER than the wave-seeds branch, whose own propagation was a geo approximation that over-expanded (+81); here BOTH seeds and propagation are byte-faithful. Gated in `propagate_waves` (REGION_EXPANSION_FAITHFUL → faithful; default keeps the geo polygon-approximation). RESULT (5-gate path = the 4 standard + REGION_EXPANSION_FAITHFUL): content-multiset **142918→142748 (−170)** (rust-unmatched −166, native −4, both converge); rust lines 146824→146662 (toward native 139498). GATES: **default byte-IDENTICAL 147987 (sha 7adae05c — new gate off by default)**; faithful path DETERMINISTIC 2× (sha d0719207); material 3859.02→3859.08 (+0.06mm, native 3858.97 = +0.003%, exposure — faithful geometry); R65 post-slice-safe. NOTE: this needed BOTH increments — a faithful propagation on approximate seeds (or vice-versa) shows no byte signal; only the full faithful path converges (as R72-R73 predicted the partial paths wouldn't). RESIDUAL: the classification is now faithful in the wave, but the ISI/Floating split still has ~33k unmatched — the remaining divergence is downstream (surface merge / fill-emitter on the now-faithful surfaces) + the seam/arc geometry floor. The `scale_`-posture + fill-emitter are the next probes.

- **R131 — WAVEFRONT-PROPAGATION SHIM `cz_propagate_wave` LANDED (unwired infrastructure) @HEAD: first increment of the byte-faithful RegionExpansion port (the blocker for the ISI/Floating classification split, ~33k unmatched — the biggest remaining chunk).** DIRECT team-lead work. Investigation resolved the structure of native `RegionExpansion.cpp`: `wave_seeds` (:278) uses **Clipper2-Z** (Clipper2Lib_Z, Z-callback intersection) but the wavefront **propagation** (`propagate_waves`/`propagate_wave_from_boundary`/`wavefront_initial|step|clip`, :391-490) is plain **ClipperLib** (Clipper1) `ClipperOffset` @1e5 — the SAME library `clipper-z-sys` already compiles. So the propagation can be made byte-faithful with existing infra; only the seeds need Clipper2-Z. FIX (clipper-z-sys shim, +~90 C++ / +FFI decl / +Rust extern): `cz_propagate_wave` ports `wavefront_initial` (jtRound, etOpenRound/etClosedLine open offset), `wavefront_step` (closed offset, orientation-aware sign + reverse), `wavefront_clip` (ctIntersection, pftPositive), and the initial→clip→(N× step→clip) loop VERBATIM — byte-identical to native by construction. The boundary bbox-trim is a result-neutral optimization (wavefront ⊆ inflated seed bbox ⟹ `wf∩(bnd∩bbox) ≡ wf∩bnd`) and is omitted. Builds clean (clipper-z-sys + full workspace link); UNWIRED (no Rust caller yet) so default+gated byte-UNCHANGED. **REMAINING for the faithful classification (next increments): (1) Clipper2-Z seeds — re-add clipper2-z-sys crate + port faithful `wave_seeds` (exists on wave-seeds branch, ~320 lines); (2) Rust wrapper for cz_propagate_wave; (3) wire `propagate_waves` gated (faithful seeds → cz_propagate_wave) replacing the geo `wave_seeds_polygon_based`/`propagate_wave_from_seeds` approximation; (4) A/B. Only when seeds+propagation are BOTH faithful does the byte signal appear (faithful propagation on approximate seeds ≠ byte-faithful) — this is a genuine multi-increment build, no partial byte-signal.**

- **R130 — zone-trim `difference` → difference_clib LANDED (gated F1_UNION) @HEAD: content-multiset 142927→142918 (−9, marginal but faithful + resolves the R74 caveat).** The expand_merge_surfaces zone-trim `diff_ex(zone, expanded)` (native ClipperLib @1e5) was geo-clipper @1µm; R74 had measured difference_clib WORSE, but that was with a GEO closing — retested now that R128 makes `expanded` the full-res clib closing output, and it's net-positive (the R74 "worse" was the mixed-grid artifact). Faithful port (native uses ClipperLib here). Default byte-IDENTICAL 147987 (sha 7adae05c); gated deterministic (sha 29a15d46). Combined R128+R129+R130 = content-multiset 143538→142918 (−620 this direct-work session). **The closing-class fill sub-levers are now tapped; the remaining fill-classification residual needs the wave-propagation un-grid = the clipper2-z-sys / faithful wave_seeds port (R72-R73 infra, NOT on this branch) = the genuine multi-session F1 piece.**

- **R129 — BRIDGE-EXPANSION CLOSING un-grid + Miter LANDED (gated F1_UNION) @HEAD: the 2nd instance of the R128 class — region_expansion.rs:1647 bridge closing (native LayerRegion.cpp:375 `closing_ex(acc, closing_radius)`, Miter@1e5) was geo-clipper @1µm + Round. content-multiset 143040→142927 (−113); combined R128+R129 = 143538→142927 (−611).** Same +7/−1 gated fix: `offset2_ex_clib(&unioned, +r, −r, Miter)` under F1_UNION, default keeps geo Round. rust lines 146950→146833 (toward native). GATES: default byte-IDENTICAL 147987 (sha 7adae05c); gated deterministic 2× (sha 73e57991); R65 post-slice-safe. (The geo `union_ex` before the closing @:1646 still grids — a further sub-lever; native `closing_ex(Polygons)` unions during the offset2 grow, so the faithful form would offset2 the raw acc directly. Left minimal to match R128.)

- **★★ R128 — FILL-CLASSIFICATION CLOSING un-grid + Miter LANDED (gated F1_UNION) @HEAD: the process_external_surfaces expand_merge closing was geo-clipper @1µm + ROUND; native = closing_ex = offset2_ex(+r,−r, jtMiter) via ClipperLib @1e5. First byte-parity lever in the "R79/F1 fill subsystem" the bank packet had parked — content-multiset 143538→143040 (−498 PRIMARY), reopening the subsystem as having bounded sub-levers.** Direct team-lead work (no subagent). LOCALIZED: region_expansion.rs `expand_merge_surfaces` (native LayerRegion.cpp:503 `expanded = closing_ex(expanded, closing_radius)`) called the geo-clipper `closing(...OffsetJoinType::Round)` — the R71-R74 fragmentation source (A/B on the old branch: Round+geo n_solid 1911 vs Miter+clib 707, native 605). R74 had measured this MATERIAL-neutral and banked it, but never tested BYTE-parity. FIX (+13/−1, F1_UNION): route the closing through `offset2_ex_clib(&expanded, +r, −r, OffsetJoinType::Miter)` (vertex-exact ClipperLib @1e5, Miter per native DefaultJoinType); default keeps geo Round (byte-locked). The zone-trim `difference` @:637 left as geo (R74: difference_clib made n_solid WORSE — not this lever). RESULT: PRIMARY content-multiset-unmatched **143538→143040 (−498)** (rust-unmatched −517, native +19 = strong net convergence); LCS differing-lines 147020→146430 (−590); rust total lines 147486→146950 (toward native 139498); filament 3859.30→3859.20 (toward native 3858.97). GATES: **default byte-IDENTICAL 147987 (sha 7adae05c, verified ungated compare)**; gated DETERMINISTIC 2× (sha 1f532c44); material toward native (filament −0.10mm); R65 untouched (post-slice fill classification). NOTE: feature-COUNTS (ISI 248→244, Floating 174→175) barely moved / slightly "worse" — but those are the segmentation artifact R107 flagged; content-multiset (the truth metric) converged −498. RESIDUAL: the wave-propagation path (propagate_waves/wave_seeds, RegionExpansion.cpp) is still geo-clipper @1µm (intrinsically tied to the Clipper2-Z open-path offset infra from R72-R73, NOT on this branch) → the `expanded` input to the closing is still gridded, so this is a partial un-grid; the full fill-classification convergence needs the wave-seeds/clipper2-z-sys port (the genuine multi-session F1 piece). But the closing was a real bounded sub-lever — the "fill subsystem = monolithic floor" framing is now WRONG (like arc R123 inside "R79").

- **★★★ SESSION WRAP / BANK PACKET (R108→R127, 2026-07-03…06) — GEOMETRY BYTE-PARITY ESSENTIALLY COMPLETE; bounded-lever runway EXHAUSTED; recommended BANK pending user direction on 3 big-decision levers.** Gated diff (content-multiset-unmatched, the PRIMARY metric adopted R119) 243105-era → **143538**; default path byte-LOCKED at 147987 throughout (every fix gated F1_UNION, verified stash-diff each round). **LANDED (11 fixes):** R108 gap-detection un-grid (−22%), R111 island-order faithful chain (−7%), R116 diff_ex_safety shim, R117 scale_faithful helper, R118 faithful bridge, R119 faithful ctIntersection, R120 top chain, R122 faithful simplify_p (DP byte-exact 578/578 islands), R124 arc-center lrint-round, R125 medial-axis lrint-round. **THE ONLY_ONE_WALL_TOP BLOCK is now fully faithful** (bridge+intersection+top-chain coupled port, R113→R120). **FLOORS REFUTED 8× — the R107 "30-40% deep geometry floor" was WRONG:** it was part measurement artifact (R121 island-mispairing) and part bounded bugs (offset op exonerated R112; arc was a ±1 rounding bug not a floor R123/R124). Geometry is byte-exact per island on 76%+ of remaining divergers. **BOUNDED-LEVER RUNWAY EXHAUSTED (4 clean re-probes):** arc→bounded/fixed (R123/R124), medial-axis→fixed (R125), reverse-cast class→inert (R126), seam→genuine FLOOR (R127, R110 strengthened to 100% on byte-exact loops). Material held at +0.268% (WATCH, never breached). Determinism (R99) held on both paths every round. **REMAINING = 3 BIG-DECISION LEVERS, all requiring USER direction (I did not commit autonomously):** (1) **crate-wide `scale_` posture** (rust `scale()` ROUNDS vs native `scale_()` TRUNCATES, point.rs:53-57) — HIGH blast-radius (touches byte-locked default everywhere) AND R127 measured its seam instance as **parity-NEUTRAL on benchy** (fires but converges nothing), so likely low byte-payoff; (2) **R79 toolpath subsystem** — seam now CONFIRMED floor, arc was bounded (fixed), order was R111-fixed → what remains is fill-generation classification/chaining (ISI/Floating/Sparse split, ~27% diff share), which is F1-geo-clipper foundational territory (R69-R77), a genuine multi-session effort; (3) **L0 base-slice topology** (1 layer, R106/R109-hard, negligible payoff). **RECOMMENDATION: BANK** — every bounded/clean lever is landed or refuted; the remainder is risky-systemic, multi-session-foundational, or 1-layer-negligible. Branch `alex/frame-unify` @d7311c2 pushed, default byte-locked, both trees clean, R65 intact. All state in this file + memory `project_byteparity_admesh.md`.

- **★★ R127 — SEAM RE-PROBE on the NOW-byte-exact loop set (the seam analog of R123's arc re-probe): VERDICT (a) FLOOR-CONFIRMED — byte-identical geometry ⟹ byte-identical seam; NO bounded seam bug (measurement-only, no code, tree unchanged @d0db4b2). Unlike R123 (which overturned the arc exoneration and found a ±1 center bug), the seam re-probe on the STRONGER dataset UPHOLDS R107/R110: seam is genuinely geometry-inherited/self-converged, a real floor component, NOT a lever. Bank-supporting.** Method: three triangulated gcode analyses on the committed gated dump (F1_UNION, the most byte-exact set), pairing outer loops by SEAM-INVARIANT geometry keys (centroid+area+perimeter; and point-set overlap ≥90%), then comparing the emitted seam (= travel-dest / first extrusion point; native carries it in pre-FEATURE-tag + WIPE, rust emits an explicit travel — parser handles both). RESULTS: (1) on **100%-byte-identical outer loops, seam MATCH = 12/12 = 100%** (vs R110's 90.3% on the weaker centroid-matched set) — byte-identical geometry always yields byte-identical seam; (2) **ZERO adjacent-vertex (2µm-2mm) divergences** — a seam-comparator tie-break bug (< vs <=) would flip seams to a NEARBY candidate on MANY loops; none exist, ruling out that class; (3) the ONLY seam divergences are **3 FAR (>2mm, opposite-side ~12mm) — all the SAME hull loop across 3 consecutive layers z=8.8/9.0/9.2**, on PRE-SEAM byte-identical geometry (24 shared vertices; the single non-shared point in each is the seam-SPLIT artifact, i.e. downstream of the seam choice, NOT a pre-seam geometry difference). That signature (opposite-side + exactly one feature + 3 contiguous layers + no adjacent-tie divergences anywhere) is a **cross-layer ALIGNMENT CASCADE** in spAligned `align_seam_points`/`find_seam_string` (the alignment string resolved to the opposite side in rust vs native for this one feature-string), NOT a per-loop rounding/tie-break — consistent with R110's inheritance (corr 0.98). Non-bounded (lives in the cross-layer alignment optimization) and negligible payoff (**3 of 772 outer loops = 0.4%**). **SEAM-POINT COORDINATE CONSTRUCTION probed per team-lead (seam_placer.rs:2884 `crate::scale` = ×1e5 + ROUND vs native `Point::new_scale`/`scale_` = ÷0.00001 + TRUNCATE, SeamPlacer.cpp:1496): applied the fully-faithful native `(x/0.00001) as i64` (gated F1_UNION) + built + measured → it DOES FIRE (gated sha b3757465→a1e84bd1, 82 lines / ~41 seam points shift ±0.001mm) but is **PARITY-NEUTRAL** (PRIMARY 143538 / SECONDARY 162460 EXACTLY unchanged) — the shifted seams are on loops that diverge from native for other reasons, so matching this one construction site moves nothing toward native. This is the crate-wide `scale_` posture manifesting (fires but doesn't converge); REVERTED, NOT landed (fails the acceptance bar + is the systemic posture held for the user, not a per-site seam bug).** GATES/HYGIENE: seam probe rust-only + gated (default 147987 never at risk), reverted; native trees READ-ONLY this round (BambuStudio2 + the actual compare-native `references/BambuStudio` both untouched); both trees clean, rebuilt to committed gated sha `b3757465`. **VERDICT: seam is a FLOOR, R110 STRENGTHENED on the byte-exact set — no bounded seam lever exists (the seam analog of R123 gives the OPPOSITE result: seam converges, arc had a bug). The bounded-lever runway (R124 arc + R125 medial-axis forward, R126 reverse, R127 seam) is now exhausted. Remaining are the big-decision items only: the crate-wide `scale_` posture (systemic, user-held), L0 base-slice topology (R106/R109, 1 layer), and the PARKED R79 toolpath subsystem. This is the clean continue-vs-bank point.**

- **★ R126 — REVERSE-CLASS SWEEP (rust ROUNDS/over-truncates where native TRUNCATES/keeps-fractional): BOTH flagged live-path candidates verified FIRE-then-WASH-OUT, NEITHER landed (measurement-only, no code — tree unchanged @bb3a271). The reverse class produces ZERO byte-relevant payoff on benchy; the R124/R125 forward levers were the whole realizable lrint story.** Mirror of R124/R125. Candidate #1 `print_object.rs:1278` (internal-bridge angle, `determine_bridging_angle` in `bridge_over_infill`): native `Point a = (start + v*(i*step_size)).cast<coord_t>()` (PrintObject.cpp:2515) = Eigen static_cast TRUNCATE; rust `.round()` — a reverse ±1 for every fractional sample. APPLIED (gated F1_UNION, truncate) + built + measured → gated **sha UNCHANGED** (`b3757465`), PRIMARY 143538 / SECONDARY 162460 FLAT. Fire-probe (BRIDGEDBG counter): the sample loop barely executes on benchy (few internal-bridge regions) and the sampled `a` point feeds a QUANTIZED nearest-anchor-line + histogram-vote for the bridge angle — robust to per-sample ±1, so the winning angle is unchanged → byte-identical. INERT. Candidate #2 `fill_base.rs:887` (grid `connect_infill`, `mark_boundary_segments_touching_infill`): native keeps `Vec2d vperp = perp(v)` FRACTIONAL and truncates only the endpoints a/b at the end via `.cast<coord_t>()` (FillBase.cpp:1132-1143); rust truncated `v`→`Point` BEFORE the perp, dropping vperp's fractional part (over-truncation). APPLIED (gated, fractional `Vec2d::new(-vn.y, vn.x)`) + built + measured → gated **sha UNCHANGED** (`b3757465`), PRIMARY/SECONDARY FLAT. Fire-probe (VPERPDBG counter): it FIRES **1116×** and the fractional-vs-truncated vperp changes the FINAL truncated integer endpoints on **every one** (diff=1116=exec) — genuinely different grid-cell traversal — yet `mark_boundary_segments_touching_infill` marks the SAME boundary segments as consumed (the thick-line ±1 endpoint shift doesn't cross a marking-decision threshold) → byte-identical output. INERT (strong: fires-with-effect-then-washes-out, R125-elephant_foot precedent). GATES (both, while gated candidate applied): default 147987 byte-lock never at risk (candidates gated), no material/R65 impact (both post-slice + gated). INSTRUMENTATION (BRIDGEDBG/VPERPDBG counters, rust-only; native READ-ONLY this round) reverted — both trees clean, rebuilt to committed state (gated sha `b3757465`). NOT TOUCHED: `seam_placer.rs:2883` (crate-wide `scale_` posture, held for the user per team-lead). **VERDICT: after R126 the bounded Point-from-double lever runway (forward R124/R125 + reverse R126) is SWEPT — the only realizable lrint levers were the arc center and medial-axis VD; every other round/truncate mismatch on a byte-relevant benchy-classic path either matches native already or fires-then-washes-out. Remaining: the crate-wide `scale_` posture decision (systemic, user-held), L0 base-slice topology (R106/R109), and the PARKED R79 seam/arc/order + fill-generation toolpath subsystem. This is the clean continue-vs-bank decision point.**

- **★★ R125 — F2 POINT-FROM-DOUBLE AUDIT (census→classify→land): MEDIAL-AXIS VD-VERTEX ROUND-FIX LANDED (gated, F1_UNION) @7d262d5. BOTH metrics improve — content-multiset 143618→143538 (−80, PRIMARY), LCS/global-diff 162542→162460 (−82, SECONDARY, own-script consistent). Second confirmed hit of the R124 lrint class.** The gap-fill medial axis builds its ThickPolyline points from Voronoi-diagram vertices via `Point(double,double)` / `Line{double,double}` / `emplace_back(double,double)` (MedialAxis.cpp:463-464, 492-493, 566, 606-607), ALL of which route through `Vec2crd(coord_t(lrint(x)), coord_t(lrint(y)))` (Point.hpp:179) — round-to-nearest (ties-even); rust `medial_axis.rs` TRUNCATED toward zero (`as Coord`) with comments that WRONGLY asserted native narrows/truncates (the same misread R123/R124 corrected for the arc center). The ±1 propagated to gap-fill toolpath XY (sites :303/:304/:580 output points) AND to the w0/w1 widths + edge-length validation (:449/:450 `edge_a`/`edge_b`, native `Line{d,d}` brace-init = `Point(double,double)` since double→int narrowing is ill-formed in a braced-init). GAP FILL IS LIVE on benchy classic (layer.rs:631 = PerimeterGenerator.cpp:1335-1364 → variable_width; ~17% diff share, R108). FIX (medial_axis.rs, +42/−10, F1_UNION-gated via cached `f1_union_vd_round()` + `vd_coord()` helper = `x.round_ties_even() as Coord` on the gated path, legacy truncate on default). The `.round()` extension/intersection sites (:664/:665/:696/:697/:960) already round (ties-away vs lrint ties-even differ only at exact-half → negligible, left untouched). GATES: default **147987 byte-IDENTICAL** (stash-diff, sha `7adae05c` both pre/post — fix is gated), gated **deterministic 2×** (sha `b3757465`), material filament **3859.30 unchanged** (WATCH +0.268% held — gap widths shift sub-unit toward native but deposited E flat), R65 untouched (medial-axis is post-slice gap-fill). Gated rust lines 147500→147486 (−14, toward native). All prior closures (R118-R124) hold. **CENSUS (F2 forward class = rust `as i64`/`as Coord` truncates where native ROUNDS via `Point(double,double)`/`Point(Vec2d)`/member `operator*`/brace-init/`emplace_back(d,d)`; vs TRUNCATE-correct via Eigen `.cast<coord_t>()`/`coord_t()`/`scale_`/free `operator*`/`operator*=`/direct-coord-assign):** swept circle.rs (only the R124 arc center was lrint; :127/:367 are `.cast`/direct-assign = correctly truncate), variable_width + perimeter_generator.rs:1540 (native `.cast<coord_t>()` = CORRECT truncate), clipper_utils/simplify/multi_point/ex_polygon (all `.cast`/`operator*=`/integer-copy = CORRECT; the F1 clip-sampling `.round()` sites are a separate class), line.rs (`.cast<coord_t>()` = CORRECT + dead), fill_rectilinear/fill_base/fill_concentric (grid+monotonic live, all `.cast`/`std::round`-matching or dead), seam_placer/edge_grid/overhang (CORRECT or dead). **ONE forward candidate verified INERT & NOT landed: `elephant_foot_compensation.rs:524`** — native `Point radius_vector(search_radius,search_radius)` (ElephantFootCompensation.cpp:386) IS `Point(double,double)`=lrint (rust truncates), but it only sizes an SDF cell-query bbox on L0 (already washed out by the slice-topology floor); applied+built+measured → gated sha UNCHANGED (`b3757465`), PRIMARY/SECONDARY flat → reverted (fails the acceptance bar; faithful-but-inert). **Reverse/other-shape flags (NOT this class, reported not landed):** print_object.rs:1278 (rust `.round()` where native `.cast<coord_t>()` TRUNCATES, internal-bridge angle — a reverse divergence for ALL fractionals); fill_base.rs:887 (extra `vperp` truncation native keeps fractional); seam_placer.rs:2883 (rust `crate::scale`=round where native `Point::new_scale`/`scale_` TRUNCATES — the DOCUMENTED crate-wide scale_ posture per point.rs:53-57, a separate F2 decision). **VERDICT: the F2 Point-from-double lrint class is now EXHAUSTED for byte-relevant benchy-classic forward sites — arc center (R124) + medial-axis VD (R125) were the two real levers; the rest of the crate's `as Coord` truncations faithfully mirror native `.cast<coord_t>()`. NEXT: the reverse `.round()`-vs-`.cast` items (print_object bridge, fill_base vperp) are a small separate sweep, or the R79 toolpath subsystem (PARKED, user bank decision).** 

- **★★ R124 — ARC-CENTER ROUND-FIX LANDED (gated, F1_UNION): the R123 bounded lever, landed. BOTH metrics improve — LCS 173973→172292 (−1681, SECONDARY), content-multiset 143877→143618 (−259, PRIMARY). The cleanest lever since R118.** Native `Point(double,double)` = `Vec2crd(coord_t(lrint(x)), coord_t(lrint(y)))` ROUNDS to nearest (Point.hpp:179, default FE_TONEAREST = ties-to-even); rust `Point::new(center_x as i64, …)` truncated toward zero (circle.rs:183) → the fitted arc center was off ±1 unit on ~75% of arcs vs native → G2/G3 I/J divergence on byte-identical loops (R123 oracle). FIX (circle.rs `try_create_circle_from_points`, +~12 lines, F1_UNION-gated via cached `f1_union_center_round()`): center = `center_x.round_ties_even()` (matches lrint's mode EXACTLY — NOT `.round()`, which is ties-away-from-zero and would diverge); default path keeps the legacy truncation. RESULT: arc-fit output match on byte-identical loops **67.7%→98.8%** (R123 measured); per-layer **80 improve / 5 regress** (net −279), L1-L20 band net −19 (improved), the 5 regressors ≤3 lines (seam/order exposure on now-faithful arc I/J). GATES: default 147987 byte-IDENTICAL (stash-diff — fix is gated), deterministic (2×), material +0.268% (WATCH held, unchanged — arc I/J don't change deposited E), R65 untouched (arc-fit is post-slice). All prior closures hold (R118-R122 geometry is upstream of arc-fit; L1-L20 improved). **NEXT R125: the F2 Point-from-double AUDIT — native `Point(double,double)`=lrint EVERYWHERE, rust `as i64` truncate at Point-from-double sites is a systemic same-class gap (arc center was one instance); census the sites, land the clean gated ones.** The 4% R123 structural residual (endpoint cascade, shared crate DP R122-class) remains a smaller secondary lever.

- **★★ R123 — ARC-FITTER PROBE (measurement-only, docs; NO code landed): R107's arc exoneration HOLDS for SEGMENTATION but a BOUNDED ±1-unit CENTER-ROUNDING divergence is exposed on the now-larger byte-identical dataset — VERDICT (b), a clean ~2-line gated fix that improves BOTH metrics (proven: LCS −1681, multiset −259). Arc is NOT a floor.** Dual-engine arc-fit oracle (ARCDBG in `ArcFitter::do_arc_fitting_and_simplify` both engines, dumping per-loop: seg types + indices + arc center/radius/start/end + tolerance, keyed by the original input-loop identity): **11227 byte-identical-input loops**; arc-fit output MATCH **67.7%**. Of the 3629 diffs, **96% (3485) are CENTER-ONLY** (segmentation + start/end points + radius BYTE-IDENTICAL — only the arc center off by ±1 unit), 4% (144) structural (a 1-unit endpoint cascade). ROOT: native `Point(double,double)` = `Vec2crd(coord_t(lrint(x)), coord_t(lrint(y)))` **ROUNDS** to nearest (Point.hpp:179); rust `Point::new(center_x as i64, …)` **TRUNCATES** toward zero (circle.rs:183, with a stale "// truncates" comment) → ±1-unit center on **74.9% of arcs** on byte-identical loops → the G2/G3 I/J params diverge (so the R121 "L80 outer +37 lines = arc over-seg" attribution was WRONG — the segmentation matches; it's the I/J center that diverges + seam). PROVEN FIX (verified by applying + measuring, then reverted per measurement-only): rust circle center = `round_ties_even()` (matches lrint's default FE_TONEAREST) → arc-fit match **67.7%→98.8%**, global **LCS 173973→172292 (−1681)** AND **multiset 143877→143618 (−259)** — BOTH improve (cleaner than R122). ~2 lines at circle.rs:183. CAVEAT: it's a DEFAULT-path change (arc-fitting runs on both paths; default 147987→147977) → must be **gated F1_UNION**. **BROADER (F2-class): native `Point(double,double)`=lrint EVERYWHERE; rust `as i64` truncate at Point-from-double sites is a SYSTEMIC same-class fidelity gap** (the arc center is one instance; audit candidate alongside the R122 scale_/Line::distance_to_squared items). The 4% structural residual = a 1-unit endpoint cascade (likely the arc-fit's shared crate DP [R122 rounded-distance class] or endpoint snapping) — a smaller secondary lever. VERDICT for the bank call: the arc-fitter is a CLEAN bounded lever (not a floor); with R122's geometry exhaustion, the next levers are this arc-center round (R124 candidate, both metrics improve) + the Point-from-double F2 audit + L0 base-slice, THEN the R79 seam/order toolpath subsystem.

- **★★ R122 — FAITHFUL simplify_p LANDED (gated, F1_UNION): the slice-simplify DP is now BYTE-EXACT on ALL 578 islands / 240 layers. Content-multiset 143947→143877 (−70, PRIMARY); LCS 173453→173973 (+520, SECONDARY exposure). The GEOMETRY SIDE IS NOW EXHAUSTED — the marginal gcode payoff confirms R121 (residual = toolpath, not geometry).** ROOT was TWO coupled bugs (R121 pinned it as "DP tie-break" — deeper: it's a tolerance-scaling bug the rounded distance masked): (1) **rust's `surface_simplify_resolution` is UNSCALED (0.2·resolution = 0.0024) but native's is SCALED (`0.2·m_scaled_resolution`, m_scaled_resolution=`scaled<double>(resolution)`=resolution/0.00001 ≈ **240** scaled units)** — a 1e5× too-small DP tolerance, so the rust DP's `dist_sq ≤ tolerance²` removal test never fires on real (scaled) coordinates; (2) the crate DP's `Line::distance_to_squared` computes the distance to the integer-ROUNDED `project_onto_segment`, which for near-collinear points on a long (anchor,floater) span ROUNDS the projection onto the point (distance 0) → removes them anyway, *accidentally masking* bug (1). Native's `line_alg::distance_to_squared` returns the pure-`double` perpendicular `(t·v−va)²` (the `nearest_point` rounding does NOT affect the return). LOCALIZATION (staged simplify oracle, per-island keyed by byte-exact input-slice first point + Python DP replay on L80's raw contour): the pure-double DP at rust's 0.0024 → 243 pts, at the scaled 240 → **235 = native** exactly. FIX (multi_point.rs + geometry/expolygon.rs + perimeter_generator.rs, F1_UNION-gated, +131 lines): `douglas_peucker_faithful` (native pure-double point-to-segment distance) + `simplify_p_dp_rings_faithful`, fed the SCALED tolerance `0.2·(resolution/0.00001)`; wired into the gated slice-simplify only. RESULT: simplify byte-exact **578/578 islands** (was ~5 layers diverging: L33/L34 dropped/L54 kept near-tolerance pts); L33/L34/L54 + sweep all close. GATES: default 147987 byte-IDENTICAL (stash-diff — fix is gated), deterministic (2×), material +0.268% (WATCH held, unchanged), R65 untouched. **SCOPE NOTE (F2-class, reported): the crate `Line::distance_to_squared` (rounded-projection) AND the unscaled `surface_simplify_resolution` are GLOBAL fidelity bugs shared by many default-path callers (arc_fitter, polyline/polygon simplify, the inner-wall simplify at perimeter_generator.rs:1311/:2531) — scoped to the gated slice DP here to protect the byte-locked default path; a global fix (+ the inner-wall simplify) is a future F2 candidate, not manifesting on the sampled offsets (R121 showed inner offsets byte-exact).** GEOMETRY EXHAUSTED — only L0 base-slice topology (1 layer, R106/R109) + the R79 seam/arc/order + fill-generation toolpath subsystem remain; the next byte-parity lever is the toolpath subsystem (or bank).

- **★★ R121 — a_last FLOOR LOCALIZED (measurement-only, docs; NO code change): the "30-40% deep ClipperOffset/slice-topology floor" R107 feared is REFUTED (8th floor refutation). On the sampled high layers the PERIMETER GEOMETRY IS BYTE-EXACT per island; the residual is the R79 TOOLPATH-EXPRESSION floor (seam/arc/order + fill-generation), NOT geometry.** ★ MEASUREMENT-ARTIFACT CORRECTION: R119/R120's "high layers = upstream-inherited, a_last_entry diverges" was a per-surface ISLAND-MISPAIRING artifact — the a_last/p0 dump overwrote per surface, capturing whichever island was processed LAST, and native (chain_expolygons order) vs rust (slices.iter order) process islands in different order → the dumps compared DIFFERENT islands (e.g. L80 "243 vs 37 pts" = two different islands with non-overlapping bboxes). Re-probed with per-island pairing (dump keyed by the byte-exact input-slice first point) + an order-independent full-island-set dump. CENSUS (21 diverging layers, `pS_sliceset` → per-island `p1_simplified` → `p2_offset_i0/i1`): **16/21 (76%: L53/L55/L75-130) have the FULL perimeter chain BYTE-EXACT** — sliceset + simplify_p + i=0 outer offset + i=1 inner offset all match on EVERY island. L80 feature-split of the (byte-exact-geometry) gcode residual: Top-surface fill 331 / Outer-wall 235 (rust +37 lines = arc over-segmentation on byte-identical loops, R79b/R107) / infill-classification (Floating/ISI/Sparse) — i.e. seam + arc + fill-generation, the R79 floor. The 5 GEOM-DIVERGE layers: **L33/L34/L54 = simplify_p chain** (sliceset byte-exact, but `simplify_p` diverges: L33/L34 rust DROPS a native near-collinear point 0.05–0.08µm; L54 rust KEEPS 3 points ~2µm off native's edge = a DP tolerance/tie-break mismatch — bounded, a faithful `simplify_p` port [DP + ClipperLib SimplifyPolygons vs BBS `ExPolygon::simplify_p`] closes it), cascading into i=0/i=1; **L0 = slice-set topology** (2 islands, the slice STAGE diverges — the known base-layer floor, R106/R109, upstream of perimeter gen). DUAL-BUILD gate not triggered: no ClipperOffset op diverges on byte-identical input (the offsets are byte-EXACT = strongest possible evidence, floor refuted by construction); the geom-divergers are simplify-algorithm-fidelity (L33/34/54) + slice-stage (L0), NOT a ClipperOffset floor. **VERDICT: the campaign's dominant remaining residual is the R79 seam/arc/order + fill-generation toolpath floor on FAITHFUL geometry — a KNOWN, previously-quantified class (R107: seam ~90% self-converges given matching geometry, arc inherited/EXONERATED, order = R111-class island/seam tie-breaks), NOT a new deep-geometry floor. Bounded geometry levers left: `simplify_p` fidelity (L33/L34/L54 + likely other single-island layers) and the L0 base-slice topology (1 layer, hard). This DECIDES continue-vs-bank: the geometry is largely done; further byte-parity gains require attacking the toolpath-expression subsystem (seam placement / arc fitter / fill generation on byte-exact regions) or the bounded simplify_p port.**

- **★★ R120 — FAITHFUL TOP CHAIN LANDED (gated, F1_UNION): upper_series un-grid + scale_faithful min_width/offset_top/inner_off deltas + top_polygons/inner_polygons ::Yes. Content-multiset 144741→143947 (−794, PRIMARY); LCS 173709→173453 (−256, SECONDARY — BOTH improve). The 4 confirmed top-chain targets L23/L32/L42/L60 close BYTE-EXACT at all stages.** Staged dual-engine oracle on the R119-confirmed a_last-OK targets (`b_upper_series→c_top_polygons→d_inner_polygons→e_lastOUT`): with the wire, ALL FOUR stages close byte-exact on ALL FOUR targets, no L1-L20 regression, lastOUT native-match 62→69 layers. **DELTA REPRODUCTION (bit-exact vs native dump on L23):** native builds the top-chain offsets in SCALED coord_t from `scaled_width()/scaled_spacing()` (= `scale_faithful`); reproduced eps=37707 pw=44999 epw=41999 psp=40707 → **min_width=44999, offset_top=87423** (`scale_(1.5·unscaled(epw+psp·(wl−1)))` minus the 0.9-reduction), **inner_off=113569** — the last needs `eps/2` as INTEGER division (`double(ext_perimeter_spacing/2)` = coord_t/int = 18853, NOT 18853.5); each scaled delta → mm via `offset_delta_mm_from_scaled_f32` (f32-cast /1e5) for `offset_expolygons_clib`. FIX (perimeter_generator.rs, F1_UNION, all 3 coupled per R115): (a) upper_series `offset(clip(upper,last_box), float(min_width))` → `union_ex_clib(clipped) + offset_expolygons_clib` (un-grids the R100/R115-gridded upper); (b) top_polygons diff → `difference_clib_safety` (::Yes, R116 shim); (c) inner_polygons diff → `difference_clib_safety` + `offset_expolygons_clib` with the scale_faithful inner_off. **EXPOSURE (why positional/Top-count drift while primary+secondary improve):** per-layer difflib 76 improve / 38 regress (net −426); material TOTAL +0.284%→**+0.268%** (toward native, filament 3859.30 vs 3858.97). Positional +614 and Top-surface count 142→144 are downstream TOP-FILL TOOLPATH churn (R79 seam/arc/order floor): the top region GEOMETRY is now byte-exact on the targets, but the top-surface FILL toolpath that consumes it is not yet faithful, so it churns on the wall-floor layers (Top-count is a segmentation artifact — Top material dE flat +5.33 vs +5.27). The remaining `e_lastOUT` divergers (L0/L33/L34/L53-55/L75-130) are all UPSTREAM-INHERITED (`a_last_entry` diverges = the deep inner-offset/wall floor, NOT top-chain — R119 split confirmed). GATES: default 147987 byte-IDENTICAL (stash-diff), gated deterministic (2×), material WATCH improved +0.284→+0.268%, R65 untouched. **NEXT: the deep wall/inner-offset floor (the a_last-diverging layers L53-130 — owns the remaining top-chain + bridge + inner residual); the top-chain is DONE for the a_last-OK set.**

- **★★ R119 — FAITHFUL INTERSECTION SHIM LANDED (gated, F1_UNION): the L2-L20 band residual was the `last=intersection(inner,last)` op, NOT the bridge and NOT the top chain. Content-multiset 145055→144741 (−314, the new PRIMARY metric); LCS marker 163161→173709 (secondary, +10548 exposure — see table). Top-surface count 143→142 = NATIVE EXACT.** RE-LOCALIZATION (dual-engine ISECTDBG stage-split oracle, both engines byte-keyed at `a_last_entry→b_upper_series→c_top_polygons→cg_temp_gap→d_inner_polygons→d2_last_isect→e_lastOUT`): on the band L2/L3/L4/L6/L8/L16, `a_last`=OK, `c_top_polygons`=EMPTY (top chain contributes NOTHING — the R118 "top chain closes these" premise was WRONG), `temp_gap`=empty, `d_inner_polygons`=BYTE-EXACT (**the bridge is NOT the driver**), yet `e_lastOUT`=+1 pt. The +1 enters ONLY at `last = intersection_ex(inner_polygons, last)`: byte-identical inputs, native single-op `intersection_ex`=147pt vs rust `A−(A−B)` double-`difference_clib`=148pt. The extra vertex is near-collinear (**0.001µm off the edge** — perp dist 0.0975 scaled units) that native's single ctIntersection drops but the second ctDifference re-processes and keeps. **★ DEAD-LIST CORRECTION (R79-style honest overturn): R118's "intersection op EXONERATED / NO cz intersection shim needed" was OVER-GENERALIZED from L7 (a closed layer where the geometry has no such vertex). 9-layer sweep: A−(A−B) closes 3 (L1/L5/L7), inserts +1 on 6 (L2/L3/L4/L6/L8/L16).** FIX (+122/−5, F1_UNION): new `cz_intersection_closed` (clipper_z_shim.cpp = `clipper_do<Paths>(ctIntersection, subject, clip, pftNonZero)`, ClipperUtils.cpp:802 `intersection_ex` ApplySafetyOffset::No) → `intersection_clib` (clipper_utils.rs, same F1_UNION `union_ex_clib` reconstruction as `difference_clib`, matching native `PolyTreeToExPolygons(clipper_do_polytree(ctIntersection,...))`); routes `last = intersection(inner_polygons, last)` (perimeter_generator.rs). CLOSES the whole L1-L20 band byte-exact AND all 7 R118-closed layers (L1/L5/L7/L9/L10/L18/L19) STAY byte-exact (oracle-confirmed). **EXPOSURE JUSTIFICATION (why LCS rose while content improved):** per-layer difflib 54 improve / 24 regress / 163 flat (net −286); positional diff 146543→146415 (−128). Every LCS regressor is stage-verified UPSTREAM-DIVERGING — the fix removes genuinely-spurious collinear points from rust's `last` on the ~69 layers whose `a_last_entry` ALREADY differs (wall-floor), so rust's line-count shifts and the global LCS cascade-re-anchors WITHOUT closing native-match. Top regressors: L92 +180 (a_last 1exp diverges grossly), L91 +51 (idem), L79 +17, L107 +10 — all a_last-DIFF layers. This is the R104/R106 exposure class (both LANDED with the marker UP). **HIGH-LAYER SPLIT (answers R118's open question):** (i) UPSTREAM-INHERITED — L53/L75/L80/L90/L91/L92/L100/L112/L113: `a_last_entry` already diverges grossly (e.g. 274 vs 77 pts) = the wall/inner-offset floor, NOT block-fixable; (ii) TOP-CHAIN TARGETS — L23/L32/L42/L60: `a_last`=OK + non-empty `top_polygons` that DIVERGES (b_upper_series 100%-on-grid rust vs 0% native = the R100/R115 gridding class) = the real top-chain lever (R120). GATES: default 147987 byte-IDENTICAL (stash-diff), gated deterministic (2×), material at native (TOTAL +0.284%, filament 3859.91 vs 3858.97 — WATCH held, was +0.285%), R65 untouched (block is post-slice). **METRIC POLICY (team-lead, R119+): report BOTH content-multiset-unmatched (PRIMARY — truer convergence, order-independent) AND the LCS marker (SECONDARY — order-sensitive, whipsaws on exposure).** NEXT R120: wire the top chain against the CONFIRMED targets L23/L32/L42/L60 (a_last-OK ⟹ full native-match closure expected, both metrics improve, no upstream exposure) — upper_series bbox-clip→union_ex_clib→offset_expolygons_clib w/ scale_faithful min_width delta + top/inner ::Yes via difference_clib_safety.

- **★★ R118 — FAITHFUL BRIDGE LANDED (gated, F1_UNION): the R113-R117 coupled-cluster fix converges. Gated diff 175185→163161 (−12024, −6.9%); 41 layers improve / 4 regress.** PHASE-1 RE-LOCALIZATION (ISECTDBG stage-split of `last` at intersection IN/OUT, L0-20, dual-engine) OVERTURNED the R114/R116 "bridge-not-L1's-root" negative: with the faithful bridge, L1 `lastOUT` closes **145→148 BYTE-MATCH** (innerP 147/147 byte-identical). R116 churned ONLY because its delta lacked the f32 cast (0.45/0.00001=45000→67500 vs native f32-cast 44999→67498.5); R117's `scale_faithful` fixes it. The intersection op (`A∩B = A−(A−B)`, double difference_clib) is EXONERATED — L7 byte-identical inputs → byte-identical lastOUT; **NO cz intersection shim needed (kills that queued idea).** FIX (perimeter_generator.rs:940, three coupled, all under F1_UNION): (a) bbox-clip lower to last_box (was unclipped); (b) ::Yes safety difference via `difference_clib_safety` (R116 shim); (c) scale_-faithful `bridge_offset` via `scale_faithful` (R117). CLOSES L1/L5/L7/L9/L10/L18/L19 byte-exact. GATES: default 147987 byte-IDENTICAL (stash-diff), gated deterministic, material at native (TOTAL +0.285%, filament 3859.93 vs 3858.97 — WATCH held). The 4 regressions (L6/L16/L112/L113, 0.4-0.6%) are TOP-NON-EMPTY layers where the faithful bridge shifts geometry but the top chain (upper_series un-grid + top_polygons/inner_polygons ::Yes + min_width/inner_off deltas) is still un-fixed — R106-class exposure, closes in R119. NEXT R119: the top chain (all pieces exist: R115 un-grid shape + R116 shim + R117 helper) → the L2/L3/L6/L8/L11-17 residual band.

- **R117 — scale_-faithful delta helper LANDED (unwired, pure addition).** `scale_faithful(mm) = trunc(f32(mm)/0.00001)` reproduces native `coord_t(scale_(m_spacing))` where the BBS macro divides by the 0.00001 f64 literal and the spacing is f32-stored: scale_(0.45)=44999, scale_(0.5)=49999, scale_(0.42)=41999 (unit-tested). `offset_delta_mm_from_scaled_f32(scaled) = f32(scaled)/1e5` converts a native scaled offset delta (post the `offset(paths, float delta)` f32-cast) into the mm value `offset_expolygons_clib` (×1e5 internally) needs — e.g. bridge_offset 1.5·44999 = 67498.5 round-trips exact. No caller yet (R118 wires it). Default 147987 / gated 175185 byte-unchanged. NEXT: R118 coordinated only_one_wall_top port (phase 1 re-localize L1 root → phase 2 wire).

- **★ R116 — diff_ex_safety CZ SHIM BUILT + ORACLE-VERIFIED + LANDED (unwired infrastructure). Shim reproduces native `diff_ex(subject, clip, ApplySafetyOffset::Yes)` BYTE-EXACT.** New `cz_difference_closed_safety` (clipper_z_shim.cpp) = `clipper_do(ctDifference, subject, safety_offset(clip), pftNonZero)`: safety_offset = raw_offset(clip, ClipperSafetyOffset=10u, jtMiter, ML3, orientation-aware per-path, NO union) — faithful to ClipperUtils.cpp:273-307/334. Rust `difference_clib_safety` wraps it (same F1_UNION union_ex_clib reconstruction as difference_clib). **SHIM ORACLE PASSED: `difference_clib_safety(last, clip(lower,last_box))` on the R113 L1 dump = native `diff_last_lower` 1329 pts BYTE-IDENTICAL.** WIRE TEST (bridge lower-diff, full faithful: clip + safety-shim + scale_-faithful bridge_offset): global 175185→174394 (−791) but **30 improve / 19 regress** (identical profile to R114's unfaithful bridge) AND **L1 does NOT close** (L1 gcode byte-unchanged) — [claimed] the bridge_checker's inner_polygons contribution is washed out by the downstream `last = intersection(inner_polygons, last)` on L1, so the bridge is NOT L1's residual root despite R113's localization. **[CORRECTED @R118: WRONG — the bridge IS L1's root. This R116 wire-test churned/failed-to-close ONLY because its `scale_-faithful bridge_offset` LACKED THE f32 CAST (0.45/0.00001=45000→67500 vs native's f32-cast 44999→67498.5, a 1.5-unit delta error). R117's `scale_faithful` (WITH f32 cast) supplies 67498.5; then the faithful bridge closes L1 145→148 BYTE-EXACT (R118 phase-1 ISECTDBG proof) and −6.9% global. The intersection op (A−(A−B)) is FAITHFUL/EXONERATED (L7 byte-identical in→out) — no cz intersection shim needed.]** Per acceptance (churns → unwire), bridge left UNWIRED for R118; the other two ::Yes sites (top_polygons@1144, upper diff) are coupled to R115 un-grid + R117 deltas → also R118. **LANDED: the shim only, UNWIRED — gated 175185 / default 147987 byte-UNCHANGED (pure addition, no existing path touched), deterministic, R65/material untouched.** R114-BAND ROOT now OPEN: L1's i=1-input divergence (145/148) is NOT closed by the faithful bridge; the driving path is elsewhere (top_polygons chain or the intersection itself) — re-localize in R118 with the shim available. NEXT: R117 delta helper → R118 coordinated only_one_wall_top port.

- **R115 — upper_series UN-GRID: WORKS BUT CHURNS ALONE, REVERTED (no commit; gated 175185 / default 147987 verified). ★ STRATEGIC PIVOT: the only_one_wall_top block is a COUPLED CLUSTER — three interlocking faithfulness gaps that must land TOGETHER.** The re-route (offset_polygons@865 → union_ex_clib+offset_expolygons_clib, F1_UNION) un-grids upper_series 100%→0% on-grid, matches native's 101-pt count, stale "clib no-op" NOTE confirmed WRONG. But: (1) R109-class DELTA — native offsets by float(min_width_top_surface) built from scale_-faithful spacings (scale_(0.45)=44999); rust passes f64 mm → upper_series full-res but **0.31µm off** (4/101 exact, max_dev 31 units); (2) downstream top_polygons diff still No-safety (R114 gap) → diverges anyway. Global 175185→175207 (+22), same failure shape as R114 → reverted per acceptance. **THE PATTERN (R113/R114/R115): only_one_wall_top has (i) geo gridding (upper_series, fill_clip), (ii) R109-class scale_/f32 deltas (min_width, bridge_offset), (iii) missing ApplySafetyOffset::Yes on ALL block differences (top_polygons@1144, upper diff, bridge lower-diff) — fixing any ONE churns because the others dominate. CONVERGES ONLY AS A COORDINATED FAITHFUL PORT, staged against native byte-identity (R113 STGDBG oracle harness).** PLAN: R116 = diff_ex_safety cz shim (raw-offset clip +10u, NO union, then ClipperLib difference; the gate for the whole block + R114 bridge); R117 = scale_-faithful delta helper (R109 shape) for min_width/bridge_offset; R118 = coordinated block landing (upper_series un-grid + 3 ::Yes diffs + faithful deltas), staged L1 byte-identity at upper_series → top_polygons → inner_polygons → last.

- **R114 — BRIDGE-PAIR FIX ATTEMPTED, FAILED ACCEPTANCE, REVERTED (no commit; tree restored, gated 175185 / default 147987 verified). **[CORRECTED @R118: the bridge fix was RIGHT in shape but failed for TWO now-understood reasons — (1) the ::Yes safety was reconstructed via union+offset (unfaithful; R116 shim fixed it), (2) the bridge_offset delta lacked the f32 cast (67500 vs 67498.5; R117 fixed it). With BOTH landed the faithful bridge closes L1 byte-exact and gives −6.9%. The "L1 washes out / not the block's driver" reading was an artifact of the two delta/reconstruction bugs, not the geometry.]** ★ SYSTEMIC DISCOVERY: rust lacks a faithful `diff_ex(…, ApplySafetyOffset::Yes)` — the safety-offset gap is EVERYWHERE difference_clib is used.** Built the 3-part gated chain: (a) clip lower to last_box, (b) safety offset (ClipperSafetyOffset 0.0001mm) via union_ex_clib→offset_expolygons_clib→difference_clib, (c) scale_-faithful bridge_offset 67498.5. FAILED: (1) L1 (the localized target) gcode BYTE-UNCHANGED (base==fix, 1775 lines); L1-15 band all +0.0 — on L1 the bridge contribution is partly clipped back out by the later last=intersection(inner_polygons,last), so the R112 snapshot divergence reaches emitted gcode less than expected; (2) 19 layers regressed >0.3% vs 30 improved, diff only −791 (churn wash) — acceptance #1 violated. WHY: native diff_ex(::Yes) = clipper_do(ctDifference, subject, **raw_offset(clip, +10u)**) — the safety offset is applied to RAW paths fed straight into the difference; rust's union+offset reconstruction detour produces different vertices → 118-layer churn without case closure. VERDICT: unlandable via approximation; needs a dedicated **cz diff_ex-with-safety shim** (raw-offset clip, no union, then difference) — which ALSO covers top_polygons@1144 + the upper diff (both ::Yes in native; rust difference_clib is No-safety everywhere) = possibly a broad residual driver. PLAN: R115 = upper_series Top-surface un-grid (bounded R100-class re-route); R116 = build diff_ex_safety shim with L1 byte-identity as the oracle (if L1 still doesn't close with the faithful shim, the band has a further root).

- **★★ R113 — L1-15 INNER ROOT LOCALIZED (stage-split, measurement-only, no commit): the R112 inner_polygons divergence = the BRIDGE_CHECKER chain — rust MISSING the lower-slice bbox clip + an R109-class delta. NOT slice-inherited, NOT the offset op — outcome (a) op-level.** Stage-split L1 (dual-engine, every intermediate): upper_slices BYTE-IDENTICAL (149/149 full-res); upper_series=offset_polygons @865 grids to 100% on-grid via geo-clipper (native full-res) BUT top_polygons EMPTY on L1 both ⟹ no cascade here (cascades on TOP-SURFACE layers — separate lever, below); inner_polygons @923 still 143/143 byte-identical ⟹ injection is LATER: the bridge_checker union (perimeter_generator.rs:940-965). BRIDGE CHAIN: `lower` (L0 slice) BYTE-IDENTICAL (1368/1368) — **FIRST DIVERGING INTERMEDIATE = diff_last_lower (rust 1254 vs native 1329 pts): rust diffs against UNCLIPPED lower; native clips first — lower_polygons_series_clipped = clip_clipper_polygons_with_subject_bbox(*lower_slices, last_box) (PerimeterGenerator.cpp:1172 → 1351 pts) — rust MISSING the bbox clip.** SECONDARY: bridge_offset delta rust 67500 vs native 67498.5 — the R109 scale_ class (native scale_(0.45)=44999 ×1.5; rust 0.45×1e5=45000 ×1.5). Cascade: diff_last_lower → bridge_checker (933 pts, ~0.1µm off) → union into inner_polygons (147/294, the 2-vs-1 topology split) → last=intersection → i=1 input (145/148) → all inner offsets inherit. FULLY EXPLAINS R112. FIX SHAPE (R114, coupled pair): (a) bbox-clip lower before the diff (~5 lines, mirror upper clip @857); (b) scale_-faithful bridge_offset (~2 lines). Payoff: bridge-condition layers (L1-15 band+), inside Inner 13% + Bridge 2% — low-single-digit est. R113b REFUTED: the geo fill_clip @981 is DEAD code in the block (reassigned before use, zero payoff — R112's flag corrected); the REAL live geo lever is upper_series @865 (its "upper pre-gridded upstream, clib no-op" NOTE is WRONG — upper arrives full-res and the geo offset grids it) feeding top_polygons→top_fills on top-surface layers = a separate Top-surface (7.9%) R100-class lever.

- **★ R112 — DUAL-BUILD TOPOLOGY PROBE: VERDICT (i) — the ClipperOffset OP IS BYTE-FAITHFUL cz-vs-native; the L1-15 divergence is LIVE-PATH-injected upstream. FLOOR NOT CONFIRMED (7th floor-claim refutation; measurement-only, no commit, both trees clean).** Case A (L1, corroborated L3/L4/L7/L15): dual-engine dump at live call sites (perimeter_generator.rs / PerimeterGenerator.cpp, keyed layer,i) — the i=0 outer offset runs on BYTE-IDENTICAL input with IDENTICAL delta f64 bits and produces BYTE-IDENTICAL output (L1 143→143 pts exact; same L3/L4/L7/L15) ⟹ cz ClipperOffset == native ClipperLib on identical input+params; cz_offset2_ex = same engine twice ⟹ inner op faithful by extension. (Standalone replay harness blocked — slicer lib test binary has 85 pre-existing compile errors — but live-path i=0 byte-identity is the replay evidence: same op, matched input, identical output.) MECHANISM: divergence enters in the only_one_wall_top block (PerimeterGenerator.cpp:1162-1201): `last` ENTERS byte-identical; temp_gap empty; but **`inner_polygons` DIVERGES** — full-res (0% on 1µm grid ⟹ NOT R100 gridding), ~3.3µm at verts 37-38 + TOPOLOGY split (native 2 ExPolygons / rust 1) → `last = intersection(inner_polygons, last)` → inner-offset input diverges (rust 145 / native 148 pts) → i=1+ INHERITS. Secondary find: fill_clip diverges via geo-clipper offset_expolygons @ perimeter_generator.rs:1038 (un-routed R100-class site — feeds top_fills, NOT last). NEXT (R113): stage-split the inner_polygons = diff_ex(last, upper-slices-derived clip) chain — localize the first diverging sub-op (prime suspect: the upper_polygons_series clip region), since `last` into the block is proven identical.

- **★★ R111 — ISLAND-ORDER TIE-BREAK ROOT CRACKED + FIX LANDED (gated, F1_UNION): the last bounded lever. Rust `make_slices` chained lslices with a simplified NN-from-index-0 instead of native's `chain_segments_greedy`; island EMISSION order (= lslices order) diverged on 67/126 multi-island layers. Fix routes through the faithful chain; total gated diff 188922→175185 (−13737, −7.3%).** VERDICT: pure (b) ORDER, ZERO geometry entanglement. Dual-engine dump (rust ISLDBG in print.rs::emit_layer_by_island + native NISLDBG at GCode.cpp:4362, both keyed by print_z, bbox-area sequence in lslices order): of 126 multi-island layers, **SET-DIFF = 0** (island sets byte-identical both engines) but **67 REORDERED** (34 exact reversals, 33 adjacent swaps of near-equal-area islands, e.g. z=19.0 swaps 1.8375e12 vs 9.94e11; odd island-counts 3/5 almost always reordered, even 2/4/6 almost always same). ROOT: both engines emit islands in lslices index order (rust print.rs:2002 / native GCode.cpp:4869), and lslices is ordered by `chain_points(ordering_points)` (Layer.cpp:69-70) = each slice contour's first_point chained. Native `chain_points`=`chain_segments_greedy` (ShortestPath.cpp:1073, KD-tree greedy); rust `make_slices` (layer.rs:1529) called a LOCAL simplified `chain_points` (layer.rs:2798, NN from index 0) — divergent on multi-island. FIX (+13/−1, F1_UNION): route make_slices through the faithful port `crate::shortest_path::chain_points` (wraps chain_segments_greedy, ShortestPath.cpp:1073-1081 exact). RESULT: island order match 59/126→**126/126** (0 reordered); gated diff 188922→175185 (−7.3%); per-feature content-multiset near-flat (−627, pure order — content was already matched, now positioned so LCS re-anchors); biggest drops Gap −337 / Inner −275 / Outer −27 (reordered layers span all features). MATERIAL unchanged (+0.285%, filament 3859.92). Default path byte-IDENTICAL (stash-diff verified, 147987); gated + default deterministic byte-identical (chain_segments_greedy deterministic — R99 held); R65 order-invariant. Instrumentation reverted both trees (native rebuilt clean). REMAINING: the deep ClipperOffset/slice topology floor (owns outer-wall 45.6% + L0 8-vs-10 + inner) and the deferred E/width byte work — all levers above it now retired (arc R107, gap-local R108, seam R110, L0-width shelved R109, island-order R111).

- **R110 — SEAM-ALGO LEVER CLOSED: (c) INPUT class, fully inherited from the wall-geometry floor (gcode-analysis only, no instrumentation, no commit).** On 195 centroid-matched (<20µm) outer loops: seam agreement <2µm = 166/195 = 85.1% (R107's 90.3% minus a ~1µm proxy floor — agreeing loops' own vertex noise median 1.41µm). The 29 divergent: ALL adjacent (<0.5mm, zero far-apart ⟹ NO term-class), ALL same candidate/vertex count, **Pearson corr(seam divergence, wall-vertex noise) = 0.980**, 26/29 noise-explained (≤3·vertnoise+3µm), the 3 largest driven by 13-122µm genuine loop-geometry deltas, min vertnoise among divergent 1.0µm (NO zero-noise divergence ⟹ no pure-FP tie-break residual). Comparator faithful both engines (strict `penalty_a < penalty_b`, SeamPlacer.cpp:711 / is_first_better:1500) — confirms R79c: same geometry ⟹ same seam; the residual is the R100-class sub-µm wall floor perturbing near-equal ADJACENT penalties past the strict `<`. Inner: centroid method inconclusive (concentric rings share centroids — mis-pairs); moot given outer verdict. **The ~1.5-2% seam payoff is INSIDE the outer-wall floor (45.6% share), not separately realizable. RETIRED alongside arc (R107) and gap-local (R108).** REMAINING LEVERS: island-order tie-breaks (~1-3%, last bounded lever) → then the deep ClipperOffset/slice topology floor (30-40%, owns L0 8-vs-10 + L1-15 inner + outer residual; dual-build probe on exact diverging input BEFORE any floor verdict) + deferred E/width byte work.

- **R109 — L0 WIDTH-SOURCE FIX REFUTED AS STANDALONE (investigation, NO COMMIT, tree clean; R107's "~1% CLEAN" estimate WRONG): the width delta is real and the 1-line fix reproduces native exactly, but L0 byte-divergence is owned by the SLICE/ClipperOffset topology floor — the fix gains ZERO native-matching lines and regresses the gated diff 188922→188953 (+31), while breaking L1 bridge neutrality.** TRUE ROOT (deeper than R107's pin, confirmed at source): BBS `scale_(v)` = `(v)/SCALING_FACTOR` with SCALING_FACTOR=**0.00001 (f64 literal, slightly >1e-5)** then coord_t-truncate → 0.5/0.00001 = 49999.9999 → **49999**; rust nsc multiplies by exact 1e5 → **50000**. Only w=0.5 diverges (0.42/0.45 truncate identically both ways — why only L0, the sole 0.5-width layer). SHELVED FIX (for the future topology-floor round): nsc = divide-by-0.00001-then-truncate (ODBG-verified: L0 delta −24999.5 = native, L1 unchanged). ACCEPTANCE FAILURES (both deterministic, 2x reproduced): (1) L1-239 NOT byte-neutral — per-layer diff exactly {L0,L1}; L1 change is BRIDGE: L0's outer-wall position defines the supported region L1 bridge detection reads — structurally unavoidable cascade. (2) L0 topology unmoved: rust L0 slice = 2 ExPolygons/8 holes at SLICE stage (upstream of width); outer blocks 5 vs native 6; L0 multiset match 0.1%→0.1%. F2-CLASS NOTE (new, cross-cutting): any rust scaling site using ×1e5 can diverge from native's ÷0.00001 wherever v/0.00001 ≠ v*1e5 in f64 — audit candidate for future scaled-width work. DECISION (team-lead): SHELVE (option a) — necessary-but-not-sufficient; bundle only with an L0 topology-floor attack. NEXT: R110 seam-algo probe.

- **R108-CODA (team-lead follow-up measurements on the R108 gated dump, /tmp/cmp_final; round CLOSED):** (1) INSTRUMENTATION: native tree (BambuStudio2) verified clean — git status empty, no GAPDBG in PerimeterGenerator.cpp; rust tree clean (comment-only reference). (2) MATERIAL PROOF: aggregate gap E/len rust/native = **1.02200** (len ratio 0.98418, nseg 5505 vs 4987) — NOT pure path-length exposure; but aggregate-over-different-geometry is the exact R103 confound and gap width varies per segment, so non-probative as a regression verdict; geometry faithfulness IS proven (on-grid 100→0%, XY multiset 20x) ⟹ exposure ACCEPTED with **WATCH flag: total dE +0.285%** (was +0.059% @R106) — the per-segment gap E/width class joins the deferred-LAST E-byte work; revisit if total drifts past ~0.5%. (3) RESIDUAL gap 53% CLASSIFIED (XY non-wipe, layer-aligned): multiset 59.5% vs positional 9.1% ⟹ **ORDER 50.4pts / GENERATION 40.5%**; inheritance control: layers with wall XY-match ≥95% → gap 84.0% (n=2, thin), walls <80% → gap 19.0% (n=41), corr 0.279 ⟹ the generation component tracks the wall floor — **NO further gap-local lever; gap converges when walls/order do.** NEW POSITIONAL SHARE TABLE (206936 layer-aligned move-line diff): Outer 45.6 / Inner 13.0 / Gap 12.2 / ISI 7.9 / Top 7.9 / Float 5.2 / Sparse 4.5 / Bridge 2.0 / other 1.7. NEXT: R109 = L0 width-source fix (~1% CLEAN, cause pinned in R107), then seam-algo outer (~2%), then island-order.

- **★★ R108 — GAP-INFILL LOCALIZED + FIX LANDED (gated, F1_UNION): the 17% gap share was CLASS (a) GRIDDING, gap-LOCAL (not inherited), 7th hit of the R100 class. Fix routes the gap-detection ops through clib; total gated diff 243105→188922 (−22.3%).** VERDICT: (a) gridding dominant, (b) ORDER refuted, (c) generation/inherited = residual. Gap detection at perimeter_generator.rs:663 ran through geo-clipper (`shrink`/`grow` @ GEO_CLIPPER_SCALE=1000, `difference` @1µm) with NO gate — while its inputs `last`/`offsets` are R104/R105-faithful. MEASURED (GAPDBG, internal on-grid of gap ExPolygons, rust engine): inputs **0.0% on the 1µm grid (full-res)** but detected_gaps output **100.0% on-grid** ⟹ the geo ops re-gridded faithful inputs, fragmenting thin gap regions into ~13x too many slivers (885 vs 67 gap-expolygons/probe-set). This is GAP-LOCAL: fixing only the ops (inputs untouched) converged gap ⟹ not inherited from the wall-floor. GCODE EFFECT (gated, gap-infill G1 XY-motion lines, layer-aligned): gap-geometry **XY multiset match 2.3%→46.9%** (20x), full-line multiset 0.1%→20.1%, output on-grid 100%→0%. Per-feature content-diff (order-independent multiset-unmatched): gap **20875→17365 (−16.8%)**, gap share 16.8%→14.4%; raw positional diff 243105→188922 (−22.3%, un-gridding also un-fragments → global LCS re-anchors). (b) REFUTED: XY multiset was only 2.3% pre-fix (pure order would be high). FIX (+33/−3, F1_UNION): `shrink`→`shrink_clib`, `grow`→`grow_clib`, `difference`→`difference_clib` (same mm deltas — geo delta is unscaled mm; clib shims take mm ×1e5; difference_clib/offset_expolygons_clib reconstruct via union_ex_clib @i32/1e5 under F1_UNION per R106). MATERIAL at native: gap dE +0.47%→+0.58% (+0.27mm, faithful-geometry-longer exposure, provably more faithful — XY match 20x), TOTAL +0.274%→+0.285%; header filament 3859.50→3859.92 (native 3858.97). Default path byte-UNCHANGED (147987 verified); gated deterministic byte-identical (R99 held). RESIDUAL gap (post-fix): ~53% XY-unmatched = INHERITED wall-floor (last/offsets full-res but only ~44% hash-match native) + E/width-per-segment (full-line 20.1% ≪ XY 46.9% ⟹ same E-per-length class as walls) + medial-axis/arc/order. Gap line count still +30% (8932 vs 6865) — other gap sources (only_one_wall_top temp_gap, thin-walls) and generation not yet swept. NEXT: L0 outer width-source (R107, ~1%) or the gap E/width + remaining gap-source gridding.

- **★ R107-MEASURE — SEAM/ARC/ORDER floor QUANTIFIED (gcode analysis on 1509c3c, no code change): the "floor" is ~80% a GEOMETRY floor — ARC-FITTER EXONERATED (do not attack), seam mostly SELF-CONVERGED.** DIFF SPLIT (243105): Outer wall 40.5%, Gap 17.4%, Inner wall 12.4%, ISI 8.8%, Top 7.3%, Floating 5.6%, Sparse 5.1%, Bridge 1.9%. ARC: G2/G3 14150 vs 11906 (+18.8%) BUT on geometry-matching loops (centroid<20µm) segment counts match **98.9% outer / 98.4% inner** ⟹ over-seg is fully INHERITED from geometry divergence — the R79b "arc fidelity rung" is DEAD as an independent lever. SEAM: on tight-matched loops **90.3% outer / 50.2% inner** within 2µm (global was 33.1% @R100) — R79c confirmed: seam_placer picks the same candidate given matching geometry; residual = small independent seam-algo divergence (~10% of matched outer loops). ORDER: churn = seam-rotation + island emission-order tie-breaks, both present (L120 dump). L0 CAUSE PINNED (ODBG delta dump, all 240 layers): ONLY L0 mismatches — rust sources outer width from `external_perimeter_extrusion_width` (nominal 0.5 → delta −25000.0) vs native `ext_perimeter_flow.scaled_width()` (49999 → −24999.5); L1-239 coincide at 0.42; nsc was a no-op here (f32(0.5) exact). Fix = source outer-offset width from ext_perimeter_flow (verify byte-neutral L1-239) — CLEAN, ~1%. FINAL ATTACK TABLE (payoff of 243105): arc ~0% NEVER (inherited 98.9%); seam-algo outer ~1.5-2% CLEAN; L0 width-source ~1% CLEAN; island-order ~1-3% bounded; **GAP INFILL up to ~17% UNSCOPED (best next probe — separate gap-fill geometry/order path, never localized)**; L1-15-inner + overhang-split residual + complex geometry ~30-40% DEEP ClipperOffset-topology floor (may hit cz-vs-native ClipperLib limit). Clean bounded wins ≈4-6% + gap unknown. **USER DECISION: BANKED @f8af6c4.**

- **★★ R106 — HELPER-LEVEL GRIDDING BUG: `difference_clib` reconstructed its output via geo-clipper `union_polygons_ex` @1µm — silently defeating the R104/R105 clib routing it served. Fixed @cc54229 (gated, PUSHED): inner region i=1 67.0%, i=2 24.8%→49.0% (DOUBLED), overall 74.4%; common lines 68746 (+11.4% vs baseline, 46.7% match).** Stage-split on R105: inner input i=1 56.4% on-grid but offsets(i=0) pre-block 1.4% and offset2_ex_clib output 1.0% → the only_one_wall_top BLOCK still gridded despite R104/R105, via its helper: difference_clib ran the ClipperLib difference full-res then RECONSTRUCTED through geo union (top_polygons 96.3%/temp_gap 100% on-grid). FIX (+13/−2): F1_UNION → union_ex_clib reconstruction; default keeps geo (difference_clib has a default-path caller in layer.rs gap-fill — global change flipped default 147987→147978, gating restored byte-identity, stash-diff verified). RESULT: inner input on-grid 56.4→1.9% (≈native 5%); material at native (+0.059%); walls held; R65 untouched. Diff-marker 197194→243105 UP = the newly byte-matching inner walls now carry the long-standing SEAM/ARC/ORDER divergence (R79 class) — content converged, order churns (exposure). RESIDUAL inner (i=1 33% off) is NOT gridding → seam/arc/order = the next frontier. AUDIT FLAG → R107: other *_clib helpers may share the geo-reconstruction bug (offset2_clib; grep union_polygons_ex in helpers) — exhaustive sweep funded.

- **★ R105 — REMAINING only_one_wall_top SUB-PATHS UN-GRIDDED @1a7c776 (gated, PUSHED): gated diff-marker drops BELOW baseline for the first time (243734→197194 vs baseline 212604); R104's ordering churn fully resolved.** PREMISE CORRECTION: `upper_slices` was NOT the residual source — it's already full-res (0.0% on-grid; benchy single-region, lslices=to_expolygons, no geo). The residual gridding came from THREE geo sub-paths R104 missed, all feeding `last`: fill_clip = offset_ex(last,−ext_perimeter_spacing); temp_gap = diff_ex(top_polygons, fill_clip) unioned into last; the bridge_checker offset_ex/diff_ex/union_ex chain re-gridding inner_polygons on bridge layers. All routed through clib (offset_expolygons_clib/difference_clib/union_ex_clib; bridge emptiness boolean stays geo). RESULT: common lines 65085→65132 (+3420 vs baseline); rust raw 147138; material TOTAL −0.017% (at native); walls held; default byte-unchanged; **but INNER REGION FLAT (i=1 60.9→61.2%, i=2 24.8%)** — the win is gap-fill/top-classification/bridge toolpaths, NOT inner walls. R103b also landed @c55279c (cz_offset2_ex stepB delta2-sign dispatch, byte-inert, correctness for delta2>0 callers). NEXT (R106): stage-I re-dump on R105 decides (a) hidden `last`-gridding source remains vs (b) genuine successive-offset divergence (then per-loop delta on first/top layers [R101 nsc covered only 0.42] before any reconstruction/FP theory). Compounding signature (i=1 61%→i=2 25%) suggests per-iteration re-divergence.

- **★★ R104 — INNER-WALL ROOT CRACKED + FIX LANDED @a9539c0 (gated, PUSHED): the `only_one_wall_top` last-chain re-gridded `last` at 1µm between i=0 and i=1 — 5th hit of the R100 gridding class. Inner region i=1 hash 0.3%→60.9%, common gcode lines +3373, material TOTAL −0.005% (at native).** Weaver's initial "offset2 build-level FP floor" verdict was WRONG (the escape-hatch class, 6th refutation) — the stage-split probe settled it: stepA control (slice-closing) 747/747=100% (shim+hash sound); the inner offset's INPUT matched native only 0.1% and was 99.8% ON the 1µm grid (native 5.2%) ⟹ outcome (a): the op inherits a gridded input; the offset2 op is FAITHFUL. ROOT: `last = intersection(inner_polygons, last)` + top_polygons/inner_polygons chain (perimeter_generator.rs, only_one_wall_top block, i==0; native PerimeterGenerator.cpp:1169+868/876/886) ran through geo-clipper @1µm before feeding ALL inner offsets and the infill-area computation (also explains why the Lever-A infill un-grid only half-worked). DELTAS integer-identical (DELTADBG: distance=39207 min_spacing=24424 d1=51418 d2=12211) — inner-delta port refuted for good. FIX (+72/−17, F1_UNION): last-chain via difference_clib/union_ex_clib (intersection as A−(A−B)); inner successive offset via faithful offset2_ex_clib + integer coord_t delta. RESULT: inner input on-grid 99.8%→53.3%, region i=1 0.3%→60.9% / i=2 0%→24.8% / overall 38.2%→66.0%; common lines 61712→65085 (42.1%→44.2%); filament 3855.52→3859.70 (native 3858.97), TOTAL dE −0.005%, inner −0.03%, outer held; default byte-unchanged. Diff-marker +31k = LCS ordering churn (seam/arc/order not yet converged) while CONTENT converged — exposure class. RESIDUAL: remaining 53.3% gridded inner inputs are TOP-SURFACE layers — `upper_slices` is pre-gridded at ITS source (R105: un-grid upper_slices where computed; routing the upper offset@868 alone was a measured no-op). Then seam/arc re-alignment on matching geometry. LATENT (approved, not yet landed): cz_offset2_ex delta2-sign dispatch (should use expand_paths for delta2>0; byte-identical for benchy).

- **R102d — FINAL INVESTIGATION (no commit): inner-delta port refuted 3rd time (inner hash 0/676 even on R102c-faithful outer regions, diff regresses → NOT the delta, distinct root); bridge_checker@854 un-grid SKIPPED (balloons: 4 geo-clipper ops, classification-only). GEO-CLIPPER SWEEP (report): NONE left on the wall-offset chain (walls fully clib); remaining geo-clipper in perimeter_generator.rs = thin-wall path (off for benchy: 535/548-554/574), gap-fill (649-652), top-surface/bridge_checker (819-896), INFILL-AREA computation (1098-1159) — the latter feed the pre-existing ISI/floating CLASSIFICATION divergence → NEW HYPOTHESIS: the ~15% classification gap may ALSO be the 1µm-gridding class (route 1098-1159 through clib in a future round). NEXT-SESSION WALL LEVER: ITERDBG i=2 (multiset-per-layer) — does the FIRST inner offset diverge on faithful input? (R102c's grow fix already un-grids inner overhang boundaries, so suspect the inner offset op/conversion itself.)**

- **★★ R102c — OVERHANG-GRID ROOT CONFIRMED + FIX LANDED @818efaa (gated, PUSHED): outer-wall contour hash 38.5%→82.8%.** The overhang-split suspect (below) was right, and the mechanism is the R100 gridding class AGAIN: `detect_overhang_wall` splits outer perimeters at overhang boundaries from `generate_lower_polygons_series` = grow(lower_slice, [off_front, +nozzle/2]) — rust grew via geo-clipper @1µm → gridded boundaries → gridded/shifted vertices INSERTED into the outer contour where walls cross overhang regions; holes rarely overhang (explains O/c 38.5% vs O/h 71.9%). FIX (perimeter_generator.rs:1598, +20/−2, F1_UNION): route both lower-slice grows through `offset_expolygons_clib`. RESULT: outer contour 38.5%→**82.8%**, outer hole 71.9%→**91.6%**, total wall hash 24.1%→**44.6%** (635/1425); diff lines 146207→145929; default byte-unchanged (147987 verified); outer-wall E toward native (+0.48%→+0.21%), total −0.20% (delta = pre-existing infill classification jitter, exposure precedent). REMAINING: inner walls 0% (re-run R102 port on now-faithful outer regions — R102d); outer top-surface `bridge_checker` (perimeter_generator.rs:854) still geo-clipper-gridded; per-loop delta on non-0.42-width layers.

- **R102/R102b/R102c/R103 — INVESTIGATION ROUNDS (no commits, all reverted; tree clean @fa442f8). Net: inner walls + material collapse to ONE open question — WHY do 54% of outer-wall offsets diverge on matching input? — now localized to OUTER CONTOURS with the OVERHANG-WALL SPLIT as top suspect.** R102: narrow inner-delta port (scaled coord_t spacings + single-pass offset2_ex_clib) REFUTED — inner hash stays 0/676 because the successive-offset region chain diverges at the outer step (ITERDBG i=0 96%/layer, i≥1 ~0%); no inner-delta fix can work. R103: the "E-per-length +0.5%" nit REFUTED — native f32 mm3_per_mm vs rust f64 differs 3.1e-8; rust E/len is actually 0.12% LOWER; the outer E delta is rust walls being 0.66% LONGER (the 54% diverging geometry) → R101's material −0.11% = geometry path-length exposure, resolves when walls match; true E-byte work (f32-trunc mm3_per_mm + E-string format) deferred LAST. R102b: fill-rule reconstruction difference (native single-ExPolygon offset_ex uses do_union=false→pftEvenOdd vs rust NonZero re-union) TESTED = zero byte change on benchy; also re-measured ITERDBG cleanly (the "region divergence" was just the AND of per-loop matches). R102c (WALLDBG split by role×contour/hole): outer CONTOUR hash 38.5% (n-count 78.5%) vs outer HOLE 71.9%, inner 0% — the 54% is CONTOUR-concentrated. SHARP CONTRADICTION: input `last` matches 96%/layer, ClipperOffset deterministic, params identical (jtMiter/ML3.0/SEF0.005) ⟹ the plain offset CANNOT drop to 38.5% — something mutates the outer contour AFTER the offset. TOP SUSPECT (explains contour≫hole): **detect_overhang_wall** — outer perimeters are SPLIT at overhang boundaries derived from m_lower_slices_polygons = offset(lower_slice, +nozzle/2), inserting vertices into the contour; holes rarely overhang. **NEXT SESSION (R102c-2 probe, teed up): pair each outer loop's INPUT-contour hash with its OUTPUT hash both engines (matching-in→non-matching-out ⟹ overhang-split/conversion, not offset); then byte-compare m_lower_slices_polygons (lower grown nozzle/2) + the overhang clip rust vs native.** Secondary candidates: per-loop delta variation on non-0.42 layers; PerimeterLoop→ExtrusionLoop conversion.

- **★ R101 — OUTER-WALL FLOW-WIDTH SCALING (F2 class): the R100 sub-micron miter residual was the FLOW WIDTH → coord_t conversion, not the offset delta. Gated fix LANDED @7d3f93c, PUSHED.** The R98-style offset-delta f32 truncation was a NO-OP for benchy (deltas already f32-exact — audited, zero byte change). DELTADBG (bit-exact, both engines): native `Flow::scaled_width()` = `coord_t(scale_(m_width))` with m_width stored FLOAT and scale_ TRUNCATING toward zero → 0.42 → f32(0.41999998) → 41999; rust carried f64 0.42 + round → 42000; outer offset delta 20999.5 vs 21000.0 → 0.5-unit inset diff amplified to tens of units at miter joins (flow.rs:238 FIDELITY-NOTE(F2) already flagged the class). `last` (perimeter input) byte-matches native **231/240** layers → R100 chain sound, divergence offset-side. FIX (+23/−1, F1_UNION-gated `nsc()` f32-trunc scaled width at the offset-delta site ONLY, extrusion width raw): outer-wall canonical-hash **0 → 45.9%** (344/749), overall walls 0 → 24.1%; default byte-unchanged. GUARDRAIL EXCEPTION (team-lead approved): material −0.02%→−0.11%, diff lines 146103→146207 — the bit-faithful (larger) geometry EXPOSES a pre-existing rust E-per-length gap (~+0.5% outer E at identical geometry: native 710.71 vs rust 714.56) that R100's less-faithful geometry masked; exposure not regression; E lever queued (R103). NEXT: R102 = scoped F2 perimeter coord-scaling port (spacings + inner-wall single-pass offset2_ex chain, local gated helpers, NOT crate-wide flow.rs) → inner walls 0→>0; R103 = the E-per-length nit (mandatory for byte-parity).

- **★★ R100 — WALL UN-GRIDDING: the ~54% wall gap's first divergence was NOT seam — rust's classic-wall perimeter INPUT was quantized to a 1-micron grid by geo-clipper. Gated fix LANDED @658b92e, PUSHED.** Localization (WALLDBG env-gated dump of every wall loop in extrude_loop, both engines, keyed by layer; loops 1425/1425, 240/240 layers): wall-vertex on-100-unit-grid rust **99.4% vs native 3.6%** (seam coords 68.7% vs 2.3%); rotation-invariant canonical-hash match **0/1425**. Entry points (GRIDDBG: input slices 5.3% on-grid = full-res, simplify_p output 100% on-grid): (1) `ExPolygon::simplify_p` (expolygon.rs:325) → `simplify_polygons_clipper` → `union_polygons_ex` (clipper_utils.rs:275) @ GEO_CLIPPER_SCALE=1000 (1/1000mm = 100x coarser than native ClipperLib coord_t 1e5/mm), gridding `last` at perimeter_generator.rs:417 BEFORE any wall offset; (2) `offset_expolygons_clib` (clipper_utils.rs:1073) reconstructed clib offset output via the same geo union — shrink_clib/offset2_clib were vertex-COUNT-exact (R91) but position-RE-gridded. Global `GEO_CLIPPER_SCALE` 1000→1e5 diagnostic = NON-VIABLE (21min/21GB RSS runaway; the narrow clib fix is the right shape). FIX (3 files +41/−3, all F1_UNION-gated, mirrors the R91 slice chain at triangle_mesh_slicer.rs:1770): classic `last` = DP full-res (`simplify_p_dp_rings` split) → `simplify_polygons_clib` + `union_ex_clib`; `offset_expolygons_clib` reconstructs via `union_ex_clib`. VERIFIED: default path byte-IDENTICAL with fix (inert w/o F1_UNION); gated: on-grid 99.4%→**3.6% (= native exactly)**, diff lines 146958→146103, G2/G3 14265→14089 (over-seg +19.6%→+18.3%), vertex-count match 59.6%→73.5%, seam-within-2µm 22.8%→33.1%; material −0.02% (walls ≤+0.76%); slice path untouched by construction (changed helpers reachable only from walls/gap-fill — verified callers; slice uses separate offset2_ex_clib). NOTE: benchy resolved config = CLASSIC wall generator (not Arachne) — R99 #4's geometric-key deviation is moot here; `generate_arachne` (perimeter_generator.rs:1969) still needs the same clib chain for Arachne configs (deferred). RESIDUAL wall divergence (now measurable on un-gridded geometry): SUB-MICRON vertex noise — on a matched loop ~half the verts byte-identical, rest differ <70 units (0.2–0.7µm) at miter joins, bbox off by exactly 1 unit → feeds seam mismatch (67% still differ) + residual arc over-seg. NEXT: (a) byte-compare rust `last` vs native per layer — pin the sub-unit input delta (R98-style round-vs-truncate / f32-boundary suspect); (b) arc-fitter tolerance/f32 audit (may be independent); (c) Arachne-path clib chain.

- **★★ R99 — RUST GCODE NOW DETERMINISTIC (default path @f3c3036). BANK POINT for this arc.** Rust emitted different gcode every run (a hard gate on byte-parity); root class = the port mirrored C++ `std::unordered_map`/`unordered_set` (stable-per-run) with Rust `HashMap`/`HashSet` (per-run-random `RandomState`), + one ASLR-leaking pointer-set. 4 sites fixed (one dominant Arachne cascade): #1 `fill_rectilinear` ACO `thread_rng`→faithful `mt19937_64` seed 5489 (ISO-verified, native-matching); #2 `region_expansion` bridge-dir HashMap→BTreeMap; #3 `fill_floating_concentric` `.iter().next()`→`.min()`; #4 `skeletal_trapezoidation` wall-start ptr-set→min-by-geometric-key; +`tool_ordering` sweep. Result: rust 3× byte-identical, material-neutral (8/10 gcode features byte-identical; 2 fill-chaining features move within pre-fix jitter), slices 238/239 + R65 intact. Determinism holds on the gated path too. **MEASURED gated byte-parity gap (F1_UNION+FRAME_UNIFY+SLICE_SIMPLIFY+SLICE_CENTER, inline config): native 139498 vs rust 146958 lines (+5.3%).** Concentration BY LINE-SHARE of the diff (the byte-parity-relevant metric; ~75% of lines have no exact content twin, only 24.9% match): **WALLS DOMINATE (~54%)** — Outer wall 41.2%, Inner wall 13.2%; Gap infill 14.7%; all infill ~28% (ISI 9.0% + Top 7.7% + Floating 6.0% + Sparse 5.3%); Bridge 2.1%. So the ISI/floating fill-CLASSIFICATION split (R69–R77) is only ~15% combined — the block-count view (ISI 389 vs 242) OVER-weighted it; by actual gcode lines the gap is **WALL TOOLPATH EXPRESSION**. Mechanism: **SEAM start-point divergence + ARC over-segmentation** (G2/G3 11923 vs 14265, +19.6%; G1 +19%) — every wall loop starts at a different seam vertex and arc-fits differently (R79b/R79c floor). First divergence = trivial machine-start template (`G29.1` vs `M629`). Material at native (−0.1%) → it's toolpath-expression (seam/arc/order), NOT geometry/volume. **DECISION: BANKED here** — 75% of lines differ, dominated by wall seam/arc fidelity = a multi-session floor. **Biggest lever (corrected): WALL seam-start + arc-fitting fidelity (~54%), NOT the classification.** IMPORTANT: the R79c–R79g attribution of seam divergence to the "+93-vert admesh mesh" is STALE — that mesh was solved (R80 admesh repair, R87 frame-unify → gated verts bit-match, slices now 238/239 match), so the seam/arc divergence on MATCHING geometry needs FRESH investigation (seam placer faithfulness on the full model + arc-fit fidelity), not the mesh. Then the ISI/floating classification (~15%, R71–R77 @1e5 process_external — retest now that slices match) + per-feature.

- **★★ R96–R98 — SLICE BYTE-MATCH ESSENTIALLY COMPLETE: per-layer slice ExPolygons match native 0/240 → 238/239 (structure nex/nholes 240/240). Gated @e99f0cc (R96 7b8b011 + R97 c054370 + R98 e99f0cc), UNPUSHED, default byte-unchanged, R65 intact, material at native.** The R88–R94 union hunt was a 7-round detour — the real slice root was NOT the union/clipper (all proven byte-faithful) but a **skipped post-union morphological close**: rust never threaded `slice_closing_radius` (0.049). **R96** — thread closing_radius into `MeshSlicingParamsEx` + a faithful `offset2_ex` via a new `cz_offset2_ex` shim (ClipperLib @1e5, not geo-clipper @1e3); L0 contour npts → exact C++ (1092/205). **R97** — the 11-layer hole-nesting residual (L0+L43..L52) was a rust EXTRACTION bug: `polytree_to_grouped` interleaved nested contours between a parent's sibling holes → hole mis-attached; 9-line fix (emit all holes before recursing) → nex/nholes 240/240. **R98** — the 43-layer 1–2-vertex residual was a double-vs-float DELTA truncation: native `offset2_ex(const float delta)` f32-truncates `scale_(0.049)`=4899.99987 → 4900.0; cz passed the un-truncated f64 → shifted negative-offset points ~1 unit; 2-line f32-truncation fix → 238/239. Each residual was mis-called "compile-env / IntPoint / F-class FP floor" and cracked by dumping/localizing instead — byte-identical clipper source+input ⟹ identical output, so always localize (pass1→pass2→extraction; grow→shrink→delta) before blaming the build. **Remaining slice nits (negligible):** L0 (8-hole cabin floor) 2 verts; a z-list off-by-one (rust dumps 239 layers vs native 240, `rust L_i ≡ native L_{i+1}`). **NEXT BLOCKER (was): rust non-determinism — FIXED in R99 (below). Remaining for gcode byte-parity:** the ISI/floating fill-classification split + arc over-segmentation + the pervasive downstream F1-walk (route fill/perimeter clipper ops through the faithful @1e5 shims). See R99 for the measured gap.

- **★ R81 — FRAME SUB-LEVER CLOSED: there is NO frame offset (the X-frame was a MIRAGE).** Tested (b)
  export-origin-alone → measurement decided it: the frame is ALREADY aligned, nothing to fix. ALGEBRA (full
  C++ chain): shift = instance_translation_xy (PrintApply.cpp:149) `+= m_center_offset` (PrintObject.cpp:108);
  m_origin = unscale(shift) (GCode.cpp:5244); point_to_gcode = unscale(p) + m_origin (GCode.cpp:7591); slice
  frame = raw − center_offset. NET gcode = (raw − center_offset) + (instance + center_offset) = **raw +
  instance**; the center_offset CANCELS. slicer_cli STL → add_instance at offset 0 (no arrange/center;
  multi-plate-3MF branch skipped) → instance = 0 → **C++ gcode = raw**. rust slices raw + export-origin 0 →
  also raw → frames MATCH with no rust change. (The R79e/f "net = raw − 2·center" was wrong; R79f's
  "export-shift toward native" was confounded by the then-unfixed mesh.) EMPIRICAL: with mesh fixed (R80,
  default), benchy object bbox native vs rust agree within micron rounding (Xc 0.8200 vs 0.8195, Δ~0.001-0.002
  — NO 0.8245/2·center shift). The R79b "first-div X.936/X3.146" = DIFFERENT START POINTS on the same loop
  (dx 2.21 ≠ dy −1.726, non-uniform → not a translate), i.e. seam/path, not frame. So R79c-g's "0.8245 offset"
  was the slice-INTERNAL frame (cancels in gcode) — invisible in the output; the real mesh root (+93) was the
  separable issue, now fixed (R80). **No source change (measurement-only); frame-pair stays gated+dormant
  (export-origin=0 is already the default).** REMAINING byte-parity gap = PATH GENERATION on the already-correct
  frame: rust emits ~18k more moves (114698 vs native 96495; only 11995 identical as multisets) — driven by
  per-feature fill CLASSIFICATION (Floating 122→173, ISI 389→237, Sparse 193→180 — the long-standing R69/R71-74
  near-cancel split, possibly F1-geo-clipper-tied), seam-start-on-different-loops, and arc-fitting (+25.7%).
  NEXT lever (if byte-parity pursued): re-localize on the now-correct mesh+frame — do perimeter loops byte-match
  C++ now? → splits "seam+arcs (bounded)" vs "fill-classification (deep R69/F1)".
- **★ R80 — ADMESH REPAIR LANDED (the +93-vert mesh root SOLVED; material-neutral, faithful).** R79h's
  "2284-line bail" was OVER-scoped: PHASE-0 ground truth showed benchy is MANIFOLD after the exact check
  (conn3 = number_of_facets) → C++ SKIPS the bit-sensitive nearby-merge entirely (the guard
  `connected_facets_3_edge < number_of_facets` is false). So the +93 decomposed into just (1) 552 degenerate
  facets removed + (2) topology-based shared-vertex generation — ~270 DETERMINISTIC lines, no
  tolerance/order ambiguity. Ported `stl_repair.rs` (faithful admesh: degenerate-facet removal + exact-edge
  neighbor graph [HashEdge byte-key, −0/+0 normalized] + `stl_generate_shared_vertices` fan traversal),
  now the DEFAULT binary-STL path, replacing the exact-f32-bit HashMap dedup that wrongly kept 552
  degenerate facets + skipped shared-vertex gen (correct-beyond-parity — rust was genuinely wrong vs C++
  `from_stl(repair=true)`). RESULT (default, re-verified): **vert count 112569 EXACT** (was 112662 — the
  +93 GONE), facets 225154, manifold — all EXACT vs C++; **material-neutral** (per-feature stable: ISI
  −30.5, sparse −12.48, floating +31.55, outer +2.12; total −0.42 ≈ baseline noise); R65 floor intact
  (li=1 loops==1, repair is pre-quantize); exact-identical moves 11964→11971; build green. Landed parity
  @0aad342. The mesh is now FAITHFUL. REMAINING byte-parity blocker = the X-FRAME centering (separable last
  sub-lever): REPAIR alone leaves the ~0.8245mm slice-frame X-offset (first-div native X.936 / rust X3.146);
  REPAIR+FRAME_PAIR regresses material +9.92 because frame-pair's PRE-SLICE per-vertex mesh-translate
  perturbs the f32 slice geometry (+ R65-quantize interaction). The faithful X-frame needs either (a)
  centering INSIDE the slice-time transform (C++ make_trafo_for_slicing fused op), or (b) the export-origin
  ALONE without shifting slice geometry. frame-pair foundation is on parity but GATED (FRAME_PAIR, default-off).
- **★ R79h — (resolved by R80) suspected the admesh REPAIR subsystem (2284 lines) as a likely bail.** Scoped the
  +93-vert root. Per-stage bisect: benchy binary STL = 225706 facets → 677118 raw verts. rust dedups by
  EXACT f32 bit-key (`[x.to_bits(),y.to_bits(),z.to_bits()]` HashMap, stl.rs:158-180) — NO tolerance, NO
  repair → 112662. C++ `ReadSTLFile(repair=true)` → `trianglemesh_repair_on_import` (admesh) →
  `stl_generate_shared_vertices` → 112569. The +93 enters at the REPAIR stage rust SKIPS ENTIRELY (stl.rs:82-85
  comment admits repair + shared-vertex generation are not reproduced). C++ repair (TriangleMesh.cpp:79-160):
  stl_check_facets_exact → **stl_check_facets_nearby(tolerance)** → stl_remove_degenerate →
  stl_fix_normal_directions, then stl_generate_shared_vertices builds the index from the repaired TOPOLOGY
  (neighbor graph, not a hash). The 93 = near verts merged by stl_check_facets_nearby (tolerance =
  stl.stats.shortest_edge, ITERATED 2× with increment — DATA-DEPENDENT, not a fixed epsilon) + degenerate
  facets removed, that rust's exact-bit dedup keeps. SIZED: NOT a bounded one-tolerance tweak — the faithful
  fix needs the admesh subsystem bit-for-bit (~2284 lines: connect.cpp 743 facet-connectivity graph, shared.cpp
  263 topology traversal, stlinit 389, util 399, normals 239, stl_io 251), and matching the EXACT 93 merges
  needs admesh's exact edge-matching + union-find ORDER + iterative tolerance — hits 2 of 3 bail triggers
  (whole subsystem + order/tolerance-sensitive). Does NOT touch R65 (repair is pre-quantize). **VERDICT:
  BAIL/BANK. Material parity (1.000×) is the achieved + landed goal; byte-identical gcode is gated SOLELY on a
  full faithful admesh-repair port — a large (multi-session), bit-sensitive lever with uncertain byte-payoff
  (porting it may still not reproduce exactly 93). Every other layer end-to-end is faithful.** frame-pair
  @9892a5d banks the faithful f64 centering (gated, NOT merged). branch stl-load (scope-only, no edits).
- **R79g — SLICE ARITHMETIC EXONERATED; true root = STL-LOAD MESH (+93 verts), a discrete root
  (corrects R79f).** Funded the slice-intersection lever — it OVERTURNED R79f's "slice-intersection is the
  wall". C++ slice_facet interpolation (TriangleMeshSlicer.cpp:261-280): `t=(double(slice_z)−double(b.z))/
  (double(a.z)−double(b.z))`, `x=coord_t(floor(double(b.x)+(double(a.x)−double(b.x))·t+0.5))`, on-vertex →
  `a.x` passthrough. rust slice_facet (triangle_mesh_slicer.rs:331-358) is ALREADY BIT-IDENTICAL (same f64
  t, same floor(+0.5), same passthrough) → **slice-intersection arithmetic is FAITHFUL.** The loop-point
  bit-check is MOOT because the INPUT mesh differs: a full-mesh FNV bit-hash over ALL transformed verts
  shows **C++ n=112569 vs rust n=112662 — +93 VERTICES** (FRAME_PAIR-independent — same count with centering
  off). So rust's STL-load / mesh-repair (vertex merge/dedup tolerance or degenerate-facet/edge collapse)
  keeps 93 verts C++ merges → different triangulation → all downstream byte-divergence inherits from this.
  **STRATEGIC: every downstream layer is now EXONERATED** — slice arithmetic, centering/frame (verts that
  exist bit-match), seam placer, perimeter-gen, entity order, coords-format all proven faithful. The mesh is
  the SOLE remaining upstream root. So the +93-vert STL-load fix is a BOUNDED discrete lever (NOT the f32
  rabbit hole) and is potentially the LAST keystone — if the mesh is made identical, byte-parity could
  cascade out. NEXT LEVER (if byte-parity pursued): scope why rust keeps 93 verts C++ merges — STL-load
  vertex-merge tolerance / degenerate handling (TriangleMesh repair/its_merge_vertices). Material parity
  (1.000×) remains the achieved + landed goal. frame-pair @9892a5d banks the faithful f64 centering (gated,
  NOT merged); default unchanged (−0.44), R65 li=1 loops==1 intact, build green.
- **★ R79f — FLOOR REFINED (corrects R79e): centering is FAITHFUL (verts bit-match C++); the wall is the
  slice-INTERSECTION f32, one layer deeper (F-class; BAILED CLEAN).** Funded the fused-matmul port — it
  OVERTURNED R79e's "matmul wall" framing. C++ instrumentation (`transform_mesh_vertices_for_slicing`,
  TriangleMeshSlicer.cpp:1840): the slice tf is essentially PURE ×1e5 scale (tf[0][3]≈0.008 negligible) — the
  centering is NOT in the slice matrix, it's baked into the MESH VERTICES (C++ vert.x 5.7975 vs rust raw
  6.622, Δ=0.8245 exact; scale step byte-identical). Build = -O3 arm64 (ffp-contract=on). FIX = clean
  f64-subtract of the exact truncated-grid center_offset (NO FMA matmul needed — R79e's regression was just
  double-rounding + a spurious export-origin). RESULT: **rust verts now BIT-MATCH C++ EXACTLY** (in_bits
  40b9851f == C++, scaled out 579750 == C++), R65 floor intact (li=1 loops==1) → the centering/frame
  arithmetic is FULLY FAITHFUL. **But the SLICES STILL DIVERGE on bit-identical verts** — material +10.22
  (broad: ISI −21/sparse −6/bridge +5/top +3), seam 0%, first-div native X.936 vs rust X2.32. Same input →
  different loops ⇒ the wall is the slice-INTERSECTION f32 edge-interpolation (`slice_facet_at_zs` line-plane
  crossing), non-faithful vs C++ and entangled with the R65 quantize hack. BAILED CLEAN: byte-identical
  perimeter coords need bit-exact f32 edge-interpolation across the whole slicer — the open-ended precision
  rabbit hole (each f32 layer reveals the next). **Material parity (1.000×) is the achieved + landed goal.**
  frame-pair branch now holds the FAITHFUL gated centering (verts bit-match) + export-origin, banked for any
  future slice-f32 work (NOT merged — regresses default w/o the deeper f32 match). (Caveat: 10 verts
  bit-checked; the broad shift points to intersection arithmetic, not incomplete centering.)
- **R79e — (SUPERSEDED by R79f) suspected the fused f32 slice-transform matrix as the wall.** Built
  `slice_center_xy` per-vertex XY −center (Z untouched, R65-safe), (2) `GCodeWriter.gcode_origin` +
  `set_gcode_origin` subtracting m_origin from absolute coords at the writer chokepoint (I/J left relative,
  correct), threaded app_slice→export. Net = raw − 2·center_offset (derived: slicer_cli `set_instances` does
  shift += center_offset so m_origin = center_offset, doubling). RESULT: the FRAME IS CORRECT — slices now
  match C++ to 0.001mm (Δx 0.8245→0), export shift lands (first-div 3.146→1.495 = −2·center) — BUT material
  REGRESSES +9.76→+9.98, seam-match stays 0%. Applied BOTH exactness fixes (exact center_offset =
  unscale(trunc(center/SCALING_FACTOR)·SCALING_FACTOR), C++ truncation not rust's round; single f64-subtract
  → one f32 cast, not double-f32): bit-check UNCHANGED → the residual is NOT the center value or rounding
  sequence. ROOT (definitive): C++ `make_trafo_for_slicing` (TriangleMeshSlicer.cpp:1827-1862) FUSES the
  −center translation AND the ×1e5 scale into ONE f32 matrix-multiply per vertex (`tf=t.cast<float>(); v=tf*v`);
  rust does TWO separate f32 ops (center-subtract in mm, then ×1e5 scale) → sub-ULP drift per vertex → facet
  on-plane classification re-quantizes (the +9.98, R65-family) → loops/seam don't byte-align. The bbox
  matches to 0.001mm; the gap is SUB-ULP. **BAILED CLEAN** per the pre-set criterion: matching it requires
  reproducing C++'s fused `make_trafo_for_slicing` Eigen-f32 matmul (FMA rounding) bit-for-bit across the
  slicer — the F-class precision port scoped as bail-worthy. **VERDICT: material parity (1.000×) is the
  achieved + landed goal; BYTE-identical gcode is gated on the fused-f32 slice-matrix port (deferred).** The
  frame+export-origin mechanism is correct/gated/reusable (banked on branch frame-pair, NOT merged — it
  regresses default without the f32 port).
- **★ R79d — ROOT PINNED: constant ~0.8245mm X PLACEMENT offset (R65-XY family, BOUNDED; diagnosis-only).**
  Scoped the perimeter-geometry rung — 3 probes. DECISIVE bisect: the RAW SLICES fed into PerimeterGenerator
  ALREADY differ — at L100 every input-slice bbox is X-shifted by a dead-constant +0.8235..0.8254mm (both
  min AND max corners), dy ±0.0009 ≈ 0; n_slices match (5=5). A pure RIGID X TRANSLATION → NOT F1 geo-clipper
  (which would give variable per-loop deltas + Y noise), NOT a PerimeterGenerator bug. ROOT = `app_slice.rs`
  mesh placement: it does `mesh.translate(0,0,dz)` with the comment *"no XY centering, matching C++
  slicer_cli"* — but C++ slices land ~0.8245mm offset in X that rust never applies. This is the **R65
  family**: R65's `quantize_f32_center_roundtrip` handles only center-**Z** (Benchy center_z=24); the C++
  X/Y placement/centering round-trip (volume mesh stored bbox-centered + instance trafo re-place, in X+Y) is
  NOT replicated. Same shape as the gap-interleave empty-collection bug: a comment-asserted no-op that isn't
  faithful. (Probe-3 note: rust raw slice CONTOURS carry ~4× points [C++ 96 / rust 389] — a separate
  densification; but matched output perimeter loops are 32=32 pts, so perimeter-gen normalizes it — the
  densification is a candidate FOLLOW-ON if arcs don't converge after the placement fix.) VERDICT: BOUNDED —
  fund a faithful XY placement fix in app_slice.rs (match C++'s 0.8245mm). Expected cascade: slices →
  perimeters → seam (proven faithful) → likely shrinks the arc gap. NOT the F1 rabbit hole.
- **R79c — SEAM EXONERATED; root descends to PERIMETER GEOMETRY (diagnosis-only).** Scoped + debugged the
  seam rung: it is NOT a missing subsystem — `gcode/seam_placer.rs` is a full 3165-line SeamPlacer (aligned
  mode, wired, running). Both-engine unit-case on one matched 32-pt external loop at L100 (both finalized):
  the seam placer is FAITHFUL — both pick the SAME candidate index (k=6), SAME local_ccw_angle (−1.51531 vs
  −1.51518), SAME visibility rank (0.166/0.169), SAME comparator decision, SAME npts (32). The map's
  "12-14mm" was index-misalignment; the true order-independent per-seam delta is ~0.825mm — and it's a
  UNIFORM shift of the WHOLE candidate set (every point x ≈ rust_x − 0.825), so the seam lands on the
  identical vertex of a SHIFTED loop. NEITHER engine has the other's exact X → not a comparator tie-break.
  ROOT = the PERIMETER GEOMETRY fed into the placer: (1) a uniform ~0.825mm X offset of the loop, and (2)
  loop-SPLITTING — rust feeds 10 external perimeters at L100 vs C++ 5 (rust splits some walls into 2: C++
  npts=93 ↔ rust 65+92; C++ 32 ↔ rust 22+32). Both are PerimeterGenerator wall-placement divergences,
  BEFORE the seam placer. NOTE: material is at-native (R77) and a pure ~0.8mm translation preserves loop
  length → consistent. **The seam placer would byte-match if fed identical perimeters.** REVISED CHAIN:
  PERIMETER-GEOMETRY (offset ~0.825mm + loop-split; possibly F1 geo-clipper precision) → seam [faithful,
  waits on geometry] → arcs (partly follows wall geometry) → coords [done].
- **R79b — STRUCTURAL MAP (diagnosis-only; SEAM is the gating rung, coords already exact).** With order
  converged (R78/R79), mapped the remaining structural divergences. **FIRST body divergence = move 13**, the
  first perimeter loop's START point (native `G1 X.936 Y2.228` vs rust `G1 X3.146 Y.502`) — a SEAM
  divergence; prelude (first 12 moves) is byte-identical. **COORDINATE FORMAT IS ALREADY BYTE-EXACT** where
  geometry aligns (same decimals/precision; 11964 moves exact-match any-order) → **no F1 coordinate rung
  needed.** SEAM: loop counts match (outer 770/772, inner 667 EXACT) but only **9.2%** of native outer
  seams have a rust loop starting within 50µm (6.6% within 0.6mm) — ~84% start at a different seam point,
  median delta ~12-14mm → a real Seam.cpp PLACEMENT-algorithm divergence (nearest/aligned/rear), gating
  because it cascades into loop direction + arc segmentation. ARCS: both engines arc-fit
  (enable_arc_fitting=1); native 11906 G2/G3 / rust 14960 (+25.7%, ~3000 extra) — an arc-fit FIDELITY
  divergence (rust over-segments), SECOND to seam (partly follows from seam's loop start/direction). Body
  lines native 139498 / rust 148631 (+6.5%). **Recommended rung order: SEAM → ARCS → (coords done).**
- **★ R79 — GAP-FILL INTERLEAVE LANDED (order rung 2; material byte-unchanged).** Next divergence after
  island grouping = gap-fill EMISSION position. Native interleaves gap-fill per-island (C++ Fill.cpp:757-762
  wraps each thin_fill in its own EEC and PUSHES it into `layerm->fills`); rust's port had the loop but
  pushed an EMPTY collection — `collection.entities.push(thin_fill)` was MISSING (latent no-op), so gap
  stayed batched in thin_fills (584 blocks vs native 816). FIX (two faithful parts): (1) actually move each
  thin_fill into `fills` so gap rides the normal infill island-assignment + chaining + per-island emission;
  (2) persistent FEATURE-role dedup — the `;FEATURE` marker used a per-CALL local role; C++
  `m_last_extrusion_role` (GCode.hpp:538) is a PERSISTENT member, so consecutive same-role entities across
  separate extrude calls don't re-emit the marker — added `GCodeWriter::last_extrusion_role` (always-on,
  independently material-neutral). RESULT: **gap blocks 584→830** (native 816, ~97%; inner-wall 667 EXACT,
  outer 770/772); **MATERIAL BYTE-UNCHANGED** (feat_e2 XY-gated: total −0.43, gap +1.28, ISI −30.53,
  sparse −12.48 — all stable vs R77/R78). Localization verdict: divergence was the GENERATOR entity-tree
  placement (make_fills), NOT the emission iteration. Un-gated → default, landed parity @15bd4f7, build
  green. RESIDUAL = the ISI-grouping rung (Internal-solid blocks ~237 vs native 389 under, Floating ~210 vs
  122 over — the near-cancel ISI/floating split, now an emission/grouping difference distinct from gap).
  Dependency chain: island-order [R78] → gap-interleave [R79] → ISI-grouping → seam → arcs (G2/G3) → F1.
- **★ R78 — ISLAND-GROUPED LAYER EMISSION LANDED (byte-parity phase opens; material byte-unchanged).**
  Material is at native (R77); the remaining gap to byte-identical is STRUCTURAL (entity emission order →
  seam → arcs → coordinates). First divergence localized = entity emission ORDER. Ported C++
  `GCode::process_layer` island grouping (GCode.cpp:4340-4392): layer extrusions grouped by island
  (`lslices` by bbox area), per-island perimeters→infill, replacing the flat per-region loop (print.rs).
  Added `extrude_{perimeters,infill}_entities` (subset emit) in exporter.rs; fixed a latent bug —
  `layer.lslices_bboxes` was never populated by the rust port (the island grouping was a silent no-op
  without it). RESULT: order converges at the ISLAND level — **outer-wall blocks 746→770 (native 772,
  near-exact); gap-fill 279→584 (native 816, ~60%)**; **MATERIAL BYTE-UNCHANGED** (feat_e2 XY-gated: total
  −0.37, sparse −12.48, ISI −30.50 — stable vs R77; the raw move-set differs ~15% but that's
  deretraction-prime re-ordering + loop re-seeding from re-chaining the re-grouped entities, NOT material).
  Time 43m28s, build green. Un-gated → default (landed @2113bbc). RESIDUAL = the INTRA-region per-perimeter
  gap-fill interleave (native emits gap ~1:1 after each perimeter LOOP, 816≈772; rust still batches gap
  per-island, 584) — a perimeter-generator/emission ordering inside each island, the NEXT sub-lever toward
  full order convergence → then SEAM becomes cleanly measurable. Dependency chain: island-order [LANDED] →
  per-loop gap interleave → seam → arcs (G2/G3) → F1 coordinate byte-exactness.
- **★ R77 — MATERIAL AGGREGATE AT NATIVE (1.0000×). The emitter-pair landed.** Ported faithful FillGrid
  `fill_surface_by_multilines` (combined two-direction sweep over a SHARED copy-rotated offset base +
  `make_fill_lines_raw` + grid-align) and wired the already-ported (never-called, proven-faithful)
  `fill_base::connect_infill` once on the combined set. The blocker was NOT connect_infill (a both-engine
  UNIT-CASE replay proved rust's connect byte-exact vs C++); it was the RAW-LINE input — rust's
  rotate-then-offset put endpoints off the shared `polygons_outer` so the connect couldn't snap them. Fix =
  copy-rotate the offset base (FillRectilinear.cpp:501) + faithful `make_fill_lines` + `align_to_grid`.
  RESULT: **sparse +55→−12.48** (519.2/531.6, 78% closed); **TOTAL +67→−0.09 (3858.88 / native 3858.97 =
  0.99998×)**; time 44m22s→43m53s. Blast radius PERFECTLY contained — ISI/bridge/top/bottom/gap/walls all
  BYTE-UNCHANGED (single-direction rectilinear path untouched). Build green. The biggest single converging
  fix of the run; the connect_infill stub is retired for grid. REMAINING (all per-feature, aggregate is
  done): the **ISI −30 / floating +31.5 split** (near-cancel material; classification/attribution — rust
  narrow-floats regions native keeps internal-solid; entangled w/ the gated fragmentation work), and small
  bridge +3 / top +2 / gap +1. The remaining path to BYTE-identical is structural (seam/toolpath ordering,
  G2/G3, coordinate byte-exactness), not material.
- **R75 — infill raster `overlap=0` LANDED (faithful, biggest single material move yet).** rust passed a
  spurious `overlap = spacing*0.15` into the raster offset (layer.rs) over-extending EVERY infill line; C++
  `Fill::overlap=0` for the main filler (FillBase.hpp:183, Fill.cpp:995/1007 — `infill_overlap` flows only
  into `no_extrusion_overlap`, NOT the raster geometry). Fix = `overlap=0`. **Total +114.83→+67.33 (−47.5;
  3926.30 / native 3858.97 = 1.017×, was 1.030×)**; sparse +68→**+55**, bridge +10→**+3**, top +15→**+2** (all
  toward native); walls/gap unchanged; time 44m23s; build green. Honestly **unmasked the ISI deficit**
  (−23→**−30**) — the spurious overlap was a compensating bug inflating solid lines (completeness-over-aggregate
  per the playbook). REMAINING material levers (post-overlap): **sparse +55** (still the biggest over — overlap
  was only −13 of it; ~+10.5% line excess remains, beyond the raster offset → re-localize the grid emitter),
  **floating +31.56 / ISI −30.28** (near-cancel net; the concentric/floating emitter mis-split, R69 lever).

- **R74 — group_fills post-loop + Ord fix LANDED (faithful, −6.87 material, no regression).** Ported the
  missing C++ Fill.cpp:361-373 post-loop (`union_safety_offset_ex` + `diff_ex` vs accumulated groups) + fixed
  a real `SurfaceFillParams::Ord` defect (rust omitted C++'s first sort key — decreasing bridge_angle,
  "bridges first"). Effect: the union merges near-touching **bridge** fragments → bridge +16.82→**+10.07**;
  total 3973.85→**3967.08**. DECISIVE NEGATIVE: union is **MOOT for sparse** (599.85 unchanged) — the grid
  emitter already unions internally, so **sparse +68 is NOT a group_fills problem**; it's in the grid
  emitter's line generation on the already-unioned area. Walls/gap/top/bottom/ISI/floating all unchanged.
  NEXT material targets (now precisely localized to the EMITTERS): **sparse +68** = grid emitter line-gen
  (spacing/boundary/connect on the unioned area); **ISI −23 / floating +32** = concentric/floating emitters.

- **Material: per-feature is the metric, NOT the aggregate.** Three big subsystem fixes landed (R65 slicer +
  R67/R68 Arachne + R69 floating). Current rust 3973.85 / native 3858.97 (aggregate +115, time 45m0s vs 43m).
  The aggregate is temporarily OVER because the Arachne port CORRECTED the under-features (the Arachne pipeline
  now produces beads where it produced 0), which UN-MASKED the pre-existing **sparse +68 / bridge +17**
  over-production. Per the playbook "completeness > coincidental aggregate closeness".
- **THE ISI/FLOATING SPLIT IS ONE FRAGMENTATION ISSUE, NOT TWO DEFICITS (R69, both-engine proven).** ISI −23
  (470.9/494.1) and floating +32 (202.7/170.8) look like independent under/over, but **COMBINED ISI+floating =
  rust 673.6 / native 664.9 = +8.75, near parity**. R69 ported the faithful `FillFloatingConcentric` (Z-clipper
  `detect_floating_line` — was thought blocked, actually fine via `clipper-z-sys`/`cz_clip_extrusion`) and
  measured: native's floating filler does NOT prune bead material (both engines emit the SAME WallToolPaths
  beads); `detect_floating_line`/`resplit_order_loops` only re-tag/re-seed. The split is a DOWNSTREAM
  consequence of rust **over-fragmenting the narrow-solid fill regions ~3× (FLOATCLASS_DBG: rust 4237 fragments
  vs native 1369)** — the same surface-classification/slicing fragmentation lineage — which `detect_narrow`
  then classifies differently against `lower_internal_areas`. The faithful floating port is a real fidelity win
  (genuine floating detection + seam, deretraction-prime 186→138) and material-neutral / no-regression, landed.
  **The real lever for floating→170 AND ISI→494 is reducing narrow-solid fragmentation (group_fills surface
  merge / the slicer fragment lineage), upstream of the fill stage.** See `docs/parity/R69_floating.md`.
  REMAINING per-feature levers: **sparse +68** (biggest single material gap, pre-existing), **bridge +17**
  (pre-existing). Walls + gap-fill at parity (untouched by the infill changes).
- **R70–R73 — THE FRAGMENTATION THEORY WAS DISPROVEN FOR MATERIAL (a multi-round investigation, banked).**
  R70/R71 traced the ISI/floating/sparse material gap to rust over-fragmenting `fill_surfaces` ~2–3× (R71:
  the explosion is inside `process_external_surfaces` — shells enter clean at 404, exit at 1929 vs native 605).
  We funded the faithful fix to de-fragment: built a reusable **Clipper2-Z engine shim** (`crates/clipper2-z-sys`,
  vendored Clipper2 + USINGZ, ODR-namespaced + symbol/full-link verified), ported the faithful `wave_seeds`
  (R72), and the faithful Miter/ClipperLib **closing** (R73, which collapses the fragmentation 1911→707).
  **BUT both-engine A/B proved the surface FRAGMENT COUNT does NOT drive the material**: the closing fix moves
  the gcode by ~0 (ISI/floating/sparse unchanged) because the fill is computed on the **unioned area** (already
  identical between engines, 404 entering, area matches). So R69–R73's "fragmentation is the lever" framing is
  a RED HERRING for material. (Three roots refuted by cheap assess-first measurement before any expensive fix
  shipped: wave_seeds-approx→units-bug-artifact, F1-difference→clib made it worse, fragment-count→gcode~0.)
  **CORRECTED next-session target:** the real ISI −23 / sparse +68 / floating +32 material gap is DOWNSTREAM in
  **FILL-PATH GENERATION on the (already-correct, unioned) surfaces** — `group_fills` + the grid/concentric/
  floating emitters — NOT in process_external surface classification. Fresh both-engine localization needed
  (no current hypothesis). The Clipper2-Z shim + faithful wave_seeds + faithful closing are PRESERVED gated on
  branch `wave-seeds` (pushed, no-regression, env-gated REGION_EXPANSION_FAITHFUL/CLOSE_CLIB) — correct fidelity
  foundations to revive when the real material lever is found. Docs: `docs/parity/R70_sparse.md`,
  `R71_defrag.md`, `R72_wave_seeds.md`, `R73`.
- **THE ARACHNE PIPELINE IS NOW LIVE (R67/R68).** The keystone `SkeletalTrapezoidation` VD→half-edge graph
  builder (`construct_from_polygons` + make_node/transfer_edge/discretize/compute_point_cell_range, ~415
  lines, SkeletalTrapezoidation.cpp:92-504) was ported against the `bv::Diagram` index API and wired into
  `WallToolPaths::generate` (replacing the stub). This unblocks the ENTIRE Arachne pipeline (concentric
  infill here + the Arachne perimeter path elsewhere). Two latent bugs fixed (surfaced now the graph is
  non-empty): `collapse_small_edges` use-after-free (LinkedList rebuild moved payloads → dangled the
  raw-pointer graph; fix = `LinkedList<Box<STHalfEdge>>` for stable payload addresses) + `generate_junctions`
  size_t underflow. See `docs/parity/R67_arachne.md`.
- **Time estimate: CONVERGED** — native 43m0s / rust 43m21s (the old "1h29m vs 43m"
  line-3 divergence is **RESOLVED**; the overhang/speed trio is landed).
- **Byte-identical: NO**, but the structural subsystems now match or are faithful:
  outer-wall vertex density matches native (offset rerouted to clipper-z-sys),
  gap-fill 81% closed, seam ~89% byte-exact at established layers, arc-fitter /
  simplification / medial-axis (boostvoronoi) / chaining / retraction / overhang-trio
  all proven faithful. `lslices` and `detect_surfaces_type` are faithful **given inputs**; the mesh slicer
  was NOT bit-faithful at the Benchy hull bottom (R62's "slicer faithful" claim was overturned by R63's
  both-engine A/B) — **R65 ROOT-CAUSED + FIXED it** (the f32 center round-trip below; floor now slices
  exactly as C++).
- **The remaining residual is ONE lever — ROUND 63 (both-engine A/B) RELOCATED it from the fill
  stage back to the MESH SLICER (F2), overturning R62's "fill reclassification / slicer ruled out"
  framing.** R62 inferred slicer-faithfulness from code reading and never measured native's slice. R63
  instrumented BOTH engines through the cascade: at **layer 1 (pz=0.4) rust's slice carries 8 spurious
  ~10mm² holes (~86mm² total) that C++ does not** → li=2 over-classifies ~290mm² as BottomBridge → steals
  from InternalSolid → the ISI leftover fragments into narrow slivers → `FillConcentric` starves them.
  internal-solid −60..−80, floating −64, bridge +15 ALL fall out of this one slicer divergence;
  `detect_narrow`/`FillConcentric`/`lslices` are faithful given inputs. **R63.5 correction:** the
  `make_expolygons:1312-1313 scale()` suspect is a runtime NO-OP (closing_radius=0 → pure union) and the
  `closing_radius=0.049` lever is refuted by magnitude (10mm² holes can't be sealed by a 0.049mm close).
  Real root = **F2 mesh-slicer on-plane facet classification** at the near-horizontal cabin floor
  (exact-f32 z==slice_z) — VINDICATES R61. NO faithful fill-stage fix converges it. **R64 took the decisive
  raw-loops-before-union measurement (both engines, branch `f2-rawloops`): at z=0.3/li=1 C++ `make_loops`
  emits 1 clean loop, rust emits 10 (outer 545.95mm² byte-identical + 8 phantom holes ~87mm²). The UNION is
  EXONERATED (the split is in the raw loops); the bug is F2 `slice_facet`/`make_loops` on-plane cap-facet
  classification — rust's cavity closes one slice late.** **R65 ROOT-CAUSED AND FIXED (LANDED, this branch).**
  Both-engine F2TRAFO dump showed the divergence is a COORDINATE-FRAME / f32-precision gap, not the facet
  logic: C++ stores the ModelVolume mesh **f32-centered** on its bbox and re-places it via the instance
  trafo (`trafo_centered() * volume.get_matrix()`, PrintObjectSlice.cpp:60 — identity + Z translate of
  exactly +24 for the Benchy). That f32 round trip QUANTIZES geometry sitting exactly on a layer-midpoint
  slice plane OFF the plane (f32 is ~21 ULPs coarser near center_z=24): the cabin floor f32(0.3)=0.300000012
  → `f32(f32(0.3−24)+24)` = 0.299999237 (7.75e-7 below slice plane zs[1]) → clean floor. Rust stores
  vertices f64 and only casts to f32 at slice time, so it kept exact f32(0.3) == slice_z → bit-coincident →
  degenerate. **FIX = `TriangleMesh::quantize_f32_center_roundtrip()`** (bakes the f32 round trip into the
  f64 vertices before slicing; app_slice.rs). RESULT (clean A/B): li=1 raw loops **10→1** (matches C++);
  layer-3 cabin-floor ISI **13.07→38.19** (native 38.08, parity); outer-wall G1 **+212→+158** and inner
  **+578→+554** (toward native); wall+ISI material all toward native; time 42m49s; gap-fill parity; build
  green; NO guardrail regression (outer-wall G1 IMPROVED — opposite of the rejected R59 slicer-fix at +371).
  The cabin-floor cascade is resolved; the REMAINING ISI deficit (−76) is the SEPARATE distributed
  **FillConcentric no-boundary-loop starvation** (body+top narrow vertical-shell strips) — that is the next
  lever. See `docs/parity/R65_floor_z.md`, R63/R64 docs, and the R63-R65 round-log.

## What's done (verified, on the branch)

- Surface classification: removed a spurious mesh-slicer `detect_surfaces_type`
  that fragmented surfaces (3/layer → 37-44) — **Top surface → parity**.
- Gap-fill: fixed a `variable_width` mm/scaled units bug + a missing
  `douglas_peucker` pre-simplify (gap-fill is at parity; the apparent gap was a
  priming-measurement artifact).
- Arc-fitting: wired `ArcFitter` into G-code export — **0 → ~12k G2/G3 moves**
  (native ~12k); also fixed a missing arc filament-length accumulation.
- Time estimator: ran the (faithful) `GCodeProcessor` and wired its accel-aware
  time into the header (correct format `; estimated printing time (normal mode) =`).
- Per-segment **speed modulation** (overhang speed + smooth-speed): toolpath
  density now matches native (outer-wall ~40k moves, ~6.7k distinct feedrates).
- **`crates/clipper-z-sys`**: vendored BambuStudio `ClipperLib_Z` (clipper.cpp +
  `CLIPPERLIB_USE_XYZ`) via a C-ABI shim; `clip_extrusion` validated. Portable
  binary (static C++; only libc++/libstdc++ residual). Wraps in
  `crates/libslic3r-rs/src/clipper_z.rs`.

## Remaining levers (all foundational/large — tackle as separate scoped efforts)

1. **Overhang trio + time-estimate — RESOLVED (ROUND 48).** `overhang_degree` is now
   `f64`, `merge_same_speed_paths` and `detect_bridge_wall` are ported + called, the
   speed interpolation is faithful, and the **time estimate converged (43m0s vs 43m21s)**.
   Overhang-wall feature matches (90 vs 91). This whole lever is done — do not re-attempt.
2. **THE FILL-SURFACE RECLASSIFICATION (the one remaining cascade root — coupled, needs a holistic fix).**
   Material (feat_e2): internal-solid 494→414 (−80, UNDER), sparse 531→598 (+66, OVER),
   floating-vertical-shell 171→107 (−64). Cascades into the systematic extrusion-arc over (+~1250).
   **ROUND 58-62 definitively RULED OUT everything upstream** (do NOT re-chase these):
   the mesh slicer (`slice_facet`/`make_loops` bit-faithful; slice grid bit-identical;
   cabin-floor facets at z≈0.3001 → cavity open at li=1 / closed at li=2 in BOTH engines —
   rust closes it correctly), `lslices` (byte-identical to `slices`), `detect_surfaces_type`
   (rust creates the CORRECT bottom-bridge at li=2, 91.7mm²), `discover_horizontal_shells`
   (no-op), `has_voids`/`surfaces_covered` (Benchy fill_density=0.15 → C++ also nullptr),
   the geo-clipper offsets in the narrow gate (A/B clib reroute byte-identical), and
   `clip_fill_surfaces` (dead code: `infill_only_where_needed` static-false).
   **ROOT (ROUND 62, definitive):** the correctly-born li=2 bottom-bridge/internal-solid is
   **reclassified DOWNSTREAM in the fill stage** — `detect_narrow_internal_solid_infill`
   (Fill.cpp:453-546, `fill/mod.rs`) narrow-detects it → routes to Concentric/floating →
   the **`FillConcentric` no-boundary-loop bug** (`fill_concentric.rs`; C++ seeds
   `loops=to_polygons(expolygon)` at FillConcentric.cpp:30) emits ~0 for sub-spacing strips.
   Native keeps/fills it (bridge or rectilinear); rust narrow-floats + starves it.
   **WHY IT'S NOT YET FIXED — it's COUPLED (3 pieces must land together or it regresses):**
   (a) **R53 gap-fill subtraction reorder** (branch `L74-fill`) — faithful, but alone un-masks
       the deficit (material 3850→3828); (b) **`FillConcentric` boundary-loop seed** (in
       `/tmp/vshell_findings.md`) — alone OVERSHOOTS +106mm (fills mis-sized regions); (c) the
       **reclassification correction** — why rust narrow-floats what native keeps as
       bridge/rectilinear (the unresolved knot). HOLISTIC NEXT STEP: land (a)+(b)+(c) on one
       branch off `L74-fill`, verifying **per-feature** convergence (internal-solid→494,
       floating→170, bridge→237, total→3859) NOT the coincidental aggregate, and guarding the
       byte-matched outer wall (G1 ~22087/native 22053 — the rejected `slicer-fix` regressed it to +371).
3. **Clipper coordinate byte-exactness (F1).** The live clipper backend is
   `geo-clipper` at scale 1000 (1 µm grid) fed via an mm float round-trip, vs C++
   ClipperLib at scale 100000. For byte-exact coordinates, feed the C++ clipper the
   same i64 inputs (raw FFI / vendor BambuStudio's exact `clipper.cpp` as a `-sys`
   crate). NOTE: F1 is *not* the cause of the toolpath-density gap (verified —
   bumping the scale changed nothing).
4. **Seam / toolpath ordering** — perimeter/seam emission order differs; needed
   before the two G-code streams are 1:1 alignable past the headers.
5. **Byte-exact bridges/overhang** would use **Clipper2Lib_Z** (`SetZCallback` +
   Z-preserving Clipper2 offset), a later `clipper-z-sys` extension.

## Notes

- `cargo test` for the `slicer` lib is **pre-existing-broken** (unrelated
  arachne/surface/fill errors) — only `cargo build` gates are authoritative on this
  branch. `clipper-z-sys` tests pass.
- Many compiler warnings are intentional: `///` C++-reference doc comments and
  faithfully-ported-but-gated/unwired code.

## WIP branches (preserved on GitHub — building blocks for the holistic fill-reclassification fix)

All are pushed; none merged to `alex/libslic3r-parity-engine` (each is a real fix or diagnosis held back to avoid regressing parity). The holistic fix (lever #2) should branch off `L74-fill` and combine the faithful pieces, verifying per-feature.

| branch | holds | status |
|--------|-------|--------|
| `L74-fill` | R53: gap-fill subtraction reorder (C++ order: subtract before infill opening) — **faithful** | use as the BASE; alone it un-masks the deficit (3850→3828) |
| `vshell-fix` | diagnosis: the `FillConcentric` no-boundary-loop bug + analysis (`/tmp/vshell_findings.md`) | the FillConcentric fix; alone OVERSHOOTS +106mm |
| `f1-fill` / `void-clamp` / `bottom-surface` / `slicer-fix` / `lslices-phase2` / `slice-facet` | diagnosis trail that RULED OUT slicer / lslices / clamp / region-partitioning (with the data) | reference only — do NOT re-chase these dead ends |

Measurement (unchanged): `COMPARE_KEEP_DIR=/tmp/cmp devbox run -- target/debug/slicer-cli compare --config tests/configs/stl-inline-config.jsonnet`; per-feature material via `/tmp/feat_e2.py`. The merged units-fix (`make_expolygons` mm-not-scaled, `dd93fc4`) is a no-op today but required the moment any nonzero `closing_radius` is plumbed.
